<!-- plateau-handoff:func_80004590:start -->
### `func_80004590` plateau handoff

- source: `src/main/objects.c`
- score: 84 differing words
- frame: 0x50
- relocations: 5
- first mismatch: +0x8
- summary: Outer start less-than-end test duplicated the for-loop check. Remaining plus-16 is five per-compare type masks minus the missing incoming-arg0 store.

The live body is `for (i = start; i < end; i++)` over `list[i]`, which unrolls.
The outer `if (start < end)` emitted a second compare-and-branch pair at plus-0x30.
Removing it dropped size_delta 24 to 16 and masked 100 to 84, with the frame
held at 0x50 and five relocations on each side.

What remains is one family. The target truncates `arg0` once in the prologue
into s0 and stores the unmodified incoming argument to its home at plus-0x50
(a store with no matching load). The candidate copies full arg0 into s0 and
rematerializes the byte mask at each compare: five candidate-only words, one
in the remainder body and one per 4x copy, and it never emits the incoming
store (target-only plus-0x10). `u8 type`, `type &= 0xFF`, and L109 or-with-zero
on type were inert. `arg0 &= 0xFF` overshot to 98 words (minus 4) by dropping the five
masks and the store together. Next: a truncating copy that stays in the
prologue so the incoming store can coexist with it.

<!-- plateau-handoff:func_80004590:end -->
