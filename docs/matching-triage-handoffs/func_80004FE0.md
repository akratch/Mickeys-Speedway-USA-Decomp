<!-- plateau-handoff:func_80004FE0:start -->
### `func_80004FE0` plateau handoff

- source: `src/main/objects.c`
- score: 70 differing words
- frame: 0x100
- relocations: 83
- first mismatch: +0x134
- summary: No packet cursor: packets in a0, modeState base in a2. Unsigned index, slot index, byte-offset address stalled at 185/-4, 290/+20, 185/-4; +0x134 naming stayed.

Summary before this remeasure: Size exact, no insertion pair. Indexed i less-than-6 fill rematerializes category; modeState[i] kills the records carrier and the modeState ILOD. playerCount equals i after the setup call stops the early s0 zero. Remaining 58 naming is packets versus modeState-base a0/a2, with 10 structural jal/8-iter/spawn rows in that shadow. Packet cursor, D_800C94F4[i], L97 barriers, and deleting offset still regress. Next: put packets in a0 and the modeState base in a2 without a declared packet cursor.

Arrival was size-exact 207 with a declared pointer-walk fill. The declared
slot carrier copied the category base. A counted `for (i = 0; i < 6; i++)
category[i] = NULL` strength-reduces to the rolled walk and rematerializes
the base; it does not unroll once `i` is reused by the object scan.
`modeState[i]` in place of the declared records walk removes the modeState
ILOD. `playerCount = i` immediately after controlGetPlayerSetup is an
identity that keeps `i` live so the spawn zero cannot hoist before that
call. The same assignment after the 8-iter D_800C94F4 loop reintroduced a
jal delay-slot pair.

Aligned 276 byte-exact, 58 naming, 2 immediate, 10 structural, displacement
tax 0. First naming plus-134 is the packets addiu in a2 versus a0, with the
modeState-base reload swapped. Frame 0x100 still has candidate-only home
0xF8 (`offset`) against target-only plus-0x34. Packet cursor, D_800C94F4[i],
L97 barriers, spawn-via-slot, and deleting offset regress on this shape.

This remains guarded NON_MATCHING C and earns no matching credit.

<!-- plateau-handoff:func_80004FE0:end -->
