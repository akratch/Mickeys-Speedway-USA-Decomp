<!-- plateau-handoff:func_80004FE0:start -->
### `func_80004FE0` plateau handoff

- source: `src/main/objects.c`
- score: 207 differing words
- frame: 0x100
- relocations: 83
- first mismatch: +0x54
- summary: Size exact after pointer-walk category zero-fill and i-indexed object scan. Remaining 3-vs-3 is category CSE copy at plus-58 versus rematerialize at plus-90, plus modeState ILOD at plus-230 and extra s0 reset at plus-2E8. Captured-end, packet cursor, spawn-without-i, L97 fill, and deleting offset all regress. Next: keep category dead during the fill without a new local.

The configured baseline on arrival was plus-8 size and 310 masked, with a
rolled pointer-walk on the target category zero-fill against an unrolled
2-plus-4 counted `i < 6` in the candidate. Spelling that zero-fill as
`for (slot = category; slot < category + 6; slot++)` and walking the object
list as `for (i = 0; i < D_800C9498; i++)` over `D_800C9494[i]` is size
exact at 346 words, frame 0x100, 83 relocations, 207 masked, 209 aligned
byte-exact, first structural plus-74. The leftover is three candidate-only
words (plus-58 copy of the category base into the walk pointer, plus-230
modeState reload, plus-2E8 records increment or s0 reset) against three
target-only words (plus-90 category rematerialize, plus-130 and plus-14C
modeState loads at the packet-loop head).

Frame still has candidate-only home 0xF8 (`offset`) against target-only
plus-0x34. Indexing `D_800C94F4[i]` and deleting `offset` grew the function
16 bytes and scrambled the frame. An explicit packet cursor, captured-end
fill, L97 around the fill, and spawn without `i` all regress. `records++`
before `i++` was aligned 208 to 209 at the same 207 masked.

This remains guarded NON_MATCHING C and earns no matching credit.

<!-- plateau-handoff:func_80004FE0:end -->
