<!-- plateau-handoff:func_8000590C:start -->
### `func_8000590C` plateau handoff

- source: `src/main/objects.c`
- score: 28 differing words
- frame: 0x90
- relocations: 99
- first mismatch: +0x6D4
- summary: 99 to 28 at delta 0 (lane s1-c): nested fixups without a declared pointer, one-width nested reads, and the copy-loop remainder as its own symbol copied from the unmasked test; left are the strength-reduced preheader init, the unk4C nested value's colour and the tail's three-way tie at 3/7.

## 2026-09-16 (lane `s1-c`): 99 -> 32 at delta 0, and two shard claims refuted by arithmetic

Baseline reproduced at 99 (661/47/2/12, first +0x6C8) on a private direct-cc
harness (score of record before use). Four cycles here; every number is
`tools/score_symbol.py`'s comparator on the whole TU. Adopted, delta 0, frame
0x90, 99 relocations, no force: 689 byte-exact, 20 naming, 3 immediate, 5
structural, candidate-only +0x984/+0x994, target-only +0x998/+0x99C, first
+0x410 (a branch offset shadowing the +0x984 pair).

### Two claims in this shard are wrong, and the arithmetic says so

- p12: "merging the two nested reads is delta -4, so the target has two reads
  there too." The baseline's delta 0 is (-1 word at +0x6DC) + (+1 at +0x7A0),
  which the aligner already lists as the target-only and candidate-only
  words. Merging the reads removes the +1 and MUST read -4; it says nothing
  about the target, which loads the nested value once (`lw a0,0x1C(t8)`,
  reused by the `addu`). The merged read is right and is adopted.
- p12: "deleting the `relocated` local ... every one is worse (199-289); the
  direction is closed." The positional 289 was L155 shadow of the -4. Aligned,
  the merged read with `relocated` deleted and the stored field re-read with
  ONE spelling is 657 exact / 20 naming (base 661 / 47): the +0x700 window
  goes 23 naming rows to 3 and +0x800 goes 4 to 0. What the records show:
  uopt forwards the stored `object->unk4C` into the CSE temp of the
  `object + field - base` expression, and ugen keeps that temp in a ring
  register across the branch into the nested store (the target's t8 at
  unk4C, t4 at unk48) because the body block has the test block as its only
  predecessor. A declared pointer is a symbol and is coloured (v1) instead;
  no symbol reaches t8. L145 in a new form: to put a value in a ring temp
  across a branch, delete the carrier and spell the re-reads identically.
- w8-recon's `offset` as the unk48 nested carrier survives: with the
  `relocated` deletion its piece no longer reaches the unk4C site, so both
  nested values take a0 as the target (a fresh `nested` local is 37 against
  32; `index`/`packetId`/`resultSize` 101; `loadType` 202).

### The preamble copy, priced

`if ((size & 3) != 0) { loadType = size & 3; ...` (p11's form) produces the
target's `andi a3; beqz a3; or a2,a3` copy: with the merged read it is one
word for one and holds delta 0. Alone it is +4 (250) and paired with the old
split read 159, which is why p12 read it as a 104-row naming cost; those rows
were the read's, not the copy's.

### The `sll a0,zero,2` is a strength-reduced init and nothing else

- Anchoring `resultSize = 0;` before `if (object == NULL)` (its def then
  sits in the block before the branch, where as1 already puts the target's
  `or a1,zero,zero`) is byte-identical to the baseline: uopt folds an explicit
  `resultSize * 4` across the block boundary. The shard's "earlier anchors
  restore the instruction" is true only of anchors that also break the
  zeroing loop (first mismatch +0xAC), which share `resultSize`.
- Indexing the copy loop by `resultSize` with no `offset`/`aligned` carriers
  makes uopt strength-reduce the subscript and emit exactly the target's
  `sll a0,zero,2; addu v1,v0,a0` (never folded, only zero-substituted). Done
  to loop 1 alone it cascades from +0x5C (63): `offset` 136/3 and `aligned`
  92/2 lose their loop-1 references, flip to 105/3 = 35 against 61/2 = 30.5,
  and the zeroing loop's a0/a1 follow. Done to all three loops (zeroing, copy
  1, copy 2) it is 37-39 at delta 0 with the preheader exact but for the
  a2/a3 order of the loadType pair, and a different residual: +0x700 two
  structural rows and seven a0/v1 rows in the unk50/unk64 fixup chain, whose
  tiny value webs tie and are ordered by web number, which the SR temps
  shift (L154). The 32 form keeps the explicit loops and the chain exact.

### What is left in the 32 form, by window

- +0x6C0, six rows: the preheader's `move a0,zero; move v1,v0` against
  `sll a0,zero,2; addu v1,v0,a0` (two structural) and the loadType pair
  a1/a2/a3 three-cycle.
- +0x780, three rows: a1->a0 x2, a1->v0, v0->a0 at the unk4C nested store.
- +0x900..+0x980 and +0xA80, eighteen rows: the s0/s3 exchange between the
  `arg1 & 1` value (web 427, 3/7 = 0.43, s0 here, s3 in the ROM) and the
  `&D_800C94A8` address constant (a 1/4 web, s3 here, s0 in the ROM), and
  the second piece of `&D_800C9498` materialised in the else arm here
  (+0x984/+0x994) and after the join in the ROM (+0x998/+0x99C). Both webs
  are decided late at low save; the ROM decides the address first. The lever
  is the ratio of those two small webs or the piece placement, not a
  spelling of the registration arms (swaps are measured inert by p11).

Do not re-run: the sixteen cast spellings (the split-width pair is refuted,
not load-bearing), the fifty-six `relocated` carrier forms on the OLD read
(the deletion is adopted), the resultSize anchor lattice, or loop-1-only
strength reduction.

## 2026-09-12 (lane `p23-colour-tier`): exhaustive colour landscape is a late additive split

The baseline was reproduced at 99/719 masked words, size delta 0, with 661
aligned exact rows, 47 naming rows, 2 immediate rows and 12 structurally
different rows. `residual_map.py` reports seven windows; the largest aligned
colour buckets are the ring phases at +0x600 (26 naming rows) and +0x800 (14),
with a smaller +0xA00 phase (7). Candidate-only words remain at +0x7A0,
+0x984 and +0x994; target-only words are +0x6DC, +0x998 and +0x99C.

Procedure mapping is unambiguous: proc 21 is the only procedure with the
handoff's 74 p1 decisions. `web_footprint.py func_8000590C --proc 21
--every-colour` enumerated 396 force cells over 45 webs. 299/396 (75.5%)
produced accepted size-zero objects; 91 were size-changing and 6 were
declined. The complete winner list is:

- `p1:w275=c5`: 98/719, footprint -1 at 0x0680.
- `p1:w319=c4`, `p1:w319=c5`, `p1:w319=c6`, `p1:w319=c7`: each 96/719,
  footprint -3 at 0x0780.

The four `w319` colours are rivals in one web and cannot be combined. All
four legal pairs of `w275=c5` with one rival were measured with
`force_lattice.py`: every pair is 95/719, exactly additive, with disjoint
radii (-1 at 0x0680 and -3 at 0x0780). Thus the measured colour floor is
95/719 across the full accepted landscape, but it only changes two late
windows and does not remove the +0x6D4 copy-loop structural mismatch or the
+0x7A0 nested-read carrier/reload phase. The surviving source question is
therefore not a further arbitrary colour force: find the natural source form
that partitions the nested `unk4C` read from `relocated` while preserving the
existing 2876-byte shape, then separately resolve the copy preamble and tail
ratio exchange described below.

## 2026-09-12 (lane `p11-big`): the asset web split on spelling, 150 -> 99

Measured with `tools/align_symbol.py`, `tools/score_symbol.py`,
`tools/frame_census.py` and `tools/register_census.py` on the configured full
TU. The frame ladders are identical on both sides, twenty slots each, and the
size delta stays 0 throughout.

Before: 150 masked; byte-exact 612, register naming 92, immediate 2, really
different 17; first structural +0x1B4.
After: 99 masked; byte-exact 661, register naming 47, immediate 2, really
different 12; first structural +0x6D4.

### The previous handoff's "next lever" was right, and the instrument was the spelling

It asked for "a source form in which the body and the tail read different
expressions at no extra instruction", having priced an explicit carrier at
naming 92 -> 72 with structural 19 -> 72 and +8 bytes. The form exists and it is
the reverse of what was tried. Three edits, each measured alone:

1. **Assign `asset = object->unk40;` immediately before the `func_80053550`
   call and read its eleven arguments through it.** 150 -> 124, naming 92 -> 66,
   size delta 0. That one carrier is what splits the range: the target gives the
   tail read its own colour (`v0`) and every body read `a2`, and with the tail
   named, all seven body reads take `a2` on their own. No instruction is added,
   because the load was already there.

2. **Delete the `asset` local from the head** and spell those four reads
   `object->unk40->unk0`, `->unk18`, `->unkA7`, `->unk1C`. 124 -> 105. This is
   L145 in the other direction: the head reads belong to the *same* name as the
   body reads, and naming them separately was costing the head read its `a2`.

3. **`cursor = aligned; cursor += X;` rather than `cursor = aligned + X;`** at
   the three layout sites. 124 -> 117 alone, and with (2) 105 -> 102. Writing
   the addition with the operands swapped is completely inert; splitting it into
   two statements is not.

4. **A full move-one hill climb over the nine-statement header block** (145
   compiles, exhausted) moves `object->unk46 = packetId;` one place later, after
   `object->unk3C = arg0;`: 102 -> 99, and the +0x1B4 header hunk that three
   previous passes named as their first mismatch disappears entirely. Separately
   `object->unk39 = 0xFF;` moves after the `unk34` multiply, which was part of
   the 124 -> 120 step.

### What is left, in four named pieces

- **The copy-loop preamble at +0x6D4, 6 rows and the one missing instruction.**
  The target computes `loadType` as a copy (`andi a3,...; beqz a3; move a2,a3`),
  so the tested value and the loop bound are two webs, and it keeps
  `offset = resultSize * 4` as a real `sll a0,zero,0x2` where the candidate
  folds `object + 0` to a `move`. Spelling the test as `if ((size & 3) != 0) {
  loadType = size & 3; ... }` DOES produce the copy (that is measured), but it
  lands +4 and the naming goes 66 -> 65 with the rest regressing; combined with
  the +0x7A0 carrier below it holds delta 0 at 181 masked with naming 167. So
  both halves of the shape are reachable and the pair is not, yet.
  Region boundaries do not reach the fold: `do { } while (0)` and `if (1) { }`
  between `resultSize = 0;` and the loop, and inside the guard, are all
  byte-identical.

- **The nested `unk4C` fixup at +0x7A0, one surplus load and 26 naming rows.**
  The candidate re-reads `*(relocated + 0x1C)` after the branch-likely; the
  target loads it once into `a0` and reuses it. That surplus load is one extra
  ugen draw, and it is why every temp from +0x7A4 to +0x820 sits exactly one
  ring position ahead of the target's -- 26 rows behind a single instruction.
  A carrier removes the load and costs far more: `offset`, `index`, `loadType`,
  `resultSize`, `size` and `packetId` all give exactly 292 masked at delta -4
  with naming 47 -> 139, and a fresh `s32` local gives 324 with the frame moved.
  **The decision variable is which of `relocated` and `*(relocated + 0x1C)` is
  the symbol.** The target's colours say it is the nested value: `relocated`
  sits in `t8`/`t4` (ring temps) and the nested value in `a0`, while the
  candidate has `relocated` in `v1` and the nested value in a ring temp. So the
  form to find deletes the `relocated` local and names the nested read -- the
  exact mirror of (1) above, which is the edit that worked at the tail. Every
  carrier measured so far kept `relocated` named, which is why they all land on
  the same number.

- **The `s0`/`s3` exchange in the tail, 16 rows.** `arg1 & 1` and the
  `D_800C9498`/`D_800C94A8` counter address are a two-web exchange: the
  candidate gives `arg1 & 1` s0 and the address s3, the target the reverse.
  Swapping the arms of either `if (arg1 & 1)` or both (101, 102, 100) and moving
  the `object->unk68` assignment out of the block (150) do not reach it. It is
  the same tie shape as the `cursor`/`size` flip that was worth 36 words here,
  so the lever is one of the two webs' `totalsave`, not the source order.

- **The `lui s1` address materialisation at +0x984, 4 rows, net zero words.**
  The candidate splits `lui`/`addiu` around the block; the target emits the pair
  together after it.

Do **not** re-run: declaration order, statement order or definition position on
the allocation pair (the phase census says the axis does not exist), the header
block order (exhausted by move-one at 99), the six carriers listed above for the
`unk4C` nested read, or the addition-operand swap at the layout sites.

Validation: `gmake verify` printed 507341c0a40ca3e9a7cee969b396ee53facfb548;
`gmake cleanroom` and `gmake check-docs` pass. The candidate remains
`NON_MATCHING` and GLOBAL_ASM-backed, so no bytes are credited.

## 2026-09-11 the allocator phase census, and a save-ratio flip: 186 -> 150 (lane `lane/p9-alloc`)

Measured with `tools/align_symbol.py` and `tools/score_symbol.py` on the
configured full TU, and with the instrumented IDO 5.3 uopt whose `.text` was
first checked byte-identical to `tools/ido/cc`'s on the unedited tree. A direct
`cc` compile of this TU reproduces the configured build exactly, so the whole
loop below is a 0.6 s compile per candidate.

### Phase census first (L108)

**74 p1 decisions -- 45 `color`, 29 `split` -- and zero p2 records.** So
[L106] has no axis on this function at all: definition position, declaration
order and statement order cannot move a colour here, and only L100's
`save = totalsave/nocs` ratio decides anything. This is the mechanical reason
the previous pass's 210-candidate move-one declaration lattice came back flat
at 186 with an unchanged frame; it was measuring an axis the function does not
have. That closure was right, and now it has a cause.

### What the residual actually is

Aligned buckets, 719 words each side: byte-exact 574, register-naming 130,
really-different 19, displacement tax 37.

The previous handoff read the naming rows as "the target holds `size` in s0 and
`newObject` in s1; the candidate has them the other way round". **That is
wrong, and forcing exactly that exchange proves it**: the two webs concerned
accept the force (the records show `forced=` on both the decision and the
colour record) and the object gets *worse*, 186 -> 202, naming 130 -> 146.

The real exchange is between **`cursor` and `size`**, and it goes the other
way. Reading the two register censuses against each other:

    s0   candidate 66   target 42
    s1   candidate 36   target 68
    s2   candidate 107  target 107
    s3   candidate 43   target 35

`object` (s2) is already exact. The loop counter of the resource loop and its
scaled form are exact too -- both sides put the counter in s1 and the scaled
index in s0. What differs is which of those two registers the *later* pair
joins: the target puts `cursor` with the counter in s1 and `size` with the
scaled index in s0, and the candidate has them swapped. Forcing the one colour
`cursor` -> s1 takes 186 -> **150**, byte-exact 574 -> 612, naming 130 -> 92,
structural unchanged at 19 and size delta 0.

### Why p1 chose the wrong one, exactly

`cursor` and `size` interfere, so whichever is decided first takes the lower
colour and the other is pushed up. p1 decides in descending `save`:

- `cursor`: `totalsave` 39 over `nocs` 11 -> **3.545455**, decided 15th.
- `size`: `totalsave` 10 over `nocs` 5 -> **2.0**, decided 22nd.

Its candidate list at decision time offers exactly two colours, s0 and s1
(s2 is excluded by interference with `object`, s3 and above still carry the
callee-save toll), **and both cost 1.000000**. A tie, broken by the ascending
colour scan -- so nothing about the source reaches the choice directly. The
lever is the ratio that orders the two webs.

### The fix, and the L109 bound sharpened

`size` has no loop in its live range, so a probe there is worth 1x, not 10x.
Measured, on this function:

- A self-assignment of `size` ORed with zero adds **2** to `size`'s
  `totalsave` and 0 to its `nocs`, and the object comes back byte-identical.
  The AND-with-minus-one and XOR-with-zero spellings are worth the same 2.
- `size = size + 0;`, `size += 0;`, `size = size;`, `(void)size;` and a bare
  `size;` are **all worth zero** -- uopt copy-propagates every one of them
  away. This is L109's first bound reproduced on a scalar rather than a
  pointer, and it is the whole difference between a lever and a no-op.
- Rewriting the round-up as two statements rather than one masked add is also
  worth 2, and is byte-identical; it is the only *natural* form found that
  moves the number at all.

Six probes carry `size` to 22/5 = **4.4** against `cursor`'s 3.545455, p1 then
decides `size` first, `size` takes s0, `cursor` takes s1, and the object is
**byte-identical to the `CDX_FORCE=p1:w156=c15` object** -- a source-only
reproduction of a forced colour. Four probes (3.6) already flip it; six are
carried for margin, because L109's own receipt records a lever sitting on the
boundary being undone by the next edit.

The probes are *not* believable C and are commented as such at the point of
use. They are a placeholder for whatever natural construct gave `size` that
weight in the original source; that construct has not been found. What is
established is the quantity it has to supply: **+8 on `size`'s `totalsave` at
constant `nocs`, or -18 on `cursor`'s.**

### What a force sweep says about the rest

A single-force sweep over every (web, colour) cell each web's own record
declares available -- 393 cells, all accepted forces checked in the records --
finds `cursor` -> s1 as the best single move by a wide margin, and a greedy
four-deep search stops at **143** (naming 92 -> 82). So **of the 130 naming
rows, p1 colouring reaches at most about 48.** The other ~82 are not colour
decisions and no ratio or spelling edit reaches them. Two are now named:

- **The asset-pointer reload is one web in the candidate and two in the
  target.** It is reloaded from the object at every test of the asset record,
  and p1 gives the whole thing one caller-saved colour; the target names the
  body occurrences a2 and the tail occurrences v0. Both of those colours are in
  this web's `forbidden0` mask, so it is interference and not preference --
  L101's second row, where the lever is a **web partition** and nothing else.
  This is 37 of the 82 remaining rows, the single largest item left.
- **A second s0/s3 exchange in the tail** (12 + 4 slots from +0x930) is
  force-*declined*: s0 is already forbidden for that web at decision time.
  Same verdict, same lever.

### Next lever

1. Split the asset-pointer web -- but **not with an explicit carrier**, which
   was tried here and priced. Assigning the existing `asset` local again before
   the registration block and reading the tail sites through it does split the
   web and the naming bucket falls as predicted, 92 -> 72; the extra pointer
   load costs far more than it buys and the structural bucket goes 19 -> 72
   with a +8 size delta. Converting the light-setup tests as well holds the
   size delta at 0 but still lands at structural 58 and naming 100. So the
   split is real and the carrier is the wrong instrument for it: this is one
   *expression* web, not one variable, and what is needed is a source form in
   which the body and the tail read **different expressions** at no extra
   instruction. 37 rows are behind it.
2. Then re-derive the remaining naming rows: after the split the ratio that was
   just set may need re-tuning, since every edit moves it (L109).
3. The 19 structural rows are unchanged by all of the above and are still the
   header hunk at +0x1B4 and the copy-loop preamble at +0x6DC that the previous
   pass left open.

Do **not** spend another pass on declaration order, statement order or
definition position. The phase census says those levers do not exist here.

Validation: `gmake verify` printed the expected US ROM SHA1; `gmake cleanroom`
and `gmake check-docs` pass. The candidate remains `NON_MATCHING` and
GLOBAL_ASM-backed, so no bytes are credited.

## 2026-09-10 region partition (lane `lane/w8-recon`): 538 -> 186 masked words

The prior handoff's `lever: none-known` was a consequence of measuring only the
positional differing-word count. Partitioning the residual first named three
levers, two of which paid.

### The instrument

Beside the positional count, align the two instruction sequences on a
*register-erased shape* (opcode, function code, shift amount, immediate and
branch offset kept; every GPR and FPR selector erased, including COP1 `fs`/`ft`/
`fd`, which the ranking's own `instr_reg_mask` deliberately leaves alone), then
classify each aligned pair as byte-exact, register-naming-only, or genuinely
different. The three buckets partition the residual by *cause*:

Reading baseline then after-this-pass, over 719 words on both sides:

- positional differing (masked): 538 -> **186**
- byte-exact under shape alignment: 465 -> **576**
- displacement tax (aligned-exact minus positional-exact): 284 (53%) -> 15
- register-naming-only rows: 231 (43%) -> 131
- rows differing in instruction kind: 23 (4%) -> 17

At 538 words the residual was 53% displacement and 43% naming: **twenty-three
words of the function were actually wrong.** That is what the positional number
was hiding.

### The partition, and the decision variable per region

Displacement is piecewise constant, so the profile of `candidate index - target
index` cuts the function into regions and each step is one nameable event.
Baseline regions, with the delta carried through each:

Baseline regions, each as *target range, delta, rows, byte-exact, register-only,
decision variable*:

- +0x000..+0x1B4, delta 0, 110 rows, 109 exact, 0 register-only --- already exact.
- +0x1B4..+0x200, delta +1, 20 rows, 8 exact, 5 register-only --- the header
  hunk: which byte stores the assembler has available as hazard filler, and the
  `a2`/`v1` colour of the asset pointer.
- +0x200..+0x6DC, delta -1, 311 rows, 236 exact, 67 register-only --- one
  instruction: the target leaves a floating-point move-to-coprocessor hazard as
  a `nop`, the candidate fills it.
- +0x6DC..+0x7A4, delta -2, 52 rows, 37 exact, 9 register-only --- a second
  missing instruction at the unrolled copy-loop preamble.
- +0x7A4..+0x814, delta -1, 29 rows, 7 exact, 19 register-only --- the first
  nested pointer fixup: one extra field reload.
- +0x814..+0x9A4, delta 0 drifting to +2, 102 rows, 20 exact, 74 register-only
  --- the second nested pointer fixup: one extra field reload.
- +0x9A4..+0xB3C, delta 0, 102 rows, 48 exact, 54 register-only --- temp ring
  permutation only.

Three of these were fixed by source changes; the fixes are independent and
each was measured alone.

**1. The header hunk (worth 311 positional words).** With `object->unk8D`
assigned after `object->unk91`/`unk93`, the scheduler has two independent byte
stores in reach of the earlier floating-point hazard and fills it; the target
emits the hazard `nop` instead and uses the same two stores to cover the *later*
byte-load delay. Moving the `unk8D` assignment ahead of the two zeroing stores
reproduces the target's choice. Measured alone this *grows* the object by one
word (719 -> 720), which is the point: it restores an instruction the candidate
was missing, and reveals that two other instructions were surplus.

**2. The nested pointer fixups (worth 2 instructions and ~180 words).** Both
`unk4C` and `unk48` fixups test an inner field and then recompute from it. The
candidate re-read the field inside the body, giving one load more than the
target, which reuses the value it tested. Holding it in a carrier (the dead
`offset` local; a fresh local was not needed) removes the reload. A
six-carrier x two-operand-order x three-site-set lattice (72 compiled and
scored candidates) ranks `offset` first at 187, `index`/`resultSize`/`size` at
189 and `loadType` at 287; applying it to *both* sites overshoots to a -4 size
delta, so exactly one carrier is right and the second surplus instruction is
still unlocated.

**3. `resultSize = 0;` before `size >>= 2;`** is worth one further word (187 ->
186) and one more aligned-exact row.

### What is now exact, and what is left

- **The stack frame and every home.** Twenty distinct sp-relative homes, 53
  accesses, identical offsets and identical access widths on both sides. This
  was not true of the baseline in any prior pass.
- **Relocations:** 99 on each side, 97 agreeing in offset and type and 97
  agreeing in full identity, against 47 exact identities recorded before.
- The first 110 words and the last two regions are in phase.

The 186 remaining words are, in order of size:

- **The saved-register exchange, 54 rows.** The target holds `size` in `s0` and
  `newObject` in `s1`; the candidate has them the other way round, and a second
  exchange (`s0`/`s3`) runs from +0x7A4. **Declaration order does not decide
  this**: a complete move-one lattice over all fifteen declarations (210
  compiled candidates, every permutation reachable by one move) is flat at 186
  with an unchanged frame. Six placements of the `newObject` definition are
  also flat or much worse (389-451). The axis is therefore neither the
  declaration list nor the definition point of `newObject`; look for the web
  that interferes with `size` across the allocation call.
- **The temp ring, 76 rows**, a position-dependent permutation of `t1`-`t9`
  rather than one global rotation, plus `a2`->`t0` at the reloaded asset
  pointer and `v0`->`t0` in the tail.
- **17 rows that differ in kind**, concentrated at the +0x1B4 header and the
  copy-loop preamble at +0x6DC, where the target keeps the scaled induction
  variable as a shift of a register that is provably zero and the candidate
  folds it to a move.

### Next lever

Locate the second surplus instruction (the one that makes the two-carrier form
overshoot) at the copy-loop preamble around +0x6DC, then re-test the two-site
carrier form. Two handles on it are already measured and closed. The missing
word is the scaled induction variable: the target computes `resultSize * 4` as
a real shift of a register that holds zero, and the candidate folds it away.
Spelling it `resultSize << 2` instead of `resultSize * 4` is **completely
inert** --- byte-identical objects at both sites, across every other variant
tried. Moving the `resultSize = 0;` definition to any of four earlier anchors
does restore the instruction (size delta returns to +4, and to 0 once the
second fixup carrier is added), but it costs far more than it buys: the first
mismatch retreats from +0x1B4 to +0xAC or +0xB8, breaking the exact opening 110
words, and the score lands at 547-624 against 186. The handle must therefore
block the fold without moving the definition. After that the residual is entirely saved-register and temp-ring
naming, and the axis for the saved pair is *not* declaration order --- that is
measured, not assumed.

Validation: `gmake verify` passed with the expected US ROM SHA1
`507341c0a40ca3e9a7cee969b396ee53facfb548` (the guarded assembly fallback is
still what links; no bytes are credited). `gmake check-nonmatching-builds`
passed for 216 TUs, `gmake overlay-syms` regenerated the alias file
byte-identical, and `gmake cleanroom` and `gmake check-docs` pass. All measured
numbers above come from `tools/score_symbol.py`'s comparator; the harness used
for the lattices reproduces its baseline exactly (538 masked, first +0x1B4).


2026-09-09 pinned-donor reopening audit:

The fresh assignment gate returned base-only for the explicitly authorized
JFG donor mechanism. A configured full-TU baseline independently reproduced
719/719 words, 538 raw/masked differences, first +0x1B4, frame 0x90,
and 99 relocations per side with 47 authenticated identities. Baseline 240
and documentation-only check 241 are lane checkpoint numbers, not new
matching attempts. The candidate body is unchanged.

PROVENANCE: the public Jet Force Gemini src/objects.c at revision
efd5abb1c79636e297b831f7c2d5bf47eac39c0c was inspected through its pinned Git
object. objSetupObject remains GLOBAL_ASM-backed, and that source file is
identical to its parent revision. No donor C was adopted. The pinned
StaticInstanceSpawn declaration uses a signed halfword ID and signed
halfword coordinates already represented by Mickey's retained packet view;
it does not provide a header-scheduling trace. This audit relies on the
pinned files directly, rather than carrying forward the earlier donor-screen
wording about another entry declaration.

The committed donor-screen handoff at 77d4281e already records the missing
counterpart body and lack of the required scheduling lever. The fresh
workbench diagnosis still reports structure-mismatch and lever none-known:
equal frames alone provide no home, temporary-ring or source-line evidence.
Its structure-buckets route therefore does not authorize another blind
allocation search. The cached 53 usable flag objects were rescored without
new compilation; configured flags remain best and nonexact. The fresh
oracle again supplies only weak similarities. The already exhausted plain
copy-loop and flag hypotheses were not repeated.

This closes the one authorized donor-evidence mechanism with zero new C
attempts. It does not claim that all possible reconstruction mechanisms are
exhausted. The next concrete lever remains a source-line/UGEN scheduling
trace for the first header hunk, followed by normal exact byte, relocation,
linked-range and ROM proof. A new packet must pass the assignment gate.

The existing candidate remains guarded and was refreshed with
finalize_plateau.py. Source provenance and handoff annotations are the only
changes for this audit. Validation includes configured full-TU comparison,
diagnosis and guide, cached flag rescore, donor Git-object inspection,
check-docs, cleanroom, full gmake verify and check-nonmatching-builds.
The original reconstruction evidence is retained below.

The retained C is **NON_MATCHING**, compiled in the configured full translation
unit. Target and candidate each contain 719 words (2,876 bytes), with 538 raw
and relocation-masked positional differences and first mismatch `+0x1B4`.
Both frames are `0x90`; all observed argument/local homes agree. The static
relocation comparison has 99 records on each side, with 47 identities agreeing
at the exact offset and type. These are diagnostic measurements, not a linked
C match. The guarded assembly remains canonical and no bytes are credited.

Boundary and call-graph evidence establishes resident
`0x8000590C..0x80006448`, ROM `0x650C..0x7048`, with no padding. The target
saves four integer saved registers and the return address; outbound stack
arguments reach `+0x2C`. Eleven resident call sites and 23 overlay runtime
call sites corroborate this identity, with no ORT export. Both outbound
runtime overlay calls are authenticated: overlay 19 at its entry and overlay
43 at its entry. The direct callees include the exact object-data sizing and
initialization siblings in this TU.

The target's 43 calls, 85 branch instructions, four backedges and common
return describe four phases: selection/zeroing/header setup; the three-kind
resource loop and its cleanup; optional component construction and allocation;
and copying, pointer fixups, registration and final initialization/rollback.
The baseline already had the same call order and broad control-flow shape.
Its shortfall was **104 bytes, or 26 words**, rather than 104 words or an
absent call subtree. It entered the copy/fixup region two words long and lost
28 words there. The draft cached the staging-base global once; the target
reloads it across fifteen fixups. Restoring those reads and their address
arithmetic accounted for the net shortfall without adding padding or guards.

The baseline was 693 words with 692 differences, a `0xA8` frame, and 99
relocations, of which five identities aligned. The original and post-sibling-
promotion baseline function bytes were identical. Reusing the authenticated TU
flag cache scored 53 rows and retained 66 compiler rejections; the configured
O2/MIPS-II preset remained best. No shared flag was changed. The weak
near-match oracle result supplied no useful exact sibling, and this packet
adopted no external source.

Ten source attempts tested, in order:

1. Repeated staging-base reads in the fixup chain: restored the 719-word extent.
2. Original packet-ID lifetime across calls: retained the original value.
3. Three-way resource switch: restored test and body order.
4. Existing carrier reuse: removed six surplus homes and restored the frame.
5. Declaration ordering: restored the observed flag, selected-ID and failure homes.
6. Separate zeroing/resource counters: restored the early global-address carrier.
7. Correct texture-call ABI and memory accesses: removed an extra argument and
   self-store, and restored the particle-list reload after alignment.
8. Early allocation-result assignment and released resource-index lifetime:
   removed result spills and restored a non-saved copy counter.
9. Explicit header and main-object pointer lifetimes: the retained best,
   719 words, 538 differences, `0x90` frame, 99 relocations and 47 exact identities.
10. Ordinary word-copy loop: did not expand under the configured preset;
    regressed to 697 words, 702 differences and 107 relocations. Restored attempt 9.

The remaining early header hunk begins `+0x1B4`: field-load/store scheduling
and the floating-point latency gap differ. Later residuals include the cursor
and allocation-size saved-register roles, the explicit copy tail, and nested
pointer-fixup carriers. Workbench reports `structure-mismatch`, heuristic owner
`cfe-spelling`, and **`lever: none-known`** because matching frames alone do
not provide home, temporary-ring or line-order evidence. Reopen with a source-
line/UGEN scheduling trace for that first header hunk, audit its load/store
ordering, and stabilize its opcode schedule before further allocator edits.
Do not repeat the exhausted flag lattice or plain-copy-loop hypothesis.

All meaningful source, object, score, first-mismatch and rationale artifacts
are retained locally under ignored `build/objects-big/590c/`, with the ten
attempts separated and the original fallback target preserved. The handoff
was created with `tools/finalize_plateau.py`; final proof and validation receipts
are retained in the same ignored directory.
This unit changes the guarded candidate and its texture-loader declaration in
`src/main/objects.c`, its tier-D boundary line in `symbol_addrs.us.txt`, and
this handoff shard. The preceding exact control-dispatch promotion is committed
as `c6fba089e789b5446724f657724546ee0f1b0282` and remains independently
ROM-proved. No toolchain flag or canonical matching status changes here.

Commands used for the retained result and handoff include:

- `MICKEY_BUILD_JOBS=3 nice -n 15 tools/wb_compare.sh --summary-json func_8000590C`
- `tools/finalize_plateau.py` with the metrics recorded above
- `nice -n 15 gmake -j3 verify`
- `MICKEY_BUILD_JOBS=3 nice -n 15 gmake -j3 check-nonmatching-builds`
- `gmake cleanroom`, `gmake check-docs`, and `gmake check-scoreboard`


#### 2026-09-11, lane f10-mid: region boundaries do not split the asset web

Still 150. Frame and all twenty homes are exact on both sides (re-measured).
The dispatch asked whether [L115] (reuse an existing carrier to import its
interference) reaches the asset-pointer web. Reading the prior pass again: the
`asset` local already exists and was already reused for the tail sites, which
is the L115 form, and it cost an extra pointer load (structural 19 to 72,
delta +8). The untried zero-width instrument was a uopt region boundary
([L97]) between the body reads and the tail reads of `object->unk40`, so the
expression web would be formed in two regions. Five placements were measured,
`do { } while (0)` around lines 2078-2086, 2087-2126, 2063-2086 and
2095-2100, and `if (1) { }` around 2078-2086: every one is byte-identical at
150. A region boundary does not partition a CSE expression web here; only a
symbol boundary does, and every symbol boundary costs a load. The remaining
lever is a source form in which the tail's reads are a different expression
at no extra instruction, which has not been found.


## 2026-09-12 (lane `p12-close`): the surplus-load reading is refuted, and the pair is worth 159

Nothing adopted; 99 is still the best form. Baseline re-measured on arrival and
it reproduces the previous packet exactly: 99 masked of 719 words, size delta 0,
byte-exact 661, register naming 47, immediate only 2, really different 12,
first structural +0x6D4.

### The nested read is two IR names on purpose, and merging it costs an instruction

The previous packet read the `unk4C` fixup as carrying "one surplus load":
the candidate re-reads `*(relocated + 0x1C)` where the target loads it once.
That is measurable directly, because the two reads are spelled with *different*
casts -- `*(s32 *)` in the test and `*(u32 *)` in the body -- and under L131 two
spellings are two ranges and therefore two loads. Making them one spelling
merges them into one load. Sixteen forms measured, every combination of `s32`
and `u32` at the test, the body read and the store, with and without an outer
`(u32)`:

  - any form whose test and body read use the SAME width is 289 masked at size
    delta **-4**, byte-exact 540, naming 138, immediate 12, really different 30.
  - any form whose widths DIFFER -- the shipped one included -- is 99.

Delta -4 is the whole finding. Removing that load does not remove a surplus; it
makes the candidate one instruction **short** of the target. So the target has
two reads there too, and "the target loads it once into a0" cannot be what the
+0x7A0 region needs. The cast mismatch in the shipped source is load-bearing and
should not be tidied.

### Deleting the `relocated` local, the previous packet's named lever, is measured and regresses

The packet's next lever was "the form to find deletes the `relocated` local and
names the nested read". Fifty-six forms: three spellings of the pointer
expression (`(u8 *)object->unk4C`, `(u8 *)(u32)object->unk4C`,
`(u8 *)(s32)object->unk4C`) crossed with seven carriers for the nested value
(`offset`, `index`, `loadType`, `resultSize`, `size`, `packetId`, a fresh
`s32`) and with the tail `loadFlags & 0x200` block either keeping or dropping
its own `relocated`. Every one is worse:

  - leaving the nested read as the expression: 199 with the tail respelled, 200
    with it kept, size delta 0, byte-exact 560, naming 148, immediate 2, really
    different 12.
  - naming the nested read in any of the seven carriers: 289 for all of them,
    identically, across all three pointer spellings and both tail modes.

Deleting the local costs a reload of `object->unk4C` at every use, and the
naming bucket triples rather than falling. The direction is closed as stated.

### Pairing the copy-loop preamble with the merged read reaches structure 12 to 9, at 159

The two named halves do compose, and the pair holds size delta 0. Measured
alone and together, with the preamble spelled
`if ((size & 3) != 0) { loadType = size & 3; ... }`:

  - preamble alone: 250, size delta +4, byte-exact 630, naming 46, immediate 18,
    really different 27.
  - merged read alone: 289, size delta -4.
  - both: **159**, size delta 0, byte-exact 560, naming **151**, immediate 1,
    really different **9**.

Against the baseline's really-different 12, the paired form is the only shape
measured here that is structurally closer to the target, and it pays for it with
104 naming rows. Seven preamble spellings were crossed with three nested-read
forms (21 combinations): four preamble spellings reach 159 with either merged
read and the other three stay at 99 or 289, so 159 is the floor of that lattice.

### The 104 naming rows are not the L109 probe ratio, and not statement order

Two candidate causes tested and both excluded.

The probe count first. The preamble edit adds a reference to `size`, which is
exactly the quantity the six OR-with-zero `size` probes were tuned against, so
the obvious reading is that the ratio flipped back. It did not. Sweeping the probe
count from 0 to 24 in four shapes -- baseline, preamble only, merged read only,
and the pair -- every one of them steps once at n=4 and is then flat to n=24:
baseline 138 then 99, preamble 291 then 250, merged 328 then 289, pair 200 then
159. The axis is saturated in all four, so the paired form's naming is not a
`totalsave` ordering the probes reach.

Nor does a probe on any other web. Holding `size` at its four-probe floor and
adding 0 to 12 probes to each of `offset`, `index`, `loadType`, `resultSize` and
`packetId` in turn, in both shapes -- 156 forms -- every one of them is either
inert or catastrophic and none is in between: `loadType` is flat at the shape's
own number throughout, and `offset`, `index`, `resultSize` and `packetId` hold
the shape's number at one probe and then jump to between 543 and 715 at two and
stay there. So the L109 axis is exhausted on this function in both shapes, not
merely saturated on `size`. (The same probe applied to `cursor`, which is a
pointer, is worth zero at every count: it is copy-propagated like the scalar
forms L109 already records.)

Then statement order. L146 says a statement-order optimum belongs to the shape,
so the previous packet's "header block order, exhausted by move-one at 99" is
void for the paired shape and had to be re-climbed. `tools/blockclimb.py` on the
paired form, guarded, 325 compiles, one pass: **159 -> 159, no move improves.**
It is a move-one fixed point on the new shape too.

So the paired form's 151 naming rows are a colour question that neither the save
ratio nor the emission order reaches, and the next instrument for it is the
allocator records on the paired object rather than another spelling.

Do **not** re-run: the sixteen cast spellings, the fifty-six `relocated`
deletion forms, the twenty-one preamble-by-nested-read combinations, the
probe-count sweep in any of the four shapes, or the statement-order climb on
either shape.

Validation: `gmake verify` printed 507341c0a40ca3e9a7cee969b396ee53facfb548.
The candidate is unchanged and remains `NON_MATCHING` and GLOBAL_ASM-backed, so
no bytes are credited.

#### 2026-09-13, lane f1: copy-index guard and tail flag identity

Fresh configured baseline: 2876 bytes, 719 words, zero size delta, 99 raw and
masked differences. The first masked difference is +0x6C8 (immediate); the
first structural difference remains +0x6D4. Aligner buckets are 661 exact,
47 naming, two immediate and twelve structural including six gap words.
The paired map has six structural rows, candidate-only +0x7A0, +0x984 and
+0x994, and target-only +0x6DC, +0x998 and +0x99C. Both frames and all twenty
stack homes agree at frame 0x90. Named Ucode maps procedure 21 of 65. The
census records 227 draws and 1099 emission records, with stock/instrumented
full-TU text identity on every probe. The complete colour landscape and all
prior copy/nested-read/ratio/order receipts were read without repeating them.

Moving the second copy-index zero initialization inside the nonempty-copy
guard preserves values: the index is used only inside that guard thereafter.
It moves two emission records onto the guard line, but changes no draw count
or order. At exact geometry it scores 102, adds one paired aligned row,
and moves the missing copy-preamble word from +0x6DC to +0x6C4. The later
windows are unchanged. This guard placement does not supply the missing draw
or repair the copy setup.

Changing only the rollback test's low-bit mask literal to unsigned changes
the repeated expression's identity while preserving its truth value. Both
flag-test lines move: the earlier insertion test gains one draw and the
rollback test gains two. Total draws rise to 230 and emissions to 1100, with
a changed sequence. The result grows eight bytes and regresses by 68 paired
aligned rows, primarily in the late windows; its positional score is 177.
There are two additional candidate-only words. Thus this expression split
has a directly measured draw and geometry cost, not a free ratio adjustment.

Changing both low-bit literals to the same unsigned form is the sharing
control. It restores all 227 draws, all 1099 emission records, every line's
counts, draw order, object bytes and the original aligned map exactly. The
split, rather than unsignedness itself, causes the preceding movement.

The guarded 99-word baseline is restored. Stop early under ADR 0018: the
copy-index move does not change draws, the isolated flag split regresses, and
the shared-type control is byte-inert. Prior nested-read, preamble-pair and
ratio/order evidence excludes the other identified routes in this packet.
The remaining questions are the copy-preamble transfer, nested-read ring
schedule and tail address/flag exchange, with their distinct source contexts.
These measurements do not prove general source impossibility.

Sources, objects, scores and census/aligned differentials remain ignored under
build/f1/func_8000590C; named mapping is under build/f1/objects-map. Commands:
configured stock/capture compile, allocator_trace_receipt.py --map-only,
align_symbol.py, frame_census.py, draw_census.py profiles/comparison,
residual_map.py --object --against, finalize_plateau.py and tools/gates.sh.
No new matching bytes are credited.


#### 2026-09-13, lane h1: authenticated schedule baseline and early evidence stop

A fresh configured compile reproduces 719 words, 2876 bytes, frame 0x90,
99 raw/masked differences and first +0x6C8. All twenty observed stack slots
and traffic agree. Static relocations are 99 on each side with 97 matching
tuples. Paired aligned buckets are 661 exact, 47 naming, two immediate and
six structural rows, with three candidate-only and three target-only words.
The ordinary aligner includes those six gaps in its twelve structural rows.
Named Ucode maps procedure 21 of 65. The census independently reproduces
227 draws and 1099 emission records, with full-TU stock/traced section,
symbol and relocation fidelity passing.

The entire shard was read, including the exhaustive colour landscape,
the corrected nested-read interpretation, the paired-preamble controls,
and f1's copy-index, isolated flag split and shared-type differentials.
Those measured controls already cover the identified copy, nested-read,
flag-sharing and ratio/order levers. No new hypothesis was identified that
changes a remaining decision variable rather than repeating their inputs.
ADR 0018 early evidence stop therefore applies with zero new source attempts.
No colour sweep or known-flat source lattice was repeated.

The guarded body remains unchanged. This is baseline confirmation, not a
new schedule-axis exhaustion or byte credit. The copy-preamble transfer,
nested-read schedule and tail address/flag exchange remain separate open
questions. Private source, object, census, frame, relocation and aligned
receipts remain under build/h1/func_8000590C. Commands: configured compilation,
allocator_trace_receipt.py --map-only, draw_census.py --save, residual_map.py
--object, workbench fidelity, finalize_plateau.py and tools/gates.sh.


#### 2026-09-13, lane `j1`: baseline confirmation against closed controls

Current full-TU evidence reproduces 719 words, zero delta, frame 0x90,
99 masked words and first +0x6C8. Alignment is 661 exact, 47 naming, two
immediate and twelve structural rows including three candidate-only and three
target-only words. Static tables both contain 99 records with 97 matching
tuples. Authenticated procedure 21 has 227 draws and 1099 emissions;
stock/traced full-TU text agrees.

The recorded colour landscape, corrected nested-read interpretation and
later copy-index/flag-sharing draw differentials were read. In particular,
the nested read's signed/unsigned split is load-bearing; the proposed
carrier deletion and paired preamble controls were already measured and
rejected. The latest copy-index edit moved emissions without draws, and the
isolated flag split added draws and width while its shared-type control was
byte-flat. These receipts cover the identified inputs; no new independent
schedule hypothesis was found. Stop early under ADR 0018 with zero new
material attempts. No colour sweep or known-flat control was repeated.

The original guarded candidate remains unchanged. This confirms a baseline,
not source impossibility or a new axis exhaustion. The copy-preamble transfer,
nested-read schedule and late flag/address exchange remain separate questions.
Private source/object/census and aligned evidence are retained. Commands:
assignment gate, configured stock/traced compilation, procedure-authenticated
draw census, residual_map.py, finalize_plateau.py and tools/gates.sh. The ROM
still uses the assembly fallback; no new matching bytes are credited.
#### 2026-09-13, lane k1: authenticated draw-census follow-up

Fresh configured stock compilation reproduces 2876 target bytes,
size delta 0, 99 raw and
99 masked differences, first +0x6c8.
Candidate frame is 0x90; the procedure-21 census records
227 draws and 1099 emission records. Stock and traced full-TU text
compare identically. Static relocation counts are 99 candidate and
99 extracted target, with 97 identical offset/type/symbol tuples.
These are static measurements; overlay runtime identity proof remains separate.

Paired alignment reproduces 661 exact, 47 naming, two immediate and six
structural rows, plus three candidate-only and three target-only words. The
ordinary aligner includes the six gaps in its twelve structural rows. All
twenty observed stack homes and their traffic agree at frame 0x90.

The entire shard was reviewed, including its corrected nested-read analysis,
paired-preamble controls, full colour landscape and later draw differentials.
The signed/unsigned nested-read split is load-bearing. Deleting its carrier,
merging its reads, pairing the copy preamble, changing ratio weights and
reclimbing statement order are already measured. The isolated late flag split
adds draws and width, while the shared-type control is byte-flat. No new
independent source-schedule hypothesis was identified beyond these controls.
ADR 0018 early evidence stop therefore applies with zero new source attempts;
no colour sweep or known-flat source probe was repeated. This confirms the
baseline, not general source impossibility. The remaining copy-preamble,
nested-read ring order and late flag/address exchange are still separate
questions requiring a different source mechanism.

The retained C remains guarded by its original assembly fallback. Sources, stock
and traced objects, frame and scalar measurements, draw profiles and aligned
maps remain ignored under build/k1/func_8000590C.
Commands: lane_status.py, configured stock compilation, draw_census.py,
residual_map.py --object/--against where compared, finalize_plateau.py and
tools/gates.sh. No executable bytes are newly credited.

<!-- plateau-handoff:func_8000590C:end -->
