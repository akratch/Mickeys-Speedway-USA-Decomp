<!-- plateau-handoff:func_80006534:start -->
### `func_80006534` plateau handoff

- source: `src/main/objects.c`
- score: 17/205 words
- frame: 0x38
- relocations: 7
- first mismatch: +0x11C
- summary: Bitfield decoding adds a capture draw but loses one word; ghost identity and local mask/load schedule remain at the guarded 17-word baseline.

The configured full-TU baseline was 207 words against 205 owned words,
198 raw and relocation-masked differences, first mismatch +0x0, and a
0x48-byte frame against the target's 0x38. The retained candidate is 205
words (820 bytes), with 142 exact words and 63 raw/masked differences,
first mismatch +0x38. All seven relocation count/type/offset/identity
comparisons are exact. Instruction opcodes, scheduling and frame size are
exact; this is compiled NON_MATCHING C, with no new matching credit.

The assignment gate returned base-only. The near-match oracle's closest
result was JFG `func_8000710C` (904 bytes, Jaccard 0.145); its available
source remains assembly-only, so no donor body was adopted. Rescoring the
53 usable objects from the TU's retained flag lattice gave no improvement
over configured O2/mips2. No shared compiler flags changed.

The source now reloads the actual loop bounds, retains the sprite frame
count across the random call, treats the stored object count as unsigned,
and models the resource list as its count, entries and trailer. Decoding
both packed descriptor indices before storing the sprite flags removed
duplicated extraction and reached the exact instruction layout. Reading
those indices after the store would add an alias-sensitive reload; that
experimental spelling was rejected and is not the retained candidate.
The cleanup sprite loop deliberately preserves the target's unchanged
sprite pointer. No extra null guard or reordered call was invented.

The campaign attempt count is global across this lane. Attempts 9–14
reduced the residual through loop-bound, flag-test and count-lifetime
corrections; attempts 15–20 tested signed flags, narrow indices, aliasing,
register qualifiers and split floating-point updates. Attempt 21 reached
63 differences and exact layout. Attempts 22, 23, 25, 26 and 27 then stayed
at exactly 63 differences with the same frame, layout and relocation
identities: raw-flag capture order, count declaration order, a typed list,
consistent unsigned flag fields, and byte-width frame count produced no
further change. Attempt 24 regressed to 68 differences. These provide the
five unchanged residual probes for this packet's stopping rule. Attempt
25 is retained because it gives the list a direct, evidenced type without
additional experimental flag or count types.

A three-minute report-only permuter batch failed before searching: its
isolated function recipe copied an unrelated whole-TU `.rodata` symbol
addition into a scratch object without that section. It produced no search
result and does not establish permutation exhaustion. The failure, all
material source/object attempts and scalar measurements remain in ignored
build artifacts. This technical failure was not counted as an attempt.

The workbench verdict is `allocation-mismatch`; its next playbook is
`register-role-audit`. The next concrete lever is to establish the competing
saved-register lifetimes of the sprite cursor and frame-count/resource-list
cursor, then use a bounded allocation search with a seed proved identical
to the configured full TU. Do not infer an allocator pool from the visible
register rotation alone. The current temporary flag/extracted-index webs
also differ, so fixing only one saved-register pair may not close it.

Validation: configured `wb_compare.sh --summary-json`, workbench diagnosis,
retained flag-object rescoring, `gmake verify` with the expected US ROM hash,
`gmake check-docs`, `gmake cleanroom`, and `tools/finalize_plateau.py`.
The canonical ROM proof covers the assembly fallback, not this nonexact C.


## 2026-09-11 (lane `lane/w3-mid`): all three residuals named and priced, none adopted

Unchanged at 63 words. Aligned: 142 byte-exact, 63 register naming, 0 immediate
only, 0 really different; size delta 0. A pure allocation residual over an
instruction stream that is already exact, which is why `frame_census` was not
needed and why every structural axis is retired by construction.

The allocator records below come from the instrumented uopt with its `.text`
checked byte-identical to the tree's object. This procedure is globalcolor
invocation 23 of `src/main/objects.c`, identified by emission order, and it
emits p1 records only, so the call test in the lane brief holds again and no
p2 or L106 axis exists here.

The register-substitution census partitions the 63 words into exactly three
independent regions, and forcing them in turn prices each one.

Region 1, worth 27 words (63 to 36): s2 becoming s1 on 22 sites and s1 becoming
s2 on 12. p1 web 3 is `sprite`, totalsave 164 over nocs 14, save 11.714286; p1
web 41 is the strength-reduced `list->entries` induction pointer, totalsave 62
over nocs 5, save 12.4. The decision variable is which save is larger, and the
target wants `sprite` decided first. This was also reproduced from source:
adding one loop-depth reference to `sprite` raises it to 184 over 14 and the
exchange flips with no force at all. The probe used for that -- advancing the
cursor in the cleanup sprite loop -- is refuted as the real source form,
because the target's loop tail is a branch-likely with a reload in its delay
slot rather than a cursor increment, so the probe costs two structural words and
lands at 38. The exact requirement is one more loop-depth reference to `sprite`
at zero instruction cost, or the induction web's totalsave cut from 62 to 58 or
below.

Region 2, worth 8 words (36 to 28): a0 becoming v1 on 7 sites, a1 becoming a0 on
5, a2 becoming a1 on 5. p1 web 77 is a phantom. It is decided first on save 50
over nocs 1, takes v1, and emits no register anywhere -- forcing it to a2 and to
a3 produces byte-identical objects, which is the proof. It is the common
subexpression of the repeated `record->unk8` read, and it sits ahead of `flags`
(web 74, totalsave 40 over nocs 2), `reference` (web 79) and `frameOffset`
(web 85), pushing all three one lane down the caller-saved pool. Displacing it
puts those three exactly where the target has them. Every source spelling that
lowers its weight also moves the instruction stream: naming the shared temp
removes web 77 and hands v1 to `flags` but costs 26 structural words and four
bytes; reading `reference` from the `flags` local drops web 77 from 50 to 40 and
costs 11 structural words; reading `frameOffset` from `flags` changes nothing at
all, which shows that read is already folded into the same web.

Region 3, the last 28 words: with regions 1 and 2 forced, the entire remainder
is a permutation of the integer temp ring, and no globalcolor force anywhere in
a c1 to c22 sweep of web 77 touches any of it. The stream is byte-exact through
+0x120 and the two ring states diverge from +0x124 onward, so the cause is a
single ring pop before +0x124 -- the L127 family, reachable with the ugen
free-list hooks and their source-line stamps, not with uopt's colour records.

Retired by this measurement: the whole structural family, the frame, and any
reading in which these 63 words are one problem rather than three.

## 2026-09-12, lane p19-reopen: one region closes 44 words

Before: 142 exact, 63 naming, zero immediate, zero structural; after: 186
exact, 19 naming, zero immediate, zero structural. Both are 205 words with
frame 0x38, zero displacement tax and seven exact relocation identities.
The first mismatch moves from +0x38 to +0x11C. An empty region immediately
after both packed-field captures and before the sprite flag store is the
only adopted edit. It changes no value, access order, call or executable size.

The configured stock compiler re-proves the result. The baseline instrument
was gated against the full configured TU, retaining all per-file flags.
Ordinal 23 still emits p1 only. On the adopted shape, sprite web 3 has
totalsave 174 over nocs 14, save 12.428572, against the unchanged list cursor's
62 over 5, save 12.4. It therefore takes s1 before the cursor takes s2.
The original sprite total was 164: this region supplies the missing weighted
reference at zero emitted width. The inherited claim that further structural
source changes were retired does not survive this experiment. L142 is inert
for this saved-register pair; L139's total-versus-cost rule remains satisfied.

The region also changes the unused packed-read web 77 from total 50 to 40
and the flags web from 40 to 50 over two components. Web 77 still wins v1,
leaving flags/reference/frameOffset one caller-saved colour too high. An
accepted force of web 77 to c5 gives eight naming differences at delta zero;
the decision and colour rows record forced=5. A c13 force is also accepted
but costs a word and is rejected. This force is diagnostic, never matching C.

The eight forced residual rows occupy +0x124 through +0x164 and are four
producer/consumer pairs: the signed scale-field load, the two packed-index
masks, and the sprite-byte copy. The instrumented ugen trace names the exact
source lines and draw sequence. The effect is local; the later ring agrees.
The next lever must remove or displace the unused packed-read web while
preserving this instruction stream, then change those mask/load draws.

Measured against the correct definition: unsigned typed field reads either
leave the object unchanged or remove a word; signed shifts change four
structural rows. Mask-before-shift adds a word. Separate loop index identities
regress or enlarge the frame. Moving sprite initialization later regresses.
Regions before either packed-field capture add a word; the boundary after
both is the 44-word gain. On that new shape, reading the fields from flags,
adding earlier boundaries, or moving the store before the captured-field
extractions is nonexact. Splitting each mask into a later assignment is
text-inert in four positions. Three final identity-operation variants on the
index locals are also text-inert, with no new decision information: ADR 0018
stall. Preserve the 19-word guarded candidate.

A harness initially selected an earlier prototype and ran ten baseline
repeats. Those are not negative evidence. The boundary selection was fixed,
every mutation was checked against the actual body, and all probes above
were rerun. The shared activity count was kept monotonic. External private
scratch retains sources, objects, scores, configured commands, decisions and
the ugen trace. Commands include residual_map, align_symbol, register_census,
frame_census, direct configured comparisons and the final plateau tool.


### New-shape order check: 19 to 17, final retained source

The region invalidates the old order closure (L146). A guarded move-one climb
ran 186 evaluations to a fixed point in two passes and swapped only the two
independent packed-field captures. Both still precede the sprite flag store;
no alias-sensitive read or call crossed that store. Final buckets are 188
exact, 15 naming, zero immediate and two schedule rows, with 17 masked and
raw differences, 205 words and first +0x11C. This is the retained source.

On this shape, a signed flags carrier gives 27; unsigned descriptor storage
and consistent signed reads are text-inert. Narrow or unsigned index locals
add 8 to 40 bytes. Forwarding frameOffset from flags adds one word; forwarding
reference from flags is text-inert. These controls do not improve the new
optimum or introduce a further route through the phantom web. The earlier
19-word object, its accepted force receipt and every new control are retained
separately. No unforced object is exact.

The four default gates passed on the region-only source; verify, cleanroom
and check-docs were rerun for this final order and evidence update. The ROM
proof covers the guarded assembly fallback and gives no matching credit.

### 2026-09-12, p23-lastmile2: exhaustive ghost-web landscape and source controls

The retained 820-byte baseline has 188/205 masked words exact, frame 0x38,
seven relocations and first mismatch +0x11C. Aligned buckets are 188 exact,
15 naming, no immediate and two structural rows, with no unmatched offsets.
The configured command retains this TU's loop-unroll override. Named Ucode
maps procedure 23 of 65; the full-TU stock and captured text agree.

All 22 coloured webs were sampled at every alternative same-kind colour:
155 accepted probes, 126 retaining size and 29 changing it. The COMPLETE
improving winners list is p1:w77=c4, p1:w77=c5 and p1:w77=c6. Each scores
14 masked differences at delta zero and has the same blast radius: four
removed in the +0x100 window, one added in the +0x180 window. These are rival
colours of one web, not additive handles. The representative singleton lattice
p1:w77=c4 independently passes stock/instrumented fidelity and measures floor
14. Its aligned residual is 12 naming, one immediate and one structural row.
No forced result is accepted as matching C, and this floor does not bound
arbitrary combinations of individually regressing forces.

The decision records retain the unused packed-read web at total 40, divisor 1,
save 40, colour 2. The emitted flags web has total 50, divisor 2, save 25,
colour 3. The two captured indices each have total 30, divisor 3, save 10.
Thus a source spelling must remove or demote the unused read without losing
the already-correct saved-register exchange. Merely moving it to a different
colour still leaves local load/mask allocation and schedule differences.
The prior reference-first shape remains a useful separate diagnostic: its
unforced score is 19 and its historical forced floor is 8. The retained
frameOffset-first form has the better source score, but its singleton force
floor is worse. These are different source baselines and must not be pooled.

Four bounded source-control families preserved every object and source:

- 24 flags occurrence/reassignment controls: 21 stay at 17, three regress to
  23, all at delta zero. Discarded OR/XOR-zero and algebraic reassignment do
  not transfer the exact strip-cursor mechanism to this flags web.
- 25 packed-load identity controls: 15 stay at 17 and ten remove a word,
  scoring 134. Unsigned typed aliases and algebraic field identities do not
  provide the desired zero-width allocation change.
- 32 literal/order controls: all 16 frameOffset-first forms stay at 17 and
  all 16 reference-first forms stay at 19. Signed/unsigned shift literals
  and mask literal types do not change either basin.
- 12 explicit packed-local controls, covering signed/unsigned carriers,
  separate/chained assignments and both capture orders, all add one word
  and score 135. Naming the common read repeats the known carrier-width cost.

The final controls provide no improved residual or new allocator identity;
the successive flags, typed-read and literal controls stall, and the explicit
carrier check only reproduces the already-known extra-word mechanism. Stop
under ADR 0018. Preserve the 17-word guarded body, not a forced object or a
larger diagnostic. The next concrete question is a source mechanism that
changes the ghost read's allocation identity while preserving the region's
saved-register pricing; none of these measured forms supplies it.

Private build/p23/func_80006534 evidence retains the baseline, procedure
mapping, all force cells, aligned maps and 93 source controls. The independent
lattice lives in the lane's private temporary evidence directory. Commands:
configured stock/capture compilation, residual_map.py, web_footprint.py
--every-colour, force_lattice.py and finalize_plateau.py. ROM verification
covers the guarded assembly fallback and gives no new matching credit.

#### 2026-09-13, lane f1: destination flag and capture-line differential

Fresh configured baseline: 820 bytes, delta zero, 188 exact, 15 naming and
two structural aligned rows, no gaps, first +0x11C. Named Ucode maps procedure
23 of 65 after the disjoint resource-release promotion in this TU. The census
records 49 draws and 307 emission records. Stock/instrumented full-TU text
identity passes for each probe. The existing 155-force landscape and source
controls were read without repeating the colour axis.

Deleting the flags local and testing the already-stored destination flags
preserves the two packed captures before the original flag-store point. It
leaves all 49 draws and their order unchanged, redistributing three emission
records from the removed definition onto the first capture and first flag
test. The result adds four bytes. The aligned paired map regresses by eight
rows overall: naming becomes 14, immediate five and structural six, with two
candidate-only words and one target-only word. This destination-field form
therefore does not remove the ghost allocation at the required geometry.

Folding both packed captures onto the same physical source line moves one
draw and four emission records onto the first capture's line. Total draws,
the draw-register sequence, the full object text and every aligned residual
row are unchanged. This is attribution movement, not a change to their
allocation/emission order, and it does not resolve the scheduling pair.

Both source/object pairs and differential profiles remain ignored under
build/f1/func_80006534. The guarded 17-word baseline is restored. Stop early
under ADR 0018: the destination carrier regresses and the capture-line tie is
inert; the existing packed-identity, literal, region and order receipts cover
the other identified variants. The remaining requirement is a ghost-read
identity change that preserves the saved-register region effect, followed by
the local load/mask schedule. No claim of general source impossibility is made.

Commands: configured stock/capture compile, allocator_trace_receipt.py
--map-only, draw_census.py profiles/comparison, residual_map.py --object
--against, finalize_plateau.py and tools/gates.sh. No new matching bytes.

#### 2026-09-13, lane h1: multiply operand and release-order differential

The configured baseline reproduces 205 words, 820 bytes, frame 0x38,
17 raw/masked differences, seven matching static relocation tuples, and
first mismatch +0x11C. Aligned buckets are 188 exact, 15 naming, zero
immediate and two structural rows, without gaps. Procedure 23 reproduces
49 draws and 307 emission records; stock and instrumented full-TU text agree.
The prior colour landscape and all recorded source controls were read.

One new source attempt reverses the operands of the final multiplication in
the sprite scale assignment, retaining the division subtree and every store
and call position. The hypothesis was that changing this evaluation order
could move the seven draws at the scale-store line and the local residual.
Those seven draw registers and every per-line count remain unchanged.
However, the later plus/minus frame-offset arms and count comparison draw
different floating-point registers. The aligned result regresses to 179
exact, 24 naming, zero immediate and two structural rows at unchanged size,
frame and static relocation tuples. The integer mask/load residual remains.

The census sequence reports this movement correctly even though its
unchanged-count footer says the schedule did not change. Equal counts are
not an order proof. The retained per-line sequence comparison isolates the
movement downstream of the edited expression. This rejects the multiply
operand route for the intended integer mask/load problem, while documenting
that the downstream floating-point schedule is sensitive to it.

Restore the guarded baseline and stop early under ADR 0018: this new probe
regresses and the existing ghost-read, capture, region and line controls
cover the other identified mechanisms. No general source impossibility or
new byte credit is claimed. Source/object pairs, census profiles and aligned
per-window deltas remain ignored under build/h1/func_80006534.
Commands: configured stock compilation, draw_census.py --save/--compare,
residual_map.py --object/--against, finalize_plateau.py and tools/gates.sh.


#### 2026-09-13, lane `j1`: current schedule evidence stop

Fresh configured stock and traced full-TU text agree. The procedure-23
census reproduces 49 draws and 307 emission records. The owned range is
205 words, delta zero, masked 17, first +0x11C; alignment remains 188 exact,
15 naming, zero immediate and two structural rows without gaps.

The complete shard and source controls were read, including the exhaustive
ghost-web landscape and the newer f1/h1 destination-read, same-line capture
and multiply-order differences. They already test every source mechanism
identified in this pass for changing the packed-read identity or local
mask/load schedule. No new causal hypothesis survives those receipts.
ADR 0018 therefore permits an evidence stop with zero new source attempts;
no colour sweep or prior source probe was repeated. This confirms a baseline,
not a new exhaustion of all schedules. The next concrete requirement remains
a different packed-read identity preserving the saved-register region effect.

The original guarded source is retained, with private source/object/census
and aligned-map evidence. Commands: assignment gate, configured stock and
traced compilation, draw census, residual_map.py --object,
finalize_plateau.py and tools/gates.sh. Full-ROM verification covers the
assembly fallback; zero new executable bytes are credited.
#### 2026-09-13, lane c2: packed bitfield decoding differential

Fresh configured baseline: 205 words and 820 bytes, frame 0x38, 17 raw/masked
differences, first +0x11C; seven identical static relocation tuples. Alignment
is 188 exact, 15 naming, zero immediate and two structural rows, without gaps.
Procedure 23 has 49 draws and 307 emissions; stock and instrumented full-TU
text agree. Existing colour, typed-read, region, capture-line and operand-order
receipts were read without repeating those sweeps.

One new source probe describes the packed word with unsigned bitfields: four
high flag bits, six reference bits, six frame-offset bits, then sixteen low
bits. Both captures remain before the sprite flag store. The reference-capture
line moves from one draw to two, and four emissions to six; the flags capture
loses one emission. Total draws become 50 and emissions 308. The object loses
one word, reaching 204/205 words and 136 masked differences, with 162 exact,
29 naming, four immediate and 11 structural aligned rows. This is a new
measured packed-read identity response, but it does not preserve the target
instruction inventory or improve the retained residual.

Whole-TU checks found no shared improvement: this probe leaves func_80007118
at its 186-word baseline, and the case-carrier probe in that function leaves
this function at 17. Their owned residuals and draw profiles were measured
separately from the full objects. No shared flag or header changed.

Restore the guarded incoming body. Stop early under ADR 0018 after this
regressive identity probe and the inherited negative controls; the available
packed-field mechanisms do not supply the missing width-preserving identity.
The next question remains a ghost-read identity change preserving the saved
register effect, followed by the local mask/load schedule. No general source
impossibility is claimed. Private build/c2 retains every source, object,
profile and aligned map. Commands: assignment gate, configured compilation,
draw_census.py, residual_map.py, finalize_plateau.py and tools/gates.sh.
The full-ROM check covers the assembly fallback; no new matching credit.
#### 2026-09-16, lane lm-c: the region opener's copy temp is what holds v1

Baseline reproduced at 17, delta zero, first +0x11C. The target holds the
packed flags word in `v1`, computes the reference field before the frame
offset, and colours them `a0` and `a1`; ours takes `a0`, `a1`, `a2` and
never `v1`, which is free in the loop body. The procedure-23 census names
the occupant: web 77, a u32 single-member expression temp confined to the
block where the carriers are born, coloured `v1`, with no instruction in
the output. It is created after `reference` and before `count`, where the
`if (1) { }` opener sits: the boundary creates a copy of `flags` for the
store beyond it, the copy is deleted, its colour is spent. Four cycles,
47 cells: shift order and shift source in every field and local typing
(17 or worse, reference-first 19 throughout); the opener's position (17
only where it is or after the store, +4 elsewhere, 61 removed); loop 1
with a declared cursor and loop 2 indexed (+4 to +44). Next: the region
exists only to rank the sprite web above loop 1's induction pointer; find
the zero-instruction form that does that without a region, and the copy
temp and `v1` are free for `flags`.
#### 2026-09-16, lane nx-b: the ghost is uopt's CSE temp, and block-set interference bounds it

Baseline reproduced at 17 masked, delta 0, first +0x11C; procedure 23. Six
batches, 36 cells, none below 17; the source is unchanged. What the cycles
establish, read off ugen's listing and the procedure's records:

- The ghost web 77 is the CSE temp of the three `record->unk8` reads: ugen
  emits the load into it, a copy into `flags`, and the two field shifts from
  it; as1 deletes the copy by renaming the temp into `flags`'s register, so
  the surviving register is the copy destination's (a0), where the ROM's is
  v1. It is not the region opener's copy: without the region the load still
  lands in a0 (63, with the sprite/cursor swap back, first +0x38).
- Both webs are born in block 17 and interference is block-set intersection,
  so `flags` can never share v1 with the temp whatever the statement order:
  writing `flags = record->unk8` after both captures is byte-identical to the
  base (17), reference-first 19. The lever is the save ranking (temp 40 over
  `nocs` 1 against `flags` 50 over 2) or the temp's existence.
- Reading the fields from `flags` removes the temp and puts the load in v1,
  but uopt then sinks both extractions into the two else-arms as
  rematerialised shifts (136, +4), in every capture order and with the
  region before, between or after. A load-derived temp is what keeps them
  at the top.
- Declared `s32 flags` is 27; `u32` on the record field with or without the
  cast is byte-identical (17); `register` is inert. Types do not make the
  temp.
- Deleting `flags` and testing through the stored `sprite->unkC` is 128/137
  (+4) with the region and 43 at delta 0 without it (head exact, load in a1,
  `move a0,zero` hoisted); reading the captures back from the stored field
  is 17 with a fresh reload/CSE pair in a0/v1. Tests reading `record->unk8`
  reload after the sprite stores (134-144, +4/+8).
- Moving the scale, unk5 and count statements above the flags load, in any
  subset, is 129-140 at +4.

Next: the ROM's decision is `flags` coloured before the temp, which needs the
temp's save under 25 or `flags`'s over 40 without moving either out of block
17. A discarded read of `flags` inside block 17 is propagated to the temp;
the untested cell is a self-defining read (flags assigned its own or-with-zero, the form that
moved the overlay 34 height web this lane) placed after the store in block
18, which adds a def and a use to `flags` at zero width, and the same on the
temp's side is impossible because it has no name. Price it from the records
before believing the score.

#### 2026-09-16, lane w1-a: the sinking gate located, five cycles at 17

Baseline reproduced at 17 masked, delta 0, first +0x11C, aligned
188 / 15 / 0 / 2; the private direct harness (nm_ranking's comparator
over the configured command, `-Wo,-loopunroll,0` included) read the same
17. Five batches, 39 cells, none below 17; the source is unchanged.
Report: [lastmile-web-membership.md](../lastmile-web-membership.md).

- nx-b's named step is measured inert: a self-defining read of `flags`
  after the store, before the store, before the chain, after the unk5
  store, spelled as or-with-zero (compound and long form), and-with-all-
  ones, xor-with-zero and shift-left-by-zero, is 17 in every cell; two
  such reads are 23. L135 holds on
  this web; the o034 mechanism does not transfer.
- The from-`flags` sinking is not a two-definition artefact: `reference =
  flags >> 22; reference &= 0x3F` (and the same for `frameOffset`) in six
  orders and placements is byte-identical to the one-expression form
  (136, +4). Regions around the chain, around the captures, or around the
  count and the chain do not block it (143, +4). `u8`/`u16` fields add
  masks (+44).
- The gate: `frameOffset` from the temp with `reference` from `flags` is
  byte-identical to the base (the `flags` read folds into the temp);
  `frameOffset` from `flags` with `reference` from the temp sinks (134,
  +4). The forwarding fires exactly when the operand is a symbol defined
  by a load with no CSE temp behind it. `register` on the fields, `s32
  flags` with casts, `u32` fields, the captures placed after the unk5
  store, and `flags &= 0xE0000000` after the store are all inert on that
  (136-144, +4 to +12).
- No `flags` local, all reads through `record->unk8`, the store typed
  `u32` through a cast: a second load of the record word is emitted (+4);
  the plain store the same (137, +4); the cast on the base's store is
  byte-identical (17). The reload is not a type-based alias decision.

Next: the decision variable is uopt's expression-forwarding gate on a
load-defined symbol, and no spelling, type, placement or region of the
captures reaches it. The ROM's shape is "no temp, forwarding refused".
Look for what makes the forwarding refuse -- a zero-width use of the
fields on the path uopt treats them as dead on (arm 1 or after the
chain), or an instrumented reading of the pass -- rather than another
capture lattice.
#### 2026-09-16, lane s2-a: four cycles at 17, the single-web shape located

Baseline reproduced at 17 (188/15/0/2, first +0x11C); the identity gate
passed on procedure 23. Forty-one cells in four batches, none below 17.

- The packed-word temp (web 77, 40 over 1) is coloured v1 in the records
  and as1 renames it into `flags`'s a0; the ROM's v1 is therefore one web
  with no copy. No `flags` local, everything through the stored
  `sprite->unkC` (forwarded), no region: byte-identical to the base (17),
  with the forwarded value a second web (40 over 2, a0) and the copy
  back. The region is not needed in that form. With the region 40; the
  reference-first order 19 and 42.
- A symbol assigned from the forwarded re-read (`flags = sprite->unkC`)
  sinks the extractions (136, +4), as does every expression-of-load
  definition of `flags` (or 0, xor 0, and ~0, plus 0, shift 0, u32 cast:
  136 with the region, 143 without). uopt normalises them all to a
  load-defined symbol before the pass that sinks.
- Tests reading `record->unk8` directly reload in every store placement,
  including all three sprite stores after the chain (140 to 153, +8 to
  +32), so the reload is not an alias decision on any store.
- Empty-if uses of the fields (`if (reference > 0x3F) { }` after the
  captures, after the count, in arm 1, after the chain, both fields, and
  against `count`) cost 4 to 48 bytes and do not stop the sinking; the
  zero-width empty check that moved func_8000590C's tail does not
  transfer here.

Next: the decision variable is still the sinking of a load-defined
symbol's single-definition expressions into the two arms. The ROM needs
one v1 web from the load through the tests with the extractions at the
top; no spelling of the definition, the captures, the tests or their
placement reaches it. Instrument the pass (the instrumented uopt's
per-pass dumps) rather than spell it again.
<!-- plateau-handoff:func_80006534:end -->
