<!-- plateau-handoff:func_80006B04:start -->
### `func_80006B04` plateau handoff

- source: `src/main/objects.c`
- score: 6 differing words
- frame: frameless
- relocations: 0
- first mismatch: +0x20
- summary: Track B 2026-09-23: 63 at delta -4 to 6 at delta 0; left: +0x7C value is a coloured web (t1), target carries it in ring t9.

Summary before this remeasure: Remeasured 2026-09-23: 63 masked at size delta -4 (78 of 79 words), unchanged; asset/count carrier and late loop registers remain structural.

Summary before this remeasure: Asset/count carrier and late loop register shape remain structural; next lever is a typed source record/output layout.
#### 2026-09-23, lane `B-obj` (Track B): 63 at delta -4 to 6 at delta 0

Reader, cycle 0: an extra-ISTR pair at the flag byte. The target stores the
OR-0x80 value and then the AND-0xBF value, two byte stores; our single u8
carrier let uopt kill the first store, which lost one word and rotated the
loop's temp ring after it.

Spellings, in order, each measured with the configured compile:

- compound OR-assign and AND-assign on the byte: 49, size delta 0 (milestone)
- u8 temp for the +6 halfword, read ahead of the flag: 43 (narrowed to a byte load)
- the same temp as u16: 43 (a declared temp takes v0, not a ring temp; L145)
- no temp, the -5 store moved ahead of the flag: 42
- AND-0xFF on the -5 source: 31 (the flag load then draws t9 as in the target; L149)
- the mask moved onto the flag read instead: 29 (the loop tail is word-exact from here)
- temp_t9 definition at five positions: 29 to 58, none better
- +0x7C as an array member or as arg1 plus one: 29, flat
- a named intermediate for the double dereference: 43 and 51
- one pointer variable for both +0x7C uses: 64
- the two +0x7C uses spelled differently, arg1 + 0x7C at the unk74 store
  and the address of unk74 plus 8 at the loop pointer: 24 (L131: two IR names)
- output assigned before the asset dereference: 16
- temp_a3 as s32 with no cast: 11 (removes a type-4 int web that took t0 and
  pushed temp_t0 down to a3)
- the -0x2B store ahead of the -0x2A zero store: 6
- stop-rule attempts, none better: +0x7C taken from output 9, one variable
  assigned at the top 64 (two placements), the two +0x7C spellings swapped 6

Records (instrumented uopt, text identity-gated against the configured
object, proc 27, p2 only because it is a leaf). On the 29 shape the +0x7C
expression was a type-4 web coloured v0, which pushed output to a3. Forcing
that web to t0 was accepted and scored 24, the number the two-spelling edit
later reached from source. Splitting it costs 12 bytes (81 masked). Forcing
output to v0 is declined: v0 is already in its forbidden mask.

What is left (6 words, all in the head before the first branch-if): the
+0x7C value is still a coloured type-4 web, now t1, and the target holds it
in t9. t9 is in no web's p2cost list on this procedure (the table ends at
t5), so in the target that value is not a globalcolor web at all and no
force reaches it. It is a ring value that survives the two ifs. The target
also dereferences through v0 before output is assigned into v0, where ours
assigns output first. That order is worth 8 words the other way on the
present shape (16 against 24 at the time it was measured), so the two
facts are coupled. Next lever: a form of the +0x7C use that uopt leaves as
a per-block expression rather than a web, with ugen reusing the register
across the fall-through edges.

<!-- plateau-handoff:func_80006B04:end -->
