<!-- plateau-handoff:func_80007C68:start -->
### `func_80007C68` plateau handoff

- source: `src/main/objects.c`
- score: 25 differing words
- frame: 0x60
- relocations: 4
- first mismatch: +0x94
- summary: Remeasured 2026-09-23: 25 masked at delta 0, frame and relocations exact; pure register naming from +0x94, one web the target colours is missing.

Summary before this remeasure: 25 words of pure register naming from +0x94, frame and stack homes exact. The target colours one value this body leaves to ugen, and a full phase-one force sweep over every web against every colour and the split path (522 compiles) never beats 25, which is what a missing web looks like. The ugen freelist trace shows the ring two draws ahead: v1 is held by the frame -12 local's web, and there is a phantom pop on the texture-fetch line. Naming the index costs the frame and still does not make a web, including across an L97 region; 23 region placements never improve.


#### 42 -> 26: three measured facts, and what is left

The frame is `0x60` on both sides, but the locals block is only 32 of those
bytes and the two `s16` carriers take two bytes each, not four. That is why the
`sp58` home read `sp+92` against the target's `sp+88`, and why every earlier
attempt to "add the missing local" grew the frame to `0x68`: the block was
already exactly full. The fix is a declaration census, not an allocation edit --
one `s16` ahead of `sp58` puts the `s32` at 88, and the two stack-home constant
words disappear. Removing the now-unused `temp_lo` and keeping `temp_v0_2` first
is the form that holds the layout with eight declared locals and no padding
local; declaring `sp58` first or second both regress (frame `0x68` in the second
case).

Two spellings then took 40 -> 32 -> 26 with the frame held. `arg1->unk18` is a
`void **`, so the texture fetch is `unk18[(temp_v0 & 0xFF) * 2]` rather than a
byte-offset cast; and the last store's scale product is spelled at the store
instead of carried in `temp_lo`. Both were measured against the real full-TU
object, not a scratch.

What remains is one site, and it is the same shape L88 describes: at row 37 the
target spends a uopt pool colour (`v1`) on `temp_v0 & 0xFF` where the candidate
spends a ugen ring temp, and every later ring draw is rotated by one. Naming
that value is the obvious answer and it is not reachable here: a fresh local of
every width in every one of ten declaration positions costs the frame, and
carrying it in an already-dead local (`temp_t3`), in a dead parameter (`arg2`),
or in `sp58` is copy-propagated away and compiles byte-identically. Also flat:
six texture-fetch spellings crossed with three condition spellings and four line
groupings, four tail spellings, three preamble spellings, the `temp_t3` and
`temp_v0_2` inline forms, and two rounds of coordinate descent over five axes.

Next lever: find the extra pool web without paying a home for it -- either a
value the target names that also *removes* an existing local, or evidence from
an instrumented uopt about which web owns `v1` at that site.

#### 2026-09-11, lane p7-res: the extra web is real, and it is not reachable by a force

The note above says "the target colours one more value than this candidate
does". That is now backed both ways.

**No single phase-one force reaches it.** A full sweep of all 18 phase-one webs
in uopt procedure 36 against all 22 named colours plus c24-c29 plus the split
path -- 522 compiles -- never scores below 25. A force cannot create a web, so
this is what a missing web looks like, and it also extends the older "c1 through
c12 on the four caller-saved webs" sweep to the whole procedure.

**The ugen side says the same thing with a different instrument.** With
`DKWB_UGEN_TRACE=1`, this procedure builds its integer temp ring from t6, t7,
t8, t9, t0, t1, t2, t3, t4, t5 and then removes v0, v1 and t0 -- the three
registers phase one has already claimed. Web 59 owns `v1` (it is the local at
frame offset -12, the one carrying the post-store bump), which is why `v1` is
not in the ring when the texture index is allocated. The target uses `v1` at the
very site where this candidate draws a ring temp, so in the target `v1` is a
phase-one colour on a web we do not have, shared with the -12 local's web, which
do not interfere.

The ring is **two draws** ahead of the target at +0x94, and the trace says why:
one is `v1` being unavailable, and the other is a genuine phantom pop on the
texture-fetch line -- a temp allocated and freed at the next emitted ordinal
that never appears in the object. Both have to go.

Freeing `v1` from source does not work. Removing the -12 local (rewriting its
two statements to use the stack slot directly) does free `v1`, and the ring then
spends it at +0x44 on the argument reload rather than at +0x94, regressing to 39
words with the first mismatch moving to +0x44.

Naming the texture index still costs the frame and still does not become a web,
including through an L97 region between its definition and its use: `s16` and
`u8` carriers declared beside the existing `s16` grow the frame to 0x68, and an
`s32` carrier in the same position holds delta 0 but grows the frame to 0x68 by
another route and scores 39, with the index still a ring temp at +0x94. So the
region does not stop copy propagation here.

**23 L97 region placements measured**: seven are byte-identical to the retained
body, eleven regress to 40, five to 86. None improves. Regions are refuted for
this symbol.

Decision variable: a source form in which the low byte of the record's flag word
becomes a phase-one web **without adding a declared local** -- the frame has no
cell. The `p1cand` list is the check: it must grow from 18 webs to 19 with the
existing saves unchanged.
<!-- plateau-handoff:func_80007C68:end -->
