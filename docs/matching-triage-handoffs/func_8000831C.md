<!-- plateau-handoff:func_8000831C:start -->
### `func_8000831C` plateau handoff

- source: `src/main/objects.c`
- score: 65 differing words
- frame: 0x28
- relocations: 11
- first mismatch: +0x70
- summary: A2-obj 2026-09-23: 62 to 65, forced floor 35 to 24 (u8 decrement, FB store first, address-form arm read); left: tail-block colours, v1/a3 unused.

Summary before this remeasure: Track B 2026-09-23: 81 at delta -4 to 62 at delta 0 (volatile arg6 read before the test, x8 plus x2); ring rotation left.

Summary before this remeasure: Remeasured 2026-09-23: 81 masked at size delta -4, frame 0x28 exact; the seventh-argument home traffic is the structural fact left to attack.

Summary before this remeasure: Still 81 masked at size delta -4, and this pass is a precise negative that redirects the axis. The transfer from func_8005A7A0 is refuted at its precondition: that lead's finding is that the frame is the variable, and here frame_census reports 0x28 on both sides, fourteen slots each, identical ladders, and not one slot either side uses alone. There is no frame cell for a lever to move. The one measured asymmetry is traffic, not layout: the home of the seventh argument is read three times and written once by the target, and read once and never written here, so the target never register-promotes that parameter while this candidate keeps it in a caller-saved register across the whole middle of the function. That is the structural fact to attack. The L100 divisor lever does not reach it. Discarded-expression probes on that parameter, OR-with-zero, AND-with-minus-one and XOR-with-zero, at five placements and applied once, twice and three times, are byte-inert in all of them, masked staying 81 with the slot traffic unchanged, so L109's narrowing extends from an already-read global to an already-read stack parameter. The one probe placed ahead of the first call is not inert and is worse, 100 masked with the frame growing 0x28 to 0x30. Two respellings of the tenfold multiply as an eightfold plus a twofold, the form the target emits reusing the eightfold it already has, leave masked at 81 and move the size the wrong way, -4 to -8, because cfe reassociates them back. Naming the decrement through the existing temp is flat. Six physical-line arrangements of the four-way OR that builds the colour word are byte-identical, so L59's tie-break does not reach inside a single statement. Call test: p1-only, three calls, so the axis is the save ratio and L106 is not available. What is left is 47 naming words carrying a closed three-cycle over t8, t9 and t7, one L127 ring-phase fact rather than 47 colour questions, plus 29 structural words.

#### 2026-09-11, lane `lane/p4-xfer`: the sibling refutes its lead's precondition

Unchanged at 81 relocation-masked words, size delta -4, frame 0x28.
Aligner: 34 byte-exact, 47 register naming, 0 immediate only, 29 really
different. No edit adopted; everything below is a measured negative.

**The transfer from `func_8005A7A0` is refuted at its precondition.** That
lead's standing finding is that the frame is the variable and colour is not,
proved there by an exhaustive forced-colour oracle and a 49-form carrier
lattice that quantises the frame without ever reaching the target's value. The
precondition for that finding is a frame gap. There is none here:
`frame_census` reports 0x28 on both sides, fourteen slots each, identical
ladders, and no slot that either side uses alone. There is no cell for a frame
lever to move, and the residual is 42.7 percent naming, which is the bucket the
lead's oracle closed for the lead.

**What the census does show is traffic, not layout.** One shared slot differs:
the home of the seventh argument is read three times and written once by the
target, and read once and never written here. The target never
register-promotes that parameter; this candidate loads it once and keeps it in
a caller-saved register across the whole middle of the function. That single
promotion decision is the structural fact worth attacking next.

**The L100 divisor lever does not reach a stack parameter here.** Fifteen
probe forms were measured: OR-with-zero, AND-with-minus-one and XOR-with-zero
on that parameter, at five placements, applied once, twice and three times.
All are byte-inert, masked staying 81 with the slot traffic unchanged at one
load and no store. This extends L109's own narrowing -- a probe reading an
already-read global is folded away -- to an already-read stack parameter. The
one placement that is not inert is the one ahead of the first call, and it is
worse: 100 masked with the frame growing 0x28 to 0x30.

**Other axes covered, all flat or worse.** Two respellings of the tenfold
multiply as an eightfold plus a twofold, which is the shape the target emits
and which reuses a product it already has, leave masked at 81 and move the size
the wrong way, -4 to -8, because cfe reassociates them back; naming the
decrement through the existing temp is flat at 81. Six physical-line
arrangements of the four-way OR that builds the colour word, including the
single-line fold and the fully exploded form, produce one object, so L59's
tie-break does not reach inside a single statement and the ordering difference
there is as1 scheduling driven by something else.

**Orientation for the next lane.** The call test says p1-only: three calls, so
the axis is the save ratio and L106 is unavailable. The 47 naming words carry a
closed three-cycle over the integer temp ring, which by L127 is one ring-phase
fact rather than 47 colour questions. Giving each display-list command its own
cursor instead of reusing the single declared one is refuted by the frame
before it is worth compiling: each additional pointer costs four bytes, the
target's frame has no room, and the measured cost of a pointer local in this
tree is recorded in the `func_80037C74` shard.

#### 2026-09-23, lane `B-obj` (Track B): 81 at delta -4 to 62 at delta 0

Reader, cycle 0: three pairs, labels missing-CSE, hoist and missing-CSE,
owned by the arg6 OR line, the func_800349A4 call line and the two
command-word lines. The traffic finding of the earlier pass holds, and
this pass closes it: the target never register-promotes the seventh
argument. It loads it from its home at every use and writes it back
inside the arm.

Spellings, in order, each measured:

- the address of arg6 taken at any one use or at all of them: 84, size delta +4
  (uopt still does PRE: one load before the branch, a speculative OR, the
  store sunk to the join)
- a volatile pointer store at the OR: 78, delta +8
- volatile s32 arg6 in the prototype and the definition, plain OR-assign: 75, delta +4
- the same with the address form: 78, delta +8
- volatile arg6, read into the existing temp_t3 before the arg10 test, OR
  applied in the arm: 64, size delta 0 (milestone; the load/OR/store/reload
  shape is now the target's)
- arg2 * 0xA respelled as temp_a1 plus arg2 times two (or shift, or plus
  arg2 twice): 70, delta -4 on its own
- (arg4 - 1) and arg4 times 0x10 as shifts: 71, delta +4 on its own
- the two together, six combinations: 62, delta 0, all identical
- the decrement kept ahead of the shift (u32 cast, 1U, plus -1, s16 cast,
  assignment inside the expression): 62 flat; uopt still emits the
  product minus 16 where the target decrements first
- all twelve legal orders of the four statements that open the first
  command: 62, byte-identical, so that block's order is inert

Records (instrumented uopt, identity-gated, proc 41, p1 because it calls).
The tail block's webs: temp_a2 takes v1, temp_a1 a1, arg4 a2, the
0x80000000 constant (a type-2 web) a3, arg2's split fragment t0. The target
leaves v1 and a3 unused there and has temp_a2 in a2, arg4 in t0, the
constant in t1 and arg2 in t2. Forcing temp_a2 to a2 alone was accepted and
scored 61. Forcing arg4, the constant and arg2 to the target's colours
was accepted but moved the size to -4 (44), so it prices nothing at delta 0.

What is left: 52 naming rows and 6 structural. The naming is mostly a temp
ring rotation, a closed five-cycle over t3, t6, t7, t4 and t8, in the two
command words. The structural words are the decrement the target keeps
ahead of the shift, and the constant and argument loads it issues earlier
in the first command. Next lever: find what the target's source does that
makes (arg4 - 1) survive uopt without being distributed. That is also the
word that shifts the ring draw order in the second command.

#### 2026-09-23, lane `A2-obj` (Track A): 62 to 65 positional, forced floor 35 to 24

The positional count rose by three; the forced floor fell by eleven, and the
floor is the number that says how close the source shape is. Floor here means:
force the tail block's five webs (temp_a2 to a2, temp_a1 to a1, arg4 to t0,
the 0x80000000 constant to t1, arg2's fragment to t2; all five accepted, read
back from the records) and score the forced object directly.

Aligner before: 45 byte-exact, 52 naming, 0 immediate, 12 really different.
After: 43, 49, 0, 18.

Adopted, in order:

- the decrement written through a u8 cast, `(u8)(arg4 - 1) << 4`: uopt no
  longer distributes it into the product minus 16; the addiu -1 then the
  shift are now emitted as in the target, and as1 folds the redundant
  narrowing mask (L149). A mask of 0xF on the decrement does the same. 62
  to 64 unforced; floor 35 with the five forces (the old shape could not be
  priced at delta 0).
- the -0x100 and 0xFB000000 stores of the second pre-call command written in
  the other order on one physical line (L59): the two ring draws swap into
  the target's t7/t6. Swapped on separate lines or folded in the original
  order is 35; this is 31.
- the arm spelled as arg6 assigned its own address-form read ORed with 4, with no temp and arg6 still
  volatile: the read is no longer a coloured web (it was v0), it is a ring
  temporary loaded before the branch with the OR in the delay slot, as in
  the target. 65 unforced, floor 24, and every one of those 24 is in the
  tail block.

Measured and not adopted: the decrement as a region-separated local (106,
+4: the region also splits the display-list address web into s0), as an
early local with multiply or shift (90, +12), negated (71, +8), plus
0xFFFFFFFF, OR with sp24 first, times 0x10000 instead of the shift, XOR
(all 62, inert); all 72 orders and groupings of the four OR terms of the
colour word under the forces (parenthesisation is canonicalised, the
source order is best at 35); a discarded volatile read before the test
(77, +8: it loads into zero and the arm reloads); a non-volatile arg6 with
the address form in the arm (83, +4: speculative OR, store sunk); a region
around or inside the read and test, and a comma read in the condition (all
inert, temp_t3 stays a v0 web); forcing that web to split spills it.

What is left, all in the tail block: the target leaves v1 and a3 unused
there, so each of the five webs sits one or two colours higher than the
lowest free one (ours: temp_a2 v1, arg4 a2, the constant a3, arg2 t0). The
records show nothing forbids v1 or a3 for them here (forbidden masks hold
only v0, a0 and the webs already coloured). Any subset of the five forces
short of all five leaves 56 or 64. Next lever: find what occupies v1 and
a3 in that block in the target without emitting code; on func_80006B04 the
equivalent was a local read back from memory across an empty region, which
uopt colours but never emits.

<!-- plateau-handoff:func_8000831C:end -->
