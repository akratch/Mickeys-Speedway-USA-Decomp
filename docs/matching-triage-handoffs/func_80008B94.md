<!-- plateau-handoff:func_80008B94:start -->
### `func_80008B94` plateau handoff

- source: `src/main/objects.c`
- score: 0 differing words
- frame: 0xB8
- relocations: 45
- first mismatch: none
- summary: MATCHED. This header claimed an open residual until 2026-09-16, when tools/check_shard_metrics.py reconciled every shard against the ranking; the function had already left the queue. The claim it carried was: Multiplier null-init dropped and four command-word pairs reordered (126 to 111); the shape multiset is now exact, so all 111 are role or placement.
The configured full-TU baseline had 420 words against 419 owned words,
397 raw/masked differences, first +0xC, the exact 0xB8 frame, and 45
relocations per side with five authenticated identities. Its frame included
an unsupported unused volatile reserve. The retained candidate removes that
reserve and reaches the genuine 0xB8 frame, exact 1676-byte size, 293 exact
words, 126 raw/masked differences, first +0xA8, and all 45 relocation
offset/type positions. Authenticated identities are 43/45. It remains
guarded NON_MATCHING C and earns no new matching credit.

The live assignment gate initially failed on an unrelated integration-ledger
entry. No source was edited while it refused. A fresh zero-exit base-only
verdict opened the target after that ledger was repaired. The oracle supplied
only weak neighbors; no external C body was adopted. All 53 usable cached
TU flag objects remained nonexact; configured flags stayed unchanged. The
assembly-owned range was separately proved against linked ROM bytes.

Mickey's target restores both default colour commands, uses signed resource
indexing and a floating-point coordinate, rechecks the palette after its
getter, retains the current halved alpha, and captures player data before
camGetNo. The target alpha clamp was restored. A single mutually exclusive
colour ladder removed redundant control flow. Correct channel computation
order and normal stack-argument conversions recovered the colour-product
sequence. Scoped command pointers and the previously proved direct global
display-list increment recovered the relocation count. Scalar and pointer
homes were reconstructed without padding. A defined optional multiplier
carrier and an explicit palette else arm recovered the exact size; binding
both palette observations fixed the initial type-register mismatch. A common
white-channel value allowed the multiplier scale to return to a direct
expression while preserving the frame.

Attempt numbers are global to the lane. Baseline 172 precedes corrections
173–179, frame/carrier work 180–188, and the improvement at 191. The initial
typed-definition preparation failed against the existing void-pointer
prototype before comparison; it was corrected without counting a source
attempt. The final five probes 192–196 tested early null initialization,
its combination with the direct scale expression, resource/multiplier
declaration order, const qualification and the byte white-channel type.
The first two regressed; the others were flat. None resolved the remaining
mechanism. These five probes are the stall evidence. Cleanup 197 retained
attempt 191. Sources, objects, comparisons and rationale remain in ignored
build artifacts.

The workbench verdict is structure-mismatch, routed to structure-buckets.
Its instruction-shape lever now points to the optional multiplier transfer:
the target stores the nonnull lookup into its home and reloads the retained
pointer, while the candidate uses a defined null initialization and register
copy. This also reverses the resource and multiplier register roles. The
remaining floating-point allocation and temporary sequence follow that
transfer. Do not introduce an uninitialized C read to imitate the target's
unconditional machine load; authenticate the source lifetime and null-path
invariant first. Two relocation identities also need resolution before
promotion. Native command-word typing and address-word spelling were flat.

Validation: configured full-TU comparison, diagnosis and guides, cached flag
rescoring, linked fallback ownership proof, finalize_plateau.py, full gmake
verify, check-docs, cleanroom and check-nonmatching-builds. The ROM verification
uses the assembly fallback for this function; the C residual remains.

#### Region partition (measured at 126, re-measured at 111)

The 419-word residual is 16 regions at 126 and 12 at 111. It is not one
decision, but it is only three kinds of decision. Counts are
relocation-masked differing words at 126:

- 16 words at +0xCC..+0x108 -- the optional multiplier transfer the previous
  handoff named. The candidate defined a null initialisation and a register
  copy; the target stores the non-null lookup straight into its frame slot at
  128(sp) and reloads it. Everything after +0xCC in that block was displaced
  by one word because of the extra initialising instruction. **closed**
- 43 words at +0x474..+0x554 and 22 at +0x5C4..+0x66C -- the two display-list
  colour ladders. Two things live here: a t-register ring rotation, and, at
  every site whose two command words are both constants, the target writes the
  second word before the first. **the ordering half is closed** (four sites);
  the ring rotation is not.
- 11 words at +0x3E0..+0x454 and the remaining single- and double-word regions
  -- a t3/t4 role reversal between the retained alpha and the multiplier, with
  the same two frame slots (172(sp) and 128(sp)) on both sides. open.

Three closures, all delta 0:

1. Dropping `multiplier = NULL;` closes 8 (126 -> 118) and takes the
   register-and-schedule-tolerant multiset distance to **0**: from that point
   every instruction in the function is shape-exact and only its register
   fields and its position differ. The previous handoff warned against
   imitating the target's unconditional load with an uninitialised read; the
   null-path invariant is what makes it safe here rather than an assumption.
   `multiplier` is read in exactly two places, the `object->unk50 != NULL`
   colour arm and the `useMultiplier` command block, and `useMultiplier` is
   set only on the branch that assigns `multiplier`. No path reads it
   unassigned.
2. Writing `unk4` before `unk0` in the four constant-pair command blocks
   closes 3 more (118 -> 115 combined). The two computed-word blocks keep the
   original order; the target does not reverse those.
3. Moving the colour-state multiply above the two flag stores inside the
   multiplier branch closes 4 (115 -> 111).

#### What is left, and what is already ruled out

The t3/t4 reversal is the head of the remaining chain: the candidate gives the
retained alpha t3 and the multiplier t4, the target the other way round, and
the two ladders' ring rotations follow from it. Measured flat or worse on:
moving the alpha clamp above or below the multiplier branch (both regress by
a size delta), assigning through the multiplier in the condition, dropping the
intermediate lookup local, and four declaration-order forms for the
multiplier, the alpha and the lookup local (115 to 148).

## 2026-09-11 matched (lane `lane/f10-fell`): the closure fell on the save ratio it never computed

`func_80008B94` is byte-exact C. `gmake verify` prints
`507341c0a40ca3e9a7cee969b396ee53facfb548`. Six source edits took 111 to 0,
size delta 0 throughout, and none of them is on an axis the previous closure
swept.

The closure named the t3/t4 reversal as the head of the residual and then
measured declaration order and statement placement, which this function does
not have: the instrumented uopt (`.text` checked byte-identical to the tree's
object) emits 66 p1 decisions and zero p2 records for it. What decides t3
against t4 is the p1 rank of two webs, alpha (web 63, home 172(sp), save
10/12 = 0.833) and the multiplier (web 53, home 128(sp), save 5/7 = 0.714).
p1 colours in descending save and each takes the lowest free colour, so alpha
took t3 and the multiplier t4; the target has them the other way round.
Forcing alpha to t4 (the force on the multiplier is declined, t3 is already
taken when it is decided) takes 111 to 78 in one compile, which bounds that
one decision at 33 words.

What moved it from source. Every one of the multiplier's six uses nets +1 and
the def nets -1, and the arm's `multiplier = candidateMultiplier;` copy is
propagated away before the occurrence census, so the arm contributed nothing
to the web. Re-reading the field as the def, `multiplier = object->unk50;`
inside `if (object->unk50 != NULL)`, with `multiplier->unk0` in the multiply,
makes that use a real occurrence: net 6, save 0.857, above alpha's 0.833,
and the multiplier takes t3 unforced. 111 to 78, the forced count exactly.
Twenty zero-instruction probes (OR-with-zero, AND-with-zero, discarded
expressions, dead defs at five positions, `register`, type changes) moved
neither web's net or nocs by one: uopt strikes them all before the census.
L109's narrowing holds at loop depth 0.

The rest was the ugen ring and four as1 ties, read from the ugen free-list
trace (`DKWB_UGEN_TRACE`), which shows the integer ring here is a FIFO over
t5..t9 with zero phantom pops on the candidate side:

  - the `flags` OR-assignment before `useColourState = 1;` inside
    `if (alpha < 0xFF)`: 78 to 73. The three registers at +0x17C are the
    same ring slots in the other order, so the target evaluated flags first
    and as1 hoisted the li.
  - the arm-5 pointer `info->unk14->unk64` carried in a fresh `s8 *` local
    rather than an expression temp: 73 to 42. The target holds it in v1, a
    pool colour, so it is a symbol there. Reusing the already-declared
    `cameraData` for it imports that local's interference (L115) and scores
    147; `palette` ties at 42, `info` 44, `colourA` 66, `colourB` 61.
  - `_SHIFTL`-style `& 0xFF` masks on the three multiplier byte fields:
    42 to 21. The masks fold away on u8 fields and each folded mask advances
    the ring one slot, which is what puts the lbu/sll pairs in the target's
    order.
  - each constant-pair command block folded onto one physical line with
    `unk0` written first, and the colour-state block the same way: 21 to 3.
    This is the shape of the SDK `gDPSetPrimColor`/`gDPSetEnvColor`
    expansion, w0 then w1 on one logical line; the closure's earlier reorder
    had fixed the store order by moving the as1 tie onto the constant
    materialisation. The literal macro spelling through the `s32` global
    measures +52 bytes, so the explicit form is kept.
  - `paletteIndex` typed `s32` instead of `u8`: 3 to 0. The u8 local's web
    (137) netted 2 and lost a0 to two save-3.0 webs; at net 3 it ties them and
    wins on web number, taking a0 as the target does. Dropping the local and
    reading `palette->unkD` twice is also 0.

Promotion note. The compiled body references no rodata itself but the ROM's
`D_80080F7C` is this function's own `0.0025f` literal: the assembled version
carried it as a late-rodata label, and compiling the function as C with the
extern instead shifted every later literal in the TU's `.rodata` down by 4.
Spelling `0.0025f` in the body restores the layout exactly. The one remaining
pre-link difference is that word's relocation encoding (addend 0 against the
label versus 0xE0 against `.rodata`), which links identically, so the
`.text` prefix digest in the `objects.c.o` POSTPROCESS rule is updated, as
the func_800056A4 match did.

Two records worth carrying to the other functions on this lane's list: an
occurrence only counts if it survives copy propagation, and a p1 pair tied on
save is ordered by web number, so one net occurrence is a whole colour.
<!-- plateau-handoff:func_80008B94:end -->
