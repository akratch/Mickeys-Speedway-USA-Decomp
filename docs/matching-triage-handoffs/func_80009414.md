<!-- plateau-handoff:func_80009414:start -->
### `func_80009414` plateau handoff

- source: `src/main/objects.c`
- score: 398 differing words
- frame: 0x198
- relocations: 11
- first mismatch: +0x0
- summary: Workbench structure-mismatch: structure-buckets. Next: authenticate the default-mode lifetime and sort-prefix source before further scheduling work.

The configured baseline contained 378 words against 421 owned words (1684
bytes), 411 raw/masked differences, first +0, a 416-byte frame against
408, and 11 relocations per side with no authenticated identities. The
retained C has 414 words (1656 bytes), 388 raw/masked differences, first
+0x40, and the exact 408-byte frame. All 11 calls remain, but their
relocation identities are not exact at target positions. This is guarded
NON_MATCHING C, with no new matching credit.

The fresh assignment gate returned base-only. The oracle supplied no useful
counterpart body; no external C was adopted. All 53 usable original TU flag
objects were rescored. The default unrolling configuration was close in
size but nonexact and does not justify a whole-TU flag change. Configured
flags remain unchanged.

Mickey's target proves an empty-count guard around sorting and rendering,
a cone pointer captured before camGetProjZ, a data reload after the scale
call, signed texture-frame indexing and signed alpha loads. The sprite
constants belong outside the render loop. Those corrections are retained.
An odd-prefix plus paired-comparison bubble sort reduced the large size
shortfall. Removing redundant aliases recovered the frame; equality
termination and payload-first graphics writes improved the residual.

The best primary residual was reached at attempt 203 and retained at 208
with the authenticated signed alpha and full-width swap carrier. The five
subsequent material probes 204–208 tested cached first-pair carriers, their
halfword form, switch dispatch, the switch with a full-width swap and the
isolated swap/alpha form. None improved the primary word residual or proved
an exact mechanism. Auxiliary alignment scores varied without resolving the
owned-byte deficit. This is the five-probe stall evidence. Cleanup 209
preserved 208. All sources, objects, comparisons and rationale are retained
under ignored build artifacts.

Workbench diagnosis reports structure-mismatch with a mixed constant,
structure and register residual. The constant-audit footer says to resolve
constants before structure and allocation; structure-buckets identifies the
sort and render control-flow shape. The target retains the first two sort
indices across outer iterations; the manual cached spelling did not recover
its code. More importantly, the target's default render-mode path loads a
home whose initialization is unproved. The baseline fallback value remains
explicitly marked as a placeholder. Do not introduce an undefined C read or
invent a guard to imitate this load. Authenticate the mode domain/lifetime
and original sort-prefix spelling before another scheduling campaign.

Validation: full-TU workbench comparison and diagnosis, guides, cached flag
rescoring, finalize_plateau.py, gmake verify, check-nonmatching-builds,
check-docs and cleanroom. The separately proved linked fallback owns the
full target range; ROM verification still uses that fallback.

Header regenerated from the ranking on 2026-09-23 (check_shard_metrics --write); it read first mismatch +0x40; score 388 differing words.
<!-- plateau-handoff:func_80009414:end -->
