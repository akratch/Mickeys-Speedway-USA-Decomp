<!-- plateau-handoff:func_80011980:start -->
### `func_80011980` plateau handoff

- source: `src/main/track.c`
- score: 195 differing words
- frame: 0xd8
- relocations: 12
- first mismatch: +0x0
- summary: Second metadata add is the missing-CSE word and the target has it too. Textures local and volatile threshold stay at 195, size +4; ra still holds D_80081790.

Summary before this remeasure: Delta +8 to +4. Target ranks inner-loop edge/sign/face webs below all outer webs (s5-s8); ours ranks them first and spills into ra.

Summary before this remeasure: Halfword record types preserve candidate bytes; 211 differences remain after five m2c forms. Next: source-attributed FP home evidence.

#### Track B pass (lane B3-track), 2026-09-23

- Start: size delta +8, frame 0xE0 against 0xC8, 211 masked. Reader: one 167-word spill/reload pair opened by two prologue stores, plus a hoist and an extra-ILOD pair; frame census showed the save area shifted 8 because the body allocates one register more than the target (the return-address register used as a general register, so it is saved).
- Typed subscripts on the plane table and the metadata table, left-associated dot products and the outer loop kept as a guarded do-while: flat at 200 (the retained byte-offset body and the typed body score the same).
- Edge-loop index incremented at the end of the body instead of after the load: 217 to 216 words, 195.
- Edge index initialised in the entry block rather than just before the loop: reproduces the target's shifted-zero preheader (a new identity), 195, delta +4, frame 0xD8.
- Flat, each byte-identical or equal: a goto-built inner loop; index counted from 1; byte-offset index to 6; subscript base spelled as node->planes, as planes + index, or with an unsigned index; reading the first plane through the encoded word before or after assigning the entry pointer; index initialised at function entry (worse, 215) or after the ratio test (loses the preheader shape).
- Decision variable, from the instrumented records: in our body the inner loop is one block and its edge, sign and face webs carry save 300 at nocs 1, so p1 colours them first with the lowest caller-saved colours; the outer webs (segment index, entry offset, valid flag, count, table addresses) then run out and one takes the return-address register. The target gives exactly those three loop webs the last three callee-saved registers and keeps a copy of the entry pointer in a fourth, so in the target they rank below every outer web. No spelling tried lowered their save. Next: find what makes those webs span more blocks or leave p1 (the sibling func_80010654 has the same shape and the same open question).

#### Mickey m2c structural/type audit, 2026-09-08

- Fresh assignment gate returned `base-only`. Baseline: 217 candidate / 215 target words, 211 raw/masked differences, first +0x0, frame 0xe0 / 0xc8, 12/12 relocations with 2 exact offset/type/identity sites. Workbench verdict `structure-mismatch`, playbook `constant-audit`; zero tests, edge mask, three-edge bound, plane stride and final offset global all agree with Mickey.
- Ordinary and carrier-preserving Mickey m2c drafts confirm the no-call scaled ray/plane query, two validity flags, hit stores, and repeated metadata access. Five attempts recover a halfword entry type, typed edge cursor, shared start coordinates, shared plane-table root, and the draft's reused plane pointer after saving the normal. Measurements: 45: 217 words, 211 differences, frame 0xe0; 46: 217 words, 211 differences, frame 0xe0; 47: 217 words, 211 differences, frame 0xe8; 48: 217 words, 211 differences, frame 0xe8; 49: 217 words, 211 differences, frame 0xe0. None improves the baseline residual or establishes an additional missing semantic identity; five consecutive forms exhaust the draft differences.
- Retained attempt 46 makes both stream entry and edge cursor `u16 *`, replacing a misleading node-pointer cast and byte cursor. Its extracted candidate function is byte-identical to the baseline candidate (868 bytes), and its candidate relocation offsets/types/names are identical. This proves the type cleanup preserves this compiler output; it is not a match to the ROM. The local receipt is retained under ignored `build/wb/tu-track/func_80011980/type-proof.json`.
- Best retained measurement remains 217/215 words, 211 differences, frame 0xe0, first +0x0, 12/12 relocations with 2 exact sites. All source/object/summary attempts are retained ignored. Original `NON_MATCHING` guard and assembly fallback remain; zero new matching bytes.
- Next concrete lever: original FP declarations or source-attributed compiler home evidence for the 0x18 frame excess. Neither decompiler mode recovers that policy; the already-closed general allocation mechanism is not reopened here.
- Validation: per-attempt configured full-TU comparison, baseline-versus-retained candidate byte/relocation comparison, `gmake check-nonmatching-builds`, finalizer, `gmake verify`, `gmake cleanroom`, and `gmake check-docs` before commit.

#### Pinned donor audit, 2026-09-08

- Assignment gate: `base-only` at `32a75d648e8954f7455897fb8f16a8ef5f05df11` for this exact symbol and source path.
- Jet Force Gemini `efd5abb1c79636e297b831f7c2d5bf47eac39c0c` has 12 C implementations and 53 assembly placeholders in `src/track.c`. Its implemented routines do not supply this target's body. The closest masked track-object hit, `func_80016EA0` (Jaccard 0.0812), corresponds by verified TU order to the still-assembly `func_80017094_17C94` in that pinned source. This is diagnostic structural context, not an adopted name, ABI identity, or exact donor match.
- Fresh configured full-TU measurement: 215 target words and 217 candidate words; 211 raw and 211 relocation-masked differences; first +0x0; target frame 0xc8, candidate frame 0xe0. There are 12 candidate versus 12 target relocation records, with 2 stable identities at matching offsets/types.
- Workbench verdict `structure-mismatch`, playbook `constant-audit`. The existing diagnosis still requires source-shape/lifetime evidence; the donor-only reopen does not authorize substituting a flag, allocator, or permuter mechanism.
- ADR 0018 stop: zero new source attempts for this target. The pinned source disproves the new matched-donor-C hypothesis. No unchanged flag lattice or previously exhausted source family was repeated. The compiled candidate is unchanged and remains behind `NON_MATCHING` and its original `GLOBAL_ASM` fallback; zero new matching bytes are credited.
- Next concrete lever: a published matched counterpart with an authenticated ABI and useful source lifetimes, followed by Mickey-specific field, branch and call proof. The remaining general levers below require separately authorized changed evidence.
- Commands: `tools/wb_compare.sh --summary-json func_80011980`, workbench `diagnose` on the configured full-TU object, the track-object masked skeleton audit, `tools/finalize_plateau.py`, `gmake cleanroom`, and `gmake check-docs`. Source, object, scores, first mismatch and previous handoff are retained under ignored `build/wb/`; no instruction rows are included here.

#### Prior committed plateau evidence (historical)


- source: `src/main/track.c`
- score: 211 differing words
- frame: -0xE0
- relocations: 12
- first mismatch: +0x0
- summary: Target control flow is restored; original declaration and FP lifetimes are needed to remove the 0x18 frame and two-word excess.
<!-- plateau-handoff:func_80011980:end -->
