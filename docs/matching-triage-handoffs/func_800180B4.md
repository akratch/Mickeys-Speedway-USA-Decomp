<!-- plateau-handoff:func_800180B4:start -->
### `func_800180B4` plateau handoff

- source: `src/main/shadows.c`
- score: 61/206 words
- frame: 0x90
- relocations: 8
- first mismatch: +0x34
- summary: Delta 0 via address-form sectorIndex after the call; 61 left: store placement, t6/t7 ring phase, volume w6 on v1 (w6=c4 prices 56)

Summary before this remeasure: L160 masks10 cursor. Unforced 154 at delta -4. Force w21=c2 is 71 at delta 0 with +0x84 spill and a 2-row store/move swap. s0 still cost 0.

Configured baseline is 91 masked of 206 at delta 0, frame 0x90. Aligned 124 exact / 71 naming / 1 immediate / 11 structural, with one extra word each at candidate +0x158 and target +0x118. The target-only stack slot +0x84 is the sector-index spill (1 load, 1 store).

Trap 20: instrumented IDO identity-gated against the configured stock object (per-file -Wab,-r4300_mul kept). `.text` identical. CDX_PROC=9 is this function (42 p1dec). Unforced records read forced=-2. Force claims below were accepted at forced=-1 or the named colour.

Web 6 is the volume pointer, type-4, bb 0, a cost-0 tie of v1 and a1; it takes v1 by colour number. `p1:w6=c4` accepted forced=4, scores 84 at delta 0, first mismatch +0x80, and the head then matches through `lh v1` of sector2E. Web 21 is the sectorIndex symbol, home +0x84, bbs 1-2, coloured s0 (callee cost 0 because the prologue already saves s0). `p1:w21=c2` accepted, emits the +0x84 spill and puts D_800CB284 in s0, but size 207. Pairing w6=c4 with w21=c2 is declined: web 18 takes v1 once volume leaves it.

Source that did not beat 91: L105 assignment-after-call (105), later-block mask copy (91 flat), L97 around the call (94) and around yMax/yMin (91 flat), unused pointer pad (95), hoist of sectorIndex (187, delta -8), address form, volatile, or-with-zero, self-assign, volume pointer local (98-100). Do not retry those.

2026-09-19 lane w33-shad. Re-measured 91 at delta 0, frame 0x90, identity-gated proc 9 PASS. The extra candidate word was the vertexOffset shift remat; the extra target word was the add-immediate that only appears once sectorIndex is in v1. L160 `sector->masks10[vertex]` (no vertexOffset carrier) deletes that remat: unforced 154 masked, size delta -4, aligned 124 exact / 68 naming / 3 immediate / 11 structural, still no +0x84 traffic.

On that L160 shape, `p1:w21=c2` is accepted and scores 71 at delta 0, 206 words, frame 0x90, +0x84 spill live (1 load 1 store), no insertion pair, aligned 135 exact / 69 naming / 0 immediate / 2 structural. The two structural rows are a post-call store versus move swap. `p1:w6=c4` remains 148 at delta -4 (no spill). Pairing w6=c4 with w21=c2 is still declined (web 18, bbs 0-1, takes v1 once volume leaves it). Triple w5=c2 + w6=c4 + w21=c2 is accepted at 71, first naming +0x4C.

Web 21 still lists s0 at cost 0 and v1 at cost 2. Loop leftover on sectorIndex raised the frame to 0xB8. Overlay41 shade-before-test was 90 positional but 14 structural. Hoisting block or vertexBase did not forbid s0. Overlay22 empty if (blockNumber) was byte-identical. block++ was 123 exact. Do not restore vertexOffset unless a v1 source form is in hand: with the carrier, w21=c2 grows one word.

Next is source-routing web 21 onto v1 on this L160 shape (s0 must not stay cost 0) without extending sectorIndex into the loops. Volume onto a1 is the leftover naming once that spill is natural.

#### 2026-09-23 (Track B, lane B-resmix): delta 0, 154 to 61

Cycle 0, `tools/insertion_pairs.py`: one pair, open to the end, label
missing-CSE, a target-only word owned by the `masks10` line. Read against
the listings it is the other half of the sectorIndex question above: the
target stores sectorIndex to its home +0x84 before the call and reloads it
after (two words), where the candidate's `s0` needs one move. The records
on this shape still show web 21 taking `s0` at cost 0.

Measured, one at a time:

- the post-call read of sectorIndex written through its address,
  `*(s32 *) &sectorIndex << 6`: 154 at delta -4 to **66 at delta 0**, frame
  unchanged, the +0x84 home carrying one store and one reload. The same form
  on the pre-call argument alone, or on both uses, is also 66. `volatile` is
  183 at delta +4. The D_800CB284 address web now takes `s0`, as in the
  target.
- the shade update without the `oldValue` and `targetValue` carriers,
  `*value += ((1.0f - D_80079464[shade]) - *value) * D_800CB28C;`: 66 to
  **61**. Keeping either carrier leaves 62 to 66; the float block is now
  word-exact.
- flat or worse: storing sectorIndex inside the call argument (61), reading
  `query->sector2E` at the argument (183), `sectorIndex * 3 * 4` (61), a
  self-assignment (61), assigning sectorIndex before the `!= -1` test or in
  it (88 each), swapping or moving the yMax/yMin lines (61, 63).

What is left, in order of size: a t6/t7 ring phase through most of the
body (the first divergence is the `* 0xC` expansion before the call, where
the draw trace shows one extra ring draw at the call line); the store
itself, which lands at the assignment (+0x74) while the target's sits just
before the call (+0xB8), the position a register spill gets; and volume
(web 6) on `v1` where the target has `a1`. Forcing `p1:w6=c4` on this shape
is accepted and scores 56 at delta 0, and the sector2E value then takes `v1`
unforced, so that colour is worth 5 words. The address form makes the store
a variable store; the target's placement says it was a spill, so a source
that gets web 21 a caller-saved colour on its own would likely close the
store and part of the ring phase together.

<!-- plateau-handoff:func_800180B4:end -->
