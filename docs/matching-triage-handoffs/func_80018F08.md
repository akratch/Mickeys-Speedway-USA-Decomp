<!-- plateau-handoff:func_80018F08:start -->
### `func_80018F08` plateau handoff

- source: `src/main/lights.c`
- score: 41/205 words
- frame: 0x48
- relocations: 7
- first mismatch: +0x20
- summary: hypothesis=keep the generated segment cursor live through x, y and z on the sole-header 0x48 form without a second cfe auto; spellings=row reassignment 190 at delta -16, comma of indexed stores 189 at +40, coordinates base 189 at delta -16; stall=no delta-0 live cursor and the 41-word body was kept

Summary before this remeasure: Header as v0 symbol serializes the +0x64 pair. A second extra auto costs 8 frame. Sole-header is frame-exact plus 10 words. Colour cannot close it.


### g1 draw-difference and frame receipt (2026-09-13)

The configured baseline is retained: 820 bytes, zero size delta, 74 masked
positional differences; alignment gives 138 exact, 52 naming, seven immediate,
and three structural paired rows, with five candidate-only and five target-only
words. The frame remains 0x58 versus 0x48. The seven relocation identities were
proved by the inherited candidate; this packet does not claim a new linked match.
No colour sweep was repeated.

The inherited zero-draw diagnosis was for the wrong procedure. The Ucode receipt
maps this function to procedure 12 of 28. Its stock and traced complete-TU text
are identical, and its baseline has 73 draws and 292 emission events. The residual
occupies seven 0x80-byte windows, with a closed ring in the second window; it is
not confined to two windows. The near-match query could not resolve the target's
missing symbol-size field, so no new donor claim is made.

Attempts 36–43 removed redundant typed-input, direction-address and coordinate-
offset carriers, then reused disjoint early index/late colour and early segment/
late direction lifetimes. Single removals left the frame at 0x58; removing the
input and offset aliases reached 0x50. The combined reuse reached 0x48, and placing
the rotation array first made all nine used stack offsets agree with the target.
This diagnostic still has 78 masked differences and only 134 aligned exact rows:
57 naming, two immediate and seven structural rows remain. Its 73 draws and draw
order did not change. Matching stack geometry therefore does not establish the
required temporary schedule. Owner-pointer snapshots across calls were preserved.
These compact candidates are ignored diagnostics, not the retained source.

Attempt 44 was a generator-boundary compile failure, repaired before attempt 45;
it supplied no matching evidence. Attempt 45 instead generated direction addresses
and reused the original index for the colour byte, keeping the segment pointer
separate. It reached the target frame and removed one insertion/deletion pair but
regressed to 79 masked differences and 131 aligned exact rows. Attempts 46–48 moved
the early locals, colour scalar, and final rotation/direction into their owning
scopes. They preserved all 73 draws and their order; the frame stayed 0x58, while
later controls merely shifted the rotation-array homes. Attempt 49 reused the
colour scalar for the early coordinate offset: unchanged draws, 76 differences.
Attempt 50 spelled the bounded nonnegative stride as shifts and subtraction:
the intended line gained two draws, giving 75 draws and 98 masked differences.
It moved the schedule but worsened alignment by 26 paired rows.

Attempts 51–53 promoted the signed index carrier, removed a redundant signed
segment-index cast, and reversed the stride multiplication operands separately.
All three produced the baseline text and unchanged draw sequence. These normalized
controls add no new mechanism or improvement; stop on this measured stall.
The best baseline, compact-frame alternatives, failed-control logs, source
snapshots, objects, census JSON and aligned maps remain under
`build/g1/func_80018F08/`. The declaration/frame axis can move; the tested alias,
scope and normalization edits cannot move the draw sequence. A next packet needs
a source form that changes the early ring schedule without sacrificing the
existing alignment, then must re-prove frame, relocations and linked owned bytes.

Commands: `build/g1/map_proc.py`, `allocator_trace_receipt.py --map-only`,
`build/g1/measure.py` (configured compile, census and aligned residual),
`draw_census.py --compare`, `residual_map.py --object --against`,
`tools/gates.sh verify`, and `tools/finalize_plateau.py --commit`.


#### 2026-09-13, lane l1: measured schedule controls

Fresh procedure-12 baseline is 205 words at delta zero, with 74 masked
differences from entry. Alignment is 138 exact, 52 naming, seven immediate and
three paired structural rows, plus five candidate-only and five target-only
words. Candidate and target have seven static relocations each. The frame is
0x58 against target 0x48. The census records 73 draws and 292 emissions.

After reading the existing frame, alias and carrier controls, change the
coordinate offset from bytes to float elements, retaining its named carrier,
load widths and store order. The intended coordinate line gains one draw and
two emissions; the complete draw sequence changes. Frame and geometry stay
fixed, but masked differences rise to ninety and alignment falls to 120 exact
words, with seventy naming, five immediate and five paired structural rows.
The same five gap pairs remain, and later windows regress. This is a measured
release-order change, not a hidden improvement behind a positional shift.
Restore the original body. Stop early with the earlier carrier/compact-frame
controls still closed: this direct element form moves the wrong draw and does
not repair the spill homes. Next action needs a trace-backed release change
that preserves the call-tail ring and reduces the frame.

Named Ucode mapping and full stock/capture fidelity pass for the baseline and
each retained experiment. Sources, stock objects, scores, frame/relocation
censuses and aligned deltas remain under ignored build/l1/func_80018F08. Commands:
configured compilation, allocator_trace_receipt mapping, draw_census profile
and comparison, residual_map object comparison, finalize_plateau, and
tools/gates.sh verify cleanroom check-docs. No matching credit is claimed.

#### 2026-09-18, lane w14: JFG control-flow re-derive

Re-derived from JFG `src/camlight.c::camlightUpdate` (matched C at efd5abb)
and `src/lights.c::func_80021434_22034` (assembly-backed sibling). Mickey
offsets remain: disabled 0x91, segments 0x68, coordinates 0x40.

Retained source: Vec3f coordinate rows, PY rotation as value58 / value5A / 0,
nested flags-bit-4 then mode 3-or-0, no named index/offset/direction/value
carriers, rotation declared first, else-path adds x then y then z then
or-assigns flags bit 1. Identity gate: instrumented IDO `.text` byte-identical to stock.
CDX_PROC=12 (28 procedures in the TU).

Measured: 820 bytes, delta 0, 205/205 words, masked 41, frame 0x48 with all
nine slots agreeing, 7 jal relocations, first mismatch +0x20. Aligned 164
exact, 39 naming, 0 immediate, 2 structural. The else-path insertion pair is
gone. Draw count stays 73; emissions 290.

The two structural rows are one swapped pair at +0x64: target loads the
header into v0, reads count, then clobbers v0 with index<<2; the candidate
computes <<2 first and keeps the header in a t-register. That is the same
t4/t5 ring (coherence 98 percent, new mapping at +0xD4). Named header or
count locals add 8 bytes of frame and regress (56 and 187). A block-scope
intensity s32 also returns the frame to 0x50. Byte-offset coordinates match
the Vec3f form exactly. L59 same-line flags fold was inert.

Next: a form that lets the header die in v0 before index<<2 starts, without
a memory-class pointer local, so the lbu/sll order and ring can share one
register. Do not retry direction/index/offset carriers, declaration-order
rotation, or named header/count/value locals.

#### 2026-09-19, lane w27-o041: header-in-v0 identity, second-auto tax

Re-measured on campaign/unchain 82bf3cfa: 820 bytes, delta 0, masked 41,
frame 0x48 with nine slots agreeing, 7 relocations, first +0x20. Aligned
164 exact, 39 naming, 0 immediate, 2 structural. Draw census proc=12: 73
draws, 292 emissions. Register census: one coherent ten-register ring
(98 percent, new mapping at +0xD4). Identity gate: instrumented IDO .text
byte-identical to stock. CDX_PROC=12, 15 p1 decisions, 0 p2. Unforced
records have forced=-2.

The swapped pair at +0x64 is one WAR on v0. Target holds the header in v0,
loads count, then redefines v0 with the scaled index (web 254, bb=5,
save 4.0, nocs 1). The candidate keeps the header in a ring temp so the
scale is independently ready and as1 emits it first; that extra live
range is the t4/t5 ring. Confirmed by decode of the three-word window:
candidate load / scale / count-load versus target load / count-load /
scale, then the same slt and beql.

Named header (pointer, register pointer, or s32) closes that window
exactly, including the v0/t1/a0 registers, and scores 56 at frame 0x50.
register is ignored at -O2. The extra home is unused (still nine used
slots); cfe adds 8 bytes for any second extra auto. Function-scope
header, both-register, and last-declared L109 zero OR-equals 0 all pay the same
8-byte tax; zero does not close the swap (93). Union with the rotation
array is address-taken and inserts a spill (196, delta +12).

Deleting the segment local and naming only header closes the swap at
frame 0x48, but the generated segment cursor dies after the first
coordinate store and the y/z path is rebuilt (189, delta +40, ten extra
words). An L160 slot index plus named header also closes the order and
grows the frame (199, delta +24). Struct-copy of the coordinate row and
flags-before-stores on that shape both regress. Operand reverse, L59
#line, and comma leftover updateRate OR-equals 0 are byte-identical to the
41-word baseline.

Forces: p1:w254=c2/c7/c8 accepted (forced equals the requested colour)
and leave the scale-before-count order, scores 45, 95, 91. p1:w18=c1 and
p1:w253=c1 declined (forced=-2), score 41. Colour cannot serialize the
pair.

Retained source is the 41-word JFG control-flow body. Do not retry a
second extra auto, register, union-with-rotation, operand reverse, L59
#line, leftover-in-updateRate, L109 zero, clobbering the rate parameter,
struct-copy of the coordinate row, or flags-before-stores on the
sole-header shape. Next: keep the generated segment cursor live through
all three coordinate stores on the sole-header 0x48 form, without a
second cfe auto, so the closed v0 pair and the y/z reuse can coexist.

#### 2026-09-24, lane p2b-lights: live cursor on the sole header

Hypothesis: on the sole-header frame-0x48 form, keep one coordinate
cursor live through x, y, and z without a second cfe auto. Baseline
remeasured at 820 bytes, delta 0, masked 41, frame 0x48, 7 relocations,
first mismatch +0x20.

Spelling 1 reassigned that only pointer from the segment to the
coordinate row and stored x, y, and z through it. Frame stayed 0x48, but
four indexed words disappeared: 190 masked, size delta -16. A row
pointer is not the target's indexed cursor.

Spelling 2 named only the header and joined the three indexed field
stores with a comma. The comma did not keep the segment cursor live:
189 masked, size delta +40, frame 0x48, the same y/z rebuild as the
earlier sole-header form.

Spelling 3 reused the header local as the coordinates40 base and indexed
the three fields through it. Frame stayed 0x48. Score 189 masked, size
delta -16, with the same four missing indexed words. The saved base did
not restore a delta-0 cursor.

None lowered the masked count at delta 0, and none kept frame 0x48 with
one live three-store cursor without raising the masked count. Reverted
to the 41-word body. Stop. Those three spellings do not produce the
header-in-v0 order and a live indexed cursor together.

<!-- plateau-handoff:func_80018F08:end -->
