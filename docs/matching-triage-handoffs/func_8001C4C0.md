<!-- plateau-handoff:func_8001C4C0:start -->
### `func_8001C4C0` plateau handoff

- source: `src/main/charControl.c`
- score: 121/403 words
- frame: 0xA8
- relocations: 38
- first mismatch: +0x3C
- summary: hypothesis=loop-weighted references on leftover const webs 10 and 3; spellings=plus-assign and minus-assign fold away, (x+C)-C folds away, xor pair at delta +16 adds 80 to the carrier; stall=totalsave 10 and 11 unchanged at 121/403 and delta 0

Summary before this remeasure: Delta 0, frame 0xA8. Shared i/j loop counters open s5/s6; left: schedule order at entry, point-loop v0/v1, ring phase.

Summary before this remeasure: Identity-gated proc 10: two type-2 webs split at totalsave 10 and 11 vs callee cost 16.25 so s5/s6 never allocate. L99 unused pointer is not the save area.

Best candidate remains the committed NON_MATCHING body: 401 words vs 403, delta -8, masked 386, frame 0xB0 vs 0xA8, first mismatch +0x0. Target saves ra and s0-s6; this candidate saves ra and s0-s4. player and actor occupy two of those saved registers on both sides.

Identity gate: instrumented IDO at proc 10 is .text-identical to stock tools/ido/cc for this function. Two type-2 symbol webs (variable table 3 and 10) record decision=split because totalsave 10 and 11 is not strictly below callee-saved bestcost 16.25. A split web has no colour, so CDX_FORCE cannot reach it. Colouring those two webs is what would take s5 and s6.

Eliminated this session:
- L99 unused pointer or extra f32 as a save-area lever. An extra pointer local grew the frame 0xB0 to 0xC0 and kept five s-registers.
- L109 XOR-with-zero and AND-with-minus-one on player, effectOwner, and particleCount. Those uses attached to already-colored coalesced copies and left the split totalsaves unchanged.
- Hoisting effectOwner and characterData to function entry. Still five s-registers, size delta +12, aligned really-different 125 to 150.
- Per-arm particle count and entries loads in isolation. The three-arm entries load into a saved register matches the target shape, but the count stays in a caller-saved temp, the function grows to 406 words, and the frame grows to 0xB8.

Next: loop-weighted references that survive copy-prop onto the leftover symbol webs at variable table 3 and 10, not onto their coalesced copies. Do not run a colour landscape until delta is 0. Do not change TU flags.
- B3-char pass (2026-09-23), Track B: size delta -8 to 0, frame 0xB0 to 0xA8 (the target's, ladder slot-for-slot identical), masked 386 to 121. Aligned buckets now byte-exact 305, naming 77, immediate 1, really different 26.
- the earlier closure is corrected: the two type-2 split webs (totalsave 10 and 11) are the constants 10 and 3 and stay split in the target too. The target's two extra saved registers come from the loop counters. It uses one `i` for the point copy counter, the effect count, the particle slot count, the byte-table loop and the spawn packet index, and one `j` for the effect counter and the particle count. Merging them took s-register use from s0-s4 to s0-s6, with player in s5 and actor in s6 as the target has, and no force.
- the edits that moved words, each measured: shared i/j plus `camSetNo(playerIndex)` with one argument plus an if/else for unk192 (386 to 335); computing `position` before the slot stores (struct rows 111 to 95); `while (j--)` for the particle loop (95 to 89); loading count and entries per arm of the level-type if (335 to 259, delta -8); hoisting `i = 0` above the playerIndex test (259 to 239); `1.0` for unk17C, which gives the target's third float materialisation (239 to 230); writing the byte-table value as `i * 8` with no carrier, which stops strength reduction (230 to 168, delta 0); dropping `effectList` and placing effectIndex third and packet eight declarations down (frame 0xB0 to 0xA8, 168 to 160; a 40-cell layout sweep); `effectIndex = player->unk1` tested through the local (160 to 122); `pointIndex = 0` inside the guarded block (122 to 121).
- flat or worse: merging pointIndex into j (frame right, 183); merging stateCursor or position into effectOwner (delta +8); `(s16)` and subscript spellings of characterData (flat); `i = 0` before characterData (flat).
- what remains (121): the entry block's store order (swc1 to unk10 and sh to unkFE, two words each way), the point loop's v0/v1 swap (output and pointIndex), a ring phase from +0x200 where effectIndex*8 and `i = 0` are emitted in the opposite order, and the tail of the particle loop. This is now a delta-0 naming and order residual, so the colour and draw instruments apply.
<!-- plateau-handoff:func_8001C4C0:end -->
