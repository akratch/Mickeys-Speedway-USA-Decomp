<!-- plateau-handoff:func_8001EC44:start -->
### `func_8001EC44` plateau handoff

- source: `src/main/charControl.c`
- score: 224 differing words
- frame: 0xA0
- relocations: 43
- first mismatch: +0xC
- summary: No natural spelling matched the dead-if bytes. Inert end regions do; (void)arg0 and a sixth parameter do not. Sibling entry is delta -4. Dead if kept.

Summary before this remeasure: Delta 0, frame 0xA0. Blocker: callee toll 4.75/4.85 vs caller 5.0 puts pos/plane in s0/s1; target has 2 more uopt blocks.

Summary before this remeasure: Target 238w, frame 0xA0, 47 relocs. The 0x40 frame excess is a declaration census and the exact target frame IS reachable -- dropping the three cross-product carriers hits -0xA0 -- but that spelling duplicates the sub-expressions and costs 24 instructions (265 versus 241 against a 238 target). So the target drops those three carriers WITHOUT duplicating the terms; find that spelling. Reassigning them into the crossY/crossZ slots instead is 230 words but 242 instructions.
- ownership/ABI: resident `0x8001EC44..0x8001EFFC` (ROM `0x1F844..0x1FBFC`), no padding; five-argument collision callback `void (s32, ControlVector3 *, ControlVector3 *, f32, ControlCollisionPlane *)`, passed to `func_80010900` from the owning TU.
- baseline: fresh configured V0 reproduced 238 target words versus 240 candidate words, 236 positional differences, frame `0xA0` versus `0xD0`, and 33 versus 47 static relocations with two exact identities.
- retained candidate: 238 target words versus 241 candidate words, 231 raw and relocation-masked differences, first `+0x0`, frame `0xA0` versus `0xE0`; candidate/target relocation counts are 45/47, eight offsets/types align, and two identities are exact.
- semantic repair: adjacent collision-state floats now retain their nine separate relocation identities; the third branch preserves the original plane X/Z normal for normalization and result-state stores instead of using Y/Z or angle-delta values.
- bounded attempts: ten configured forms covered scalar relocation identities, point-coordinate lifetimes, separate and reused angle webs, a full distinct-web m2c draft, direct plane reloads, block-scoped original normals, `register` qualifiers, raw `f32 *` vector spelling, and a register-qualified distance web. Attempts 8-10 were byte-flat; no exact result emerged.
- donor/oracle evidence: JFG's assembly-only `controlSquashCheckPrior` is a different high-level routine. A direct five-reference masked four-gram scan over the proved Mickey range tops out at 0.0648 on BK `guLookAtStereoF`; no JFG function enters the top 15, so there is no credible code donor.
- flag/permutation disposition: `flag_sweep.py` refuses this unsymbolized resident fallback before compiling because no unique `symbol_addrs` owner exists. No generic permutation was run from the broad structural basin.
- finalizer disposition: `finalize_plateau.py` refused because this shared TU contains the unrelated legacy inline `func_8001D880` handoff. The target marker was appended without moving measured source lines, and `plateau_handoff_audit.py --write` generated this fixed shard.
- next lever: find a source-authentic macro/aggregate or alias shape that keeps the plane-dot calculation in the common prefix while assigning original X/Y/Z normals to the target saved-FP pool and leaving both pointer parameters in caller-saved registers; do not repeat flags, qualifiers, raw vector types, or these ten web forms.
- B3-char pass (2026-09-23), Track B: size delta +12 to 0, frame 0xE0 to 0xA0 (the target's), masked 231 to 224. Aligned buckets now byte-exact 52, naming 94, immediate 9, really different 101. Reader cycle 0 labelled the main pair missing-CSE; the real causes were carrier shape and the callee toll below.
- what closed size and frame: the m2c body was rewritten naturally. Point components live in x/y/z; the third branch updates them in place (`x -= u` and so on), which is what stops uopt copy-propagating u/v/w away (without the in-place update the u/v/w webs vanish into expression temps). The cross product reuses u/v in place. `value` is reused as each branch's delta. One unreferenced f32 is required: removing it gives masked 228 at delta 0 with a different frame (L99). Branch-1 carrier spellings (x/y/z against u/v against a rotated set) were byte-inert across 12 cells; a `(s16)` cast on the Arctanf result is inert.
- the named decision variable: the target keeps pos in a1 and plane in a3, saving and reloading them around each call. Ours gives them s0/s1: p1 records show w16 (pos) totalsave 17 with a1 cost 5.0 and s0 toll 4.85, and w2 (plane) totalsave 10 with a3 cost 5.0 and s1 toll 4.75. `CDX_FORCE=p1:w16=c4,p1:w2=c6` was accepted (forced=4, forced=6) and reproduced the target int-bank shape. Any dead `if` adds two uopt blocks and lifts the toll to 5.0/5.35, which does the same from source, so the original procedure has two more uopt blocks than this body. The committed `if (arg0 == 0x7FFF)` is a diagnostic stand-in for them, not the original; without it the same body scores 215 at delta -8.
- tried for that block and rejected: a `hit` flag variable for the first test (the right int bank, but it emits code, delta +12); the same flag as a boolean expression (the toll does not move); a dead `if (angle == 0.0f)` before the angle test (the right int bank, delta +16).
- what remains: (1) FP bank. The target's f26/f28/f30 hold branch-1 t-values and the branch-3 new point; ours opens f20 for `value`, which spans the branch-3 calls. Using a separate delta in branch 3 removes that but costs size (sweep of 48 cells over entry order, delta carriers and cast: only the all-`value` shape is delta 0). (2) The target loads plane->distance first and keeps it in a split local at 0x70; a `d` local moves the loads but leaves pos components in memory homes. (3) The target stores crossY and crossZ to 0x54/0x4C, the homes it later uses for the point components in branch 3.

#### 2026-09-24, lane w4-char: natural spelling of the labelled dead if

Stock configured NON_MATCHING compile of the retained body: 238 words, size delta 0, 224 masked words, first mismatch +0xC, frame 0xA0, 43 text relocations. The dead if emits no instruction. Removing it is 236 words, size delta -8, 217 masked, first +0x0, with pos in s0 and plane in s1.

Byte-identical to the dead-if object, so not a better residual, and not adopted as natural source: one empty do-while, two empty do-whiles, an empty if (1), an empty if on arg0, if (arg0 == arg0), the same assignment split across lines, switch (arg0) with only a default break, and if (1) wrapped around the two tail stores after the dead if is deleted. Those are the same inert end region.

Eliminated as a natural replacement for that region:
- (void) arg0 in place of the if is 236 words, the same failure as deleting the if.
- Naming the callback's sixth parameter (the actor) on the prototype and the definition is byte-identical while the dead if stays, and 236 words when the if is removed or replaced with (void) actor. The extra parameter does not raise the toll.
- The sibling distance-first dot product, assigning plane->distance to value and then value = (z * nz) + ((nx * x) + (ny * y)) + value, with the dead if kept: size delta -4, 218 masked, first +0xC. ny stays off f20 and f30 stays unsaved. Not adopted; size left delta 0.

No spelling was ROM-exact. The dead if stays. Stop: the natural block-source hypotheses are eliminated and no delta-0 residual improved.
<!-- plateau-handoff:func_8001EC44:end -->
