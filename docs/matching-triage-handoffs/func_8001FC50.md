<!-- plateau-handoff:func_8001FC50:start -->
### `func_8001FC50` plateau handoff

- source: `src/main/models.c`
- score: 277 differing words
- frame: 0x78
- relocations: 3
- first mismatch: +0x18
- summary: Delta 0, frame 0x78; naming left: target keeps modeBytes homed in both arms and gives pointBytes ra, here modeBytes takes ra

Lane B3-cmt, 2026-09-23. Start: delta -12, frame +8, 299 words (aligned 227).
The census label missing-CSE was not a CSE. Measured steps: allocation-size
local removed, all locals at function scope in an 18-slot order that puts
matrixBytes at -0x10, modeBytes at -0x30 and instance at -0x40 (frame 0x78
closed); the state reset as four indexed stores through `state[i]` with the
goto kept (the for and do-while forms unroll to +220; the goto form matches the
target's per-store pointer reload); modeBytes a scalar (the array form costs +68
because the sum stops being a CSE temp across the call); then matrixBytes
carrying the count before its shift, which reached delta 0 at 277. Four other
carriers for the count (i, j, modeBytes, reordered lines) stayed at -4.

Left, all naming: modeBytes (web at -0x30, total save 8 against caller cost 4)
is coloured, where the target stores it to its home in both arms and reloads
it; pointBytes then takes ra in the target. Named intermediates for the two
products moved the frame to 0x88 and were dropped.

Summary before this remeasure: JFG size and cursor lifetimes cut the frame by 0x8; next lever is the target constant audit.
<!-- plateau-handoff:func_8001FC50:end -->
