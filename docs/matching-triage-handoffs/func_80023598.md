<!-- plateau-handoff:func_80023598:start -->
### `func_80023598` plateau handoff

- source: `src/main/camera.c`
- score: 13 differing words
- frame: 0x90
- relocations: 32
- first mismatch: +0xA8
- summary: Delta 0, frame 0x90; baseScale wins the f16 tie with distanceScale (force w30=c29 gives 3), plus a propagated copy and Arctanf load order

Lane B3-cmt, 2026-09-23. Start: delta +8, frame +16, 263 words. The reader's
missing-CSE label was a varargs call: `func_80034434` takes one argument, and the
four-argument call spilled its extra operands (delta to -12). Measured steps:
GBI colour macros with the unmasked colour (Gfx local gone) 260 to 247;
declaration order laid on the target ladder (all 19 locals take homes in
declaration order down to the last one needed) closes the frame to 0x90, 239;
the colour else written as if/else, 243 at -12; an empty region at the join
raises the callee toll (L56, 8.70 to 9.20) past dlist's total save 9, so dlist
splits to its home and s1 goes, delta 0 at 127; the index reassigned to the
scale index, 49; angle finished after frameCount is read, 13.

Decision variables left, each measured: webs 30 (baseScale) and 53
(distanceScale) tie at total save 6 over 4 blocks and ascending web number
gives baseScale f16; forcing w30=c29 scores 3. Declaration order (4 permutations)
and eleven statement placements for the two scales did not reverse the tie.
The last 3 words are the distance multiply reading the propagated baseScale
copy instead of distanceScale, and the order of the x-home and transform.z
loads before Arctanf (four line layouts inert, a camera pointer local worse).
The empty region is an inert spelling; placements before the then-end or in
the else arm before distanceScale's definition return to 227.

Summary before this remeasure: JFG efd5abb adds no counterpart for Mickey's high-level sprite path beyond the donor already exhausted here; next lever is Mickey-authenticated source context.
<!-- plateau-handoff:func_80023598:end -->
