<!-- plateau-handoff:func_80024978:start -->
### `func_80024978` plateau handoff

- source: `src/main/camera.c`
- score: 71 differing words
- frame: 0x30
- relocations: 13
- first mismatch: 0x8
- summary: Force pair 0 at delta 0 via --object. Unforced +4/71. Pair ilda splits only under peel caller-save pressure. a2/a3 tie independent. Forced-0 is not a match.

#### 2026-09-17, lane w12-cam: dest cursor closed; two named forces score 0

JFG `camCopyOrthoMatrix` is a 15-iter copy; Mickey multiplies each slot by
`D_80079F48`. IDO peels 15 % 4 = 3, so a 12-iter remainder whose dest is
`((f32 *)matrix)[i]` with `i` starting at 3 gives the target walking pointer
at matrix+12 and zero immediate rows. The second and third coefficients share
one locally-defined symbol (`D_80079F50` and `(&D_80079F50)[1]`).

Unforced: 84 vs 83 words, masked 71 first +0x8, aligned 72 exact, 9 naming
(a2 to a3), 0 immediate, 3 structural, extra word at +0x3C. Frame 0x30
unchanged. A 15-iter loop over `&D_80079F4C` is 78 exact but one word short.
A 14-iter loop over `&D_80079F50` is delta 0 with the shared high-half but
walks dest from matrix+8 (10 immediate rows).

Identity gate: instrumented IDO .text matches stock. Procedure 62, seven p1
decisions. Web 47 is the type-1 two-use ilda of the `D_80079F50` pair, save 1,
nocs 1, block 1 only, coloured a3. Web 0 is the matrix parameter, save 2.5,
nocs 2, available only a2 and a3 at cost 2, coloured a2.

Force-split web 47 (accepted): delta 0, masked 8, 75 exact, 8 naming, 0
immediate. Force web 0 to a3 (accepted): still +4. Both together: masked 0,
delta 0, 83 aligned exact, 3 raw reloc-addend artifacts. That is the match.
as1 shares `lui $at` for the split locally-defined pair; the matrix copy
must land in a3.

Do not retry: dest[i+1] 14-loop (cursor +8), standalone 2-iter pair loop
(+52), walking `*src++` (+16), `if (1)` / `do { } while (0)` / volatile /
u8*+4 spelling of the pair (ilda stays), L109 OR-zero on i or unread height
in the 12-loop (CSE, no colour move). Next source: split web 47 without
CDX_FORCE, and break the a2/a3 tie toward a3.

#### Surplus word located, 2026-09-09

The `+4` is a third `%hi` address materialisation. The target materialises two
for the three matrix coefficients at `D_80079F4C`, `D_80079F50` and
`D_80079F54`: one serves the first load and one serves the other two, whose
low halves are folded into their own loads. The candidate materialises three,
one per symbol, because each is a separate C object and the assembler cannot
know two distinct symbols share a high half. The scale factor at `D_80079F48`
is referenced three times and is correctly a coloured base in both.

Declaring the three coefficients as one three-element array closes the count
exactly -- 83/83 words, 34 differing, down from 84/83 and 67 -- but it is a
count coincidence rather than the shape: the array makes IDO colour a base
register for it, where the target uses the assembler temporary. It also cannot
be committed as it stands, because the still-generated fallback for this
function names `D_80079F50` and `D_80079F54`, so removing those definitions
breaks the link; adopting it would need the extent recorded in
`symbol_addrs.us.txt` and a re-extract. `(&D_80079F4C)[1]` and `[2]` reproduce
the same 83/34 without touching the data, which confirms the mechanism is the
shared base rather than the storage.

Measured and rejected: a two-element array at `D_80079F50` (+1, 73), the same
`static` (+1, 73), `(&D_80079F50)[0..1]` (+1, 73), `static` on the three
coefficients (+1, 69), `static` on all four globals (+1, 69), and
`((f32 *)matrix)[i + 3]` for the twelve-element loop (+1, 67).

The second, independent difference is the unrolled loop's cursor: the target
biases it to `matrix + 12` and stores at negative displacements, while the
candidate keeps `matrix` and stores at positive ones. Same instruction count,
and the index rewrite above does not move it.

Next lever: evidence for how two distinct-symbol coefficient loads come to
share one high half under this toolchain -- an extent or storage-class fact
about the `.data` block, not another access spelling.
#### 2026-09-13, lane k1: authenticated draw-census follow-up

Fresh configured stock compilation reproduces 332 target bytes,
size delta 4, 67 raw and
66 masked differences, first +0x8.
Candidate frame is 0x30; the procedure-62 census records
26 draws and 88 emission records. Stock and traced full-TU text
compare identically. Static relocation counts are 13 candidate and
12 extracted target, with 9 identical offset/type/symbol tuples.
These are static measurements; overlay runtime identity proof remains separate.

Paired alignment has 63 exact, seven naming, ten immediate and three
structural rows, plus one candidate-only word at +0x5C. This is 84 candidate
words against 83 target words. The positional 66 is dominated by the surplus
address-materialization shadow and unrolled cursor displacements; it is not
66 independent colour decisions. The census records three draws on each of
the three coefficient-store lines.

The shard and source annotations already cover scalar/aggregate views,
end pointers, loop forms and storage classes. The recorded shared-base
experiment repairs extent by changing a data identity, while also creating
a coloured base the target does not use. No new Mickey-authenticated declaration
or TU-layout evidence was found that permits adopting that identity. Repeating
access spellings would hold this decisive input fixed. ADR 0018 early evidence
stop applies with zero new source attempts; no colour landscape was repeated.
The remaining question is source-authenticated coefficient ownership together
with the unrolled cursor bias. This confirms the baseline, not a new source
impossibility or general layout closure.

The original guarded body and assembly fallback are retained. Sources, stock
and traced objects, frame and scalar measurements, draw profiles and aligned
maps remain ignored under build/k1/func_80024978.
Commands: lane_status.py, configured stock compilation, draw_census.py,
residual_map.py --object/--against where compared, finalize_plateau.py and
tools/gates.sh. No executable bytes are newly credited.

#### 2026-09-18, lane w19-cam: force pair re-proved; split needs peel pressure

Re-measured unforced: 332 B, size delta +4, masked 71, first +0x8, frame 0x30
on both sides (four shared slots). Identity gate: instrumented IDO .text
matches stock (decomp-workbench fidelity PASS). Procedure 62.

Forces scored with score_symbol.py --object, acceptance read from forced=:

- p1:w47=s accepted (forced=-1): masked 8, delta 0. Aligned 75 exact, 8
  naming, all a2 to a3.
- p1:w0=c6 accepted (forced=6): masked 68, delta +4.
- both accepted: masked 0, delta 0, 83 exact. That forced object is not a
  match.

Base ladder, seven p1 colours: web 17 v0 save 35.5, web 16 v1 save 30.5,
web 46 (D_80079F48, type-1, bbs 1 and 5) a0 save 21, web 48 (loop
D_80079F4C, type-1, bbs 5) a1 save 10, web 0 (matrix, type-3) a2 save 2.5
with a2 and a3 tied at cost 2, web 12 (i) a1 save 1, web 47 (D_80079F50
pair, type-1, bbs 1) a3 save 1 totalsave 1 bestcost 0. L139: 1 greater than
0, so it colours. Split-only leaves web 0 on a2; the a2/a3 tie is decided
before web 47 and is independent of the split.

Source attempts that held the dest[i] i=3 cursor and did not repeat the
scored 15-iter / 14-iter dest-walks:

- if (1) / do-while (0) around the second pair store, or around each: masked
  69, still +4. Web 47 becomes nocs 2, bbs 1 and 2, save 0.5, still colours
  (now v0). Aligned 68 exact, 10 naming, 4 structural, extra words at +0x64
  and +0x68. L97 region is not an L110 ilda split.
- goto between the pair stores, i=3 between them, comma, u8*+4 on the second
  only, s32 address plus 4, volatile on the second, *(&D_80079F50+1), 2-D
  peel, dest after the call, dest before the call, peel via i and i+1: all
  byte-identical to the 71 / +4 base.
- if (width) around the second store: +16, masked 74.
- i = 3 before the call: +24, masked 81.
- L144 dest = *(f32 **)&matrix: +8, masked 83.
- width/height as s32[2]: still +4, masked 73.
- pair stores before the call: +12, masked 86.
- two loops 0..3 then 3..15 over D_80079F4C: delta -68, masked 81.

Pressure diagnostic (eight peel copies of width/height sunk into a volatile):
+64 bytes. The pair ilda (same table 1412) then reads decision=split,
bestcost 4, all caller-saved taken. Matrix (table 1114) still colours a2 at
save 2.5; the extra webs are save 1 and cannot steal a2 from it.

Blocker: a same-block two-use locally-defined ilda colours because totalsave
1 is greater than bestcost 0. Natural split needs every peel caller-saved
occupied. The peel is loop-depth 0, so L109 probes cost instructions; eight
extra integer webs cost a prologue of moves. A real branch splits the ilda
into two macros, not a shared high-half. Higher-save webs take v0 through a1
at cost 0 and will not take a2 while those colours remain cheaper, so the
a2/a3 tie cannot be broken by save-1 pressure. Next lever: a zero-width way
to occupy peel caller-saves, or a form that never builds the two-use ilda
while still emitting ugen's split locally-defined high-half. Do not treat
the forced-0 object as a match.

<!-- plateau-handoff:func_80024978:end -->
