<!-- plateau-handoff:func_80028564:start -->
### `func_80028564` plateau handoff

- source: `src/main/main.c`
- score: 306 differing words
- frame: 0x58
- relocations: 245
- first mismatch: +0x4
- summary: Pair 2 extra-ISTR owns line 1196 gDPFullSync w1. Block-scope and neg-index drop the frame; chained 1s is inert. Stall: Olimit 100 skips uopt (needs 137).

Summary before this remeasure: V0: 492/489 words, exact 0x58 frame, 426 positional and 427 raw diffs. Relocs 245 versus 201; 59 sites and 25 identities align. Saved-register web persists.

Cycle 0, stock `-Wo,-Olimit,100`: size +8, frame +0, positional 306, shadow 53, aligned residual 253. Seven pairs. The function label is pair 2, extra-ISTR, shadow 19: the candidate-only store is line 1196 `gDPFullSync(D_800CF518++)`, the w1 write after ugen spills the macro's `_g` (uopt warning: this procedure needs `-Olimit` >= 137, so uopt skips it). The net +8 is a dead s0 save at +0x4 and reload in the epilogue; s0 is never used in the body. Candidate relocations 245, target 201. First mismatch +0x4.

Spellings, all stock, none better aligned residual:
- Block-scoped the function locals and replaced the two display commands with negative-index stores. s0 remained. Frame fell to 0x50, size +4, aligned 273.
- Negative-index commands alone (locals kept). Same frame loss: the two `_g` homes are part of the 0x58 frame. Aligned 274, size +4. The extra-ISTR window still had a candidate store and a candidate load.
- `D_8007A198 = D_8007A19C = 1` for the tail constant. Byte-flat: size +8, frame +0, aligned 253, pair 2 still the line 1196 store.

Diagnostic only, not stock: `-Olimit,200` deletes s0, matches the full-sync double store and w1-then-w0 order, and puts ra at 0x24, but the function is 52 bytes short and one open pair from +0x6C leaves aligned residual 263. Not retained.

Stall: three stock attempts, no better aligned residual. The extra store is the unoptimized `_g` spill; deleting it drops the frame. Do not colour-sweep while the size is off, and do not raise the TU `-Olimit` without an impact review (it also covers `func_80026FB4`).
<!-- plateau-handoff:func_80028564:end -->
