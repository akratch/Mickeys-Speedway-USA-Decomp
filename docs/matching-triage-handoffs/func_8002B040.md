<!-- plateau-handoff:func_8002B040:start -->
### `func_8002B040` plateau handoff

- source: `src/main/matrix.c`
- score: 34 differing words
- frame: frameless
- relocations: 0
- first mismatch: +0x0
- summary: O2 35w/34 masked. Force p2:w15=c28 or driver -O3: 34w 3 mtc1 (sdk_copies+force=18). f16 INF on scalar webs; copy/L144/L97/store-kill stay 2-of-3.

Configured measurement is 136 bytes, size delta +4, 34 masked, first +0x0, frameless, zero relocations. Identity-gate: instrumented IDO text is byte-identical to stock at -O2 (35 words). Procindex at -O2 is six procedures; this function is CDX_PROC=5 (seven p2 decisions, a leaf).

Decision variable: class-2 web 15 (arg2 home at 8(sp), save 3, nocs 1) is no-color because totalsave 3 is not greater than callee bestcost 4 (L139). Caller f12/f14 colour the other two scalar webs at cost 0; f16 (c28) is infinite-cost for all three incoming-scalar webs even though it is not in forbidden0 (only f12/f14 bits). CDX_FORCE=p2:w15=c28 is accepted (forced=28) and yields 34 words, three mtc1, zero sw, 24 masked. The same force on the matrix-first copy body scores 18 masked at +0x44, matching the best driver -O3 object. Force onto f0 is 30 masked; onto callee c30 is 38 words; onto already-taken f12/f14 is forced=-2.

Driver -O3 on this TU succeeds and matches that 34-word census. tools/ido-phases.py phase-all-O3 appends -O3 after the driver's -O2, and uopt uses the first, so the lattice row is inert here.

Source axes that still 2-of-3 mtc1 at configured -O2: extra copies, L144 address-of-formal, L97 if(1) and do/while(0) regions, goto-split copies, store-kill of one formal, register formals, K&R, 2-D indexing, accumulator, mul-by-1 copies. They rotate which of a1/a2/a3 is spilled. Assignment order of copies is live at driver -O3 (18 to 24 masked) and floors at 18 on matrix-first copies. Declaration order is flat. Do not adopt -O3 for this shared TU (ADR 0007); the other five functions in the file remain unmatched.

Next: a source form that makes c28 finite for the third incoming-scalar web at -O2 so L139 colours it without a force, or evidence that the 18-word residual is unreachable from C at -O2.
<!-- plateau-handoff:func_8002B040:end -->
