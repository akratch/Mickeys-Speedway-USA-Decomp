<!-- plateau-handoff:func_8002B524:start -->
### `func_8002B524` plateau handoff

- source: `src/main/memory.c`
- score: 116/116 words
- frame: 0x58
- relocations: 12
- first mismatch: none
- summary: MATCHED. This header claimed an open residual until 2026-09-16, when tools/check_shard_metrics.py reconciled every shard against the ranking; the function had already left the queue. The claim it carried was: 14 -> 2 (2026-09-11, lane w3-low), and the recorded 'no source change adopted' is refuted. The 10 naming and 2 structural words were ONE decision and the structure was downstream of the colouring: the slot pointer's field was a single web where the ROM has two ranges, an argument register for the range tests plus a copy for the split-call offset. Caching the field at exactly three of its five uses, with the carrier declared last, closes all twelve; neighbouring partitions regress to 49-56. What is left is 2 immediate-only words, and 2026-09-11 lane p6-tight restates them as a local COUNT rather than a frame identity. Every declared local consumes a four-byte cell assigned from the frame top downward in declaration order, regardless of storage class, of whether it is ever referenced, and of block scope; the compiler temporary area sits strictly below the last cell. Four measured layouts fit that rule and no other, so the ROM declares exactly SIX locals and the seventh cell this candidate spends on the carrier is the whole residual. That also confirms and widens the falsification of L99's clause that an unused s32 is eliminated before the frame is sized: a register-class local is not eliminated either. Newly flat at two words: the carrier declared register in three integer spellings, retyped u32 and s32 with casts at the use sites, and declared inside the search loop or the free-slot test rather than at function scope, so block scope does not overlay cells here. Folding the two module outputs into a two-element array is 15 words; dropping the carrier and re-reading the field at all five sites is 24. The open question is exact: two live ranges for the field without a seventh declared local, which on this compiler means two expression webs from two spellings of one load that do not common with each other.

#### 2026-09-11, lane p6-tight: the residual is a local COUNT, and the cell law is now fitted

Re-measured unchanged: 464 bytes, 116 of 116 words, size delta 0, positional
masked 2, aligner buckets 114 byte-exact, 0 register naming, 2 immediate only,
0 really different, first immediate-only difference +0x138.
`tools/frame_census.py` says the two frames are both 0x58 with twelve slots
each, the ladders agree everywhere except one cell, and the disputed cell is a
four-byte compiler temp with one load and one store: the target puts it at
+0x3C, this candidate at +0x38.

**The cell law, fitted from four measured layouts rather than assumed.** Every
DECLARED local consumes a four-byte cell, assigned from the frame top downward
in DECLARATION ORDER, whether or not it is memory class, whether or not it is
ever referenced, and whether it is declared at function scope or inside a
block. The compiler temporary area sits strictly below the last cell. The four
readings that fit it and no other rule:

- the shipped source declares seven locals and its cells run 0x54 down to
  0x3C, with the temp below at +0x38; the three cells that carry traffic are
  the two address-taken module outputs and the volatile caller-address slot,
  and they sit fourth, fifth and sixth in declaration order exactly where the
  rule puts them;
- an unused `s32` added after the last declaration moves the temp to +0x34 and
  moves nothing else, so an unused local is NOT eliminated before the frame is
  sized (this confirms the 2026-09-11 falsification of L99's clause and
  extends it: a register-class local is not eliminated either);
- two unused `s32`s grow the frame by eight and lift all three traffic-bearing
  cells by eight, to 0x54, 0x50 and 0x4C, with the temp still at +0x38;
- the seven declaration positions of the slot-data carrier walk the three
  traffic-bearing cells down one word at a time, giving 10, 10, 10, 8, 6, 4, 2
  differing words, which is the same ladder read from the other end.

**Therefore the ROM's translation unit declares exactly SIX locals**, in an
order whose fourth, fifth and sixth are the two module outputs and the
caller-address slot; the seventh cell this candidate spends on the slot-data
carrier is the whole residual. The requirement is now exact and does not
mention registers at all: produce the two live ranges for the slot's data
field without a seventh declared local.

**Measured and flat at two words this pass, all at zero size delta and all
leaving the cell in place**: the carrier declared `register` (three integer
spellings), retyped `u32` and `s32` with the casts moved to the use sites, and
declared inside the search loop's body or inside the free-slot test's block
rather than at function scope. Block scope does not overlay cells here, so it
is not a route to sharing one. Folding the two module outputs into a
two-element array is 15 words. Dropping the carrier entirely and re-reading the
field at all five sites is 24 words at a different frame, which is the earlier
recorded one-web form.

Not yet tested, and the only remaining direction: a carrier that is not a
declared local at all, which on this compiler means two uopt expression webs
for one field read. Two spellings of the same load that do not common with each
other would do it; the four tried here all common.

#### MATCHED, 2026-09-11, lane p6-tight

`gmake verify` prints the expected ROM hash with this function compiled from C,
the `NON_MATCHING` guard removed and its extracted assembly pruned. 464 bytes,
116 of 116 words, size delta 0, positional masked 0.

The edit follows straight from the cell law fitted above. The seventh declared
local existed only to split the slot's data field into the two live ranges the
ROM allocates, and it cost a cell, which pushed the slot-pointer save one word
low. Deleting it and writing its three uses -- the two range tests and the
split call's offset -- as unsigned arithmetic on the field produces the same
two webs through two expression webs instead of one symbol, and an expression
web costs no cell. The equality test and the direct return keep reading the
field as a pointer, and that difference in expression identity is what keeps
the two webs apart: written as a pointer at all five sites the value commons
into one web (24 words), and written through the unsigned round trip at all
three contested sites it splits (0 words).

Five spellings of the round trip reach zero and are interchangeable: casts
through `u32`, `s32`, `unsigned long`, a doubled pointer cast, and the fully
unsigned comparison form adopted here. A cast to `void *` does not, which is
the control: the split needs an INTEGER intermediate, not merely a different
cast. Retiring this closes the `nonmatching-ranking` row and makes
`src/main/memory.c` a fully-C translation unit.

<!-- plateau-handoff:func_8002B524:end -->
