<!-- plateau-handoff:func_80035F48:start -->
### `func_80035F48` plateau handoff

- source: `src/main/textures_354C8.c`
- score: 371 differing words
- frame: 0x88 (target 0x90)
- relocations: 4
- first mismatch: +0x0
- summary: Complete semantic C emits 362 versus 383 instructions; delaying the TMEM carrier improves positional differences by 14 words, but the texture pointer occupies s0 and displaces the target's rtile carrier.

Fresh configured evidence on base `4f79f7586293c65bb12387d9b703a29854f90781`:

- the unique resident owner is `0x80035F48..0x80036544` / ROM
  `0x36B48..0x37144`, exactly `0x5FC` bytes with no target padding before
  `func_80036544`. Its three resident calls are all in `func_80035E88` and
  authenticate the four-argument display-list/texture/tile/TMEM ABI;
- the guarded body reconstructs both format jump tables, mipmap sizing and
  accumulation, texture-load commands, per-level tile commands, and the final
  display-list cursor. Jet Force Gemini's permitted public
  `src/textures.c:func_80057C50` supplied the broad source shape; Mickey's own
  fields, branches, constants, assembly, and ROM bytes decide the candidate;
- configured IDO `-O2 -mips2 -32` emits 362 instructions and a natural `0x88`
  frame versus the target's 383 instructions and `0x90` frame. There are 369
  differing target-offset words from `+0x4` (371 raw), with shift-tolerant
  normalized distance 374. The assembly fallback remains canonical;
- moving the initial TMEM carrier assignment to its first control-flow use is
  the retained strict positional gain: it reduces differing words from 383 to
  369 and opcode mismatches from 321 to 294 without changing semantics, extent,
  or frame. Typing the texture parameter removes caller casts and is otherwise
  byte-neutral;
- the candidate has four jump-table `HI16`/`LO16` text relocations, currently
  expressed against anonymous `.rodata`; the target has the corresponding four
  records against `jtbl_8008263C` and `jtbl_80082654`, plus two local-label
  `R_MIPS_PC16` records. Those PC-relative static relocations make the standard
  resident preflight fail closed as designed, so this is not linked promotion
  evidence;
- all 119 supported flag identities are nonexact. MIPS-I happens to reduce
  positional differences but cannot describe the target's MIPS-II
  branch-likely instructions. Ten bounded source forms covered a clean
  donor-shaped rewrite, artificial-pad removal, direct argument carriers,
  register declarations, a typed texture parameter, named field access,
  independent tile/TMEM carriers, assignment timing, and parameter storage;
- a fidelity-clean allocator trace maps the texture pointer to the natural
  `s0` web while TMEM and tile remain in caller-saved registers. Forcing the
  texture web out and tile into `s0` reproduces that one target carrier but
  expands the candidate to 369 instructions and normalized distance 508; it is
  diagnostic only. The clean donor-shaped rewrite is farther away at 413
  instructions and a `0xB8` frame.

Preserve the fallback. Reopen only with new source-authentic declaration or
lifetime evidence that keeps the texture pointer out of `s0`, carries `rtile`
there naturally, and accounts for the target's extra eight non-save frame bytes
without the candidate's additional saved-register pressure. Do not repeat the
119-way flag lattice, forced-color diagnostic, donor rewrite, or the ten bounded
source forms above.

Header regenerated from the ranking on 2026-09-23 (check_shard_metrics --write); it read first mismatch +0x4; score 369 differing words.
<!-- plateau-handoff:func_80035F48:end -->
