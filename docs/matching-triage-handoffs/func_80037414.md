<!-- plateau-handoff:func_80037414:start -->
### `func_80037414` plateau handoff

- source: `src/main/frontend_37D50.c`
- score: 13/145 words
- frame: 0x30
- relocations: 42
- first mismatch: +0x34
- summary: Delta 0 and frame closed; last 13 words are uopt propagating the expression, not var_a1, into the TrapDanglingJump argument

Summary before this remeasure: Declaration and independent global-store scheduling remove two residual words; the 0x30 target frame and callee-saved carrier remain.
#### 2026-09-09: the frame is three decompiler spill names

Three of the seven declared locals (`sp2C`, `sp20`, `sp1C`) are written and
never read: they are the decompiler's names for the compiler's own spill slots
around the `func_800371BC` call, not source variables. Deleting them and their
two assignment groups takes the frame from `0x40` to `0x38` with no change to
the instruction count, and the target's is `0x30`. `sp28` is different -- it is
read again at `sp28 * D_8007BEAC` -- so it stays.

Read the target's homes as a declaration census before anything else: with
frame `0x30`, `var_a1` is homed at `0x2C`, `sp28` at `0x28`, `var_t0` at
`0x20` and `var_a2` at `0x1C`, so the declared block is at least five long and
its order is fixed by those four positions. `arg0` and `arg3` and `arg6` are
not locals at all -- they live at `0x30`, `0x3C` and `0x48`, their own incoming
homes.

The remaining `+4` is the same spill-versus-colour question as
`func_8000A39C`: the candidate colours one truncated value into `s0` and pays
a save and a restore where the target spills to a local home. No JFG donor
covers this function; PR #37's `src/menu.c` is the menu renderer, and neither
it nor any other reference file carries this transition state machine.

#### 2026-09-09 (second pass): where the last four bytes are

With the three spill names gone the residual is unchanged at 59 words but the
frame is `0x38` against `0x30` and the source no longer claims variables the
target does not have. The `+4` itself is the same spill-versus-colour question
as `func_8000A39C`: the target keeps `var_a2` in `a2` and spills it around
`func_800371BC` (to `sp28` at `0x28` *and* to its own home at `0x1C`, reloading
both after the call), while the candidate colours it into `s0` and pays a save
and a restore. Read that pair together -- the two functions want the same
mechanism and neither has it yet.

No donor covers this function. JFG pull request #37 takes `src/menu.c` to 48
bodies and zero pragmas, but that file is the menu renderer; no JFG source
carries this transition state machine, and nothing in the reference farm
computes two frame counts from two `f32` seconds the way this entry block does.

#### 2026-09-23 (Track B, lane B-resmix): delta 0, frame closed, 57 to 13

Cycle 0, `tools/insertion_pairs.py`: two pairs. Pair 1 (label missing-CSE)
was a candidate-only constant word on the `func_800371BC(...)` call line plus
a target-only stack load; pair 2 was the callee-save restore of the `s0`
carrier. Both came from one construct: the call passed four arguments to a
function that takes none, so `var_a2` had to be moved into `a3` and was given
`s0`. Declaring `func_800371BC(void)` and calling it bare took the function to
size delta 0 at 22 masked words and the frame to `0x30`, and moved
`func_800371BC` itself from 144 to 114.

Then, measured one at a time:

- the three tail byte stores in address order (`D_8007BE9C` first): 22 to 19.
- declaration order of the four locals, all 24 orders: floor 13.
- dropping `var_t0` and writing `D_8007BE94 = (s32) (arg1 * 60.0f)` so uopt
  CSEs it: still 13, but the slot ladder now equals the target's word for
  word (three declared homes at 0x2C, 0x28, 0x24 and two compiler temps).

The last 13 rows (5 naming, 8 immediate) are one decision. The target keeps
the `var_a1` web in `a1` for the TrapDanglingJump argument and both
multiplies, and the expression temp in `t0` for the `D_8007BE94` store only.
Here uopt propagates the expression into the call argument, so the
expression web is the one in `a1` and `var_a1` keeps only the multiplies;
the two webs trade `a1` and `t0` and their spill slots. The `sp28` path
shows the same propagation in both objects, so the source is asymmetric
between the two frame counts somewhere this lane did not find.

Flat, all at 13 or worse: copy direction and chained assignment over all 24
declaration orders; `var_a1` versus the expression at each of the four use
sites (16 forms, only the multiplies move anything); the declared type of
`var_a1` (u32, long, s16, u16, register, volatile); six prototyped trap
aliases for the call (int, u16, s16, u8 and variadic parameters); implicit
conversion and a commuted product; an unknown external call in place of
`func_800371BC` (the kill hypothesis). The single-force landscape on
procedure ordinal 2 found no force below 13 at delta 0.

<!-- plateau-handoff:func_80037414:end -->
