<!-- plateau-handoff:func_80037C74:start -->
### `func_80037C74` plateau handoff

- source: `src/main/frontend_37D50.c`
- score: 314 differing words
- frame: 0xE0
- relocations: 18
- first mismatch: +0x34
- summary: Frame closed and one missing emit site restored, 324 to 314 words and size delta -76 to -64. The frame was a pointer census, not a scalar one: every block-scoped Gfx cursor inside the emit macro owns four bytes of frame even though it is register-allocated, so the frame is 0x40 plus four bytes per cursor plus four per declared scalar plus twelve bytes of compiler temp, rounded to eight -- collapsing twenty-one per-site cursors to one shared cursor moved the frame 0xF0 to 0xA0, exactly four times twenty. The target's 0xE0 solves to twenty-two cursors and sixteen declared scalars where the m2c draft had twenty-one and twenty-one; four of the draft's temps were inlined to reach it and the masked count fell only ten words, so the frame was never the dominant cause. Counting the target's cursor write-backs gives twenty-two emit sites against the draft's twenty-one; the missing one repeats the loop's first command word against a different second word, immediately after the fourth command of the loop body, and restoring it is worth six words of the size deficit. What remains is one axis and it is not colouring: uopt hoists this loop's opcode and address constants into callee-saved registers and the ROM does not, re-materialising each one inside the loop through the assembler's own temporary. Both sides use all nine callee-saved registers and disagree only on which invariants win them, so the lever is loop register pressure, not declaration order.

#### 2026-09-11, lane `lane/p4-xfer`: a pointer census, not a scalar one

324 to 314 relocation-masked words, size delta -76 to -64, frame 0xF0 to 0xE0.
Aligner before: 40 byte-exact, 136 register naming, 4 immediate only, 172
really different. After: 46 byte-exact, 137 register naming, 5 immediate only,
161 really different. Relocations 18 on both sides throughout.

**The transfer this lane was dispatched to test is refuted, and the refutation
is cheap to state.** `func_8002F618` closed because its 279-word residual was
one coherent four-cycle over the integer temp ring covering 195 of 279 words,
at size delta +4, with both frames already identical, and with a real node
cursor in the source to replace. `register_census` here does print a closed
cycle, so the dispatch's stated tell fires -- but the tell is not sufficient.
The mapping here is not functional: the same source register maps to three
different target registers at different sites, only 136 of 324 words are naming
at all, and the largest bucket is structure. There is also no array cursor in
the body to index instead. A lane that reads "closed cycle, therefore the same
animal" will spend a day on the wrong axis. **Add the coherence of the mapping
and the size delta to the test, not just the presence of a cycle.**

**The frame law, which is new and reusable.** Every block-scoped pointer inside
a display-list emit macro owns four bytes of frame even though it is
register-allocated. Measured directly: collapsing twenty-one per-site cursors
to one shared function-scope cursor moved the frame 0xF0 to 0xA0, which is
exactly four times twenty. So

    frame = round8(0x40 + 4 * cursors + 4 * declared_scalars + 12)

where 0x40 is the outgoing-argument area plus the ten register homes and the
twelve bytes are compiler temporaries measured on this body. Putting the
target's 0xE0 in and reading the count out gives twenty-two cursors and sixteen
declared scalars; the m2c draft carried twenty-one and twenty-one. This is
L112 applied to a pointer count rather than an array length, and it is what the
previous handoff's "declaration census (lever 46)" was reaching for without the
arithmetic.

**One emit site was missing, and counting found it.** The target performs
twenty-two cursor write-backs; the draft performed twenty-one. The missing one
repeats the loop's first command word against a different second word, placed
immediately after the fourth command of the loop body. Restoring it is worth
six words of the size deficit on its own and one masked word.

**Closing the frame is worth ten words, so the frame was never the cause.**
Seventy declaration-census combinations were measured, four of which land on
0xE0 exactly. The best masked at 0xE0 is 314, reached by three different
four-removal sets, so the particular set is not load-bearing; what is
load-bearing is the count. Two removals are not free: dropping the loop-bound
temp alone costs three words of size delta, and dropping the outer-loop
increment temp alone regresses masked to 324.

**The remaining axis, named.** uopt hoists this loop's opcode and address
constants into callee-saved registers; the ROM does not, re-materialising each
one inside the loop through the assembler's own temporary. Both sides use all
nine callee-saved registers, so this is not a spare-register question -- they
disagree only about which invariants win them. The ROM spends its nine on two
table addresses, two large constants and one mask; this candidate spends
several of its own on opcode words the ROM re-emits. The lever is therefore
loop register pressure, not declaration order and not colour: raise the number
of genuinely live values in the loop body and the hoists have to lose. Two
association respellings of the address arithmetic were measured against this
and both made the size deficit worse, so expression shape is not the handle.

Header regenerated from the ranking on 2026-09-23 (check_shard_metrics --write); it read first mismatch +0x0.
<!-- plateau-handoff:func_80037C74:end -->
