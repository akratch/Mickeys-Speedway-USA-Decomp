<!-- plateau-handoff:func_8003A5A0:start -->
### `func_8003A5A0` plateau handoff

- source: `src/main/menu_3B1A0.c`
- score: 56/56 words
- frame: frameless
- relocations: 22
- first mismatch: none
- summary: MATCHED. This header claimed an open residual until 2026-09-16, when tools/check_shard_metrics.py reconciled every shard against the ranking; the function had already left the queue. The claim it carried was: Accepted p2 force w0=a1,w73=v0 cuts direct residual to 3 aligned rows; declaration and empty-comparison forms cannot reproduce it.
#### Owned pass, 2026-09-10 (lane/nm-resident2)

Still 9. The residual is one colouring decision and nothing else: the target
puts the walking key pointer in `a1` and the fixed bound in `v0`; every
spelling reachable from this shape puts the walker in `v0` and the bound in
`a1`. The offset carrier is `v1` on both sides, so the two colours simply
exchange.

**Measured law (new).** IDO gives `v0` to the loop's *walking* pointer and the
next free colour to the bound, independently of which variable is declared or
initialized first. Two synthetic probes compiled in this TU settle it: the
same loop written twice, once with the low pointer walking and once with the
high pointer walking, puts the walker in `v0` in both. The rule is structural,
not lexical, so no cursor/end spelling can reach the target's assignment. A
reopen needs a shape in which the walking pointer is *not* the loop's primary
induction variable.

Retired at this residual, each measured over a full lattice rather than
sampled, all flat at 9:

- All 36 declaration-order x initialization-order permutations of the three
  locals, crossed with both compare operand orders.
- Variable names, including reverse-alphabetical renamings: cfe's symbol
  order does not reach the colouring.
- 72 body spellings: three key-read forms (offset deref, `s16` index, address
  of element) x three return-index forms x four loop-tail forms (test, swapped
  increments, `continue`, `break`) x two declaration heads.
- Signature variants: `void *` / `s32` / `u8 *` return, and a K&R
  parameter list.

Regressions, all worse, all explained:

- Comparing against the address constant instead of a local (with or without
  an `end` local) reverses the compare's operand order: 10.
- Deriving the cursor from the end minus 0x30, in three placements: 10.
- An extra unused parameter buys a parameter home and one extra word: 57.
- Top-tested and rotated shapes cost three words (the guard is not folded
  away): 59. `while (end != cursor)` and the equivalent `for` still unroll to
  155 words, as the earlier pass recorded.

#### 2026-09-12, lane `p12-tight`: MATCHED. The target walks the tables by index, and the pointer pair was the whole residual

Zero relocation-masked words at size delta 0; `gmake verify` printed
507341c0a40ca3e9a7cee969b396ee53facfb548.

The nine words were never reachable from the pointer-pair shape, and the
record above was right that a colouring decision caused all of them. What it
could not see is that the decision is not the compiler's to make differently:
it follows from which values exist as webs, and that follows from the loop
idiom.

**What the records say about the shape this shard describes.** The procedure is
globalcolor ordinal 0 in its TU and emits phase two only, which is the call
test reproduced on a leaf. Four webs are coloured, in ascending web number with
each taking the lowest free colour, exactly as L141 says: the cursor at web 0
takes colour 1, the offset at web 6 colour 2, the parameter at web 9 colour 3,
and the end pointer at web 73 colour 4. The saves run 23.67, 6.25, 13.33 and
3.33, which is NOT the decision order, so no ratio edit was ever going to move
it -- the axis is the number.

Web 73 is a **type-1 address-constant web**, and every type-1 web in this
procedure is numbered above every type-3 symbol web. The eight address
constants take numbers 67 through 74 in the order of their first occurrence in
the instruction stream, and the end pointer's first occurrence is the LOOP
TAIL, because uopt propagates `end = &D_8007C118` into the comparison and then
hoists the constant. So it is numbered last, coloured last, and hoisted last
into the preheader -- which is also why the two low-half materializations come
out in the wrong order. One cause, three symptoms.

Forcing web 0 to colour 4 prices it: 9 to 4 at size delta 0, with three of the
four remaining words the preheader emission order and one the comparison's
operand order. Writing the comparison as cursor-first takes that to 3. Nothing
takes it lower, because the end pointer's web number is not a source axis.

**The shape that does reach it.** With `i` as the only induction variable, uopt
strength-reduces the key subscript into the walking pointer and the index-times-
eight into the byte offset the value reads use, and linear-function-test
replacement rewrites the `i < 6` exit as a comparison of the walking pointer
against the end of the key table. Both registers are then compiler temporaries
created in that order, phase two colours them in ascending number, and the
assignment is the target's. Zero words, delta 0, first try, in both a `for`
form and an equivalent interior-exit `while` form.

The loop is not unrolled despite this TU using the default unroller, which the
record above correctly warned about: the four interior returns make the body
multi-exit.

**Negatives measured this pass on the pointer-pair shape, all at delta 0 and
none below 9:** 288 cells over all six declaration orders, all six
initialization orders, all four physical-line groupings of the initializers and
both comparison operand orders; and under an accepted colour force, 24 further
cells over the same order and grouping axes, which floor at 3. Also flat: the
end pointer derived as the cursor plus 0x30, the comparison written against the
address constant with no end local at all in both operand orders, and
initializer-in-declaration forms.

**The transferable finding.** A bound that the source spells as a local
initialised from an address constant becomes a type-1 web numbered from its
first USE, not from its definition; a bound the compiler creates by
linear-function-test replacement is numbered with the induction temporaries.
Where a leaf's residual is the colour order of a walking pointer and its bound,
ask which of the two the source declares before sweeping any spelling axis.
<!-- plateau-handoff:func_8003A5A0:end -->
