<!-- plateau-handoff:func_8003F154:start -->
### `func_8003F154` plateau handoff

- source: `src/main/particles.c`
- score: 297/297 words
- frame: 0x58
- relocations: 16
- first mismatch: none
- summary: ROM-exact and promoted: 297 words, frame 0x58, delta 0, unforced; the velocity-head zeros are the integer literal `0`, which shrinks the `0.0f` web to the disableTransform arm and the comparison so globalcolor colours it whole on scale's colour, with the inlined x-first sum and the direct Z accumulate.

#### 2026-09-16, lane lm-a: 13 to 11, the head closed on the literal's type

Fresh baseline reproduced 13 masked at delta zero, first `+0x21C`. Six
cycles, all against the score of record; the retained candidate is 11
masked, delta zero, frame 0x58, first `+0x3E4`, aligned 286/11/0/0.
Report: [lastmile-region-boundary.md](../lastmile-region-boundary.md).

- **The head.** `residual_map`'s GPR names are misattribution here (the
  rows are FP); read off the objects, the `+0x200` window was the value3C
  load in `f2` (a piece of the `speed` web) where the ROM draws a ring
  register, and a ring draw spent on the head zero that lands in the pool
  colour anyway (L149). Writing the two emission-direction zeros as the
  integer literal `0` -- a different IR constant from the comparison's
  `0.0f` (L151) -- with `offset[2] = -config->value3C` direct closes the
  whole window: 13 to 11, first mismatch `+0x3E4`. The same head with the
  comparison also spelled `0` (or `(f32)0`) merges the two constants into
  one call-spanning web and is 168; `0.0` (double) is 53; the direct store
  alone with `0.0f` zeros is the shard's 31 (the ring one step off).
- **The post-sqrt region, 11 rows, unchanged from the base.** Inlining
  the sum x-first is exact through `+0x3F0` and the first mismatch becomes
  the comparison zero (`mtc1 zero` into a ring register where the ROM
  writes `f2`, `scale`'s pool colour); the tail then reads one ring step
  off, 17 in all. The ROM's zero is therefore a pool web with a single use
  in its block, and no tested source makes one: `scale = 0.0f` hosted at
  the head (168, +12: a web spanning six calls), immediately after the
  sqrt call (11, propagated into the compare), or immediately before it
  (17, likewise propagated -- so it is not a rematerialisation across the
  call); `scale == magnitude`; integer or double literals at the compare
  (168 / 53); integer-literal zeros in the disableTransform arm or at the
  velocity head (168 / 18, they merge with the head's constant); the
  direct Z accumulate (12 on the retained form, 18 on the inlined sum).
- **Decision variable for the next lane:** what makes a single-use FP zero
  in the block after the sqrt call a globalcolor web rather than a ugen
  ring draw. Every symbol that could carry it is constant-propagated away
  before colouring, and every other spelling of the literal joins the
  wrong constant. Candidates not yet measured: a second surviving use of
  the same `0.0f` in that block that the ROM's arms would not show (none
  found), or a source in which the sqrt is not a call.

- 2026-09-09 pass, the residual localised to a register *class*, not a colour.
  The workbench's fp lanes say it exactly: the candidate spends two fp-pool
  colours the target spends as ugen ring temps, first at aligned row 135. Both
  sites are named locals -- `speed = config->value3C` and the sum of squares
  assigned to `scale` -- and the target's own zeros at rows 131 and 253 are
  pool colours where the candidate's are ring temps. So the target names its
  two zeros and does not name the two values the candidate names.
- Neither half is reachable. Naming a zero costs an instruction every way it
  can be written: `X = 0.0f; offset[0] = X; offset[1] = X;` re-materialises the
  constant instead of copying it (298 versus 297 instructions) for `magnitude`,
  `scale`, `speed` and a fresh local alike, and a fresh `f32` local also moves
  the frame `0x58` -> `0x60` because the declaration census is saturated.
  Un-naming the two candidate webs keeps 297 instructions but scores 33 to 36:
  `offset[2] = -config->value3C` alone is 33, inlining the sum of squares alone
  is 22, both together 34 to 36.
- Inlining the sum of squares into `sqrtf` *does* close rows 249 and 250
  exactly -- the target's ring registers for the two products -- and then turns
  row 252 into a commutative-order word and costs eleven more downstream. Every
  parenthesisation of the three squares was measured; z-first is the best of
  them at 22.
- Flat this pass, each measured: eight spellings of the emission-direction head
  (the comma-operator `flags` assignment is *not* load-bearing -- writing it as
  its own statement before the zero is byte-identical, and that cleaner form is
  now the retained candidate -- but the `offset[1] = offset[0]` copy is: any
  form that stores two independent zeros costs an instruction, and `flags`
  after the zero costs 151); a 288-cell cross product over the random-scale
  constant, the magnitude-zero branch shape, the three scale multiplications,
  the three final adds, the `speed = -offset[2]` placement and the
  `D_800D413x` publication order (floor 17, and the whole product is flat at
  17 or worse); nine inert probes that try to hand the zero a colour by naming
  its result afterwards (`magnitude = offset[0]` and friends -- all exactly 17,
  so the class is not source-reachable at the definition either); and the
  chained `offset[0] = offset[1] = 0.0f` forms (65 and worse, 296 instructions).
- Next lever: nothing in the naming lattice. This needs either an
  instrumented ugen ring trace, or evidence about the *declaration set* -- a
  form with the same eleven-slot census that gives the two zeros webs without
  spending an instruction on them.

#### 2026-09-10, lane c2-resident: composition tried, and the class re-read

Still 17. Confirmed against the current tree, and the residual re-read off the
objects rather than the aligned rows, which sharpens the description: the
seventeen words are **three clusters**, and every one of them is the same
question of which value gets a low register.

- Five words where the emission-direction zero and the `value3C` read trade
  the two lowest fp registers between them.
- Five words in the magnitude block where the sum of squares, the third
  square and the comparison's zero rotate through three registers.
- Seven words in the three final adds, where the second component's load and
  the first sum trade one lane and the rotation carries to the last store.

In every cluster the target puts a **zero on the low register and the computed
value on the higher one**, and the candidate does the reverse -- one statement
of the class that fits all three sites, where the previous handoff described
two.

Composition was the obvious untried move, since the previous pass established
that naming a zero costs an instruction alone and un-naming a candidate web
regresses alone. A **210-cell cross product** of six emission-direction heads
(inlining `speed`, and hosting the zero on `magnitude`, `scale`, `speed` or a
fresh local, with and without keeping `speed` named) x seven sum-of-squares
forms (named, four inlined parenthesisations, two casts) x five final-add forms
(compound assignment, explicit, reversed, and a cast on either operand) is flat
or worse at every cell; 17 is the floor and only the untouched baseline reaches
it.

Falsified specifically, because it is the lever that closed `func_8005716C` the
same day: the **`(f32)` weight cast does reach these adds** -- it is not inert,
it moves five words -- but it moves them the *wrong way*, from 17 to 22, and so
does the reversed written order. So the final-add cluster is not a
commutative-order residual at all; the operand order already agrees and only
the destination register differs, which is a ring-position question the weight
rule does not govern.

Next lever unchanged in kind but narrower: the declaration census is saturated
at frame `0x58`, so any route runs through trading a declaration for a compiler
temporary rather than adding one. `cc -Wo,-zdbug:2` prints the census directly
(one `isvar M` row per declared local) and is the instrument to use before
guessing at the trade.

#### 2026-09-10, lane c4-resident: the sum of squares is inlined, x-square first

The 13 words are still three clusters and still one class. What this pass adds is
a source form for the middle one, and the ring accounting that explains the rest.

**The receipt.** Replacing

```c
scale = (offset[1] * offset[1]) + (offset[0] * offset[0]);
magnitude = sqrtf(scale + (offset[2] * offset[2]));
```

with

```c
magnitude = sqrtf(((offset[0] * offset[0]) + (offset[1] * offset[1])) + (offset[2] * offset[2]));
```

makes the sqrt argument region exact from `+0x03CC` through `+0x03F0` -- the two
component loads, the three products, both sums, all of it -- where the retained
`scale` form gets three of those words wrong. The operand order is the whole
result: the same inlining written y-square first scores 23, z-first 22, x-first
**19**. The earlier note here recorded "z-first is the best of them at 22" and
concluded inlining was worse than the carrier; it was measuring the wrong order.
So the evidence now says the target does **not** carry that sum through `scale`.

**What it costs, and why that is the last question.** Inlining hands the sum a
ugen ring temp instead of `scale`'s pool colour, which advances the ring one step
for the rest of that region: the magnitude test's zero moves one slot along and
the final-accumulate cluster follows, 12 tail words instead of 6. The target
spends no ring slot on that zero at all -- it materialises the comparison
constant on the same pool colour `scale` takes immediately afterwards. So the one
remaining decision variable is a uopt fp web for the comparison constant. Give it
one and the inlined form returns the slot it spends; that the tail then realigns
is ring accounting rather than measurement, so treat it as the prediction to
falsify first.

Eliminated this pass, all delta 0 unless noted, against the 13-word base:

- A **900-cell product**: five emission-direction heads x three zero-fill shapes
  x five sum forms x four branch shapes x three Z-accumulate forms. 13 is the
  floor and only the untouched sum spelling reaches it. The composition that
  looked most likely -- un-naming all three sites at once -- is 33.
- The three un-namings measured alone and in every combination: the `value3C`
  load direct is 31, the inlined sum 23, the Z accumulate direct 14; pairs and
  the triple are 24 to 34. Each is a regression alone and composition does not
  rescue them.
- A **65-cell carrier lattice** over the three final accumulates (each component
  staged through `magnitude`, `scale`, `speed`, or not at all). `scale` and
  `magnitude` are byte-identical to each other at every position, `speed` is
  worse everywhere, and nothing is under 13.
- Every zero-carrier identity at the magnitude test, before and after the sqrt
  call: through `scale` it is byte-identical to the bare constant (the assignment
  is propagated away), through the offset elements it costs 41 to 57 words, and a
  fresh `f32` local moves the frame.
- Nine branch shapes for the magnitude test (`!=` with the arms swapped, the
  guard hoisted out of the `if`, both ternaries, the constant on the left, a
  negated `>` test) on both sum forms: flat or worse.
- An **fp force sweep over the inlined form**: 41 webs x the six pool colours,
  59 forces applied, nothing under 19. So on that form the remaining words are
  not reachable by moving any existing web's colour either -- the zero simply is
  not a web.
- `f32 offset[4]`, to buy a spare pool competitor out of the tail padding, moves
  the frame. The declaration census is still saturated at `0x58`.

Instrument note for whoever picks this up: `DKWB_UGEN_TRACE=1` on the
instrumented `cc` prints one free-list record per fp allocation with ugen's own
source line, and it reads this function cleanly at proc 22. It shows the ring
here rotating five wide, not four, and it is what makes the ring accounting above
checkable rather than inferred.

**Corroboration for the inlined form, from matched code in this tree.** Every
matched three-component magnitude in the resident segment writes the sum flat
and inlined, never through a carrier: `src/main/anim.c` does
`distance = sqrtf((deltaX * deltaX) + (deltaY * deltaY) + (deltaZ * deltaZ));`
at seven sites and `src/main/audio_manager_36D0.c` at four more. Written that
way here -- flat, x-square first, which is the same tree as the parenthesised
x-first form by left associativity -- it scores the same 19 and closes the same
region, while the y-first flat form scores 23. So the source shape is not in
doubt; only the comparison constant's web is.

**Also measured and flat, this pass.** No free fp ring pop exists to pay for the
slot the inlined form spends: `scale * 1.0f`, `(f32) scale`, `speed / magnitude`
cast on either operand, the explicit and reversed multiply forms, and
`-(-scale)` are either byte-identical or cost instructions, which reconfirms
[L89] on this function -- the fp ring has no analogue of the integer ring's
redundant-mask lever. Semantic rewrites of the guard (testing the sum instead of
its root, dividing before the test, naming the zero through `scale` around the
call) are 22 to 121.

#### 2026-09-10, lane c5-resident: the fp ladder mapped, and the zero priced

Still 13. What this pass adds is the machine's own arithmetic for the decision
variable, which was previously stated only as a class.

**The fp ladder and the ring.** Forcing every fp web in this procedure to every
colour and reading which register moved gives the mapping outright:
`c24 = $f0, c25 = $f2, c26 = $f12, c27 = $f14, c28 = $f16, c29 = $f18`. The
instrumented `ugen` free-list shows the fp ring rotating over
`$f4, $f6, $f8, $f10, $f18` -- that is, **the ring is `$f4` to `$f10` plus every
ladder register that no uopt web claims**. `$f18` is in it because `c29` is
unclaimed; `$f2` is out of it because `c25` is claimed. This is the mechanism
behind "pool colour versus ring temp" and it makes the residual countable: the
target simply spends one fewer fp web here than the candidate does.

**The zero is priced, and the price is why it is not a web.** With
`CDX_DETAIL_WEB` the magnitude test's `0.0f` is uopt web 203, a float *constant*
web, and its decision record reads: `totalsave 1.0`, `cost 3.0` at every
available caller-saved colour (`c26`, `c27`, `c29`; callee-saved is 13.0),
`bestcolor c26`, `forbidden c24, c25, c28` -- contributed by interference with
the zero-fill web at `$f0`, the `value3C` web at `$f2`, and the `offset[2]` web
at `$f16`. `globalcolor` colours a web only when its cost is below its
totalsave, so 3.0 against 1.0 is `decision=split`, and a split web gets no
colour at all: ugen materialises the constant into a ring temp. That is the
whole of cluster 2.

So reaching the target is not one condition but two, and both are numeric:

1. the `value3C` web must stop interfering, or `c25` stays forbidden and even a
   coloured zero would land on `$f12`; and
2. `totalsave` must exceed `cost`, which means either four or more references to
   that same constant, or a live range that crosses no call.

This also explains, retroactively, why the fp force sweeps in the two previous
passes found nothing: **there is no colour to move.** The web is never coloured.
Forcing it is declined at both sites, which by the standing rule is itself a
result.

**New, and reusable elsewhere: [L97] has an fp form.** A uopt region boundary
does move the pool-ring split. Opening a `do { } while (0)` region at the sqrt
statement frees `$f16` from the pool and widens the fp ring to six; the same
boundary at four other places does the same. That is the first time a region
boundary has been shown to change fp *ring membership* rather than integer ring
phase, and it is worth trying on any fp residual described as pool-versus-ring.
It does not close this one: 21 region placements were measured across the whole
`config->flags & 4` block, none frees `$f2`, and the best of them is 89.

Eliminated this pass, delta 0 unless noted:

- the four-way composition of un-naming the `value3C` load (31 alone),
  un-naming the Z accumulate (byte-identical alone), the inlined x-first sum
  (19 alone) and the sqrt region boundary (89 alone): all sixteen cells, floor
  13, and only the untouched base reaches it;
- three region shapes (`do while (0)`, `if (1)`, bare braces) placed around the
  magnitude test alone: byte-identical, so that statement does not open a
  region;
- carrying the scale factor on `magnitude` instead of `scale` (130 words and
  frame -8; it *adds* an fp web and narrows the ring to four) and on `speed`
  (141).

Instrument note: the ring width is the cheapest readout in this function.
`DKWB_UGEN_TRACE=1` plus a two-line filter on `ALLOC_FP_RESULT` for proc 22
prints the ring membership for any candidate in about eight seconds, and it
answers "did this edit free a pool colour" without scoring anything.

#### 2026-09-11, lane f9-audit: the record the closure rested on belongs to a different constant

Re-measured at 13 (297/297, frame `0x58`, delta 0) on the direct-`cc` loop with
the TU's own `-Wab,-r4300_mul` (without it the function is 294 instructions and
56 words -- a stale-flag trap for anyone scoring this TU by hand).

**L108 census: 254 p1 decisions, zero p2.** L106 (first-definition order) has
no axis on this procedure; only L100's ratio orders anything. Instrumented
object `.text`-identical to the stock one (identity gate passed).

**The closure misattributed its decision record.** With `CDX_LINEAGE_TABLES`,
web 203 (`totalsave 1.0, cost 3.0, forbidden c24/c25/c28, decision=split`) has
its members in blocks 6, 9, 12 and 21 -- the four `mathRnd(...) *
0.000015258789f` blocks. It is the random-scale constant, not the magnitude
test's zero. The zero at the comparison is **web 212**, one member (block 35),
and its record reads `p1cand save=0.000000 nocs=1 best=6.0`: it is a colouring
candidate that has nothing to save because the constant occurs once, so it is
never coloured and ugen materialises it on the ring. The two "conditions"
recorded here (value3C must stop interfering; totalsave must exceed cost 3.0)
were derived from the wrong web, and `CDX_FORCE=p1:w203=c25` is declined while
`=c26` moves the random-scale constant (70 words, first mismatch `+0x8C`),
which is what confirms the identity.

**What the residual is, re-read off the ugen trace.** The fp ring rotates
f18, f10, f4, f6, f8 (five wide). At the sqrt statement the target pops the
ring for the x-y sum and the z square and then lands the zero on `$f2` --
c25, the colour `scale`'s division piece takes immediately afterwards -- and
`$f2` is not a ring member on either side. So in the target the comparison
zero *is* a coloured uopt web, which needs a web with more than one
occurrence (save > 0) and no live overlap with a c25 claimant. The candidate's
named sum sits on a c25 piece of `scale` that dies before the zero, so
interference is not the block; the occurrence count is. **The variable every
previous lattice held fixed is the occurrence count of the comparison's
constant web: every measured form gives the compare exactly one reachable
`0.0f`, so `save` stays 0.0 and no spelling, carrier, region or force can
colour it.**

Tried against that variable, all delta 0 unless noted, floor 13 (only the
untouched base reaches it):

- constant respellings at the compare (`0.0`, `0`, `(f32)0`, `-0.0f`, `!x`):
  all +4 to +12 bytes -- a second symbol, one more instruction;
- respelling the velocity zeros or the fill zeros as `0.0`/`0` (L103 split):
  238 / 20 / flat; a double-spelled triple store becomes a coloured web and
  the target's three separate ring materialisations are lost;
- a second reachable zero near the compare: `scale = 0.0f` before the test
  (dead def, folded), `magnitude`/`speed` zeroed in the taken arm (folded),
  the test or-ed with itself (+24), a post-hoc `if (scale == 0.0f)` (+20),
  `scale = speed; if (magnitude != 0.0f) ...` (-8);
- the fill routed through `scale` (16), through `speed` (210, +24), as two
  constants (168, +4), and `offset[1] = offset[0]` (16), crossed with the
  three tail carriers and both sum forms (18 cells);
- L105 block moves of `speed = -offset[2]` (into both arms 213/+24, after the
  rotation block 142/+8, after the call 179, inside `if (1)` 89);
- declaration trades: dropping `header` 19, dropping `scale` 24, `header`
  swapped for a named `f32 zero` 19;
- `if (1)` regions around the rotation setup alone, the `mathOneFloatPY` call,
  the `mtxf_transform_dir` statement (92) and the scale block: flat or worse;
- a 60-cell product sum x fill x disableTransform-zeros x compare form, and a
  48-cell product sum x fill x compare: floor 13.

**Verdict: the closure stands, on a corrected variable.** The blocker is a
single-occurrence constant web (`save 0.0`), not an interference or a cost
ratio. A route has to give the comparison's `0.0f` a second occurrence that
uopt neither folds nor propagates away, at no instruction, in a web that does
not overlap `scale`'s c25 piece -- and the inlined x-first sum (19) is the
form to build it on, since that is the form on which the sum takes the ring
slot the target gives it.


#### 2026-09-11, lane s1-one: the residual modelled, the recommended route closed, four corrections

Re-measured, not carried forward: 13 relocation-masked words, raw 13, delta 0,
1188 bytes, 297 of 297 instructions, first mismatch +0x21C. Aligner: 284
byte-exact, 13 register naming, 0 immediate-only, 0 really-different,
displacement tax 0. The frame census agrees on both sides -- frame 0x58, twelve
slots, identical ladders, no slot on one side only -- so the declaration set is
right and the residual is purely allocation.

**The 13 words are now a model rather than three clusters of prose.** The fp
ring rotates five wide, and the whole residual falls out of three pool-versus-ring
decisions plus their free-list cascade; a replay reproduces every register on
both sides. First, the `value3C` load takes the second pool colour where retail
pops the ring, and the two lists reconverge two instructions later, which is why
the long middle of the function agrees. Second, at the sqrt statement the
candidate colours the x-y sum and pops the ring for the comparison zero while
retail pops the ring for the sum and the z square and colours the zero. Third,
the tail is a cascade of those two plus one more: the candidate colours the
`offset[2]` read where retail pops the ring.

**Two things that were not on record.** Retail's tail accumulate is the direct
form, not the named one: retail's register order is only producible if ugen
allocates the destination's load first, which the direct form does and the
named form does not. And the fp ladder for this procedure, decoded by
force-and-diff rather than inherited, is c24 f0, c25 f2, c26 f12, c27 f14,
c28 f16, c29 f18.

**The closure's recommended route is closed, by a mechanism rather than a
list.** The variable is right: the comparison's constant web has one occurrence
and therefore nothing to save. What is new is *how* a zero becomes coloured
here, and it rules the route out. The emission zero is coloured only because of
its carrier: un-carrying it keeps the instruction count and drops the colour
(16 words), and storing two independent zeros costs an instruction (168 at +4).
So a zero-valued local with two references does take a pool colour. But **uopt
rematerialises a 0.0f-valued local across a call instead of carrying it**: on
the inlined-sum base, where the carrier is genuinely free from the head to the
compare, a zero defined at four different points before the call is
byte-identical to the inlined sum alone in every placement. The carrier never
survives the call, so the compare always sees a fresh single-reference constant.
Post-call definitions fold, as recorded, and that now extends to
multiply-by-zero, subtract-from-self, and the definition or the compare wrapped
in each of the three region shapes -- all byte-identical, so a region boundary
does not block fp constant propagation the way it blocks address reassociation.

**Restated variable:** the comparison's `0.0f` needs a second reference *after
the sqrt call*, in the compare's own region, at zero instruction cost. Nothing
else in that region is zero-valued.

##### Four corrections to this page's record

1. **`best` in a candidate record is not a cost.** It is the running best
   `save` among the candidates scanned so far -- visible in a small procedure
   where it starts at a sentinel and climbs. The previous pass read web 212's
   `best=6.0` as a colouring cost.
2. **`CDX_LINEAGE_TABLES` and `CDX_DETAIL_WEB` emit nothing in the installed
   instrumented build.** Neither produces any extra row for this procedure or
   for web 212, so the previous pass's claim about which blocks web 203's
   members sit in cannot be reproduced here. Web 203's decision record itself
   does reproduce.
3. **`nocs` is not the occurrence count.** Split pieces with `nocs=1` carry a
   non-zero total save while web 212's `nocs=1` carries zero; the ordering is on
   `save = totalsave/nocs` descending.
4. **Forcing web 212 proves nothing.** It never reaches a decision record at
   all, so the force is silently declined -- L101's first kind. Check a force
   was accepted before reading an unchanged object.

##### A trap that fired here and will fire again

Four candidates scored retail's exact comparison encoding at +8 bytes, from a
zero carrier placed before the transform with the compare written against it.
They are **semantically wrong**: with the sum of squares still named, the
carrier's definition is killed and the candidate compares the magnitude against
the sum of squares. The register looks right only because the sum is already on
that colour, and the +8 is the sum being spilled across the call. Any sweep that
writes the compare against a carrier must inline the sum in the same edit or it
is measuring this artefact.

##### Axes covered this pass, and therefore held fixed

About 96 source forms and 77 force cells, floor 13, only the untouched base
reaching it: a nine-cell head-carrier lattice crossed with both sum forms (the
`value3C` carrier name is entirely inert -- three carriers, byte-identical
objects); ten `value3C` head spellings; an eight-cell tail-carrier by sum-form
product; nine fill and emission-zero respellings; ten fold-defeat forms
including three region shapes in two placements; six hoisted shared-zero forms;
and a force oracle over every class-2 web crossed with the six fp colours plus
the split path. Held fixed, and the earlier sweeps of them stand: the branch
shape of the magnitude test, the random-scale constant's spelling, the three
scale multiplications, the publication order, and the 900-, 288- and 210-cell
products.

##### Instrument note

The warm candidate loop leaves the *last* candidate in its scratch translation
unit. An instrumented compile run straight after a batch therefore reads that
candidate, which looks exactly like "the trace perturbs the object". Write the
baseline back before the identity gate. With that done, the instrumented text
is identical to the stock text and to the configured object, all 297 words.

#### 2026-09-11, lane p6-mid: the census was lying about this function, and the corrected reading changes the routing

Baseline reproduces: 1188 bytes, 297 of 297 instructions, delta 0, 13 masked
words, first mismatch +0x21C. Aligner: 284 byte-exact, **13 register naming, 0
immediate-only, 0 really different** -- the residual is one bucket and nothing
else.

`tools/register_census.py` reported this function as four integer substitutions
with 100% coherence and a closed cycle over two argument registers, printed with
"coherent cycle: one ring-phase fact, not N colour problems -- see L127" beside
it. **Every part of that was an artefact of the tool.** It treated float
loads and stores as plain I-type, so their float datum was printed under a GPR
name, and it returned no register fields at all for COP1 register format, so
every float arithmetic and move site was invisible. Nine of the thirteen sites
were dropped and the other four renamed. Fixed this pass; the corrected reading
is:

- thirteen sites, **all thirteen floating point, zero integer**;
- mapping coherence 71%, not 100%, over four source registers, the least
  coherent splitting two ways;
- **three windows, not one**, with new mappings starting at +0x3E4 and +0x468.

So this is not one ring-phase fact and L127's no-op lever is not the instrument
for it. It is three separate fp regions, which is consistent with the 2026-09-10
reading of "three clusters" and with the standing closure that each cluster is
the same question of which value gets the low fp register. A lane arriving at
this symbol should read the fp pool, not the integer expression ring, and should
not expect one edit to move all three windows.

The closure itself is untouched by this: the decision variable is still a second
occurrence of the comparison constant at zero width, and the route through a
carrier defined before the call is still closed because uopt rematerialises a
0.0f-valued local across it.
#### 2026-09-12, lane `p10-near`: the closure survives an exhaustive force oracle over both banks

Baseline reproduces: 1188 bytes, 297 of 297 instructions, delta 0, 13 masked,
first mismatch +0x21C. Instrumented `.text` byte-identical to the tree's.

The standing closure says the blocker is a single-occurrence constant web with
`save 0.0` -- a web that is never coloured, so there is no colour to move. This
pass tests that as an existence statement rather than accepting it: **every p1
web this procedure decides, all twenty-seven of them, integer and float, forced
onto every colour from c1 to c7 and c24 to c31**. 188 forces were accepted --
acceptance read from each `p1color` record's `forced` field, never from whether
the object changed -- and **the floor across all 188 is 13**, reached only by
each web's incumbent colour.

That is the L140 existence proof the closure needed and did not have: the
residual is not reachable by moving any existing web's colour in either bank,
so the missing thing really is a web that does not exist rather than a web on
the wrong register. The route stays as the previous lane wrote it -- a second
reference to the comparison's `0.0f` after the sqrt call, in the compare's own
region, at zero instruction cost -- with one addition from today's law index:
L148 says the float no-op family draws no ring temp, and the integer probe
family that would supply such a reference for free does not exist on this bank,
so the reference has to be a real one that uopt deletes later.

#### 2026-09-12, lane p23-lastmile2: exhaustive landscape and comparison-occurrence control

Fresh configured baseline: 1188 bytes, zero size delta, 284 aligned byte-exact
rows, 13 naming, 0 immediate and 0 structural, with no unmatched offsets;
first +0x21C. All differences concern floating-point traffic. The GPR names
printed by residual_map's substitution subsection are not used as attribution
for these FP rows. Named Ucode authenticates procedure 22 of 44, and the
instrumented full-TU text compares byte-identically with stock IDO.

The exhaustive same-kind landscape samples every one of the 21 coloured webs:
143 probes, 141 accepted and two declined cells without a unique final-colour
receipt. Forty-five accepted cells preserve size; the others change it. The
complete improving winners list is empty. The empty winner-set packing retains
floor 13; no general claim is made about arbitrary regressing combinations.
This extends the retained oracle to each web's own available same-kind colours,
without silently treating a declined force as a measurement.

A twelve-cell source experiment targets the named missing occurrence after
sqrtf. Empty duplicate equality/inequality tests, discarded comparisons,
reversed equality operands and an otherwise overwritten duplicate scale
assignment are each tested on the retained sum and on the inlined x-first sum.
All six retained-shape cells remain exactly 13 at 1188 bytes. All six inlined
cells remain exactly 19 at the same size. Thus the cheap repeated-comparison
route supplies no emitted improvement; it does not establish a second usable
constant web. Source snapshots, objects and scores are retained separately.

ADR 0018 stop: the expanded force pass supplies no colour handle, and the new
comparison-occurrence family leaves both known source shapes unchanged. The
previous carrier, region, declaration-trade and float-identity receipts still
exclude the available alternatives. The named source question remains a second
usable zero-constant reference after sqrtf, at no instruction cost, together
with the inlined x-first sum. The guarded baseline body is unchanged and no
new byte credit is claimed.

Evidence: ignored build/p23/func_8003F154. Commands: residual_map.py,
allocator_trace_receipt.py --map-only, web_footprint.py --every-colour,
configured full-TU source checks, and finalize_plateau.py.

#### 2026-09-13, lane e1: measure the FP draw sites and surviving schedule

Fresh configured baseline: 1188 bytes, 297 words, delta zero, 13 raw and masked
differences. Alignment: 284 exact, 13 naming, zero immediate and structural,
with no unmatched offsets. First +0x21C. Frame 0x58, twelve identical stack
slots, sixteen text relocations on each side. These counts are not an exact
relocation-identity or linked-C acceptance claim. Stock and draw-traced full-TU
text agree. Named Ucode and the contiguous UGEN index authenticate procedure
22 of 44, freshly rejoined for each changed source. No colour sweep was run.

The baseline has 117 draws and 424 emission events. The first offset-zero
store draws one FP scratch register; the subsequent scalar copy and second
zero store draw none. The named speed load also draws none. Thus the head's
scratch cost belongs to zero materialization, not the surviving speed register.
The two-term magnitude assignment draws twice and the sqrt argument once;
the post-call zero comparison draws once. At the tail, the X and Y additions
each draw three times, the named Z-offset load draws none, and the Z addition
draws twice. Emission counts include source-location directives and must not
be read as machine-instruction counts.

Measured source controls, with snapshots, stock objects, profiles and aligned
deltas retained independently:

- Direct Z-offset addition adds one draw at that addition and changes only the
  final residual window from two naming rows to three: 14 total, delta zero.
- Move the named Z load before the Y update: the draw sequence is unchanged,
  but the result has 16 positional differences. Alignment has 12 naming rows
  plus two candidate-only and two target-only offsets. The smaller naming
  subtotal is not an improvement over the baseline with no unmatched offsets.
- Name the head zero before its first store: that store's draw remains, and
  the second zero store acquires another materialization and draw. The result
  is delta plus 4, with 250 exact, 32 naming, one immediate and 14 structural
  rows, plus one candidate-only offset. It does not supply a free pool value.
- Fold X and Y updates onto one source line: events are reattributed to that
  line, but the draw sequence and entire full-TU text are identical.
- Inline the x-first magnitude sum: four arithmetic draws replace three.
  The zero-comparison line still draws once. At delta zero, the aligned naming
  residual changes from 13 to 19: the +0x380 window improves by two rows while
  +0x400 regresses by seven and +0x480 by one. This prices the known local gain
  without mistaking it for an overall improvement.

ADR 0018 stall: folding the sqrt call and comparison onto one line, reversing
the comparison operands, and adding an empty duplicate comparison each preserve
the baseline draw order and full-TU text. The fold merely reattributes events;
the operand reversal changes no per-line counts; the duplicate adds two source
location events but leaves exactly one comparison draw. These three controls
supply no new usable occurrence or schedule handle. The retained guarded body
is unchanged at thirteen words, and no new matching credit is claimed.

The next requirement remains a real zero-constant reference surviving after
the call at zero instruction cost, together with the magnitude and tail
emission order. The measured tail draws also distinguish a scratch Z load from
a named pool load; changing just that choice regresses. The residual is all FP:
the GPR names in residual_map's substitution subsection are not attribution
for these rows. Alignment and its unmatched-offset lists remain useful.

Evidence is ignored under build/e1/r3f and build/e1/map-particles. Commands:
draw_census.py and its compare API, residual_map.py --object and --against,
register_census.py, skeleton_scan.py, align_symbol.py, frame_census.py,
score_symbol.py, configured stock IDO, named Ucode/UGEN authentication,
text-only relocation census, and finalize_plateau.py. Full-ROM gates validate
the guarded tree, not an exact C promotion.

#### 2026-09-16, lane nx-c: the missing web priced as arithmetic; a 3-word basin that is not the ROM's arm

Baseline reproduced at 11 masked, delta zero, first +0x3E4 (true offset;
the earlier +0x3F4/+0x430 figures on this page counted relocation lines).
Five cycles, 49 cells, nothing adopted; the retained body is unchanged at
11. Direct-`cc` harness base agreed with `score_symbol.py` on this TU.

**The record, corrected again (instrumented uopt, identity gate passed).**
After the lm-a head edit the `0.0f` constant is web 212 with `nocs 11,
totalsave 5, cost 9`, split. `seedcand` lists its reference blocks -- 2
(velocity head, 3 refs), 39 (disableTransform arm, 3), 42 (the compare, 1)
-- and each split seeds the FIRST reference block in that walk order, not
the largest: 39 first (piece coloured c25, totalsave 2 = refs - 1), then 42
(piece `save 0.0`, never coloured, so ugen draws the ring), then 2. The
c25 piece at block 39 emits nothing: uopt folds its materialisation into
`offset[0]`'s own register (web 77, f0), which is why the arm reads
`mtc1 zero,$f0` on both sides while the ladder says c25. Forcing that piece
to c26 is accepted (`forced=26`) and byte-identical.

**Positive controls settle the mechanism.** A store of `0.0f` added in
block 42 (`particle->velocityX = 0.0f` after the sqrt line) puts the
compare's zero on f2 exactly as the ROM (piece totalsave 1.0, c25); the
same store in the then-arm or the multiply block lands on f12, because the
piece then shares a block with `scale` (c25) and interference forbids it.
So the requirement is one hidden `0.0f` reference consumed INSIDE block 42.

**Refuted this pass, all on the inlined x-first base (17):** every
arithmetic zero (`+ 0.0f`, `0.0f + x`, `- 0.0f`, `(x - 0.0f) == 0.0f`) is
folded by cfe before uopt and is byte-identical; embedded assignment in the
compare, `!(x != 0.0f)`, `+scale`, `(f32) scale`, `scale * 1.0f`,
`magnitude * 0.0f`, `-x == -scale` (+8), `-0.0f` (+4), two compares (+16),
a volatile view (+12), an integer `0` (168, +4). A dead `speed = 0.0f` after
`scale = speed` is eliminated before colouring (17); before it, it is
propagated (6, see below). A pre-set `scale = 0.0f` with an empty then-arm
collapses the `if` (-16); with `scale += speed` or `speed + scale` uopt
emits an `add.s` (+4) -- uopt does NOT fold `x + 0.0` when the zero reaches
it through a symbol, only cfe folds the literal spelling.

**A 3-word basin, recorded, not adopted.** With the then-arm written
`speed = 0.0f; scale = 0.0f;` (or just `scale = 0.0f`) the zero web gains a
second reference, the piece {42,43} is coloured c26 and the sqrt block
closes but for the register; with the direct Z accumulate
(`particle->velocityZ += offset[2]`, retail's shape per the tail ring) the
whole function is **3 masked at delta 0**: `mtc1 zero,$f12 / c.eq.s $f0,$f12
/ mov.s $f2,$f12` against the ROM's `mtc1 zero,$f2 / c.eq.s $f0,$f2 /
lwc1 $f2,48(sp)`. The effect is equivalent (the offsets are all zero on
that path) but the ROM's then-arm is `scale = speed`, so this is a
diagnostic, not the source. It proves the direct tail is right and that the
only open question is the second reference.

**Next:** a `0.0f` reference confined to block 42 that emits nothing. The
FP names available are `magnitude`, `scale`, `speed`, `offset[]`; a def of
`scale` before the compare is dead (both arms redefine it) and eliminated,
a live one changes the arms. `residual_map`'s GPR names are still
misattribution on this function; read it off the objects.

#### 2026-09-16, lane w1-a: matched and promoted, 11 -> 0 in one batch

Baseline reproduced at 11 masked, delta 0, first +0x3E4, aligned
286 / 11 / 0 / 0; the private direct harness (nm_ranking's comparator over
the configured full-TU command) read the same 11. One measured batch of
eight cells; the retained body is 0 masked at delta 0, 297 of 297 words,
frame 0x58, sixteen relocations, unforced. `tools/score_symbol.py` reads
0 / 0 on the tree and `gmake verify` prints the expected SHA1 from the C.

The reading that closed it came from the split trail, not from the block
42 premise. The instrumented uopt's `webblocks`, `seedcand` and `grow`
rows for the `0.0f` web (212 on the incoming source) say: its members are
created in blocks 39, 42 and 2 in that order; the parent spans blocks 2
through 42 (nocs 11, totalsave 5, cost 9, split); the block-39 seed is
carved first (totalsave 2, coloured c25); the block-42 seed is carved
next with one reference and never becomes a candidate; and the block-2
seed then GROWS forward through the velocity head and the three `mathRnd`
call blocks, reaching a negative save, which is why the velocity head
materialises three ring zeros on both sides. So the comparison's zero has
nothing to save only because the velocity head's three `0.0f` stores keep
the constant's web reaching back to the entry.

Spelling those three stores as the integer literal `0` (L151, the same
lever lm-a used on the emission-direction fill) makes them a different IR
constant. On the promoted source the records read: the `0.0f` constant is
web 210 with members in blocks 39 and 42 only, `nocs 2, totalsave 2,
cost 0, decision=color, bestcolor c25`; the integer-zero constant is web
206 with members in blocks 19 and 2, split, its block-19 piece coloured
c24 (the one `mtc1 zero,$f0` before the two fill stores) and its block-2
remainder growing negative as before. c25 is only free in block 42 when
`scale` does not live in the sqrt-argument block, which is what the
inlined x-first sum buys; the direct Z accumulate is then the tail's ring
phase, as nx-c's diagnostic predicted.

The batch, all delta 0: inlined sum alone 17; inlined sum with the
velocity head `0` 3, with `0.0` (double) 3, with `(f32) 0` 3; each of the
first two with the direct Z accumulate 0; the named sum with the head `0`
18 (lm-a's cycle 4 number, now explained: `scale` in block 41 forbids
c25, so the whole web takes another colour); the inlined sum with the
direct Z accumulate and a `0.0f` head 18. The `0.0` and `0` heads are
byte-identical; `0` is adopted to match the fill zeros.

What was wrong on this page: the decision variable "a second reference in
block 42 that emits nothing" was one route and not the only one. A
single-reference piece has nothing to save, but the piece exists only
because the web is split, and the web is split only because the head's
references stretch it across six calls. Removing the head's references
from the web (by literal type) is the zero-instruction edit that every
lattice held fixed.
<!-- plateau-handoff:func_8003F154:end -->
