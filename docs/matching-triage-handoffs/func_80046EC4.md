<!-- plateau-handoff:func_80046EC4:start -->
### `func_80046EC4` plateau handoff

- source: `src/main/fx.c`
- score: 23/110 words
- frame: 0x48
- relocations: 6
- first mismatch: +0x68
- summary: as1: arg3/arg4/arg7 loads share store lineno and lose to cone+0x38. Need a surviving first-use that is not the store. Copies copy-prop; comma-assign is 41.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 444 bytes / 111 words,
target 440 bytes / 110 words; size delta +4 bytes.
Raw and relocation-masked differences are 60 and 60, respectively,
first mismatch +0x68; candidate/target frames are
0x48/0x48. Candidate/target static relocation counts are
6/6; 3 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(structural:1, register:16)`;
playbook `structure-buckets`, lever `none-known`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new cone-allocation donor C explaining the remaining address-base instruction and register coloring,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

#### Address-spelling sweep, 2026-09-09 (no gain)

The extra word is confirmed exactly where the previous pass placed it, and is
now bounded to one instruction with everything around it aligned: the target
forms `cone + 0x38` once, stores it as `cone->vertices`, and adds the first
sub-block size to *that* register; the candidate forms `cone + size` and then
adds `0x38` to the sum, which is the surplus word and the register rotation
that follows it. Every remaining difference in the function is downstream of
it.

Eighteen source spellings now reassociate identically. New this pass, on top of
the five already recorded: a local carrier defined before the field store; the
same carrier with three uses instead of a `+=` chain; the full parenthesised
expression repeated at each of the three sites; `(u8 *)(cone + 1)`;
`(u8 *)cone + sizeof(FxCone)`; `(u8 *)cone + 0x38`; the field read chained
through `cone->addresses[0]`; `s32` and `u32` integer carriers with the casts
at the stores; a running `u8 *` cursor initialised to `(u8 *)cone` and advanced
by `0x38` in its own statement; the same with the mode assignment moved above
it; reversed addition operands; `&temp[sp44]` index form; and computing all
three sub-addresses before the `cone->vertices` store. Repeating the whole
expression and the three-use carrier are +2 rather than +1: IDO materialises
`cone + 0x38` once for the store and still reassociates each of the two sums,
so the surplus doubles. Reading the field back at each site is +6.

Reading: the partial sum has to survive as a common subexpression, and every
spelling that leaves `0x38` visible as a constant addend on the same tree is
linearised before the CSE can see it. Resume with a mechanism that makes the
`cone + 0x38` value opaque to reassociation, not another spelling of the sum.

#### L97 region, 2026-09-17 (60/+4 to 23/0)

Identity-gated instrumented IDO matches stock `.text` on this TU. Procindex
puts this function at ordinal 1 (26 decisions; proc 0 is `func_80046E70`).
JFG `src/fx.c` at efd5abb still has no C body; none was adopted.

The extra word was a constant `0x38` add after adding the cone base to the
sub-block size. A named
`temp_v1 = (u8 *) ((s32) cone + 0x38)` stored to `cone->vertices`, then an
empty L97 `if (1) { }` (equivalent `do { } while (0)` and a `goto` join, all
23), then `addresses[0] = temp_v1 + sp44` and
`addresses[1] = temp_v1 + sp44 + sp40`, drops size delta to 0 and masked 23.
`tools/score_symbol.py` and `align_symbol.py`: 440 bytes, delta 0, 90 exact /
11 naming / 0 immediate / 12 structural, frames 0x48/0x48, 6 relocs, first
mismatch still +0x68. Candidate-only +0xD8/+0x100/+0x104 against target-only
+0xCC/+0xD0/+0xD4 is the arg3/arg4/arg7 load hoist sitting after vertices
instead of before it.

Two names for `cone+0x38` or for `cone` leave delta +4. Wrapping each use is
88 at delta 0. No-region named carrier is 62/+8. xor-0/or-0/and-minus-one on
cone, temp_v1, or a header local is +8 to +24. L144 address-form and
`volatile` overshoot. Comma-operator and discarded reads of arg3/arg4/arg7
do not hoist. Forwarding-kill copies (or-equals 0, xor-equals 0) cost +24. Real
`if (temp_v1)` / `if (cone)` regions add a branch (+4, 69). Colour force
`p1:w45=c1` on proc 1 is declined (`forced=-2`). `p1:w91=c2` and the v0/v1
swap accept (`forced=1`/`2`) and stay at 23.

Decision variable (superseded 2026-09-19): an opacity for `cone+0x38` that
does not open a basic block before the arg3/arg4/arg7 uses. L97 is still
required for size, but it is not what blocks the hoist.

#### as1 lineno vs copy-prop (2026-09-19)

Identity-gated instrumented IDO still matches stock `.text` on this TU
(proc 1, 26 decisions). Re-measure: 440 bytes, delta 0, masked 23, frame
0x48, 6 relocs, first mismatch +0x68. Candidate-only +0xD8/+0x100/+0x104
against target-only +0xCC/+0xD0/+0xD4.

L97 emits no branch: vertices through the arg stores sit in one as1 block.
The three target-only words are the arg4, arg3, and arg7 stack loads issued
before `cone+0x38`; the candidate issues `cone+0x38` first because that add
has the lower source line. The loads share a statement with the field
stores, so they share lineno. as1 picks among initially-ready nodes by
minimised lineno, then ready-list position.

Authorized recipes, scored on retained objects:

- Overlay22 empty `if (arg4)` / `if (arg3)` before the add: extra branches,
  108 at delta +8.
- `arg4` or-equals 0 leftover on the s16 parameter: sign-extend plus store-back,
  87 at delta +28. The s32 leftover from `func_800376CC` does not transfer
  to these halfword homes.
- Named s16/s32 copies before the add: uopt copy-props them away, object
  byte-identical to 23.
- Volatile copies: loads do hoist before `cone+0x38`, then homes and
  reloads overshoot, 83 at delta +28.
- Overlay40 comma-assign of the real field stores onto the vertices
  statement: loads hoist and mode/segmentCount fill the addresses[1] gap,
  but the stores issue immediately after vertices, 41 at delta 0. Discarded
  comma operands and `arg4 = arg4` self-assigns in that comma DCE back to
  23. `+ (arg4 & 0)` folds back to 23.
- L145 delete `temp_v1` and re-read `cone->vertices` for the sub-block
  adds: 26 at delta 0. Do not retry.
- L144 address-form or-equals 0 on `arg4` after a copy: unaligned lwl/lwr, 121 at
  delta +72.

v0/v1 colour force still accepts and stays at 23: the 23 is insertion
shadow of the three-word hoist (L155), not a colour that force can close.

Decision variable: a first use of arg3/arg4/arg7 that survives uopt, has
lineno less than or equal to the `cone+0x38` add, and is not the field
store itself. Named copies copy-prop; volatile, s16 leftover, and empty
`if (arg)` overshoot; putting the stores on the add's line hoists the
loads and the stores together. Do not retry L97 cone-carrier spellings,
discarded comma reads, or field re-read.
<!-- plateau-handoff:func_80046EC4:end -->
