<!-- plateau-handoff:func_80049B14:start -->
### `func_80049B14` plateau handoff

- source: `src/main/fx.c`
- score: 181/206 words
- frame: 0x18
- relocations: 4
- first mismatch: +0x4
- summary: Delta +4 to 0 by reusing carry as the case-2 mode byte; allocator regime then shifts (p1 colours 4 webs), constants land in s0-s4.

Track B pass (2026-09-23, lane B-fx). Start: 154 masked at size delta +4,
aligned residual 117. The insertion reader put one pair on the flags-clear
line (the ~4 mask stays in the loop; the target hoists it to a register) and
the size pair in case 2, where the difference is computed into a temporary
and copied into carry. The target has no copy: it keeps carry and the 0x1E
mode byte in one register.

Reusing carry as the mode carrier (this source) closes the size to 0, at 181
masked, aligned residual 126. The spelling also moves the function out of the
high-pressure allocator regime. globalcolor (p1) now colours 4 webs instead
of 14. The ten hoisted constants fall to p2 in web order and take s0-s4. The
target has them in v1 through t5, with the ~4 mask hoisted to t5, and carry
and mode sharing s3 in p2.

Measured and flat, or worse:
  - flags clear as &=, flags declared s32 or u32: byte-inert
  - (u16)~4, 0xFFFB, ^4: delta 0, but an andi-class word the target does not
    have
  - carry = 0 in either else arm of case 2, next carrier on or off, mode
    declared s32: byte-inert
  - value1E read twice: inert. Ternary condition: delta -16
  - carry = delta moved into the else-if: +8 (-4 on this source). As a for
    loop: +16 (+4 on this source)
  - forcing carry to s0, s2, s3 or s4 in p1 on the +4 shape: 200 at +12, so
    carry cannot simply be recoloured. It has to leave the p1 candidate set
    while the constants stay in it

Decision variable to read next: what makes carry a p1 candidate on the +4
shape. Read the seedcand/seed/grow records, the region the constants and
carry share, before trying another spelling.

Summary before this remeasure: Switch/carry declaration probe was byte-flat; target state-machine allocation remains unresolved and the donor supplies no C body.


Reopening audit (2026-09-08), evidence D: PROVENANCE inspection of Jet Force
Gemini public decomp `src/fx.c` and `src/fx.h` at
`efd5abb1c79636e297b831f7c2d5bf47eac39c0c` found no new target C body.
No donor source, names or values were adopted. Mickey remains authoritative.

Configured full-TU measurement: candidate 828 bytes / 207 words,
target 824 bytes / 206 words; size delta +4 bytes.
Raw and relocation-masked differences are 154 and 154, respectively,
first mismatch +0x8; candidate/target frames are
0x18/0x18. Candidate/target static relocation counts are
4/4; 4 tuples agree in function-relative offset, type and symbol.
These are fallback-object comparisons, not linked-C promotion evidence.

Workbench comparison: `structure-mismatch`. Diagnosis: `mixed(structural:21, register:88)`;
playbook `structure-buckets`, lever `none-known`. The named guides were read.
This heuristic routing supplies no new donor evidence to reopen exhausted forms.
Next concrete lever requires new updateClearScreen donor C explaining switch/carry allocation and the countdown carrier,
then a fresh gate and configured full-TU comparison.

Stopping evidence: zero source attempts; the authorized donor mechanism supplies
no new implementation. This is the assignment's early-exhaustion stop, not a
five-attempt stall. The prior TU lattice is not repeated: since its recorded
`func_8004ACC4` audit at `4be95a3d`, this TU differs only in EOF handoff comments.
See [the anchor donor audit](func_80049E4C.md) for the full delta and batch disposition.

Ignored evidence is retained in `build/tu-fx-audit/` and `build/wb/`: source and
full-TU object, gate receipts, summaries, diagnoses and relocation tuples.
Commands: `lane_status.py --symbol`, `wb_compare.sh --summary-json`, workbench
`diagnose` and `guide`, and `finalize_plateau.py`. The unchanged guarded C stays
NON_MATCHING and receives zero new exact bytes.

<!-- plateau-handoff:func_80049B14:end -->
