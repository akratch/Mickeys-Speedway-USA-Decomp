<!-- plateau-handoff:func_8004C690:start -->
### `func_8004C690` plateau handoff

- source: `src/main/font.c`
- score: 105/146 words
- frame: 0x70
- relocations: 9
- first mismatch: +0x0
- summary: u8 parameter closes the -4; rolled carrier-free copy and target homes give 105 at delta 0; characterIndex split point and loop-web colours remain

Summary before this remeasure: tmp copy plus L99 homes at 0x40 close one word to 145/146. Remaining is the rolled sltiu save; OR-zero on copyIndex emits it but takes s0 and frame 0x78.
#### 2026-09-13, lane h1: saved-header placement separated from draw schedule

The fresh configured baseline has 144 candidate words against 146 target
words, frame 0x70, 105 raw/masked differences and first +0x0. Nine static
relocations occur on each side, with six matching tuples. Paired aligned
buckets are 53 exact, 56 naming, four immediate and 29 structural rows,
plus two candidate-only and four target-only words. The frame sizes agree
but the homes do not: the candidate saved-header array begins at 0x60,
whereas the target uses 0x40. Named Ucode maps procedure 24 of 30. The census
has 29 draws and 253 emission records; full-TU stock/traced section, symbol
and relocation fidelity passes for both probes.

Two declaration-placement diagnostics test the measured 32-byte displacement.
Moving savedHeader after the seven pointer locals moves its base down 28 bytes
to 0x44, leaving the score, size and draw/emission profile fixed. Moving it
past the following index local reaches the target base at 0x40, again with
identical per-line counts and draw sequence. This second private variant
improves paired aligned buckets to 55 exact, 58 naming, two immediate and
27 structural rows, with the same gap counts and 105-word positional score.
The gain is two aligned rows in the final window; it is invisible in the
scalar score. Other spills and the incoming parameter home still disagree.

Direct inspection distinguishes the remaining copy shapes: the target has a
rolled four-word save loop, while the configured candidate expands the copy.
Correcting array placement does not change that schedule or restore the two
missing words. Both source/object variants are preserved privately; neither
is adopted because this packet's intended draw/emission schedule did not move.
The original guarded baseline remains canonical, with no matching credit.

Stop the measured declaration-placement route under ADR 0018 after identifying
its limit; do not infer matching homes from equal frame sizes or byte identity
from a fixed draw count. The next concrete source question is the rolled copy
and its pre-call arithmetic, with the private 0x40 placement available as a
controlled input. No colour or flag sweep was repeated. Evidence is under
build/h1/func_8004C690. Commands: configured compilation, allocator_trace_receipt.py
--map-only, draw_census.py --save/--compare, residual_map.py --object/--against,
frame comparison, workbench fidelity, finalize_plateau.py and tools/gates.sh.

#### 2026-09-19, lane w31-font: one missing word closed; rolled save still blocked by s0

Re-measured the inherited body at 144/146 words, size delta -8, masked 105,
aligned 53 exact / 56 naming / 4 immediate / 35 structural, candidate-only
at +0x1C and +0x234, target-only at +0x2C +0x150 +0x15C +0x230. Identity gate:
instrumented IDO .text matches stock. The configured do-while *d++=*s++ copy
unrolls. A tmp load / dest++ / src++ / dest[-1]=tmp body (the m2c store-at-minus-4
shape) keeps the frame at 0x70 and closes one word: 145/146, size delta -4,
masked 106. Moving savedHeader after the seven pointer locals and index (L99)
puts the array at 0x40-0x4C without changing size; aligned structural falls
32 from 36. Remaining target-only words are +0x2C (incoming a0 store), +0x150
(the rolled sltiu save), and +0x230 (epilogue v0 copy).

OR-zero on copyIndex inside that tmp loop is the only unroll preventer that
emits the target's sltiu-at-4 counted save. It also colours header as s0,
grows the frame to 0x78, and returns size delta -8. if (1), goto, empty if
(copyIndex), and indexed savedHeader[i]=header[i] either stay unrolled, use
li 4 / bne instead of sltiu, or block strength reduction. Leftover OR-zero
on character, overlay22 empty if (index) after the search, and overlay40
comma-assign on the copy inits are byte-identical on the 145-word shape.
Deleting fontIndex / characterIndex / entries (L160) adds extra global
reloads and overshoots to 148 with s0 still present.

Next: keep header in a caller-saved register across the copy so the sltiu loop
does not take s0; dest belongs in v0 (addiu from sp+0x40) rather than a1. Do
not colour until size is 0. GLOBAL_ASM stays canonical.

#### 2026-09-23 (Track B, lane B-resmix): delta 0 at 105, aligned 92 to 80

Cycle 0, `tools/insertion_pairs.py`: pair 1 (missing-CSE) was a candidate ALU
word on the characterIndex line against the target-only incoming `a0` store
in the prologue; pair 2 (split-not-copy) was the rolled-save load and the
epilogue `v0` copy. The prologue store is what a narrow parameter does:
declaring `func_8004C690(u8 character)` (prototype and definition) took the
function to size delta 0 at 138 masked. It is a positional regression but a
size milestone, and the caller `func_8004B1DC` moved 465 to 451 with it.

Then, one measured edit at a time, all at delta 0:

- ROM offset into a local computed before the header copy (the target's
  multiply sits before the loop): 138.
- OR-zero on copyIndex (the increment written as copyIndex OR 0, plus 1), which keeps the
  loop rolled with the sltiu counter: 141, header then on `s0`.
- the copy without the tmp carrier, `*destination++ = *source++`: 117. The
  load goes to a ring temp and header returns to `a1` with its +0x3C spill.
  With the carrier, `a1` was taken by the counter and header was offered
  only callee colours.
- declarations reordered so eight scalars sit above savedHeader, header
  directly below it and result four slots lower: 113, slot ladder identical
  to the target's. A pairwise-swap climb over all declarations (120 compiles)
  found nothing further: this procedure calls, so declaration order reaches
  homes only.
- `return result;` at the end of the found branch: 110. The target sets `v0`
  in each path.
- the three copy initialisers in the order destination, copyIndex, source
  (all six orders measured, 106 to 110), then folded onto one line: 105.

Flat or worse: using `character` instead of characterIndex at any use (it is
homed, so each read reloads; 146 to 148 with size moves);
`characterIndex = character` without the mask (110); placing the destination
initialiser earlier (106).

Remaining, aligned 80 (74 naming, 1 immediate, 5 structural): the
characterIndex copy lands at entry (+0x24) where the target splits the web at
the fill block (+0x108), and the search-loop webs take the caller-saved
colours in a different order. The target has index on `v1`, runLength on
`a0`, blockCount on `a1`, fontIndex on `a2` and entries on `t1`; here they
are `a1`, `v1`, `a2`, `t1` and `v1`. Web order follows first occurrence
(the copy-initialiser result shows it), so the statement order of the
search prologue is the next axis to try.

<!-- plateau-handoff:func_8004C690:end -->
