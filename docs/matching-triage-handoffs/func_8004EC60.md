<!-- plateau-handoff:func_8004EC60:start -->
### `func_8004EC60` plateau handoff

- source: `src/main/block_4F4E0.c`
- score: 15/82 words
- frame: 0x8
- relocations: 12
- first mismatch: +0x2C
- summary: JFG global do-while closed +4 and extra s-save. Remaining 15 is inner u8 on a0 vs outer byte on a1. Identity-gated p2:w9=c4 scores 0.

#### Re-open under the extra-instruction family (2026-09-19, lane/w25-4ec60)

Arrival: 328 B, size delta +4, masked 79, frame 0x10 vs 0x8. Leaf (p2 only). Identity gate: instrumented cc .text is byte-identical to stock. CDX_PROC=4 (17 p2 decisions, no p1). --every-colour refused until size 0.

JFG/DKR find_next_subtitle adopted as the body (PROVENANCE on the function). Direct global cursor plus do-while inner scan closed the extra word and the extra s-save: 82 words, frame 0x8, 12 relocations, size delta 0, masked 15, all 15 register-naming, first mismatch +0x2C. Aligned: 67 byte-exact, 15 naming, 0 immediate, 0 structural.

The 15 is one a0/a1 swap of two interfering byte webs. Inner u8 new_var is p2 web 9 (save 155, nocs 2) and takes a0. Outer D_800D6AE0[0] is web 10 (save 18, nocs 4) and takes a1. Target wants those colours swapped. Identity-gated force p2:w9=c4 is accepted; web 10 then takes a0 unforced; the object scores 0 masked, delta 0.

s32 sentinel = 12 with sentinel == new_var2 emits the target 12-compare operand order (constant register as rs). Without sentinel, the forced object still had bne a1,s0 vs bne s0,a1. With sentinel plus the force, the object is byte-exact. The unforced sentinel body remains 15 naming.

Eliminated as colour numbering levers (each grew the frame or merged the webs):

- Deleting new_var, or spelling inner as D_800D6AE0[0] / *D_800D6AE0 / (u32)[0]: inner merges with outer, residual becomes an a1/a2 swap of line-count vs copy (13 at delta 0).
- An outer u8 next/bump assigned from [0] and used in the while test, in a while ((next = [0]) != 0) condition, or reassigned at loop top: extra s-save, frame 0x10, size +12 to +24.
- if-peel plus do-while with bump for the first load only: frame 0x8 but one extra instruction (delta +4, masked 66).
- Declaration order, register u8, unsigned new_var, L97 if (1) around the inner scan (merges), L97 do { } while (0) around the assignment (flat 15).

Decision variable that blocks: p2 colours by ascending web number, and the inner u8 symbol is numbered before the outer byte temp, so it takes a0. No source form measured here numbers the outer byte first without either merging the two webs or keeping an extra live-across-inner u8 that pays an s-save. Next lever is an outer-byte spelling that is a type-3 symbol numbered before new_var and dead during the inner scan, or a type/first-use change that numbers web 10 before web 9 at zero width.

#### Outer-byte type-3, dead during inner (2026-09-19, lane/w25-4ec60)

Re-opened to find an unforced source for the proved p2:w9=c4 colouring. The 15-word sentinel body was restored after each trial. None of the following numbered the outer byte before new_var at zero width:

- Loop-top next = [0] & 0xFF (split spelling vs the while test): extra andi plus extra s-save, delta +12, frame 0x10.
- if-peel plus do-while with next carrying every outer-byte use (complete CSE): frame 0x8 and size 0, but 29 naming. The pointer web moved onto a0 (cycle a0/v1/a1) and the new_var2 copy became a nop. Not adopted.
- Block-scope next used only for the ID: CSE with while [0], extra s-save, delta +24.
- while ((next = [0]) != 0) plus ID via next, 10-compare still [0]: extra s-save, delta +12. The while test keeps next live across the inner scan.
- if ([0] != 0) do { next = [0]; ID } while ([0] && !done): extra s-save, delta +24. next = [0] at body start CSEs with both tests.
- Inner dummy = 0 with new_var OR dummy in the 0x80 test: dummy is DCE'd, byte-identical 15.

L131 is the bound: any next that CSEs with the while [0] expression inherits that expression's hull through the inner scan, and as a type-3 symbol it then pays an s-save on top of new_var. A next that does not CSE is a third byte web and also pays the s-save. A dummy live only in the inner scan is either folded away or an extra instruction. The forced object remains 0 and is not a match.

<!-- plateau-handoff:func_8004EC60:end -->
