<!-- plateau-handoff:func_800580F0:start -->
### `func_800580F0` plateau handoff

- source: `src/main/flash_58C10.c`
- score: 0/51 words
- frame: 0x30
- relocations: 27
- first mismatch: none
- summary: MATCHED. do-while(0) regions around the three inner calls keep each &D_800D7830 as an address constant so IDO rematerializes instead of spilling the pooled copy; an unused OSPfs * restores the 0x30 frame. The first status call keeps the signed address identity.

#### 2026-09-17, lane w8-flash: matched

The remaining 20-word residual was one spilled copy of `(u32)&D_800D7830` shared by CheckConnector and the power-off call (candidate `sw` at +0x44, target rematerialize at +0x8C). Dropping those integer casts and putting each inner call in its own `do { } while (0)` region makes IDO rematerialize the address constant per call (L97/L110). That also drops the extra home and shrinks the frame to 0x28; an unused `OSPfs *` restores 0x30 (L99). Configured IDO is 51/51 byte-exact, frame 0x30, relocation layout identical. `if (1)` regions do not substitute; dropping any one of the three `do-while`s regresses.

#### 2026-09-13, lane h1: signed address identity restores target extent

The initial configured candidate has 50 words against 51 target words,
frame 0x30, 32 raw/masked differences and first +0x10. Its paired aligned
buckets are 33 exact, nine naming, one immediate and six structural rows,
plus one candidate-only and two target-only words. The extra stack home at
frame offset 0x28 is written once and read twice; the target has no such
traffic. Named Ucode maps procedure 3 of eight, and stock/captured full-TU
text agree. The draw census has two draws and 69 emission records.

Retained source change: the first status call converts the object address
through signed s32 rather than unsigned u32 before converting back to OSPfs.
Both spellings preserve the target's 32-bit address and call arguments. This
separates its IR address identity from the later unsigned occurrences.
The first-call line loses one emission record and the connector line gains
one. Draw count and sequence remain two and unchanged. The compiler removes
one reload and restores the exact 204-byte extent. Masked and raw differences
fall to 20, first +0x44; paired aligned buckets become 41 exact, two naming,
zero immediate and seven structural, plus one candidate-only word at +0x44
and one target-only word at +0x8C. The aligner counts nine structural rows
when those gaps are included. The frame stays 0x30, but one spill and one
reload still use the extra home at 0x28.

Current static relocation counts are 25 candidate and 27 target, with 15
matching offset/type/symbol tuples. The older header's 17 is not the measured
static count for this retained candidate. These incomplete identities and
the nonzero executable residual prohibit promotion; GLOBAL_ASM remains active.

Four rejected controls retain separate source, object, score and census
receipts. A call-local region adds two location records but is byte-inert.
After the signed-first gain, changing the release call to a plain pointer
scores 42 at delta minus eight and changes saved-register traffic; changing
it to the signed integer address scores 29 at delta zero and moves the spill
earlier. A zero-extending 32-to-64-to-32 address round trip adds two draws at
the release line, increases extent by four bytes, and scores 39. Its aligned
paired buckets are 33 exact, nine naming, one immediate and seven structural,
with two candidate-only words and one target-only word. This diagnostic is
not adopted. The complete improving colour landscape was not rerun.

Stop the tested address-type family under ADR 0018 after preserving the
20-word result; the follow-ups provide no further improvement. The next
question is a source-authentic separation of the remaining pooled address
that removes the spill/reload while retaining rematerialization at the
release call. The two integer address identities tested do not provide it.
No general source impossibility or matching-byte credit is claimed.

Private evidence is under build/h1/func_800580F0. Commands: configured stock
compilation, allocator_trace_receipt.py --map-only, draw_census.py profiles
and comparisons, residual_map.py --object/--against, align_symbol.py,
finalize_plateau.py and tools/gates.sh. Canonical ROM verification covers
the assembly fallback rather than this nonexact C.


#### 2026-09-13, lane `j1`: connector and rematerialized-release identities

The dispatch's 32-word deficit is stale. Fresh baseline is the retained
51-word, 204-byte candidate at masked 20, frame 0x30 and first +0x44.
Alignment is 41 exact, two naming, zero immediate and nine structural rows
including one candidate-only and one target-only word. Static relocation
counts are 25 versus 27 with fifteen exact tuples. Procedure 3 has two
draws and 69 emissions. Every private source experiment passes full-TU
stock/traced text identity; no colour sweep was repeated.

Three new address-identity controls retain their own source, object, census
and aligned result. Changing just the connector call's unsigned address
conversion to signed keeps the twenty-word positional score but shrinks the
frame incorrectly to 0x28. Alignment falls to 34 exact, seven naming, five
immediate and six structural rows; both gap locations move. Draws and order
remain unchanged; one emission moves between the first and connector calls.
This is an aligned regression despite the score tie.

At the original baseline, expressing the release argument as a one-past
OSPfs address converted through unsigned integer and then decremented back
to the original object separates another address expression. It preserves
the passed 32-bit address, but grows the candidate by four bytes, shrinks the
frame to 0x28 and scores 27. Alignment is 34 exact, seven naming, seven
immediate and four structural rows including one extra candidate word.
Draws remain two in the same order; one emission moves from connector to
release. On this new shape, changing only the first status call back to an
unsigned address adds one emission without changing the two draws, scores
34 at the same four-byte excess and leaves the wrong frame. Alignment is
30 exact, nine naming, five immediate and nine structural rows including
two candidate-only and one target-only word.

No source change is adopted. These three measured regressions extend the
prior typed-address controls; they provide no target-sized schedule with
the correct frame. Stop under ADR 0018 and preserve the existing guarded
20-word candidate. The next requirement remains separation of the pooled
address without the extra instruction or frame change, followed by full
relocation authentication. No general source impossibility is claimed.
Commands: assignment gate, configured stock/traced compilation, draw census
before/after, residual_map.py --object --against, finalize_plateau.py and
tools/gates.sh. Full-ROM verification covers the assembly fallback and
credits no new executable bytes.
#### 2026-09-13, lane l1: final-status address identity

Fresh baseline: 51 words, delta zero, frame 0x30, twenty raw/masked
differences, first +0x44; 41 aligned exact, two naming, seven paired structural
rows and one gap pair. Procedure 3 has two draws and 69 emissions. Named Ucode
mapping and complete section, symbol and relocation fidelity pass for each
saved source/object pair. The prior first/connector/release identity controls
were read; these tests isolate the previously untested final status call.

Converting that call's object address through signed s32 preserves the 32-bit
address but adds one emission at the initial status call, without changing
the two draws or their order. Geometry and frame stay fixed; masked differences
rise to 33. Alignment becomes 33 exact, nine naming, two immediate and five
paired structural rows, with two gap pairs. Every aligned window regresses.

The unsigned u32 version leaves every per-line count and the draw sequence
unchanged, yet shrinks the function by four bytes and scores 34. Alignment is
35 exact, two naming, two immediate and ten paired structural rows, with one
candidate-only and two target-only words. The frame remains 0x30. Census-count
identity therefore does not establish object identity: this form changes code
without buying a draw, and its extra missing target word rejects it.

Both probes are preserved privately under build/l1/func_800580F0 and the original
guarded twenty-word candidate is restored. Stop early under ADR 0018: the new
final-call identity alternatives regress, while the prior three call-site
families already cover the other identified type-splitting routes. No general
source-impossibility claim. The remaining requirement is zero-cost separation
of the pooled address with exact frame, extent and relocation identities.
Commands: configured stock/capture compilation, allocator_trace_receipt mapping,
draw_census differences, residual_map --object/--against, finalize_plateau and
tools/gates.sh. ROM verification proves the fallback; no new byte credit.

<!-- plateau-handoff:func_800580F0:end -->
