<!-- plateau-handoff:func_80058250:start -->
### `func_80058250` plateau handoff

- source: `src/main/vehicle_sounds.c`
- score: 19/22 words
- frame: frameless
- relocations: 24
- first mismatch: +0x0
- summary: O2 address-commoning is the +16 (addiu vs $at). O1 same-symbol tails are 22 words but f4 not f0. Separate globals never share $at.

Lane w31-vsnd (2026-09-19). Re-measured V0: 88 bytes, size +16, masked 19,
first +0x0, 24 relocs, frameless. Identity-gate PASS: instrumented IDO
.text is byte-identical to stock. CDX_PROC=0 is this leaf (p2 only, 13
p2dec). Align: 19 byte-exact, 0 naming, 0 immediate, 1 structural after
removing 6 candidate-only and 2 target-only words. The +16 is four extra
high-half materials, one per slot.

Decision variable: uopt -O2 address-commoning of a two-field aggregate.
Named globals never share a high half (each symbol is one lui). A
handle-plus-two-field tail struct at -O2 is still 26 words because the
tail base becomes a GPR plus addiu (v0, v1, a0, a1). The same tail form
at -O1 is 22 words and uses as1 $at expansion of same-symbol field
stores, matching the target's two-lui-per-slot shape, but ugen scratch
colours the zero float f4 not f0 (no globalcolor at -O1). Copy-propping
later pitches from slot0.pitch at -O1 keeps that 22-word shape with one
float-zero materialization; at -O2 the same spelling reloads and grows.

L160 deleting the tail carrier, overlay40 comma-assign, overlay22 empty
if, L97 regions, leftover OR-zero, volatile tails, negative-offset
racer-base, and slot-index casts do not produce $at sharing at
the configured -O2. CDX_FORCE strike or split of the four save=1 address
webs would home them to the stack, not $at. L112/L99 do not apply
(frameless). Do not colour while size is +16 (L155). Resume only with a
configured -O2 spelling that keeps the tail as two symbolic stores of
one symbol (so as1 shares the high half) while globalcolor still places
the zero float in f0, or with handwritten-origin evidence.
<!-- plateau-handoff:func_80058250:end -->
