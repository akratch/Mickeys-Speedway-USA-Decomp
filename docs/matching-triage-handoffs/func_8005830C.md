<!-- plateau-handoff:func_8005830C:start -->
### `func_8005830C` plateau handoff

- source: `src/main/vehicle_sounds.c`
- score: 697 differing words
- frame: 0x118
- relocations: 88
- first mismatch: +0x8
- summary: 758 vs 762 instructions; frame now exact at 0x118 and the whole saved-register and spill-temp block matches by offset and use count, so the residual is the declared-local ORDER plus eight unhomed float values
- boundary/ABI: resident `0x58F0C..0x59AF4` (`0xBE8`) with no padding before
  `func_80058EF4`; one Overlay 1 call at `+0x2BC4` passes `updateRate` in `a0`
- current result: 758 candidate versus 762 target words (census `-4`), frame
  `0x118` exact, 88/88 static relocations, 278/762 rows identical under an
  insertion-tolerant alignment and 490/762 with registers masked
- frame closed: the declared-local list was one 4-byte slot short. `unused0`
  is that slot, a census slot of unknown identity; adding it moves the frame
  from `0x110` to `0x118` (the local block quantizes in 8-byte steps) and
  lifts identical rows 273 -> 278 and masked rows 484 -> 490
- where the surplus lives: with the frames equal, all twenty slots from `0x10`
  to `0x84`, the saved registers and every spill temporary, match the target
  by offset and by use count. The entire residual is in the declared-local
  block above `0xA0` and is an order problem: the local with eight uses sits
  at `frame_top-4` here and at `frame_top-16` in the target, so the target
  declares about three more 4-byte locals ahead of it. Permute the declaration
  list against the target's use-count fingerprint, `(8,4,3,2,2,8,6,2)` reading
  down from its first homed local, against `(8,6,2,1,1,1,4)` here
- loop idiom settled: the candidate emits thirteen `sltu reg,zero,counter`
  booleans the target does not have (the target has none). Do not change the
  loop form to remove them -- the target's back-edge tests the counter and
  decrements it in the branch delay slot, which is `while (x-- != 0)`, the
  committed form. All sixty-four combinations of four loop forms across the
  three loops were measured: `--x != -1` removes every `sltu` but substitutes
  thirteen equally spurious `addiu x,x,-1` in the same branch-likely slots and
  costs a masked row, and `--x >= 0` reaches census `-6`. The target fills
  those slots with float stores instead: twenty `swc1` against twelve here
- next lever: the eight unhomed float values (`swc1-8`, `move-10`). They are
  the same eight stores that would give ugen something real to sink into each
  branch-likely slot, so they and the `sltu` surplus are one question
- retained changes: the target-supported unsigned `vehicleId` field and
  racer-pointer setup order reduce opcode mismatches from 561 to 557 without
  changing the positional score; a full-width sound-ID carrier is byte-flat
- bounded attempts: unsigned sound-ID carrier (flat); full-width priority
  (701 differences, 752 words); widened volume-call ABI (717/755); volatile
  random-scale global (flat); unsigned `vehicleId` (two opcode gains); removal
  of the first engine-intensity reload (716/757); racer-pointer ordering (two
  more opcode gains); cross-phase index reuse (frame regressed to `0x108`);
  full-width sound-ID carrier (flat); removal of four shadow locals (733
  differences, frame `0x100`); the sixty-four-point loop-form lattice above;
  the flag lattice re-run over seven configurations, where the shipped
  `-Wab,-r4300_mul -O2 -mips2` is best on the alignment and `-mips1` costs
  about a hundred instructions
- donor: the permitted DKR vehicle-audio source supports only the organization
  already disclosed in the C body. No exact or structurally credible donor is
  known

Header regenerated from the ranking on 2026-09-23 (check_shard_metrics --write); it read first mismatch +0x0; score 692 differing words.
<!-- plateau-handoff:func_8005830C:end -->
