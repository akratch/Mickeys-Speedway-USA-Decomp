<!-- plateau-handoff:func_8005A948:start -->
### `func_8005A948` plateau handoff

- source: `src/main/models_5B300.c`
- score: 0 differing words
- frame: 0x38
- relocations: 13
- first mismatch: none
- summary: MATCHED. This header claimed an open residual until 2026-09-16, when tools/check_shard_metrics.py reconciled every shard against the ranking; the function had already left the queue. The claim it carried was: Declaration order moves all six stack homes exact; the reloc-normalized residual is exactly one ugen temporary-ring pair (index scaling vs the folded equality test), proved by patching the two names in the ugen listing to a 0-word difference.

#### Owned pass, 2026-09-09 (lane/fin-mod)

- **Reloc-normalized residual is exactly three words, and they are one pair of
  compiler temporaries.** The loop's index-scaling web takes `t7` where the
  target takes `t8`; the loop's equality test -- the temporary the `!= 0U`
  boolean normalization creates, which the assembler folds into the branch and
  which therefore emits no instruction -- takes `t8` where the target takes
  `t7`. Every other word, the 0x38 frame, all six stack homes and all 13
  relocation offset/type/identity tuples are exact.
- **Proof that nothing else is left:** patch those two register names in the
  ugen listing, re-assemble through the exact `cfe -E` + `as0` + `as1` replay
  (see the `func_8005ABA8` handoff for the recipe) and the reloc-normalized
  difference over all 94 words is **0**.
- **The ring is a FIFO free list**, not a stack and not a phase: a temporary
  pops the head and is pushed back on death. Reading it off both sides, the
  parameter sign-extension pops `t6` on both; the target then pops one
  *invisible* temporary at `t7` before the index scaling pops `t8`, while this
  candidate pops `t7` for the index scaling and `t8` for the invisible
  normalization temporary. Every downstream pop (`t9`, `t2`, `t3`, ...) agrees
  on both sides. So the two sides differ only in which of the two webs is
  created first.
- **Consequence, and the shape of the fix:** the target's loop test creates no
  normalization temporary at all (a plain equality), and the target consumes
  one extra invisible ring pop somewhere between the parameter sign-extension
  and the index scaling. Dropping the `!= 0U` on its own costs 26 words --
  every downstream pop then trails by one. This is L88's "each half is a
  regression by itself" shape; the second half is the open question.
- **Eliminated (40+ spellings, all flat at 3 or worse):** address spellings
  (`&a[i]`, `a + i`, two-dimensional cast, byte offset, explicit shift,
  s32-pair index), comparison operand order, boolean normalizations (`!= 0U`,
  `!= 0`, `(u32)`, `^ 0U`, `& 1U`, `!!`, nested), `entry` inlined, declared
  outside the loop, or assigned inside the condition, `id` and `existing`
  locals, `for` versus bottom-tested loop, swapping the two tests, `count` and
  `cache` locals, sign-extension carrier locals, and dead-store dummy locals at
  four placements (uopt removes them before web numbering, so they buy no pop).
  Normalizing the *guard* (`(D_800D7D04 > 0) != 0U`) does buy a pop before the
  loop, but it emits a visible set-on-less-than and costs a word (95).
  Normalizing the loop-exit test instead lands the pop after the disputed pair
  and also leaves its comparison visible; the best of that family is 8.
- **Tooling: this TU's permuter scratch is unfaithful for this function too.**
  It reports base score 15 against a real reloc-normalized residual of 3,
  because the scratch scores the 13 relocation-carrying words as differences
  that no candidate can close. Its search is therefore aimed at a floor of ~13,
  and no score from it transfers -- the same verdict the `func_8005ABA8`
  handoff already records for that function's scratch.

<!-- plateau-handoff:func_8005A948:end -->
