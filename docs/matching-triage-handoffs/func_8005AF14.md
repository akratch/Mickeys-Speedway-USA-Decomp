<!-- plateau-handoff:func_8005AF14:start -->
### `func_8005AF14` plateau handoff

- source: `src/main/models_5B300.c`
- score: 377 differing words
- frame: 0x110
- relocations: 27
- first mismatch: +0x0
- summary: split-not-copy at rotation2 line 771. Frame-close reorder hits 0xF8 but aligned residual 376>327; the +0x24 callee save stays missing. Stall.

Summary before this remeasure: Frame remains 0x110 versus target 0xF8; camera/matrix allocator structure remains unresolved after the full flag lattice.
- assignment base: `a4bf95c18049fe173aa62f2b9349682afd0aa8b8`
- owned range: resident `0x8005AF14..0x8005B644`, ROM `0x5BB14..0x5C244`, 1,840 bytes / 460 words, with no trailing padding
- ABI and callers: three pointer arguments, no resident export, and nine authenticated `R_MIPS_26` callers from overlays 8, 9, 41, 75, 86, 88, and 94
- baseline: configured full-TU V0 emitted 463 words against 460, with 377 raw and relocation-masked differing words, first mismatch `+0x0`, and frame `0x110` versus target `0xF8`
- retained result: the positional score remains 377 differing words, while writing `rotation2` before `rotation0` and `rotation1` follows the target store order and reduces workbench normalized distance from 365 to 361
- relocation proof: target and candidate each emit 27 static records; all candidate identities resolve, but only 9/27 offsets, types, and identities are exact
- residual: workbench reports `structure-mismatch`; its aligned view contains 104 structural, 200 register, and 40 constant sites, with the model/matrix-node saved-register swap and scalar/aggregate stack packing still causal
- attempts: ten coherent direct/volatile camera-index, aggregate scope/order, transform-store, declaration-order, ABI-prototype, and model-alias forms were measured; byte-flat forms were discarded and the volatile, float-ABI, and alias variants regressed
- flags: all 119 lattice rows completed; six `-O2 -mips2 -32` rows tie the configured result, while no alternate optimization, ISA, debug, hazard, unroll, warning, or phase combination improves it
- donor evidence: the nearest permitted masked skeleton is Conker at 0.049 and the nearest JFG row is `objDrawTracer` at 0.032; coddog finds no exact 20-word window in the owned range, and JFG's model peer remains assembly rather than usable donor C
- permuter: not run because no natural source hypothesis produced the strict score gain required before a bounded batch
- next action: reopen only with source-authentic evidence for the model/matrix-node saved-register ordering or for packing the camera scalar homes below the matrix/transform aggregates; do not repeat the completed source forms or flag lattice
- 2026-09-24 lane w4-models, base fe58b9c1. Cycle 0: insertion_pairs label split-not-copy, the shadow pair open from the rotation2 add (then line 771, eval move). Frame census 0x110 versus 0xF8, same slot count, saved registers already matching through ra. Named homes (g3, frame unchanged) put every declared local on the stack, including unaccessed pointers.
- Spellings, all stock -O2 -mips2 -32 -Wo,-loopunroll,0, scored against the retained 377-word / aligned-327 baseline. register on the angle and index locals did not move the frame. Deleting cosine, the four clamp s16s, temp, index, and pointOffset, and walking the point lists, closed the frame to 0xF8 but grew the text (direct object 0x754 versus baseline 0x73C) and raised aligned residual to 385. The size-neutral subset is: reuse assetPart for the camera pointer, inline nodeData, inline matrixBase and matrixNode, and walk pointA/pointB instead of index/pointOffset. That keeps text size 0x73C (delta +12) and frame 0xF8, but drops the s5 save (mask 0x801F0000, no slot at +0x24) and leaves the homes in the wrong order.
- Reordering those remaining declarations (asset, four pointers, matrixList, vertex, assetPart, output, temp, transform, matrix, scale, deltas, sine, four clamp s16s, yaw, pitch, cosine) lands the accessed homes on the target offsets: asset 0xF4, matrixList 0xE0, transform 0xB8, matrix 0x78, scale 0x74, deltas 0x70/0x6C/0x68, sine 0x64, yaw 0x5A, pitch 0x58. Immediates fell from 29 to 5 and structural from 121 to 99, but aligned residual rose to 376 because naming rose to 193 and one-sided words to 79. The two slot mismatches left are the missing save at +0x24 and activeMatrices spilled at +0xEC instead of the target's 3-load pointer at +0xD8. asset at 0xF4 is 3 loads versus the target's 4.
- Adding matrixNode back as its own local grew the frame to 0x100 and did not restore the save. Reusing vertex, which already occupied an unaccessed gap, to hold the matrix-node pointer left the mask, frame, and 451 masked words unchanged. None of these beat aligned residual 327, so the source stays the previous candidate.
- Stall: three frame spellings after the home-order identity (pointer deletion, matrixNode restored, vertex reused) produced no better residual. Do not repeat them. The missing lever is a seventh callee save inside frame 0xF8 without a new local home, so +0x24 exists and activeMatrices is not spilled at +0xEC.
<!-- plateau-handoff:func_8005AF14:end -->
