<!-- plateau-handoff:func_overlay_001_F000438C_185076C:start -->
### `func_overlay_001_F000438C_185076C` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 1113/1542 words
- frame: 0x138
- relocations: 184
- first mismatch: +0x34
- summary: Line 1428 volatile speedLimit reload closed delta -4 (1196 to 1114). Line 1463 volatile actionMode reaches 1113. Stall: delay-slot spellings do not beat 1113.

Summary before this remeasure: Size and frame are exact; web955 c7 reaches 1143 diagnostically only. Remaining blocker is FP/local-home shape plus unauthenticated global bindings.
#### Final reopening result — September 8

Plateau, with no newly matched bytes. The final configured full-TU candidate
is 6,164 bytes / 1,541 words against the owned 6,168 bytes / 1,542 words:
one word short. It has 1,203 raw and 1,196 relocation-masked differing words,
first mismatch `+0x24`, the target's `0x138` frame and identical callee-save
slots. The fresh baseline was 6,400 bytes / 1,600 words, 1,510 raw / 1,508
masked differences, first mismatch `+0x0`, frame `0x230`.

The final source retains the best positional candidate plus independently
proved callback, query and global type corrections. Its 184 relocation
records equal the shipped count, but only 46 identities are independently
resolved by the formal checker and 25 agree at exact offsets. All 48 direct
callee identities agree in order in a separate runtime-table audit. These
receipts do not establish exact relocation type/offset/identity for the whole
function, object identity, or linked candidate-C identity.

Two meaningful alternatives remain preserved in ignored lane evidence: the
exact-sized 1,542-word candidate with 1,288 masked differences, and the
1,543-word structural branch with 1,381 masked differences and an opcode
distance of 58. The latter restores target-supported slope/counter branches
and acceleration staging, but its positional residual is worse. The final
selection is deliberate; no attempt or committed milestone was reset away.

Stopping evidence is the five consecutive source attempts 30 through 34:

- Separate path-curvature magnitude: unchanged size and opcode distance,
  masked differences regress from 1,381 to 1,383; no new mechanism.
- Explicit symmetric clamp bounds: identical instructions.
- One countdown rule expressed as a for-loop: identical instructions.
- Ordered Boolean slope result: ten extra words and opcode distance worsens
  from 58 to 78; no useful structural receipt or new identity.
- Direct early exit: identical instructions.

Attempt 35 selects and remeasures the retained candidate with the accepted
identity/type corrections; it reproduces the 1,196 masked residual. Baselines,
unchanged reruns and an edit-script refusal are not counted as source attempts.

Workbench still reports a mixed structural/constant/register residual. Its
`structure-buckets` / `stack-home` lever names declared-local and compiler-temp
lifetimes; equal total frames do not imply equal homes. The remaining evidence
includes the surface entry copy, alternate-angle constant lifetime, differing
FP allocation, and unauthenticated global/pool bindings. The explicit
threshold carrier, split surface counter, dropped predicate local, angle
staging and final five variants are exhausted at these inputs.

Next concrete action: repair the friendly-alias comparison preflight in a
separately owned tooling task, authenticate the remaining global bindings,
and obtain independent local/aggregate lifetime evidence before reopening
`stack-home`. This structural-reconstruction authorization does not authorize
a flag lattice, permutation search or inert source shaping. No such mechanism
was substituted here.

The candidate remains under its original `NON_MATCHING` guard and assembly
fallback. `tools/finalize_plateau.py` records the measured score, frame,
relocation count and first mismatch. Final validation includes configured
candidate compilation and comparison, runtime relocation comparison, the
canonical two-phase `gmake verify`, `gmake check-nonmatching-builds`,
`gmake cleanroom`, and `gmake check-docs`. ROM verification covers the assembly
fallback, not this nonexact C candidate.

#### Earlier evidence and retained reconstruction milestones

Fresh evidence on base `67991097249736eab861570908d30f2116290aa9`:

- authenticated owner: overlay 1 `.text` offset `0x438C..0x5BA4`
  (`0x1818` / 6,168 bytes), exactly 1,542 executable words ending at the
  target epilogue; the next fallback owner begins at `+0x5BF4`;
- ABI evidence: the current Mickey-local draft is
  `void (f32 *object, s32 step)`, keeps the object carrier in `s1`, obtains
  its state pointer into `s0`, and saves `ra`, `s0`, `s1`, and `f20`, matching
  the target's opening call and callee-save set. No independently named caller
  or export provides stronger type evidence;
- configured full-TU V0: 1,600 candidate words / 6,400 bytes (`+58` words /
  `+232` bytes), 1,510 raw and 1,508 relocation-masked positional
  differences from `+0x0`. This exactly reproduces the retained scheduling
  geometry rather than trusting its stale ledger;
- frame evidence: the target uses `0x138` bytes and 292 non-save stack bytes;
  the draft uses `0x230` bytes and 540 non-save stack bytes while retaining
  the same 20-byte save area. The overallocated m2c scalar/array lifetime web
  is therefore the first structural blocker, not a local expression-order
  residual;
- workbench verdict: `structure-mismatch`, with 186 alignment gaps, 1,407
  opcode mismatches, 252 aligned structural residuals, and a net 58-word
  geometry surplus. Its constant-audit footer is downstream of the
  unauthenticated frame/layout and cannot make this candidate permutation
  ready;
- relocation evidence: the assembled target fallback exposes 65 symbolic
  records while the provisional full-TU candidate emits 452 records in its
  owned range. Workbench reports 272 metadata and 276 symbol-identity
  mismatches; many candidate `LOCAL_*`/`ext_o*` bindings are not authenticated
  retail owners, so linked relocation identity is far from promotable;
- donor evidence: `skeleton_scan.py similar --target '1:+0x438C' --top 15`
  found no credible sibling (best permitted published-decomp similarity was
  `0.054`). The same-offset JFG kiosk symbol is unrelated scheduling evidence,
  not a donor;
- tooling limitation: `wb_compare.sh` currently fails preflight because the
  same TU's unrelated `overlay1UpdateObjectPhysics` friendly alias does not
  select a unique C definition. V0 was therefore compiled as the configured
  `NON_MATCHING=1` full TU and compared against a local untracked target
  wrapper; no target-derived rows or workbench artifacts are tracked.

Next lever: replace the m2c local soup with source-authentic typed object and
stack aggregates that naturally recover the `0x138` frame, while resolving
the provisional `LOCAL_*` and `ext_o*` owners. Re-run preflight after the
friendly-alias issue is fixed; do not repeat flags, constant tweaks, or bounded
permutation until geometry and relocation identities improve naturally.

#### Structural reopening: identity audit

The September 8 decompiler-assisted reopening rederived the configured V0 on
`a4431db5ba447021d43bd780f8dfb1a1e0bc1db0`: 1,600 candidate words,
1,510 raw / 1,508 masked differences, first mismatch `+0x0`, frame `0x230`.
The preprocessed full-TU input accepts its own context comparison and produces
identical owned instructions and symbolic relocations when recompiled. The
ordinary comparison wrapper still refuses the unrelated friendly-alias
preflight; direct workbench diagnosis remains `structure-mismatch`.

Mickey's shipped relocation tables establish 184 runtime records in the owned
range: 48 calls, 68 high records and 68 low records. The assembled fallback's
65 symbolic ELF records are a different surface, not the retail relocation
count. A fresh near-match scan again finds no credible donor (best 0.054).

The identity audit establishes these corrections before type/CFG work:

- The opening local helper receives the object and initializes the cached
  object/state globals; its omitted argument in V0 is not a zero-argument ABI.
- `LOCAL_DATA_4` and `D_4` address the same initialized float; the update-count
  aliases address the same BSS integer. The duplicate cached-object aliases
  likewise have one owner.
- Path descriptors and mode callbacks belong to initialized data, not BSS.
  The path descriptor has three function slots and a mask in a 16-byte record;
  the mode descriptor has two function slots and a mask in a 12-byte record.
  Their pointer relocations identify Mickey's own predicates and callbacks.
- The call to overlay 8 offset `0x49A4` is the already-reconstructed
  `overlay8SetBuffer`: its input is the state pointer, not the stale float
  argument inferred by an untyped draft.
- Resident offsets `0x1D920` and `0x1E174` are integer-returning collision
  helpers. Resident offsets `0x2B90` and `0x2D70` take floating coordinates;
  their current integer reinterpretations are not the source ABI.
- The local path-resolution call orders the cached start coordinates before
  the cached destination coordinates; V0 reverses those pairs.

These are runtime-relocation and callee/call-site observations, not match
credit. The fresh generated and relocation-annotated drafts, identities,
compiler inputs, objects and comparison reports remain ignored local evidence.
Next: recover typed call arguments, callback signatures and stack aggregates,
then reconstruct control flow from that corrected draft.

#### Typed reconstruction checkpoint

The owned-range census corrects the older V0 claim of 452 candidate records:
V0 actually has 184, comprising 48 calls and 68 high/low pairs. The shipped
surface also has 184. Equal counts did not prove identities: V0 resolved six,
with five at exact offsets. Typed shared globals resolve eight, with seven at
exact offsets; remaining placeholder names still need authenticated binding.

Four material attempts recovered call ABI, object/state fields, contiguous
angle/vector outputs, initialized callback descriptors and ordinary scalar
lifetimes. The best positional result is attempt 3: 1,613 candidate words,
1,503 raw / 1,498 masked differences, frame `0x1B8`. Attempt 4 retains the
more explicit typed source at 1,610 words, 1,516 raw / 1,513 masked differences,
frame `0x180`. Both have 184 relocations and first mismatch `+0x0`; both
source/object pairs are retained locally. Neither is object-exact.

The frame reductions from `0x230` through `0x1B8` to `0x180` support the
workbench's declaration-home diagnosis: naming every decompiler temporary
allocated homes even when its live value occupied a register. This checkpoint
removes the generated stack diagram and opaque access macros. The next step
is the authorized CFG reconstruction, including inclusive countdown loops and
the remaining call-result/callback control flow. The series has not stalled.

#### Control-flow reconstruction checkpoint

Attempts 5–12 recover the inclusive surface/update countdowns, compiler float
literals, promoted level type and shared scalar lifetimes. Attempt 12 compiles
to 1,552 words (ten more than the target), with 1,371 raw / 1,367 masked
differences. The frame and save set now agree at `0x138` and 20 saved bytes.
The comparison remains a structural mismatch, not an exact result.

Both loops test their old counter before decrementing. The raw extracted
m2c draft had tested the decremented counter, a real behavior error at count
one. The clamped byte level is promoted to a signed integer before conversion
to float; this removes the compiler's unnecessary unsigned-conversion block.
Each adopted literal reproduces its authenticated pool owner's f32 value.

All 48 direct calls agree in order with the shipped runtime identities.
Canonical generated callee names allow the independent checker to resolve
46 call identities, seven at exact offsets in attempt 12; it conservatively
withholds the two callees with nonexact canonical definitions. The static and
runtime relocation totals remain 184. Global/constant bindings and the precise
stack-home layout still need proof. Direct object extraction places the first raw difference at `+0x24`,
the exit branch whose displacement reflects the remaining size surplus.
This is an object measurement; a linked owned-range proof remains pending.

The successful series has not stalled. Call-delimited measurements isolate
the remaining net ten-word surplus to the surface scan, angle/control blocks,
steering, and late motion integration. All earlier candidates remain ignored
local evidence; the currently retained source is ordinary typed C.
#### Call-read and callback reconstruction checkpoint

Further Mickey-only reconstruction removes unproved 16-bit narrowing at the
two steering trig calls, reads the speed-limit field after each damping call,
and completes horizontal velocity before integrating displacement. The two
inclusive countdown loops and three ordered slope-enable cases are now
explicit. The local steering interpolation negates its bounded signed input
before positive multiplication, as the target does.

The shipped descriptor pointers independently identify three zero-argument
mode predicates, three heading callbacks with signed integer returns, and
signed integer action predicates with void action updates. Their C function
pointer types now distinguish predicates from updates. These names/signatures
come from Mickey's canonical functions and data relocations, not a donor.

The retained comparisons are deliberately distinguished:

- Best exact-sized candidate: 1,542 words, 1,300 raw / 1,288 masked
  differences, frame `0x138`, 184 runtime records, 20 exact-offset identities.
- Best positional residual: 1,541 words, 1,203 raw / 1,196 masked
  differences, frame `0x138`, 184 runtime records, 25 exact-offset identities.
- Current structural branch: 1,543 words, 1,380 raw / 1,378 masked
  differences, frame `0x138`, 184 runtime records, seven exact-offset
  identities. It recovers a missing slope branch and old-count entry copy;
  aligned structural differences fall from 98 to 95 and opcode distance from
  67 to 63 relative to the exact-sized checkpoint. Positional scores worsen
  because the recovered instructions shift later regions.

All 48 direct callees still agree with the shipped identities in order. None
of these candidates is object-exact or linked candidate-C proof. The original
assembly fallback remains canonical, and all meaningful source/object attempts
remain in ignored lane evidence. The remaining structural regions concern the
surface entry copy, alternate angle control, and steering/speed-limit FP
lifetimes; stack homes and full global bindings remain unresolved.


#### tu2-o1tail: measurement only, and the FP allocation is right after the first seven webs

Re-measured: 1196 masked words, 1541 instructions against the target's 1542
(-1), 0x138 frame exact, prefix exact to row 11, 210 relocation sites naming a
different symbol on each side, so 1196 is a floor rather than the residual. No
source attempts were made; the committed record already reports the structural
reconstruction stalled after five no-information attempts, and this lane's
budget went to the near-miss rows instead.

One bounded new fact for whoever resumes: the fp-pool lane diverges only in its
first seven entries. The target opens f0 f2 f2 f12 f2 f2 f2 and the candidate
f2 f12 f12 f14 f0 f0 f0, after which the two lanes agree entry for entry
across the range inspected. The FP allocation is therefore not globally wrong,
it is rotated by an early web the candidate creates and the target does not --
the same named-local-versus-CSE-temp shape that closed most of
overlay1AppendPathPoint's residual. Start there rather than at the whole-body
reconstruction.

Tooling: `tools/wb_compare.sh` cannot resolve this symbol; see the note on
`func_overlay_001_F0003750_184FB30` for the cause and the manual route.

#### Phase census, region partition and where the residual actually lives (2026-09-11)

**Census first: uopt procedure 9 of 39, 162 globalcolor decisions, all phase
one, zero phase two.** 91 are integer and 71 are floating. The phase-two
web-numbering axis -- first-definition order, coloured ascending -- is
therefore not live for this function, so definition position, declaration
order and statement order are one dead end here and only the phase-one
`save = totalsave/nocs` ranking decides anything. The rule behind it is
general: over the 61 procedures of this unit, `overlay_008.c` and
`overlay52TailB.c`, every procedure that issues a call emitted phase-one
records only and every leaf emitted phase-two records only, 59 classified with
no counterexample (`docs/ido-learnings.md`).

Aligned against the target on a register-erased shape
(`tools/align_symbol.py`): 1541 candidate words against 1542, positional 1196,
displacement tax 374, and the residual splits 731 byte-exact / 557
register-naming / 265 really-different.

**The -4 is a net, not a site, and it is an allocator artefact.** The aligner
places 11 extra candidate words in 8 spans and 12 extra target words in 11
spans -- 23 separate structural sites that nearly cancel. There is no single
missing instruction to chase, and the earlier reading of the deficit as one
exit-branch displacement does not survive alignment. It is also not a missing
source operation: forcing integer web 955 to a caller-saved colour
(`CDX_FORCE=p1:w955=c7`), where the candidate's allocator splits it, produces
a 1542-word object with no source change at all.

**Region partition, 49 call-delimited regions.** The nine largest, as
words / byte-exact / naming / really-different:

  - region 17, +0xBB4: 154 words, 60 exact, 75 naming, 19 different
  - region 14, +0x6D4: 129 words, 51 exact, 53 naming, 25 different
  - region 15, +0x8C8: 101 words, 56 exact, 28 naming, 17 different
  - region 16, +0xA5C: 86 words, 36 exact, 36 naming, 14 different
  - region 27, +0x11CC: 83 words, 45 exact, 30 naming, 8 different
  - region 13, +0x598: 79 words, 30 exact, 39 naming, 10 different
  - region 33, +0x13AC: 62 words, 19 exact, 22 naming, 21 different
  - region 21, +0x100C: 57 words, 23 exact, 28 naming, 6 different
  - region 7, +0x26C: 56 words, 37 exact, 15 naming, 4 different

Regions 0 through 3 carry one naming row and three really-different rows
between them: the prefix is exact for nine words and the first two call
intervals are 66 and 12 words long. Nothing before +0x16C is worth
re-spelling.

**Named decision variable per residual class.**

- *The float residual is the largest single class and it is mostly not the
  allocator's.* 312 of the 557 naming rows differ in a floating register -- 294
  in a floating register only, 18 alongside an integer one -- and they appear
  in 27 of the 49 regions, so this is not a local slip. Only 87 of those rows
  have both sides inside the class-2 pool (c24=`f0`, c25=`f2`,
  c26=`f12`, c27=`f14`, c28=`f16`, c29=`f18`, decoded by force-and-diff and
  recorded in `docs/ido-learnings.md`); 225 touch `f4`, `f6`, `f8` or `f10`,
  which globalcolor never assigns. The dominant item is an `f6`/`f8`
  transposition, 99 rows across the two directions. Whatever the earlier
  "fp-pool lane diverges only in its first seven entries" note measured, it was
  not the emitted float registers: those disagree over more than half the
  function.
- *The integer residual is likewise mostly ugen's ring.* 209 of the 557 rows
  name a register globalcolor never assigned in this procedure; only 36 are
  purely among the eight it did assign (`v0`, `v1`, `a0`, `a1`, `a2`, `t0`,
  `s0`, `s1`). Combined with the float split, only about 123 of the 557 naming
  rows are a colour globalcolor actually picked. That is a statement about
  what *kind* of decision each row is and not a bound on reachability: the
  ring's phase is downstream of how many pool colours are consumed, and the
  force ceiling below closes 127 naming rows.
- *One hundred and seventy-two of the 265 really-different rows are not
  different code.* 80 are the same instruction at a different `sp`
  displacement, 48 at a different non-`sp` displacement and 44 the same
  mnemonic with another immediate; 70 are a genuinely different opcode and 23
  are present on one side alone.
- *The frame is a layout problem, not a shift.* Both frames are 312 bytes, but
  unlike this tree's other two whales the home sets do not pair by a constant.
  The shipped frame uses homes at +0x4C, +0x4E, +0x50, +0x54, +0x58, +0x60,
  +0x7A, +0x7E, +0x80, +0x88, +0x8C, +0x90, +0xA0 and +0xAC that the candidate
  does not use at all, while the candidate crowds +0xAE through +0xF0. The
  `sp` displacement deltas cluster at -4 (21 rows), -52 (12), -36 (11), -48
  (8), -20 (4) and -16 (4): at least five distinct home groups, each displaced
  differently. This confirms the earlier `stack-home` diagnosis and sharpens
  it -- the declared-local census is wrong in *count and grouping*, not in
  order, and no single declaration move can close it.

**How much of this is the allocator's to give: 136 words of 822.** Greedy
force ceiling, sweeping all 152 webs against every colour and the split path
each round and keeping the best (2,506 compiles per round): baseline 731
byte-exact / 557 naming / 265 really-different, positional 1196; then
`p1:w955=c7` -> 759 / 539 / 255; `+p1:w928=c7` -> 829 / 468 / 256;
`+p1:w533=c28` -> 849 / 448 / 256; `+p1:w87=c25` -> 867 / 430 / 256,
positional 1090. The fifth round found nothing. That is 17% of the residual,
and the other two whales in this batch converged at 16% and 15%, so a sixth is
what the allocator is worth on a function of this size. Note that the
really-different bucket moves by nine words at the first force and then not at
all: the frame and the code shape are not the allocator's to fix.

**Recommended order for the next attempt.** Fix the home census first (it owns
80 of the really-different rows outright and displaces the reading of the
rest), then region 17 and region 14,
which hold 128 of the 557 naming rows between them. Do not spend a lattice on
declaration or statement *position*: the census says that axis is dead, and
the measured allocator share of the naming residual is under a quarter.

Nothing above is a source change, a match claim or a credit claim. The
`GLOBAL_ASM` fallback remains canonical and `gmake verify` passes at this
commit.
<!-- plateau-handoff:func_overlay_001_F000438C_185076C:end -->
