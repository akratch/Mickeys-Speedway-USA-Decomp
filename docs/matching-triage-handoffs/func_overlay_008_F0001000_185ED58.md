<!-- plateau-handoff:func_overlay_008_F0001000_185ED58:start -->
### `func_overlay_008_F0001000_185ED58` plateau handoff

- source: `src/overlays/o008/overlay_008.c`
- score: 7/165 words
- frame: 0x18
- relocations: 18
- first mismatch: +0xBC
- summary: Delta 0 at 7: sample local plus else copy emits the join b. Left: count web takes v0, target v1; it interferes with the sample web in the else block.

Lane B-ovsmall (Track B, 2026-09-23) on base `3c9e59be`. Cycle 0: `insertion_pairs.py` read one pair, target-only branch at `+0x10C`, owned on line basis by the rollover-control `if` (jump handler), label control-flow; aligned residual after shadow 29 (7 naming inside the pair, shadow 76).

What closed the delta: the target's extra `b` is the jump over a non-empty else arm. The clamp now computes a separate `s32 sample` and copies it in an explicit else. The same shape (delta 0, 7) also comes from one variable with a no-op else that survives to ugen: `x ^= 0`, `x = x * 1`, `x = (u8)x`. Spellings that stayed at -4: empty else, `else {}` on the one variable, `if (1) {}` or `+= 0` or an OR-assign of 0 or `&= -1` in the else (uopt folds them), ternary on one variable, goto past the clamp, `n = sample` before the if. Spellings that went the wrong way: `sample < 181` with the copy in the then arm (+4), 120 moved into an else of the control test (+8), nested ternary (+8), s16 or s8 carriers (+8 or +16). Declared type (s32, u8, s16, u32, s8, u16) and placement (inner or function scope) of the two locals: flat at 12/13 wherever the size held. `if (1)` and `do { } while (0)` around the new clamp: flat (re-tested after the shape change, L146).

Case 2: swapping to the flags OR-assign of 0x8000 before `countdown--` removed the t6/t7/t8 cycle outright (12 to 7, masked). Draw order follows statement order there.

Remaining 7 (aligned: 158 exact, 3 naming, 4 structural). Records (`CDX_PROC=6`, 11 decisions, p1, identity gate PASS): the count local is web 41 (bbs 11,14,15,16) and takes c1 `v0`; the sample value is a type-4 expression web (bbs 13,15) and takes c2 `v1`. The target has both in `v1` with no copy. Forcing the count web to c2 was accepted and measured 6 masked, but the sample web was then forbidden `v1` (mask gains c2) and took `v0`, leaving `move v1,v0` in the bnez delay. Forcing both to c2 is refused the same way. So the two webs interfere in the else-arm block (block-set interference, L115), and no colour gets both into v1. The target's `beqz` with a nop at `+0xC0` (ours `beqzl` stealing the join lui) and its `bnel` stealing lui at `+0x104` both follow from that copy being gone.

Resume only with a source shape where the else-arm copy does not share a block with the sample web, or where uopt does not sink the sample's definition into the else arm. Do not repeat the carrier-type sweep, the wrapper probes, or colour forces on the count web.

Summary before this remeasure: Empty-if on unused closed the a0 home; state stays in a3. Still -4: rollover skip fills lui; target is beqz nop plus extra b at +0x10C.

Lane w32-o008 on base `0242379f`. Identity-gate PASS: instrumented IDO `.text` is byte-identical to stock. `CDX_PROC=6` (10 decisions, p1; the body has two calls).

Overlay22 empty `if (unused) {}` at entry closed the unused-a0 home. Frame slots now match (`+0x20 +0x1C +0x14`). State copies `a1` to `a3` and the emit site copies `a3` to `a0`. Positional 159 to 105; aligned 102 to 136 byte-exact, naming 36 to 7. Size still 164 vs 165 (`-4`). First mismatch moved `+0x8` to `+0x4C`. Relocations stay 18.

The remaining target-only word is `+0x10C` in the rollover join. Candidate fills the control-skip delay with lui 1.0f (`beqzl`). Target does not fill (`beqz` plus nop) and uses an extra `b` from the clamp path onto a shared lui. First structural difference is that skip at `+0xC0`. The leftover `t7-t6-t8` cycle is one ring-phase fact (L127) in the shadow of that missing word. Two float rows (`f6`/`f18` to `f0`) remain.

Join-axis attempts that did not emit the extra branch: L97 `if (1)`, overlay40 comma-assign of the clamp and of the three stores, ternary clamp, `do { } while (0)`, empty else, inverted control, volatile weight, L103 `(f32)1`, leftover OR-zero on unused, `s32 unused` (byte-identical to `void *`). L160 loop-local countdown is worse (`+4`, 121 exact). Overlay41 weight-before-clamp reached size 0 but aligned worse (110 exact, 48 naming); not retained.

The three-argument ABI is still authentic. A four-argument probe that parked state in incoming `a3` remains disproved by both callers and the target's initial `a1` load. IDO rejects an unnamed parameter in a prototype-style definition.

Resume only with a spelling that stops as1 filling the rollover-control skip delay with lui 1.0f, so the clamp path emits the extra join branch at `+0x10C`. Do not repeat the join-axis lattice above, the four-argument ABI, or colour work while size is `-4` (L155).
<!-- plateau-handoff:func_overlay_008_F0001000_185ED58:end -->
