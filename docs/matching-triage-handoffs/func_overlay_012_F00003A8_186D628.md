<!-- plateau-handoff:func_overlay_012_F00003A8_186D628:start -->
### `func_overlay_012_F00003A8_186D628` plateau handoff

- source: `src/overlays/o012/func_overlay_012_F00003A8_186D628.c`
- score: 226 differing words
- frame: 0x110
- relocations: 33
- first mismatch: +0x50
- summary: Size 0, frame 0x110, masked 226. Colour floor 224. Blocker: volatile value pointer vs collision-load hoist; physics loads; vertex nop plus sh.

Identity-gated instrumented IDO (proc 0, 45 p1dec, 32 p1color). Arrival was +12, 340 masked, extra early cvt into f20 plus f22 copy.

Levers that moved size or prologue:

- L160 indexed `effect = &gOverlay12Effects[i]` closed +12 to 0. Walking `for (i++, effect++)` is +12.
- Named `gRate = gOverlay12Gravity * updateRateF` puts cvt.s.w in the trackGetTrack delay slot and kills the f22 copy. Deleting the local restores the copy and +12-class extras.
- Particle test `if (((u32)((u32)particle->active < 1u)) == 0)` emits sltiu. `!= 0` emits beq; signed `>= 1` emits blez; unsigned `>= 1u` folds to beq.
- `volatile f32 *vp = &effect->value; *vp *= ...` then the collision test emits bnel plus sb in the delay. Without it the collision load hoists into the value multiply and bne delay is the value store. `*(volatile f32 *)&effect->value` does not suffice.
- L112 `collision[10]` (indices 0-9) pays for the pointer home and keeps frame 0x110. `[11]` with the pointer is 0x118.

Exhausted this pass: L144 on updateRate (delay-slot cvt but extra stack loads), delayed conversion without gRate (cvt after jal, delay still sw of mode), particle walking restored (keep it; indexed particles become pointer-to-end), colour.

Colour: 151 probes, 33 webs. Four delta-0 winners, all rivals of one radius: p1:w39=c26 and c27 score 224; p1:w43=c28 and c29 score 225. L159 packing is `--force p1:w39=c26` predicted 224. No 0-score force. Remaining extras are structural: swc1 through s1 (the volatile pointer), physics lwc1 order, vertex nop plus sh, mode home +0xC8 versus target +0xA8.

Next: replace the volatile pointer with a barrier that still stores `effect->value` as `swc1 ft, 40(s0)` after the multiply and before `lw` of collision[0], so bnel plus sb survive without an s1 cursor.
<!-- plateau-handoff:func_overlay_012_F00003A8_186D628:end -->
