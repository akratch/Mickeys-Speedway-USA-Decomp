<!-- plateau-handoff:func_overlay_014_F0001830_1871108:start -->
### `func_overlay_014_F0001830_1871108` plateau handoff

- source: `src/overlays/o014/func_overlay_014_F0001830_1871108.c`
- score: 201/201 words
- frame: 0x90
- relocations: 14
- first mismatch: none
- summary: ROM-exact and promoted: 201 words, frame 0x90, 14 of 14 relocation identities. Case 7's increment must carry a later source line than the call's loads (as1 line tie, L59), so the draw block sits before the case label and case 7 is the increment plus a jump back.

Lane A2-ov (Track A, 2026-09-23) on base `373ba8d1` matched and promoted it. The five words were one as1 selection, read from the object and confirmed by a `#line` diagnostic: with the increment on a later line than the call (`#line`, not kept) the function is 0 masked. as1 sinks the increment below the four loads with offset fixups (case 6 already shows it), and the only key separating them is `lineno`; the increment written first takes the smaller line, is picked first, and the last load is left over for the delay slot. So the target's ugen order was never the question. The kept spelling puts the draw statement and its `break` in a labelled block before `case 7:`, whose own body is `stream += 8; goto case7Draw;`. uopt merges the two blocks at the case-7 position and the increment keeps its own later line: 201 of 201 words, frame 0x90, unforced, `gmake verify` and `promotion-proof` pass.

Spellings measured on the way: the four temporaries with `drawArgs` shrunk to pay for them (lengths 3 to 6) move `drawArgs` off +0x50, because it is anchored from the frame top (49 to 60); three new u8 temps declared beside `saved`, with `saved` carrying one byte, pack into its slot and close frame and ladder exactly (47, all naming, 0 structural). The records (proc 0, identity gate PASS) name that 47: the three new temps are symbol webs of save 20, nocs 1, coloured v0, v1 and then t0, since a0 to a3 and every s register are forbidden there, and a globalcoloured t0 leaves ugen's ring for the whole procedure, one ring phase from +0xDC. Which temp carries `saved` is flat (47 at all four positions). A multi-line increment statement, a forward `goto` to an increment block after the call (uopt moves the increment but it keeps the `goto` line), and the increment inside the first argument each stay at 5.

Promotion needed the jump-table form overlays 14 and 58 already use: the compiler's seven-entry pool duplicates the retained table at data_rodata +0x174 (rodata-relative +0x54), so the two text references are rebound to `gOverlay14DrawJumpTableReloc`, the pool is externalized by digest and the atlas ownership row is marked externalized.

Lane B-ovsmall (Track B, 2026-09-23) on base `3c9e59be`. Cycle 0: `insertion_pairs.py` read one pair, a candidate-only move at `+0x50` owned on line basis by `cellWidth = func_8004D5C0(2)` (move_to_dest), label split-not-copy; aligned residual after shadow 46, shadow 140. `frame_census.py`: candidate 0x98 against 0x90, and every home sits at the same distance from the frame top on both sides, so the 8 bytes are all at the bottom of the frame, not in a declared home.

Records (proc 0, 27 decisions, p1, identity gate PASS): the move is a type-4 expression web with one occurrence, coloured v1, separate from the stack-homed `cellWidth` symbol web. It is the u8-to-int widening of the call result. Declaring the callee as returning `s32` (or `u32`, or `s16`) removes that web: the division reads v0 directly as in the target, the move goes, and the frame drops to 0x90. 186 to 5 masked at delta 0 in one edit. `u16` stays at 186. The resident definition in `main/font.c` returns u8, so the declaration here models an undeclared or int-returning call from this overlay; that is argued in a comment at the declaration.

Spellings that did not work before that edit: `cellWidth` as u8 (delta 0 and 33 masked, but an explicit mask web and frame still 0x98); as u32 (frame 0x90 but divu, -24); as u32 with a signed cast in the division (delta 0, 102: the two 0x58 literals then differ in type and stop sharing s0, L151); s16 (+12); an assignment inside the division (no change).

Remaining 5 masked words (aligned: 196 exact, 5 structural), all in case 7. The target loads the four colour bytes at +1, +2, +3, +5 and puts the `stream += 8` in the call's delay slot. Here as1 picks the increment first and puts the last load in the delay slot. The `-Wa,-R` trace shows why: ugen emits the increment before the four loads, all at time 0 with the same aftercycles, and as1 takes them in emission order. The last load before the jal goes into the delay slot. So the target's ugen emitted all four loads before the increment. Measured spellings: the increment written after the call (-4); the increment on the same line as the call (no change); a separate u8 temp for the fourth argument (no change: uopt moves it past the increment); the increment inside the first, second, third or fourth argument (the fourth gives 2 masked, but that reads and writes `stream` in the same argument list, which is unsequenced, so it is not kept); a comma expression in the fourth argument (worse: ugen evaluates that argument first); a pointer copy (worse, +4). Four block-scope or function-scope temps loaded before the increment give exactly the target's case 7, but every added local grows the frame by 4 bytes (0xA0, 22 masked). Reusing the loop locals as temps changes their webs (+12).

Resume with a case-7 spelling that makes ugen emit the four loads before the increment without adding a local. The as1 trace settles which order is needed. Do not repeat the return-type or carrier-type sweeps above.
<!-- plateau-handoff:func_overlay_014_F0001830_1871108:end -->
