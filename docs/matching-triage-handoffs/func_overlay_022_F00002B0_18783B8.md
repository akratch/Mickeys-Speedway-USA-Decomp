<!-- plateau-handoff:func_overlay_022_F00002B0_18783B8:start -->
### `func_overlay_022_F00002B0_18783B8` plateau handoff

- source: `src/overlays/o022/func_overlay_022_F00002B0_18783B8.c`
- score: 448/499 words
- frame: 0x90
- relocations: 29
- first mismatch: +0x1C
- summary: Pair 4 +0x5F0 owns L213 stack-load, L224 move, L237 stack-load. Unowned call gone. f20 if speed crosses calls; volatile reload stays masked 448.

Summary before this remeasure: Frame 0x90 exact, saved s0/s1/ra at 0x1C/0x20/0x24, no f20; size -12.
- assignment base: `34c65d552d156afb169e9c7909f863de9f202001`
- owned range: overlay 22 `+0x2B0..+0xA7C`, 1,996 bytes / 499 words
- fresh baseline: candidate is 1,984 bytes / 496 true instructions with 448 of 499 positional words differing, size delta -12; first mismatch `+0x28`
- frame proof: candidate `0x90` versus target `0x90`. Saved GPR homes match. An unused pointer is the 8-byte rounding cell (without it the frame is `0x88`).
- size proof: 3 missing instructions. Displaced dt/step homes (candidate 0x84/0x40/0x80 vs target 0x88/0x34) remain in the prologue; body still lacks target spills at 0x28, 0x3E, 0x48, 0x58, 0x5C.
- new mechanism evidence: nested exclusive-block f32 carriers were the 80-byte surplus. Storing scaled bounce speed to `state->speed` before the later calls removed the callee-saved FP scratch. Cross-term rotation on this function remains compiler-flat; do not repeat those four probes.
- retained source: this candidate is best
- blocker: size delta -12 precedes colour/L159. Close the three missing words and L99-place the dt/step/angle/animationMode homes before any landscape.

Header regenerated from the ranking on 2026-09-23 (check_shard_metrics --write); it read first mismatch +0x28.

2026-09-24 remeasure, stock IDO, after testing the squared bounce length before sqrtf. Positional masked stays 448/499, size delta -12, frame 0x90, 29 relocs, first mismatch +0x1C. Aligned residual after shadow moved 369 to 366 (structural 122 to 117). The candidate is fully owned: the old unowned one-sided call is gone.

Pair 4, open from +0x5F0, label spill/reload, shadow 26, is the whole size gap. Three target-only words, all owned:
- +0x5F0 stack-load, line 213, iloadistore. Post-sound reload of scaled speed from 0x64(sp) into the delay-slot store of state->speed.
- +0x68C move, line 224, eval2. Second zero materialization during the slide divides.
- +0x6E0 stack-load, line 237, iloadistore. Reload of the post-friction speed for the c.le.s against zero.

Spellings, all reverted except the pre-sqrt test:
- Keeping the local speed live across the bounce calls and storing state->speed after func_8000309C reintroduces callee-saved f20 (sdc1), frame 0x98, and a shorter body. The early field store is what keeps f20 out; the target reload is a stack spill, not f20, and f20 is otherwise free.
- Declaring step after animationMode moves &distance and &animationMode up by 4 bytes, away from target 0x6C and 0x40.
- Four unused s32s between step and distance grow the frame from 0x90 to 0xA0. Distance stays at 0x7C. The four-home gap cannot be inserted inside 0x90.
- A volatile scaled speed on every slide use emits the compare reload but also a reload per multiply: 502 instructions.
- A volatile round-trip only before `speed <= 0` emits swc1+lwc1 at 0x48(sp), not 0x64(sp). Size delta becomes -4 and the masked count stays 448.

Stall: the three pair-4 words are still missing at masked 448. Do not repeat the f20 live-range, the step-last declaration, the four-pad frame growth, or a volatile compare copy. Do not colour-sweep while the size is off.
<!-- plateau-handoff:func_overlay_022_F00002B0_18783B8:end -->
