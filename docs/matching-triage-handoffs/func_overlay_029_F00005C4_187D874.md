<!-- plateau-handoff:func_overlay_029_F00005C4_187D874:start -->
### `func_overlay_029_F00005C4_187D874` plateau handoff

- source: `src/overlays/o029/func_overlay_029_F00005C4_187D874.c`
- score: 374 differing words
- frame: 0xC8
- relocations: 43
- first mismatch: +0x8
- summary: Size and frame closed at 583 words and 0xC8. Masked 374, first +0x8. L99 unused pointer/f32 grew the frame; skipTargeting inverted CFG added the missing word. Remaining: convert of updateRateF takes f20 so f20 is saved early; target converts to a temp and stores 0xC4. Identity-gated proc 0.

#### 2026-09-17, lane w11-o029: size and frame closed; 374 masked

Arrival was 2,332 B / 583 words, delta -4, frame 0xC0 vs 0xC8, 438 masked,
first +0x4. `align_symbol.py` read 265 exact / 123 naming / 26 immediate / 199
structural, displacement tax 90.

Levers that moved the residual:

- L99: unused pointer and unused f32 (plus a second unused pointer after the
  CFG edit) close the frame at 0xC8. Declaring `updateRateF` first homes it at
  0xC4, matching target traffic there.
- Inverting `skipTargeting` to `if (skip) { linked = NULL; useLinkedPosition
  = 0; } else { ... useLinkedPosition = 0; if (distance < D_C) ... }` adds the
  missing word. Size delta 0, 583 vs 583, masked 374, first +0x8.

Aligned at the 374-word shape: 292 exact / 116 naming / 17 immediate / 183
structural, displacement tax 58. Candidate-only +0x10 is the early `f20`
save; target-only +0x24 is that save in the normal callee-save sequence.
Volatile `updateRateF` produces the target convert-to-temp / late-f20-save
prologue and overshoots to +56 words. Address-store of the convert is +44.
Declaration-init of `updateRateF` is byte-identical to a first-statement
assignment. A records for-loop is not unrolled (420 words). Member-wise
`oldPosition` copy matches the 0x9C/0xA0/0xA4 homes but is size delta -4.

Identity gate: instrumented IDO `.text` is byte-identical to stock. Procedure
0, 50 p1 decisions. Highest-save FP web 262 (save 14, nocs 4, color 30 / f20):
split accepted, size +8, 387 masked; `c31` accepted, 373 masked still with the
early f20 save. Colour landscape not run beyond those probes.

Eliminated on this body:

- unused `direction` local: drops the frame to 0xC0
- `*(f32 *)&updateRateF = (f32)updateRate`: size +44, frame 0xC0
- volatile `updateRateF`: size +56, frame 0xC0
- records `for` loop: size delta -652
- member-wise oldPosition stores: size delta -4, 390 masked

Next: a source-authentic way for the updateRateF convert to land in a temp
and store to 0xC4 without keeping f20 live across the first call, so the
prologue save order matches. Then the records-unroll extra/missing triples.
<!-- plateau-handoff:func_overlay_029_F00005C4_187D874:end -->
