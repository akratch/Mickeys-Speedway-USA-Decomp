<!-- plateau-handoff:func_overlay_035_F0000B40_1882820:start -->
### `func_overlay_035_F0000B40_1882820` plateau handoff

- source: `src/overlays/o035/func_overlay_035_F0000B40_1882820.c`
- score: 130/528 words
- frame: 0x130
- relocations: 7
- first mismatch: +0xA4
- summary: Delta 0 at 130 on the DKR collision shape. Left: edge-loop IV order (i*8 before scratch+i*8) rotates t6/t9; s5-s7 colour order.

Lane B3-rl35 (Track B, 2026-09-23) on base `6d05abd3`. Start: delta -48, 486 masked, aligned residual after shadow 472, reader label missing-CSE with five pairs across both span loops. The body was rewritten from the target's semantics on DKR's `track_init_collision` shape (a triangle normal pass, then a per-edge bisector-plane pass), with `planes` an `f32` array indexed `4 * plane`. Measured steps, each on the previous:

- Rewrite (for loops over spans and triangles, s32 indices, DKR variable set, declaration order laid against the target's home ladder): delta +36, aligned 424. Declaration order then closed the home ladder: x0..z1 at sp+0x118..0x104, the three normals at sp+0xF4..0xEC, and the span end, vertex base and span index at sp+0xC8..0xC0, all from declaration order (every declared local reserves a slot, L99).
- `s32` neighbour instead of `u16`: removes a copy, aligned 331 at delta +8.
- The opposite-vertex test reads the vertex into x3..z3 first: aligned 265.
- A plane pointer taken before the edge maths and used only by the opposite-vertex test (the target keeps it in s3 across the neighbour-fix loop, so `s->planes` is not reloaded): aligned 152.
- Plane stores indexed by `counter << 2` with `counter++` after them, in both passes: delta 0, 143 masked. `4 * counter` lets uopt strength-reduce the first pass (an extra IV), and DKR's `temp = counter++` block costs one copy per pass.
- Span fields read by subscript, with no span pointer and no offset variable: uopt creates the span offset IV, which the target spills at sp+0x88. 130 masked; the unreferenced `span` and `spanOffset` declarations are kept as frame holders.

What is left, 130 masked (122 naming, 6 immediate, 1 one-sided word each way): the edge loop's preheader creates the `i * 8` IV before `scratch + i * 8`; the target creates them the other way round. That one-word shift rotates t6/t9 over 71 sites (L149) and swaps t1/s8. The rest is s5/s6/s7 colour order for the span offset IV, the span index and the counter (a p1 save-ratio question, L100). Flat: moving the neighbour read first, taking the scratch record by address, and swapping the two edge-neighbour stores were all byte-identical. Not tried: the instrumented toolchain on either question.
<!-- plateau-handoff:func_overlay_035_F0000B40_1882820:end -->
