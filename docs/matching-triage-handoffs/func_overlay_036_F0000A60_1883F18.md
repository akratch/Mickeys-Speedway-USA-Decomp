<!-- plateau-handoff:func_overlay_036_F0000A60_1883F18:start -->
### `func_overlay_036_F0000A60_1883F18` plateau handoff

- source: `src/overlays/o036/overlay36ChooseWeightedState.c`
- score: 71/170 words
- frame: 0x38
- relocations: 33
- first mismatch: +0x3C
- summary: Mode-branch delay tie: extra object reload vs shared 5. Target blend order exposes size +1. Copy-prop blocks parent 5. Identity gate PASS proc 0.

Re-measured on campaign/unchain 82bf3cfa. Size 170 vs 170, frame 0x38 both, positional masked 71, displacement tax 40. Aligned: 140 byte-exact, 7 naming, 9 immediate, 15 really different. Candidate-only +0x3C (1 word), target-only +0x12C (1 word). Unique stack slot is candidate +0x28 vs target +0x24 (the later position home, 1 load 1 store each).

Identity gate PASS: stock IDO `.text` equals instrumented `CDX_PROC=0` `.text`. 15 p1 decisions, 0 p2, 13 p1color, every `forced=-2` on the unforced run.

The candidate-only word is the homed object reload scheduled before the mode-equals-3 branch. The target emits the shared 5-argument in the parent and fills that branch delay with the same reload, then fills the choose-call delay with a zeroed first argument. ugen already emits two separate 5-arguments (then: zero a0, then 5, then the call; else: reload, then 5, then the call). as1 delay-fills from the fall-through arm, so the then-path 5 wins the slot and the else-path reload is hoisted as an extra word.

Proved identities this pass:
- Target blend order `(blend * 10.0f) + ((1.0f - blend) * value)` emits the +0x12C pad and grows to 171 with zero target-only words. V0's inverted mix size-compensates the extra reload. Do not treat that pad as a second insertion.
- Inverting to `mode != 3` with measure as fall-through does hoist the 5 before the branch, but the compare becomes equality and the object reload disappears because a0 is never clobbered. Keep `mode == 3`.
- A shared `s32` / `register s32` 5 copy-propagates back into both arms; if the local is not eliminated the frame grows (incoming object home slides from +0x38 to +0x40) and the score moves to 80 with first mismatch +0x0.
- OR-zero / XOR-zero on the shared 5, `if (1) { five = 5; }`, ChooseReloc varargs prototype, comma `(object, 0)` as choose's first arg, splitting the choose result, then-block OR-zero on total, L59 `#line` on the then-path 5, and address-form of the object pointer were all byte-flat on the insertion. Volatile address-form adds a stack-address temp and grows to 172.

L160 is already in V0: both countdown walks emit generated `weights[i][position]` and match the target from the clamp through the second walk aside from i := 13 versus neighbour scheduling and the +0x28/+0x24 home. Do not retry FP-term rotation of the initial measure/scale/strength expression, or explicit `(*inner).strength`.

Decision variable: ugen must emit one 5-argument in the parent so the then-block no longer contains it; copy-prop of a constant 5 currently puts it back. Colour is not the next lever (size-delta 0 still has the insertion pair).
<!-- plateau-handoff:func_overlay_036_F0000A60_1883F18:end -->
