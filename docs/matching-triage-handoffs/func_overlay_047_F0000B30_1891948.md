<!-- plateau-handoff:func_overlay_047_F0000B30_1891948:start -->
### `func_overlay_047_F0000B30_1891948` plateau handoff

- source: `src/overlays/o047/func_overlay_047_F0000B30_1891948.c`
- score: 1580 differing words
- frame: 0x280
- relocations: 316
- first mismatch: +0x4
- summary: Pair 1 +0x4 line 199 rate=updateRate is move_to_dest. register, same-line fold, dying s32: stall, still +4/1580.

Summary before this remeasure: Live +4/1580/0x280. Extra word is ugen param-home reload; L144 -908; or-eq 0 is size 0 at 1964; no force removes the copy.

#### Structural reconstruction packet, 2026-09-08

Outcome: the configured candidate's 2,176-byte overrun is closed, its frame
matches, and all 83 call-site identities occur in target order. This is a
compiled `NON_MATCHING` plateau, with zero new matched bytes. Executable size
agreement is not instruction, relocation, CFG-equivalence, or ROM proof.
The assembly fallback remains selected in the canonical build.

Ownership is overlay 47, text offset 0xB30, extent 0x21E0, ending at 0x2D10;
the corresponding ROM range is [0x1891948, 0x1893B28). Assignment base was
`cbd755d9b2e828b5f4c05a4efe81d70353d205cb`.

Re-derived configured measurements:

- Target: 8,672 bytes, 2,168 words, frame 0x280, 321 runtime text relocation
  sites, comprising 83 calls and 238 HI/LO sites.
- Baseline: 10,848 bytes, 2,712 words, frame 0x320; 2,701 raw and masked
  differing words, first mismatch +0x0. Its 191 text relocations plus six
  jump-table relocations explain the earlier handoff's total of 197.
- Retained attempt 15: 8,672 bytes, 2,168 words, frame 0x280; 1,975 raw and
  1,972 masked differing words, first mismatch +0x4. There are 318 candidate
  text relocations and six further jump-table relocations in `.rodata`.
  The score above counts differing words, not matching words.

The runtime tables supplied identities for all 321 target sites. A diagnostic
source-binding audit resolves 304 of the candidate's 318 text sites, with 49
identities agreeing at the same offset and relocation type. The unresolved
14 sites reference compiler-owned `.rodata`. All 83 calls agree in identity
and order, but most offsets still move. These bindings are an analysis aid,
not an authenticated canonical relocation receipt. The extracted fallback
object has placeholder names and fewer static relocations than the runtime
surface, so workbench symbol-mismatch totals are not a missing-call count.

#### Evidence and source recovery

`generate_ctx.sh` succeeded; the full context initially failed m2c parsing at
an unrelated flexible array in `Overlay8ScaleOutput`. Unannotated extracted
assembly also collapses call and global names. Both runtime relocation tables
were therefore decoded using the project's overlay and relocation tools,
then applied to an ignored diagnostic assembly copy. A focused typed context
and the six-case dispatch table allowed a useful m2c draft. The committed C
was manually reconstructed in ordinary project C; generated drafts, annotated
assembly, objects, and aligned comparison rows remain ignored.

The near-match oracle's best sibling score was only 0.057, providing no useful
body donor. No external function body, flag lattice, permuter, object patch,
dummy executable padding, or additional mechanism was used.

Recovered evidence includes:

- The actual call identities and signatures, including the overlay 45
  field-22 setter, distinct from its field-20 sibling.
- Player stride 0x34, icon stride 0x2C, position and texture-node stride
  0x10; actor field widths corroborated by Mickey's spawn, animation and
  object consumers; three `MtxF` matrices replace untyped scratch storage.
- Signed animation-frame indexing with an unsigned switch selector,
  captured texture-scroll pointer, four correctly ordered halfword updates,
  and ready/unready table row addressing.
- Initial selector movement before the occupied-slot search, ordinary
  update-rate interpolation, and recovered packed-colour channel extraction.
  High colour shifts use unsigned operands; no inert code was inserted.
- Six readonly floating constants agree in value; compiler pool order and
  reference forms still differ. Physical graphics pointers retain the target
  base/addend interpretation.

The typed reconstruction was banked in `99b161f3`; the size/frame-exact
candidate was banked in `1a2232dc`. Both commits change only the owned source.
Earlier attempts and the displaced final probes remain locally recoverable
under ignored `build/whale2/attemptN.c`, `.o`, summary and audit files.

#### Attempts and stopping evidence

Attempts 1-5 recovered the structure, call identities, physical-pointer
addends, selector search and table addressing. Attempts 6-15 recovered literal
constants, frame geometry, signed frame access, scroll-pointer reuse and
colour packing. Attempt 12's lower masked residual of 1,899 was on a shorter
candidate with the incomplete unsigned frame type; it is not the retained
size-exact candidate. Fixed table extents and a separate player-end symbol
were flat; retaining departure speed as an external scalar regressed. These
inputs do not justify repeating those probes.

The assignment's five consecutive no-information attempts were all measured
from the retained attempt-15 source. All remained 2,168 words with frame
0x280 and the same 83 ordered calls:

- 16: guarded equality-terminated interpolation loop; 1,975 raw / 1,972 masked.
- 17: explicit post-test four-player traversal; 1,975 raw / 1,973 masked.
- 18: explicit post-test statistics traversal; 1,975 raw / 1,972 masked.
- 19: explicit guard/post-test icon traversal; 1,975 raw / 1,972 masked.
- 20: explicit post-test player search within icons; 1,975 raw / 1,972 masked.

None improved the residual or established a new identity or causal fact.
The best source was restored and remeasured before this handoff. Baseline,
unchanged reruns and initial tool failures are excluded from the 20 attempts.

#### Validation and next action

Commands run include `tools/wb_compare.sh --summary-json` for each configured
candidate, workbench `diagnose` and `guide structure-buckets`,
`gmake check-nonmatching-builds`, `gmake cleanroom`, `gmake check-docs`,
`gmake extract`, the canonical object build, the full ELF build, and
`gmake verify`. All 243 candidate-bearing TUs compile. Clean-room and
documentation gates pass. `tools/finalize_plateau.py` generated the guarded
source marker and refreshed the structured handoff metrics.

The canonical fallback object's 8,672 text bytes and 165 static relocation
tuples agree exactly with the extracted fallback target. This confirms the
guard preserves that fallback; it is not linked-range or full-ROM proof.
Full `gmake verify` fails at the assignment base's unchanged
`overlay94UpdateController.c`, including unresolved `func_800254FC`,
`func_8002A878`, `func_8002565C`, and `func_8002A910`, plus truncated call
relocations. Fresh extraction and rebuilding reproduce the failure. No
out-of-scope source was changed to address it. See ignored
`build/whale2-fresh-verify.log` and `build/verify/rom-build.log`.

The workbench verdict is `structure-mismatch`; its final diagnostic footer
says "mixed residual: fix constants first (they cascade), structure second,
register classes last." Equal aggregate size has not proved every block.
The next packet should first distinguish remaining literal/reference
mismatches from stack offsets using `constant-audit` lever 1 and authenticated
runtime identities, then apply `structure-buckets` lever 6, the spill-slot
census, with lever 4's full-TU context check. In particular, the target saves
through f30 while this candidate saves through f28; matrix/local homes differ,
the first two readonly pool values are reversed, and unready-table reference
multiplicity differs. Establish which discrepancy causes a local residual
before changing declarations or allocation. Do not repeat the five flat CFG
spellings or substitute a flag/permutation search under this packet.

Final concise measurements and diagnostics are retained in ignored
`build/whale2/final-summary.json`, `final-audit.txt`, `final-diagnose.txt`,
`final-nonmatching-builds.log`, and `notes.md`. All target-derived detailed
evidence stays outside Git.


#### Lane p17-o47 address partition, 2026-09-12

The current inherited source was remeasured before any edit. `align_symbol.py`
reports 1256 byte-exact rows, 552 register-naming rows, 88 immediate-only rows,
and 241 structural rows, with 1580 positional masked differences. The target
extent is 8672 bytes and the candidate is 8676 bytes, so the candidate is 1
word over.
The displacement tax is 636 words. `register_census.py` reports 49 percent
mapping coherence and 50 changing GP windows; the whole-function cycles are
incoherent, but address windows expose stable local cycles.

`residual_map.py` prices +0x1380 through +0x1F7F at 479 aligned differing
rows: 356 naming, 12 immediate, and 111 structural. The largest single 0x100
window is +0x1E00 with 27 rows, 9 naming and 18 structural, in the bar
rectangle loop. The dominant regional cause is GP temp-ring phase: +0x1400
through +0x1600 carries t6 to t8 to t7 to t6, +0x1700 through +0x1A00 carries
t6 to t8 to t7 to t9 to t6, and the later windows return to the three-cycle
phase. The frame census is exact at 0x280 with identical 38-slot ladders.

Deleting the `rate` carrier reaches the target size delta of zero, but scores
1914 masked differences and shifts the home ladder, so it was rejected and the
inherited source was restored. No source axis was adopted in this lane.

#### Localization packet, 2026-09-09

This packet did not match the function. It converted four assumptions into
measurements, closed three of them as identities, eliminated two hypotheses,
and moved the masked residual from 1,972 to 1,906 differing words. It also
reclassifies the residual: the workbench's `structure-mismatch` verdict is an
artifact of positional comparison, and the disagreement is register
allocation, not structure.

**Classification, measured three ways.** Aligning the two instruction streams
under progressively weaker keys separates selection from allocation:

- keyed on the mnemonic alone: 2025 of 2168 aligned, 93.4%.
- keyed on mnemonic plus register class, blind to stack displacements:
  1771 of 2168, 81.7%.
- keyed on mnemonic plus register identity: 648 of 2168, 29.9%.

Instruction selection is therefore essentially correct; almost the whole gap
opens when register identity is added. The cumulative drift between the two
streams stays inside +-16 words across the entire 8,672-byte body and returns
to zero repeatedly, so there is no long-range structural displacement: the
residual re-converges everywhere. Positional word equality is a poor score for
this function precisely because a pervasive renaming destroys it.

**Compiler flags: settled, and two hypotheses eliminated.** The whole lattice
was compiled and scored (119 combinations, 53 scored). `-O2 -mips2 -32` with
no extras -- the flags `mk/overlays.mk` already gives this object -- is the
unique optimum, and the only group that is both size-exact and best-scoring.
`-Wab,-r4300_mul` costs four bytes of size and thirteen words even though
`mul_scheduler_scan.py` names this TU (one pair, one nop, its weakest possible
signal): eliminated. `-Wo,-loopunroll,0` costs 968 bytes and `-Wo,-loopunroll,2`
240, so the target's loops are unrolled at the default and the sibling
`func_overlay_047_F0000000_1890E18`'s unroll override does not extend here:
eliminated. `-Wo,-loopunroll,4` is byte-identical to no extra, confirming 4 is
the default. `-O2 -g3` and both other ISAs are worse. No further flag work is
justified on this TU.

**Frame partition: now an identity, and it was not one before.** IDO assigns a
home to every declared local in declaration order, descending from the frame
top, and the law is exact here: the previous candidate's source order
predicted sixteen observed home offsets with no residual, including all three
matrix bases, the height pointer, the pair passed to the overlay 45 pair
reader, and every spilled scalar. Reading the target's homes back through the
same law fixes the original order: 34 declared words precede the first `MtxF`,
the last two of them the float pair carrying the rate spill and the saved FOV;
the three matrices follow; two words, then the height pointer, then five more
close a list of ninety words. Reordering to that census put every observable
home on the target's -- matrix bases at frame+440/+376/+312, height at +300,
the pair at +560/+556, rate at +508, FOV at +504 -- with access counts
agreeing one for one and all four prologue zero stores landing on the target's
slots. The previously reported "frame exact" was only the 640-byte total: the
partition inside it was wrong on two counts that happened to cancel.

**Player record: 0x34, and the scroll member is phantom.** The reconstruction
gave the player record a texture-scroll pointer that the function never reads;
its only scroll access is the actor's. That made the record 56 bytes and
pushed every field past the actor pointer down four. The target settles it
twice: its player cursor advances by 52 at all five increment sites, and the
ready/active/leaving triple sits at +42/+43/+44 against our +46/+47/+48.
Dropping the member matches both. The icon stride 44, position stride 16 and
texture-node stride 16 were checked at the same time and were already right.

**Animation frame byte is unsigned.** The byte at actor+0x3B is read once
signed and once unsigned by the target; the reconstruction read it signed
twice. The dispatch reads it plainly and the animation-rate table index takes
the signed cast, not the other way round. That single correction moved 77
positional words -- far more than the two reads -- because the dispatch is the
hub of the largest block cluster in the function.

**The residual is one FP colouring fact.** Callee-saved float use agrees
exactly at f20 (27 uses), f22 (10), f24 (7) and f28 (13). The target has a
sixth callee-saved double and we have five: its f30 carries the same web our
f26 carries, so its f26 is a web we never create. That web is a pool constant
hoisted into the entry block. The consequences cascade in a way that explains
most of what is left:

- the sixth save adds eight bytes to the save area, which is why the target's
  integer save block sits eight bytes above ours even though the frame totals
  agree;
- the hoist changes which pool constant is referenced first, and the two
  pools' *internal* geometry is identical -- two floats, the 24-byte dispatch
  table, four floats, at relative offsets 0/4/32/36/40/44 on both sides -- so
  the earlier "first two pool values reversed" reading is confirmed and is a
  *consequence* of the hoist, not an independent defect;
- we spill three scalars the target keeps in registers, and emit nine more
  word stores overall.

The constant in question is the one assigned to the movement interpolation
factor in the not-leaving/not-active branch.

Two of IDO's three placement rules were isolated by measurement, and both
behave the same in our build as in the target:

- **Multi-reference constants are lifted to their common dominator.** A
  controlled probe swapped the reference counts of the animation-frame
  threshold pair. The constant that gained three references took pool slot 0
  and the entry-block register; the one that dropped to a single reference
  fell to slot 32 and was loaded at its use site. The pool is therefore
  ordered by first *emitted* reference, which is why hoisted constants come
  first and why our first two entries are in the opposite order to the
  target's. That ordering is a symptom, not a defect to fix directly.
- **Single-reference loop-invariant loads are hoisted when unconditional.**
  The icon-scale divisor is referenced once, and both sides hoist it to the
  icon loop's preheader. Our build puts it in the register the rate spill
  vacates; the target puts it in the second live range of the register whose
  first range holds the movement constant.

What does not follow either rule is the movement constant: one reference,
inside a conditional, hoisted by the target and not by us. Two spellings that
would create a second reference were tested and both are **eliminated**:
moving the assignment above the branch so that it dominates collapses the
branch and costs 84 words; adding a dominating assignment before the loop is
dead and is removed before common-subexpression elimination can see it, with
no effect on any measurement.

That leaves two causal orders, which this packet could not separate. Either
the original references this literal a second time in a live position not yet
found, and the resulting web takes the sixth callee-saved double; or the zero
web takes the sixth register for an independent numbering reason, and the
freed register is what lets the compiler speculate the conditional hoist. The
instrumented uopt profile named in the colouring-lever handoff
(`CDX_LOG=1`, `CDX_DETAIL_WEB`) is the instrument that can distinguish them,
by showing whether the hoist was considered and declined; it should be the
next packet's first move, before any further source spelling.

**Still open.** 318 candidate text relocations against the 321-site runtime
surface, unchanged. The candidate is one word long (0x21E4 against 0x21E0),
which trips `wb_compare.sh`'s TU-ownership guard and suppresses its summary;
the surplus is two register copies in the prologue -- one on the argument
feeding the integer-to-float conversion, one on the player cursor, where the
target materializes the array address straight into its callee-saved cursor --
against one word recovered elsewhere. Two spellings of the four-player end
pointer were tested and are byte-identical, so cfe canonicalizes there. The
texture-node writer sinks its pointer increment ahead of the two stores and
addresses them negatively; the target increments last.

**Next lever, in order.** Find the second reference to the movement literal
that makes IDO hoist it, which buys the sixth callee-saved double and with it
the save-area offset, the pool order and a large part of the colouring. Then
recover the one-word overrun. Do not re-run the flag lattice, the five CFG
spellings recorded above, or the dominating-assignment probe.

#### Structural recovery packet, 2026-09-09 (whale lane)

This packet did not match the function. It converted the FP-colouring
question the previous packet handed over into a mechanism, closed nine
further structural identities, and left the residual as an integer
colouring-order question with the webs named. Every measurement below was
taken with a direct `tools/ido/cc` compile at the object's recovered flags,
proved byte-identical in `.text` to the asm-processor `NON_MATCHING` build,
scored by a local shift-tolerant scorer (masked positional words, identical
rows under insertion-tolerant alignment, and the same alignment keyed on
register class) and, where a decision was in question, by the instrumented
uopt at `~/Desktop/dev/ido-instrumented` with `CDX_PROC=0`. The scorer and
all objects live under ignored `build/scratch-w47/`.

**Measurements.** Candidate 2160 words against 2168 (eight short), frame
0x280 exact, six callee-saved doubles saved in the target's order, integer
save block at the target's offsets, pool order 0.015 then 0.3. The stack-slot
census (every `n(sp)` offset with its access count) is identical to the
target's at every slot except the argument home at +640, which the target
reads 15 times and this candidate 12. Workbench masked words 1906 to 1963
(that count grows with the eight-word shortfall and is not the instrument
here); shift-tolerant identical rows 648 to 1051 of 2168; register-class
identical rows 1771 to 1847; mnemonic alignment 2025 to 2062. The worst
512-byte windows by register class are +0x1800 (68 percent, the colour blend
and vertex commands) and +0x0600 (75 percent, the unready target table);
+0x0A00 through +0x1600 are 92 to 98 percent.

**The sixth callee-saved double is reachable from C, and the lever is not the
movement constant.** Writing the integer literal `0` for the y argument of the
icon-strip `func_8002FB34` call, where the previous source had `0.0f`, makes
uopt keep that zero as its own web (the target's second live range of f24,
materialised in the icon-loop preheader) instead of merging it into the
function-wide zero web. With that one change uopt hoists the 0.015f movement
constant into the entry block as f26, pushes the global zero to f30, gives
the icon-scale divisor f26's second range, and reverses the first two pool
entries: the four facts the previous packet attributed to a second reference
to 0.015f. `0.0` (double) behaves like `0.0f`; the same literal spelled as a
double for the movement constant is byte-identical to the float spelling.
Why an integer zero is a distinct expression to uopt while the double is not
was not determined; the previous packet's "second live reference" hypothesis
is eliminated as the cause.

**The frame word was the texture-node pointer.** Adding the sixth save made
the frame 0x288 with the previous locals. Dropping `actor` or `scroll`
restores 0x280, but each costs words (`scroll` inlined re-reads the pointer
after every halfword store, plus eleven words). Keeping `actor` and spelling
the texture-node cursor as an index through the idle `count` local restores
0x280 with all observed homes unchanged, and it also gives the target's
post-increment node writes instead of the negative-offset form.

**Counter identities from the homes.** The target spills its icon-loop
counter to +632, the player-loop counter's home, and never touches +628. The
icon loop therefore runs on the player-loop counter; the inner player loop
and the icon search run on the other counter, which the target keeps in s4;
the rotation loop reuses that same counter rather than the interpolation
counter, and with that change colourIndex and unready stop spilling (the
target keeps them in s7 and s8, which this candidate now reproduces).
Positional scores fell while the shift-tolerant and class scores rose; the
positional count is the wrong instrument for this function.

**Smaller identities, each verified in place.** `allReady = 0` follows the
selector-search loop, which puts the target's `lh` of the selector before the
position stores and the zero store after the search load. `((s32)icon->x +
160) << 4` keeps the add before the shift; the multiply spelling distributes
into `x * 16 + 2560`. `actor = player->actor` is re-read in both branches
before the second animation call, which is the target's callee-saved reload
plus argument copy. Both selector comparisons are spelled
`(f32)player->selector == icon->selector`; the reversed spelling flips both
`c.eq.s` operand orders. The icon search runs inside `if (ov47Bss_0 > 0)`
with the icon reset inside the guard: the target's zero-trip branch skips the
reset. `ov47Data_550 = ov47Data_550 + ov47Data_554 * updateRate` instead of
the compound assignment moves 127 shift-tolerant rows with no structural
change, by reordering the tail's temp webs; the same respelling of the float
fade update is byte-identical.

**Eliminated, do not repeat.** `volatile` on the fade globals (plus nine and
plus seven words). One-element array declarations and `a = a + b` forms for
the fade update (byte-identical). `1.0f == frame` (byte-identical). A separate
cursor variable for the first player loop (the LDA is still CSE'd and the
cursor becomes a temp). Indexing the first loop (`D_800D3058[i]`, `continue`
forms): the target's prologue shape appears only when the second loop is
also indexed, and that form spills the second cursor to its home for 36
words. A macro cursor for the second loop (minus 176 words: the stores stop
aliasing the globals, which proves the original goes through a pointer
variable). Indexed cursors for the icon-inner and label loops. Every spelling
of the icon search with the counter initialised before the guard (`for`,
`while`, `do`/`while` with `++i`, increment at the top, `continue`): all
canonicalise to the same for-loop shape. Nested single-condition `if`s for the
join condition chain (byte-identical). A user-written `if (n > 0)` around a
`for` (double guard). Dropping the ready-table's minus-one, the flat-index
and `[4][4]` declarations and shift spellings of the unready table
(byte-identical). Mixed table spellings across the four unready statements.
Reordering the scroll halfword updates (byte-identical, uopt reorders).

**Instrumented-allocator facts.** uopt colours webs in decreasing
`totalsave / nocs`; a temp costs 0 for a call-free web and is infinite for a
call-spanning one; an unpaid callee-saved register costs 60 and a paid one 0
plus small affinity penalties (player's best cost is 64). `CDX_FORCE` is
refused for a colour an already-coloured neighbour holds. In this candidate
the first callee-saved decisions are colourIndex (web 748, totalsave 8100 over
5 units), two one-unit webs, the display-list pointer address (web 974, 31
units, cost 130.5), selected, and only then the second-loop cursor (web 22,
9183 over 59 units, priority 156), whose s0 through s3 are by then forbidden.
The target's registers imply the opposite order: player s0, speed s1, actor
s2, icon s3, counter s4, slot-table address s5, slot s6, the constant 9 s7,
the start position s8; and in the icon loop resultMatrix s0, selected s1,
list pointer s2, icon s3, camera matrix s4, matrix pointer s5, local matrix
s6, colourIndex s7, unready s8. The candidate already agrees on icon, speed,
selected-versus-j, slot, 9, start position, unready, resultMatrix, camera
matrix and the list pointer; the disagreement is the relative priority of the
second-loop cursor against colourIndex, actor, the counter and the slot-table
address, and of local matrix against colourIndex.

**Open causal questions, sharpened.**

1. The icon search. The target initialises its counter early (the activation
   block, before the selector search) yet compiles the loop with the exit
   test in the header, a plain `bc1f` with a nop, and the slot-table address
   hoisted into the preheader register. Initialising inside the guard gives
   exactly that body shape but a late init; every early init gives the
   for-loop shape. Something in the original prevents uopt's for-loop
   canonicalisation while keeping the init early.
2. The unready target table. The target rematerialises `%hi` for all four
   reads of the table in the not-ready branch while it keeps the ready
   table's address (used with a folded minus four) and both position-table
   addresses in registers. This candidate keeps all three. A probe showed
   uopt folds the bare address at two uses and keeps it at three, but a
   mixed two-plus-two spelling kept one pair.
3. The fade global. The target keeps the fade global's address in a register
   and reloads the value after storing it; this candidate folds the address
   and forwards the stored value. `volatile` overshoots.
4. The first player loop. The target materialises the cursor straight into
   s8 from its own address pair, and the second loop's cursor into s0 from a
   second pair; this candidate CSEs the pair into a temp and copies it into
   both cursors. Indexing the first loop removes the CSE but puts the cursor
   in a temp.
5. The interpolation bound. The target reloads the argument from its home at
   the remainder test and every unrolled exit test (three words); this
   candidate carries it in a temp.
6. The join trampoline at +0x2F8. The target's four condition skips and the
   search guard branch to a block holding only a jump to the join; this
   candidate threads them to the join and fills the likely slots with the
   join's first load.

**Next lever, in order.** Take question 1 to the instrumented uopt with a
loop-structure trace, since its answer also fixes the `at` pressure that
forces the hoisted address. Then find the priority lever for the second-loop
cursor (a web spanning more units with the same uses would need a different
loop shape; a colourIndex web spanning more units would need its definition
moved) using `CDX_DETAIL_WEB` on webs 22 and 748. The permuter, run from
this source (`tools/permute.sh`, base score 27017 on its scale), finds
colouring levers; vet each for semantics, since two of its top candidates
drop a loop initialisation.

#### Structural recovery packet, 2026-09-09 (whale2 lane, second pass)

This packet did not match the function. It closed packet questions 5 and 6,
proved four further structural identities, brought the candidate to the
target's exact size, and left the residual as one named allocator-priority
fact plus a sharpened question 1. Every measurement was taken with the
direct `tools/ido/cc` compile (byte-identical in `.text` to the asm-processor
`NON_MATCHING` build on this TU) and the local shift-tolerant scorer under
ignored `build/scratch-w472/`, against the 2168-word target.

Measurements, start of pass to end: candidate size 2160 to 2168 words (now
exact); register-class identical rows 1956 to 1985; mnemonic-aligned rows
2062 to 2075; shift-tolerant identical rows 1106 to 1179; masked positional
differing words 1964 to 1828. Frame 0x280, six FP saves, the integer save
block, the pool order and every stack slot including the spill area below
the locals agree with the target at every offset with the same access count;
the argument home is read 14 times against the target's 15. The literal
pool's absolute offsets in the extracted assembly sit four bytes above the
candidate's because the sibling function's constant precedes them in the
overlay's rodata; the relative geometry is identical and the masking hides
it, so it is not a residual.

Identities proved in place, each reverting the residual when undone:

1. The then/else pair on player->active is a switch, case 0 holding the
   activation and default the button handling. The four short-circuit
   condition edges and the icon-search guard edge all land on the case's
   break: a block holding only the jump to the join, with the join's first
   instruction duplicated into its delay slot. Written as if/else, cfe
   threads those five edges straight to the join and makes them
   branch-likely. This closes question 6 and is what brings the size to
   2168 exactly.
2. The texture-scroll update is a four-trip loop over the four halfwords,
   fully unrolled by uopt. As four statements the speed carrier's references
   weigh 10 each (allocator save 7.1); as an unrolled loop they weigh 100
   (save 58.6). The same edit produces the target's interpolation-bound
   handling, question 5: the guard's branch-likely with the argument reload
   in its slot and two further reloads after the remainder body.
3. ov47Data_530 is an s32 table: integer load, then convert, then add the
   screen x. An f32 table loads it as a float.
4. Case 1 of the animation switch reads actor->frameValue directly at all
   three tests, with no `frame` carrier. cfe orders a compare of a memory
   operand against a literal with the literal first, and a local against a
   literal with the local first, whatever the source order; the target has
   the literal first at all three sites. The dead `frame` local stays
   declared for the frame census.
5. Case 0's actor re-read follows the if/else-if rather than sitting in each
   branch: uopt's PRE then keeps the load on the store path and the call path
   and skips it on the false path, which is the target's shape exactly.
6. The icon loop's inner player loop and rotation loop run on the label
   loop's counter; the selector search and the interpolation loop on the
   other. A 3584-combination sweep of counter assignments over the four
   loops has this family as its register-class optimum.

Allocator ordering, verified on this function with the instrumented uopt.
Every integer web, temporaries included, is decided in decreasing save,
where save is totalsave over nocs and nocs is close to the reference count;
the chosen colour is the lowest-numbered register available at minimal cost;
temporaries cost infinity for a call-spanning web, callee-saved registers 60
unpaid and 0 paid; a web that finds no register is split and rematerialised.
Under that rule the target's loop-2 assignment (speed s1, actor s2, icon s3,
counter s4, slot table s5, slot s6, nine s7, start position s8) needs exactly
one fact this candidate lacks: the speed web must be decided before the
search-and-interpolation counter's. At the end of this pass speed is 58.6
and the counter 73.0, the counter's ten references being three at weight 10
in the search and seven at 100 in the unrolled interpolation loop. Lowering
the counter or raising speed past it is the named lever; no spelling tested
moves either by enough.

Question 1, sharpened. The counted body shape (exit test computed at the top
of the body, plain FP branch, slot-table address materialised in the search
preheader) appears if and only if the counter's zero is written in the
loop's preheader; then the counter becomes a temporary and the slot-table
address is hoisted to the loop-2 preheader into s8, costing 24 class rows
elsewhere. The target has the early callee-saved init, the counted shape and
the address in a temporary at the search preheader. Writing both inits
deletes the early one as dead. The target's address placement is what a
loop-2-spanning constant web gives when the allocator splits it, which needs
its save below the start-position address web's (both 100 over 48 units, a
tie broken by web number); that requires the search-loop address and the
icon loop's three slot-table addresses not to share one CSE web, which they
do in every spelling tried here. Find the spelling that keeps them apart.

Eliminated this pass, do not repeat: the argument escaping by address (kills
unrolling, minus 232 words); every early-init spelling of the icon search on
the switch base (while, for, double init); the reset as pointer subtraction
of the counter (a multiply chain); `selected` or `j` as the loop-2 counters
(class rows fall); named channel carriers in the first colour block
(folded), the packed colour as a statement, the blend channels assigned back
to their locals (minus 25 class rows); the fade globals volatile (each access
gets its own address pair) or through a pointer local (folded); speed as a
CSE temp; the scroll fields through player->actor->scroll; the first player
loop as for, with a not-equal end test, or with plain truth tests (identical
or worse); an early `stat = 0` before the label textX branch (gives the
target's placement but duplicates the colour-table address into both
branches); the permuter, whose scratch is not faithful here (its base object
differs from the real one by 37 identical rows and 7 mnemonic rows, so its
scores do not transfer).

Still open, in order of expected value: the loop-2 priority fact above;
question 1's shared constant web; the first player loop's address pair (loop
1 and loop 2 CSE the array address into one temporary in every spelling, the
target materialises two pairs); the ready-branch temp order and the
not-ready table folding, question 2; the fade global's address register and
post-store reload, question 3; the label loop's stat before the rectangle
constant (stat's save 122 against the constant's 286 needs a depth-3
reference); the blend block, where the target holds the unmasked channel
shifts and the constant 255 in pool registers and presses ra into service as
a temporary.


#### Residual partition and phase packet, 2026-09-10 (wb-o047 lane)

This packet did not match the function. It partitioned the residual, named the
mechanism that carries most of it, closed the previous packet's open question 1,
and moved the workbench score from 1834 to 1580 differing words. Every
measurement below was taken with the direct `tools/ido/cc` compile at the
object's recovered flags and cross-checked against
`tools/wb_compare.sh --summary-json`; the local scorer, its objects and every
candidate variant live under ignored `build/scratch-o047/`.

**The partition, which was the assignment's first deliverable.** Against the
2,168-word target, the previous best source scored 1,841 raw and 1,834 masked
differing words. Positional word equality is the wrong instrument here and the
number badly overstates the distance. Aligning the two streams on opcode
identity and classifying each aligned pair gives, for that source:

- identical: 1172 rows.
- temporary-register naming only, t0 through t9 on both sides: 670.
- callee-saved colouring, s0 through s7 on both sides: 93.
- a callee-saved register against another class: 60.
- other integer registers, the value, argument and assembler-temporary
  classes: 58.
- immediate or displacement only: 16.
- floating registers: 7.
- no aligned partner, an insertion or a deletion: 92.

So two thirds of the substantive residual -- 670 of 996 rows -- is which
temporary register holds a value, with the opcode, the other operands, the
displacement and the stack slot already correct. Relocation-representation
artifacts, the third thing the assignment asked to separate, are small: only
14 positional rows are a candidate relocation site whose target carries the
resolved value, and they vanish once each side is masked by its own relocation
evidence rather than by the other's.

**The 670 rows are 22 regional bijections on the temporary ring, not 670
decisions.** Walking the aligned pairs and starting a new segment only when a
disagreement contradicts the mapping built so far, 22 segments cover all 670
rows and ten of them carry every row but 45. The first two segments -- target
words 78 to 488 and 519 to 1058, half the body -- carry the *same* three-cycle
t7 to t8 to t9 to t7, 244 rows between them, with a short scramble at the
boundary that resolves back into it. That is the signature the assignment
predicted: a uniform phase offset in the ring, one decision, not hundreds.

**The phase is set by the icon search's induction variable, and the lever is
one line.** Copying the search counter into a second local in the loop's
preheader and running the loop on the copy collapses the first-half phase
offset entirely: the 244 rows drop to 12, the temp-naming bucket falls from
670 to 408, identical rows rise from 1172 to 1434, and the workbench's
`aligned_register` falls from 887 to 616. The same edit produces the target's
loop shape exactly, which is the previous packet's open question 1: the exit
test is computed at the top of the body, the inner floating compare takes a
plain branch with an unfilled slot instead of a likely branch with a
duplicated test, and the slot-table address is materialised in the search
preheader and held in a register across the body. The previous packet had
established that the counted shape "appears if and only if the counter's zero
is written in the loop's preheader"; the missing half was that the preheader
write must be to a *different* local, which keeps the early callee-saved
initialisation alive instead of deleting it as dead.

The workbench numbers, before and after, on the same target:

- differing words: 1834 to 1580.
- raw differing words: 1841 to 1593.
- aligned differences: 1155 to 921.
- aligned register: 887 to 616.
- aligned structural: 188 to 208.
- normalized distance: 1299 to 1143.
- instruction delta: 0 to +1.

**The debt this buys with.** The candidate is one instruction long again, and
the surplus is localised: the drift curve is flat at zero through the whole
body and steps to +1 only across the label loop's first rectangle block,
target words 1917 to 1943, where the target lifts the two row-coordinate
sub-expressions into the loop preheader and this candidate does not. That
block is also where the target's `stat` and row-coordinate carriers occupy the
opposite pair of callee-saved registers to ours. Recovering the word is the
next packet's first move; it is the same class of debt the previous pass
already closed once.

**A second, weaker phase form was measured and rejected.** Writing the
increment as a fresh definition and a copy back inside the body -- rather than
a preheader copy -- also collapses the first-half phase and keeps the
instruction delta at zero, reaching 1827 differing words with `aligned_register`
629 and `aligned_differences` 911. It does not produce the target's loop shape
and it costs 42 callee-saved rows, so it is recorded here as evidence that the
phase is a counting fact independent of the loop shape, not as a candidate.

**Falsified this pass, do not repeat.** Both cursor initialisations spelled
through an explicit first element (byte-identical; the front end canonicalises).
The loop-one bound spelled as an inequality against the fourth element (worse
by 274 identical rows). Hoisting the loop-two cursor initialisation above the
first loop (worse). Merging the first, second and label loop cursors into one
local, in either direction and in each single-loop half-step (all worse; the
address is still shared). Renaming a cursor local (byte-identical, so the
earlier "in every spelling" reading is about the shared address web, not the
name). The preheader-copy shape applied to the first player loop, the icon
loop, the inner player loop, the label loop and its inner loop: byte-identical
in every case, so the front end canonicalises it in counted loops and only the
bottom-tested search accepts it. The same shape driven from an already-live
local (`slot`, `showMode`) scores better but is semantically wrong and was
rejected on that ground alone. Hoisting the row-coordinate sub-expressions of
the rectangle block into declared locals by hand: byte-identical, so the
candidate's failure to lift them is placement, not a missing hoist.

**Tooling finding, and it costs a lane a cycle.** `tools/wb_compare.sh
--summary-json` refuses this symbol whenever a linked `build/mickey.us.elf` is
present, with either "candidate relocation symbol D_8007C0B8 has conflicting
runtime identity" or "candidate function escapes TU ownership" depending on the
candidate. Both messages are false: moving the linked image aside and re-running
summarises the identical source without complaint. Take the score before a full
build, or move the image aside first. The refusal reads like a defect in the
candidate and is not one.

**Still open, in order of expected value.** The one surplus word in the label
loop's rectangle preheader, above. The remaining ten temp-ring segments, 408
rows, whose largest are target words 1264 to 1478 (89 rows), 1702 to 1917 (86),
1944 to 2167 (63) and 1498 to 1607 (59) -- each is a phase offset of the same
kind and each should have its own one-line lever in the loop that precedes it.
The interpolation counter, which the target keeps in the second callee-saved
register and this candidate does not. The four rematerialised references to the
slot table in the not-ready branch, the previous packet's question 2, now
measured precisely: the target rebuilds the upper half of the address at each
of the four reads and folds the lower half into the load displacement, while
this candidate keeps the whole address in an argument register; the ready
table's address survives in a register on both sides because its folded minus
one makes it a different constant.


#### Paired-site audit and allocator census, 2026-09-11 (a9-o047)

The 63 gap words are **not 31 movements plus one surplus**. The complete
inventory below contains 45 entries corresponding to reordered operations,
11 address-construction/reuse entries, three relocation-representation
entries, two extra candidate copies, and two target hazard slots. These are
counts of **gap endpoints**, not distinct edits or missing source statements.
Five movement pairs have both endpoints in the gap inventory:

- C +0x1320 -> T +0x132c: Physical vertex-address flag extraction.
- C +0x170c -> T +0x1708: Colour-table element-address sum.
- C +0x1758 -> T +0x1760: First colour-pack high/middle channel combination.
- C +0x19e0 -> T +0x19cc: Blue complement multiplication by fade.
- C +0x1a88 -> T +0x1a7c: Second vertex-command header combination.

Those five pairs account for ten of the 45 movement entries. The other
35 movement entries point to operations that the aligner paired with a
*different* operation. For example, the missing target argument-home store
at +0x5c corresponds to candidate +0x10, while the surplus candidate
callee-save store at +0x20 corresponds to target +0xc. Pairing +0x20
with +0x5c simply because both are gap entries would join different stores.
Movement here means an operation correspondence established from operands,
value flow, and enclosing blocks; it does not prove that as1 scheduling alone,
rather than earlier allocation or code motion, caused the order.

The earliest genuine surplus is candidate +0x4, an update-rate argument
copy absent from the target. A second genuine surplus copy is candidate
+0x1968, the masked blue-channel carrier. Address-web differences also add
and remove instructions: the unready table has three repeated address-high
constructions at target +0x7ac/+0x7d4/+0x7fc, while the candidate retains
one full address. The target's distance-square hazard slot at +0x1028 has
no candidate counterpart. The second target hazard slot at +0x19c0 follows
the blue-complement transfer; candidate +0x19ac serves a different transfer,
so the two empty slots are not a proved movement pair.

**The label-loop surplus claim is not reproduced.** The first rectangle
command/count-setup block is candidate [0x1df8, 0x1ea8) versus target
[0x1df4, 0x1ea4): 44 words on each side. Three missing and three surplus
gap words inside it all have existing operation counterparts. The four-byte
displacement is already present at entry and is unchanged at exit. The
candidate sets up the row-coordinate terms before the zero-count guard;
the target sets them up after it. That is an ordering/control-dependence
residual, not an extra word in this block. This supersedes the previous
packet's instruction to recover the net surplus there.

Baseline, retained result, and remeasurement are the same source body:

- Owned candidate / target words: 2169 / 2168 before; 2169 / 2168 after.
- Size delta, bytes: +4 before; +4 after.
- Positional masked differences: 1580 before; 1580 after.
- Displacement tax: 636 before; 636 after.
- Aligned byte-exact words: 1256 before; 1256 after.
- Aligned register-naming words: 552 before; 552 after.
- Aligned really-different words: 392 before; 392 after.
- Raw positional differences: 1593 before; 1593 after.
- Candidate frame, bytes: 640 before; 640 after.
- Candidate owned text relocations: 316 before; 316 after.
- Extracted target static relocations: 165 before; 165 after.

The target has 2168 owned words; 32 candidate-only and 31 target-only words
occupy 25 and 26 spans respectively. The aligner's 392 really-different rows
are not all genuine content differences: branch displacement and relocation
representation remain in its shape comparison. Its positional union mask can
also mask a different operation after displacement. The inventory below therefore
uses the assembled objects and full operand/dataflow context rather than
inferring identity from the masked key. Static fallback relocation totals are
not the runtime relocation surface and do not authenticate a promotion.
No new matched bytes, object-exact claim, or C promotion results from this audit.

**Allocator phase is proved: p1 only.** The named retained Ucode stream maps
this single-procedure TU to run-local ordinal 0. Its 192 decisions comprise
164 integer p1 decisions (129 colour, 35 split) and 28 floating p1 decisions
(21 colour, seven split), with zero p2 and zero unknown-class decisions.
`allocator_trace_receipt.py` passes against the stock direct compile: the
traced and stock objects are whole-file identical. Configured asm-processor
output separately agrees with that direct output in text, readonly data,
and relocations. Its sole symbol difference is asm-processor's local absolute
prelude-guard symbol; the direct/traced receipt does not silently waive that
difference. L106 is not an allocator axis for this baseline. L100's save
ratio remains relevant to p1 pool choices; this census does not turn every
ugen temporary or as1 scheduling decision into a p1 decision.

**The earliest surplus has a narrower phase diagnosis.** The stock `cc -S`
listing attributes an argument-home reload into a temporary to
`rate = updateRate;`. The byte-inert `cc -Wa,-R` capture already contains
the corresponding register copy in as1's initial scheduling graph. Thus the
extra destination is present before final scheduling: the assembler forwards
the argument-home value into that destination, while the target conversion
uses the incoming argument directly. Merely moving the final scheduled copy
cannot remove it. No source spelling that removes the reload destination has
been tested or proved in this packet.

**Assignment stopped source experiments, not a numerical stall.** The lane
started at `b5a3c5147cd71cadc529e3b535b26b47d7fca5d0`. The mandated
`tools/lane_status.py --symbol func_overlay_047_F0000B30_1891948` returned
`already-integrated/exhausted`, reporting source commit
`dbd497a8ffd9dbd3eb988a4eaec4b350417ede6f` and handoff commit beginning
`f5e62ca85678`: the reopen authorization's handoff pin still named the source
commit. The authorization file was not edited, no alternate-base assignment
gate was substituted, and there were zero candidate-body attempts or adoptions.
The read-only baseline analysis requested by this assignment was completed.
The guarded candidate remains intact; only its handoff marker and this evidence
record are refreshed through `tools/finalize_plateau.py`.

Next action: the coordinator must reconcile the current source/handoff reopen
pins and obtain the zero-exit `base-only` verdict before source experiments.
Then investigate the argument-home reload destination first. Retain both the
site map and the p1 census when choosing a source lever; do not restart a
p2 declaration-order sweep or treat the first rectangle block as a surplus.
The old flag-lattice and donor results were reviewed, not repeated.

The complete baseline artifacts, exact source snapshot, configured and direct
objects, assembled target, streams, alignments, scheduler trace, Ucode capture,
fidelity receipts, and reproducible map coverage check remain ignored under
`build/a9-o047/`. `site-map.json` verifies that every expanded gap word appears
exactly once. No instruction listing, raw trace, ROM byte, or machine-local
absolute path is included in this record.

**Complete gap inventory.** C and T offsets are relative to this function's
candidate and target entries, respectively. A counterpart may already be in
an aligned row. `address` marks changed construction/reuse, `representation`
marks the same access expressed through different relocation forms, and
`hazard` is deliberately not paired merely by identical empty-slot encoding.

- C +0x0004 -> none: content: Extra update-rate argument copy; target transfers the argument directly.
- C +0x001c -> T +0x0084: address: Shared player-array address completion; target constructs the first cursor directly. See also candidate 0084/0110 and target 00f0/0108.
- C +0x0020 -> T +0x000c: movement: Save of the incoming final callee-saved integer register; displaced across other prologue work.
- C +0x0110 -> none: address: Second player cursor copied from the shared address; target constructs that cursor with its own address pair at 00f0/0108.
- C +0x0304 -> T +0x012c: address: Ready-summary global address high part: target materializes it in the second-loop preheader; candidate materializes it at the read.
- C +0x0530 -> T +0x0590: address: Active-player-count address high part: candidate hoists a full address before the ready split; target materializes the read address inside the unready arm.
- C +0x0538 -> T +0x0534: movement: Ready/unready branch; same source decision, relocated target edge. Not a missing conditional.
- C +0x079c -> T +0x0798: representation: First unready-table byte read exists on both sides; the target folds the table low part into its displacement.
- C +0x082c -> T +0x0674: movement: Interpolation counter zero is later in the candidate and uses a different carrier; target initializes it in the projection-call delay slot.
- C +0x1320 -> T +0x132c: movement: Physical vertex-address flag extraction, before versus after the display-list address construction.
- C +0x136c -> T +0x133c: movement: Vertex-address spill to the same stack home; target stores it earlier.
- C +0x13d4 -> T +0x13d8: movement: Sprite-pointer-array address completion; the adjacent position-state address completion is separately reordered.
- C +0x170c -> T +0x1708: movement: Colour-table element-address sum, moved across matrix-pointer update.
- C +0x1750 -> T +0x174c: movement: First colour-pack middle-channel mask; its source shift is also reordered.
- C +0x1758 -> T +0x1760: movement: First colour-pack high/middle channel combination.
- C +0x1900 -> T +0x1954: movement: Next display-list command pointer reload, scheduled later in the target colour blend.
- C +0x1904 -> T +0x18f8: movement: Blend colour-table element-address sum.
- C +0x1908 -> T +0x1904: representation: Blend colour-table word read; the target folds the table low part into the load.
- C +0x1920 -> T +0x1988: movement: Blend command header store; target interleaves it with colour arithmetic.
- C +0x1968 -> none: content: Extra masked blue-channel carrier copy; target consumes the masked value directly.
- C +0x19cc -> T +0x19e0: movement: Blend middle-channel placement into packed output.
- C +0x19dc -> T +0x19c4: movement: Blue complement conversion to float; distinguish it from the other same-shaped channel conversions.
- C +0x19e0 -> T +0x19cc: movement: Blue complement multiplication by fade.
- C +0x19e4 -> T +0x19dc: movement: Blue interpolated-channel addition.
- C +0x1a84 -> T +0x1a64: movement: Second vertex-command count/flags placement into its output field.
- C +0x1a88 -> T +0x1a7c: movement: Second vertex-command header combination.
- C +0x1a94 -> T +0x1a8c: movement: Second vertex-address command payload store, before versus after its header store.
- C +0x1e50 -> T +0x1e58: movement: First rectangle row-coordinate carrier copy; same operation despite the gap.
- C +0x1e58 -> T +0x1e3c: movement: First rectangle zero-count guard; target guards before row-coordinate setup.
- C +0x1e5c -> T +0x1e40: movement: First rectangle initial count decrement in the guard delay slot.
- C +0x2134 -> T +0x2138: movement: Positive fade-bound direction negation; the later negative-bound negations are different operations.
- C +0x2164 -> none: address: Negative fade-bound path rematerializes the value address high part; target reuses the address from 20f8/20fc.
- T +0x005c -> C +0x0010: movement: Argument-home store reordered within prologue.
- T +0x012c -> C +0x0304: address: Ready-summary high part moved to second-loop preheader; see candidate inventory.
- T +0x0130 -> none: address: Explicit low-part completion for that preheader address; candidate read folds the low part.
- T +0x01b0 -> C +0x01a8: movement: Zero sound-handle argument, moved across activation position stores.
- T +0x0584 -> C +0x0590: representation: Activation sound-ID halfword read; same call-delay operation, with different relocation representation.
- T +0x0668 -> C +0x0670: movement: Projection output-pointer stack argument store.
- T +0x07ac -> none: address: Second unready-table read rebuilds the high part; candidate reuses the full table address.
- T +0x07d4 -> none: address: Third unready-table read rebuilds the high part; candidate reuses the full table address.
- T +0x07fc -> none: address: Fourth unready-table read rebuilds the high part; candidate reuses the full table address.
- T +0x1028 -> none: hazard: Target has a hazard bubble between the two distance-square products; candidate has adjacent products. Not paired to an unrelated later empty slot.
- T +0x132c -> C +0x1320: movement: Physical vertex-address flag extraction; direct gap-to-gap pair.
- T +0x1334 -> C +0x1334: movement: Display-list-address spill; its ordering relative to the neighbouring vertex-address spill differs.
- T +0x13e4 -> C +0x13bc: movement: Icon interpolation counter zero; target retains a saved-register carrier and initializes it later.
- T +0x1708 -> C +0x170c: movement: Colour-table element-address sum; direct gap-to-gap pair.
- T +0x1760 -> C +0x1758: movement: High/middle channel combination; direct gap-to-gap pair.
- T +0x17c4 -> C +0x17a0: movement: Orthographic-matrix call argument setup, interleaved with display-list commands.
- T +0x1968 -> C +0x1998: movement: Second vertex-data address high part, identified by the eventual vertex payload.
- T +0x1984 -> C +0x1914: movement: Blend command header constant, interleaved with colour arithmetic.
- T +0x19a4 -> C +0x1994: movement: Third vertex-data address low part; do not pair it with the nearer second address.
- T +0x19b0 -> C +0x1980: movement: Vertex command constant high part.
- T +0x19b4 -> C +0x1990: movement: Vertex command constant low part.
- T +0x19bc -> C +0x19d4: movement: Blue complement transfer into the floating-point arithmetic chain.
- T +0x19c0 -> none: hazard: Target blue-complement transfer/conversion hazard bubble; candidate 19ac serves a different float-to-integer transfer, so equality of empty slots is not a movement proof.
- T +0x19cc -> C +0x19e0: movement: Blue complement fade multiplication; direct gap-to-gap pair.
- T +0x1a5c -> C +0x1a60: movement: Second vertex-command count/flags combination.
- T +0x1a7c -> C +0x1a88: movement: Second vertex-command header combination; direct gap-to-gap pair.
- T +0x1df4 -> C +0x1e20: movement: Label player-selector read, moved ahead of command setup.
- T +0x1df8 -> C +0x1e14: movement: Label selector-table address high part.
- T +0x1e00 -> C +0x1e2c: movement: Label selector-table indexed-address sum.
- T +0x20fc -> none: address: Persistent fade-value address low part; candidate folds it at each access and rematerializes high parts in the bound branches.
- T +0x2100 -> C +0x2104: movement: Fade increment multiplication, moved before the value read.

#### Size-close reopen, 2026-09-18 (lane w21-o047)

Assignment was `base-only` on `38add711`. Live remeasure before any edit:
8672 target bytes, candidate 8676, size delta +4, masked 1580, raw 1593,
displacement tax 636, frame 0x280 with identical 38-slot ladders, 316 text
relocations, first mismatch +0x4. Align: 1256 byte-exact, 552 naming, 88
immediate, 304 really-different. Instrumented IDO identity-gated against the
configured object (`.text` byte-identical). `CDX_PROC=discover` then
`CDX_PROC=0`: 192 p1 decisions, 150 colour, 42 split, one procedure.

The extra word is the entry conversion of `updateRate` into `rate`. ugen
stores the incoming argument to its home, reloads it into a temporary, then
converts; as1 forwards the store into that temporary, which is the surplus
copy. The target conversion uses the incoming argument directly. uopt's own
IR already names the parameter as the conversion source; the reload is ugen's.

Authorized size levers, all scored on direct `cc` objects that match the
configured `.text` on the unforced body:

- L144 address-form of `updateRate` at the conversion, as a dummy take-address,
  or at any later use: either inert or a 908-byte collapse. Eliminated for
  this parameter.
- L97 around the conversion, the prologue inits, the first loop, and
  `ov47Bss_30B`: byte-identical.
- L131 two names for `D_800D3058` (index 0, plus 0, u8/s32 casts, xor 0,
  plus `start`/`back`, `0 & updateRate`) and mixed spellings of the unready
  table: cfe/uopt canonicalise; byte-identical.
- L103: `0.00f` and `0.000f` on the icon-strip y are byte-identical to the
  integer `0`; `0.0f` is -12 bytes and undoes the sixth callee-saved double.
- Deleting `rate = updateRate` (local kept): -16 bytes, homes move. Same
  rejection as the inherited size-only rate deletion.
- `updateRate or-eq 0` (also xor-eq 0, times-eq 1, shift-eq 0) after the
  conversion: size delta 0, masked 1964. The conversion result leaves the
  callee-saved float that holds `rate`. Rejected; do not adopt a size-0
  that destroys that web. `+= 0` is not deleted and stays +4 / 1580.
- Unsigned `rate = (u32)updateRate`: conversion uses the incoming argument,
  but the unsigned compensation is +20 bytes.
- Inlining the blend `blue` local: byte-identical. `blue = colour >> 8`
  without the byte mask: size -4 (one short), masked 1590. Narrow `u8`/`u16`
  `blue` grows the function.

Force splits of high-save address-like webs, acceptance checked
(`forced=-1`), scored with `--object`: none removed the extra copy.
`p1:w427=s` is 1465 masked at +8; `p1:w447=s` 1482 at +8; `p1:w289=s`
1539 at +8; `p1:w254=s` is -4 / 2064. Recolouring web 427 or 112 off a0
is 1580 / +4.

Stall: three consecutive size-preserving probes after those identities
(canonical L131, L97 wrap of the first loop, blend inlining) moved nothing.
The leftover decision variable is a ugen/as1 spelling that emits the
conversion from the incoming argument without killing later forwarding of
`updateRate` and without taking its address. Colour / L159 is still blocked
on the +4.

#### Insertion-pair pass, 2026-09-24 (lane w4-o047)

`tools/insertion_pairs.py` and `tools/residual_map.py` were run before any
edit. Live remeasure: 8672 target bytes, candidate 8676, size delta +4,
positional masked 1580, shadow 636, aligned residual after shadow 944,
frame 0x280, 316 text relocations, first mismatch +0x4. Trace identity
gate passed, proc 0. The function label missing-CSE is pair 2 (shadow
166, lines 266, 306, 311, 334, 351 and 352). The net extra word is not
in that pair. It is pair 1's candidate-only move at +0x4, owned by line
199 `rate = updateRate`, construct `eval_int_flt_cvt`: `move_to_dest`
from the incoming argument register into a temporary, then the
conversion into the callee-saved float. The target conversion uses the
incoming register directly and writes that same float. Pair 13, the
label-loop rectangle block, has shadow 0 and names lines 640, 641 and
643. The count assignment on line 642 is not an owner, so the unlanded
fin-o047 reorder (count computed before the colour command) was not
applied.

Spellings of line 199, each reverted:

- `register` on the parameter: byte-identical, +4 / 1580. The temporary
  copy remains. Eliminated.
- Folding the conversion onto the same physical line as `ov47Bss_30B = 0`:
  byte-identical, +4 / 1580. Eliminated.
- A dying `s32` between the parameter and the conversion: not coalesced.
  Frame 0x288, masked 1591, copy remains. Reverted.
- `updateRate` or-eq 0 before the conversion, not the recorded
  after-conversion or-eq: still +4, masked 1766, callee-saved float kept,
  copy destination changes. Reverted. The after-conversion or-eq (size 0
  at 1964, float web destroyed) was not repeated.

Stall: the first three spellings produced no better residual and did not
make the `move_to_dest` destination the incoming register. The fourth
was worse. Colour stays blocked while the size is +4. Next lever is a
source shape that emits the conversion from the incoming register
without a temporary and without taking the parameter's address.

<!-- plateau-handoff:func_overlay_047_F0000B30_1891948:end -->
