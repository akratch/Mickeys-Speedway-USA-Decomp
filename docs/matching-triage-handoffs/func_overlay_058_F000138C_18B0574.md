<!-- plateau-handoff:func_overlay_058_F000138C_18B0574:start -->
### `func_overlay_058_F000138C_18B0574` plateau handoff

- source: `src/overlays/o058/func_overlay_058_F000138C_18B0574.c`
- score: 3614/3614 words
- frame: 0x138
- relocations: 1253
- first mismatch: none
- summary: ROM-exact and promoted: 3614 words, frame 0x138 and all 1253 runtime relocation identities; case 12's title loop subscripts the title array by i and a self-reading reset after the first call keeps the delay-slot reset live through dead-store elimination, so the guard reset folds the cursor init and is deleted as redundant.

Matched by [wv-y](../whale-self-read.md).

## 2026-09-16 (lane `wv-y`): matched on a self-reading reset; promoted

6 to 0 at delta zero, unforced; aligned 3611/0/0/4 (tax 2) to 3614/0/0/0
(tax 0); frame and relocations unchanged; `gmake verify` and
`gmake promotion-proof` pass from the C. Full account in
[whale-self-read.md](../whale-self-read.md).

- The title loop is `D_o058_5C98[i]` with `i = 0` inside the guard, as
  wv-x's `c9a`, plus one statement after case 12's first call: `i &= 0;`.
  It reads the delay-slot reset, so dead-store elimination keeps that
  def; it cannot be folded because its own def follows its read in the
  block; and it is deleted once liveness has been judged, because it is
  then a store of the value `i` already holds. With the delay-slot value
  available, the guard reset is deleted as redundant after strength
  reduction has folded the cursor init from it, and the row reset at 190
  sinks to the loop-exit block as before. Zero instructions, no web in
  183/184/190.
- Six rules of uopt's pass order were read from a two-loop mini TU before
  the cell was compiled: dead-store elimination first, counting only reads
  that survive an earlier constant fold; a read followed by a def of the
  same variable in its block is not folded; the redundant-store pass
  deletes a store only as its block's first reference to the variable
  (which is also why a read at 190 blocks the row reset's sink); the
  strength-reduction fold uses the block's own def, not propagated
  knowledge; a conditional store of a known value is deleted before
  strength reduction; a self-reading def is not a dead-store candidate.
- Five spellings measure 0 (`i &= 0` on the first call's line, its own
  line, call 2's line, call 3's line; `i = i & 0`); the control without it
  is 46.
- Promotion needed two things the score does not show: the compiler's
  duplicate jump-table pool (rebound to the shipped table at rodata
  `+0x124` and externalized by digest, overlay 14's form) and three
  resident data symbols named without `_o058Reloc`, whose bare value
  lines moved 95 resident bytes.

## 2026-09-16 (lane `wv-x`): W closes on a pointer walk; the residue is one load's schedule

9 to 6 at delta zero, unforced; aligned 3605/9/0/0 to 3611/0/0/4 (one
candidate-only word at +0x1314, one target-only word at +0x1320), frame
and relocations unchanged. Full account in
[whale-pointer-walk.md](../whale-pointer-walk.md).

- wv-t's pointer body had every property of the target's title loop but
  one interferer: `title = D_o058_5C98` inside the guard put the
  `&D_o058_5C98` lineage's remainder at 184 (that array is read in cases
  1/2/3/10), and the 5E9C piece then rejected 191 by one and took 202.
  Initialising the pointer from `D_o058_5C80 + 12` (the same bytes after
  link, a lineage that already spans the window) removes that interferer:
  W accepts 185 (+2), rejects 191, keeps 202 (margin 0) and colours t0;
  the 5E9C piece accepts 191 (margin 0) and rejects 202. That is the 48
  body's growth on both pieces, unforced. The pointer is the unused
  `cursor` cell (a fresh `char **` moved `slot`'s home by one slot).
- The residue is the title-loop load: ugen emits it in the same place on
  both bodies, but the strength-reduced form carries `.noalias $17,$sp`
  and the pointer form carries nothing, so as1 keeps the load above
  `sw t8,16(sp)` instead of sinking it into the delay slot (L95). The
  alias profile reads the base as `isvar`/may-alias against `islda`/
  no-alias.
- Closed here: `D_o058_5C98[i]` with the reset at 180 is +8 on every
  loop form (the known-zero reaches the cursor init only when no call
  separates the reset from the loop: measured at 182, 183, 180 and on
  four mini TUs, `register` and `-Olimit` inert); the reset inside the
  guard is 46, at 183 is 39 (the 48 form, seven aligned rows: only the
  `move s7,zero` position); an uninitialised index is loaded from its
  home; pointer-difference subscripts cost 16-28 bytes; physical-line
  and `cursor[0]` spellings are byte-identical; `*cursor++` is +4.
- Every dead carrier at 183 or 184 is one web too many for both growth
  tests at once (W's 202 test and the piece's 191 test both sit at
  margin 0 on the adopted body), so the target's title loop has no
  index web, and its load has the fact: a walking pointer base that
  uopt classifies as a load-address expression, which this lane did not
  find a spelling for.

## 2026-09-16 (lane `wv-w`): the slot rows close on a sibling's spelling; W is a closed system of margins

11 to 9 at delta zero, unforced; aligned 3603/11/0/0 to 3605/9/0/0,
frame and relocations unchanged. Full account in
[whale-growth-margins.md](../whale-growth-margins.md).

- The slot-address operand order is the tree's evaluation order, not a
  canonical sum: `&saves[f()]` and `&saves[i]` evaluate the index first,
  `(u8 *)saves + i * 32` through a declared local evaluates the base
  first (measured on a mini TU, then on the whale). overlay60Prefix.c
  writes the lookup that way; wv-v had measured the local and the byte
  arithmetic separately. Adopted at both sites: 9.
- W: wv-t's rule `2*left_after >= numintf + new` verified on all 477
  growth tests; W's 23 and the 5E9C piece's 24 window neighbours named
  (six spilled or late candidates, eight lineage hulls, nine coloured,
  and the 5EA0 remainder for the piece only), all the target's. W needs
  185 (v1 or s4 free) AND 202 (13 colours, since the 5E9C remainder is
  new at 202 whenever the 5E9C piece rejects 202, which it must); the
  piece needs s4 held or v1 lost. The consistent configuration needs
  w40 above 3.6, the 5EA0 remainder above 3.0 at seed 183, and the 5E9C
  202 seed before W's 183 seed; each is closed at zero width.
- The seed walk is a CFG DFS: case 1 immediately, then the arms in
  descending case value (read from the jump table), so case 13 always
  precedes case 12 and drags the 5EA0 remainder to 95/42 = 2.26.
- Closed: in-block statement order of the case-12 head (byte-identical);
  w40 split by literal type (3/1 = 3.0, width moves); `*&x` (folded by
  cfe); `volatile` on the five spilled candidates (frame moves: they are
  candidates in the target too); forcing W to t0 (+64); alias symbols
  for the 5EA0 lineage (diagnostic only: -12 to -104, the seed moves as
  predicted).
- Ten cycles; cycle 11 is a form that changes a lineage's weight or a
  seed's order without an instruction, which this lane did not find.

## 2026-09-16 (lane `wv-v`): the reset web is coloured by choosing its symbol

18 to 11 at delta zero, unforced; aligned 3596/18/0/0 to 3603/11/0/0,
frame and relocations unchanged. Full account in
[whale-index-carriers.md](../whale-index-carriers.md).

- The seven folded index resets are dead defs uopt still colours, as one
  web per SYMBOL. On a symbol of their own they take a0 (lowest free; no
  earlier web holds a0-a2 at any preheader and no preheader has a call),
  and a0 collides with D_o058_5E94's value at block 9, which cascades the
  argument colours through the function (120 on the cycle-15 body).
- Written on a local that is dead where it is used and already coloured
  harmlessly elsewhere, the resets join that local's web and inherit its
  colour: textY (s4, case 1's text y, dead in cases 3/13) carries the
  case-3/13 index, letter1 (s1, case 10, dead in cases 1/2) carries the
  cases-1/2 index. The two groups cannot share one carrier: the
  D_o058_5EF8 address web spans the entry loop and both draw loops, so
  case 1's draw loop must share the cases-1/2 name, and textY is live
  there.
- Closed: moving a reset out of its preheader (before or inside a head
  call, or above the if/else join) keeps the index live into the loop
  (+12/+36); a duplicate declaration of the carrier is silently shadowed
  by cfe (byte-identical, a no-op); s0 carriers (letter0, portraitIndex,
  countdownX) take s1 at the merged blocks and flip their own case
  (15); columnStep/columnCount and savedPosition/erase cost width.
- Remaining: W (nine rows, a2 for t0) and the two slot-address rows. On
  the 11 body W's identity reads the same, now itemised from neighbour
  captures: its 185 step fails by one (left 11, numintf 23) with v1
  taken by w40 between the 5E9C piece's split (idx 127, 3.60) and W's
  (idx 178, 2.26). Closed here: forcing w40 off v1 (w251 takes it); a
  late case-12 carrier live at 191 (W t0, the piece takes 202, +4);
  deleting the unused `s32 portraitX;` (284: symbol order is a
  tie-break); seven more slot-sum spellings (one object).
- Eleven cycles; cycle 12 is w40's decision after W's split, or the
  `&D_o058_5EA0` remainder above 3.0 at its 183 seed.

## 2026-09-15 (lane `wv-t`): the split is a growth test, read to the block

Held at 18 at delta zero; the instrument wv-s named is built and its
readings are in [whale-split-growth.md](../whale-split-growth.md).

- The instrumented uopt emits each decided web's block sets and every step
  of `split()`: seed, `grow`/`growv` with `new`, `left_before`,
  `left_after`, `numintf`, and the liveblocks that move.
  `tools/lineage_census.py --web N` prints a web's growth with margins.
- A piece accepts a block only while `new < left_before` and
  `2*left_after >= numintf + new`; a call block is accepted, never expanded.
- W is `a2` because its piece rejects the title loop's call block by one;
  the `&D_o058_5E9C` 202-piece survives because a dropped 183-seeded piece
  rejects 202 by one; both margins are the dead carrier's web and its `s4`.
  With any dead-carrier web at 183 the two tests contradict (W needs one
  more colour than the 202-piece may have, and they differ by exactly the
  `v1` `w40` takes in between). Measured both ways: `textY` fails W, a fresh
  carrier fails 5E9C.
- Explicit pointer cursor: W right, one interferer at 184 from the
  `&D_o058_5C98` reference, 202 stolen; four init spellings identical.
- Case 13 with `opponent` as coordinate: the `a2` piece's 254 step fails by
  one because `portraitX`'s web remains at 241; probes on `portraitX`
  converge the ratio on 10, under the cursor temp's 15.5.
- Twelve cycles; cycle 13 is a web in W's blocks coloured `v1` before the
  5E9C remainder splits at save 3.60.

## 2026-09-15 (lane `wv-s`): the gap closes on a split, and an interference token

48 to 18 at delta zero; aligned 3597/11/1/8 to 3596/18/0/0; the gap pair
(candidate +0x12F8, target +0x1260) and its 30-word tax are gone; 1,253
relocations unchanged. Full account in
[whale-split-tokens.md](../whale-split-tokens.md).

- Case 12: `fontColour(..., (0xFF - (i = 0)))` puts the reset in the
  delay slot; the title loop subscripts by the dead carrier `textY`, whose
  reset folds into the constant cursor init and is deleted; a discarded
  read of `columnCount` (dead on this path) before `D_o058_5EA0 = 0` keeps
  the `&D_o058_5EA0` fragment from adjoining the zero-store block.
- Every cost in this family is an address-constant lineage re-split, never
  the reset: fourteen dead carriers shrink an `&D_o058_5E9C` fragment (+4),
  `textY`/`columnCount` grow the `&D_o058_5EA0` one (-4), live carriers
  keep the reset word (+8/+12). The probe is zero-width only in that one
  block; elsewhere it emits a load, and a declared token moves nine rows.
- Remaining: that fragment coloured a2 for t0 (nine rows; a2 is offered
  once its numintf reaches 25, base 22, regsleft equals the offer-list
  length, no spanned-call denial); case 13's pair, where `opponent` as the
  coordinate lands both colours and sheds one `&D_o058_5E9C` head piece
  (one word, 27 cells inert); the two slot-address operand orders (carrier
  deletion 28-36).
- Corrections: `p1color forced=<colour>` is acceptance, `-2` never applied;
  "twice the available registers" is not a colouring bound (56 of 140
  coloured webs exceed it). Forcing the fragment to t0 costs +64 because
  t0 is a ring temporary on this body.
- Next: a per-web block-set dump from the instrumented uopt; both fragment
  questions are readings, not lattices. Instruments banked with suffix 18.

## 2026-09-14 (lane `wv-r`): one 5E50 name, two role swaps, two line ties

147 to 48 at delta zero in five composed edits; aligned 3498/108/1/8 to
3597/11/1/8; 720 draws in the identical sequence; the gap pair unchanged
and now the first mismatch. Full account in
[whale-role-carriers.md](../whale-role-carriers.md).

- Entry store, decrement and draw loop read `D_o058_5E50[portraitX]`: one
  web, spans calls, s0 on its own (85). The decrement loop counts `i`
  beside it so the post-loop reset survives; every `[i]` draw-loop
  spelling costs width because two reaching `i = 0` block the constant
  cursor init.
- `opponent`/`columnX` exchange roles in cases 12/13 (63); case 13 keeps
  the stride in `columnStep` and the count in `columnCount` with one
  depth-1 probe each so the order is stride, count, `i` (48).
- Probe between `savedX`/`savedOffset` removed (83); case 8 `0x5B +
  D_o058_5EAC + ...` (82); case 9 `columnX = 0; do {` on one line (60).
- Closed: inner-index and grid-coordinate carriers (58 cells, inert or
  width), case-13 coordinate carriers (-4 each, again), portraitX probes,
  cursor-web probes, case-12 cursor loop (201 best; +24 on the 63 body),
  slot operand order (14 spellings), and the first-order landscape at 48
  (140 probes, no single same-kind winner).


## 2026-09-14 (lane `wv-q`): the second-order landscape, and case 2's shared carrier

157 to 147 at delta zero, one edit: case 2's rank difference carried by
`columnX` instead of `textY`. Aligned 3488/118/1/8 to 3498/108/1/8; 720
draws in the identical sequence, 5,068 emissions, 1,253 relocations, the one
gap pair unchanged. Full account in [whale-second-order.md](../whale-second-order.md).

- `tools/web_footprint.py --hold` (new) probes every other web with a set of
  forces held. At the packed five (116) it finds `w101=c17` (+19) and
  `w26=c4` (+7), neither visible at first order; the seven measure 90 as
  predicted. At 90, 906 same-kind probes find no single winner.
- uopt colours a symbol as one web across every case. `textY` spans cases
  1/2/8/9/10 (nocs 29) and takes s4 early; the target's case-2 value is in
  s1 next to the format-string address in s4, the colours of a late web.
  Sixteen carriers: `columnX` 147, `countdownX` 151, four at 156, the rest
  160 to width.
- The target's cases-1/2 cursors are per-case webs (s3/s0 for `D_o058_5EC8`,
  s2/s3 for `D_o058_5EF8`); ours span both cases (L131). Twelve per-case
  spellings: `letter0` in case 2 measures 160, all others cost width.
- The entry store cursor and case 2's decrement cursor are one
  `D_o058_5E50[i]` web when the entry loop indexes by `i` (byte-inert at
  157); forced to s0 with the five it measures 86. It takes a callee-save
  colour on its own only when the draw loop shares the name too, at +4.
- Closed: case-13 def placements (158, -16, -8), second-loop index/counter
  merge (+8/+12, 177), a discriminant copy across the entry calls (every
  placement changes size), entry-loop index carrier (byte-inert x7).

## Earlier passes

This shard carried every pass in one file until 2026-09-16, when it reached
277,288 bytes and crossed the clean-room 262,144-byte tracked-file limit. The
function matched that day, so the log is closed and was split rather than
exempted:

- [2026-09-13](../whale-pass-log-2026-09-13.md) — the `w3`–`w10` lanes, to and
  along the 187 plateau.
- [2026-09-12 and earlier](../whale-pass-log-through-2026-09-12.md) — 733
  through the 227 and 187 plateaus.

<!-- plateau-handoff:func_overlay_058_F000138C_18B0574:end -->
