<!-- plateau-handoff:func_overlay_066_F0000040_18C64A8:start -->
### `func_overlay_066_F0000040_18C64A8` plateau handoff

- source: `src/overlays/o066/overlay66SmoothAndDraw.c`
- score: 249 differing words
- frame: -0x18
- relocations: 28
- first mismatch: +0x4
- summary: Colour floor 247 on 117 probes (w21=c13). Comma-assign, empty-if, OR-zero inert; L160 index +20. Five insertion pairs remain; not colour.

#### 2026-09-19, lane w36-o066

Re-measured on `dec888e9`, identity-gated instrumented IDO vs stock PASS
(function `.text` identical), proc 0, 19 p1 decisions, 17 p1color, 0 p2.
1184 B, delta 0, frame 0x18 (slots +0x18 and +0x14 match), 28 relocs,
249 masked / 251 raw, aligned 48 exact / 200 naming / 0 immediate / 53
structural. Displacement tax -4. Mapping coherence 68 percent, 10 windows;
cycles are not a ring phase.

Five candidate-only words and five target-only words cancel in size:
candidate extras at +0xE8, +0xF4, +0x104, +0x2B4, +0x450; target extras at
+0x90, +0xA4/+0xA8, +0xC4, +0x294. First naming +0x84, first structural +0x94.
The +0x4 raw mismatch is a relocation addend (masked). Reloc identities stay
partial; none were invented from the target.

Named levers on this exact-size body:

- overlay40 comma-assign in the bssControl condition: inert at 249
- overlay22 empty `if (remaining) {}` after the loop: inert at 249
- leftover remaining OR-zero in the loop: inert at 249
- nested else plus control temp, `* 4` / `* 8` vs shifts, dropping the
  component-copy blocks: all byte-identical to 249
- L160 indexed `pixelBase[i]` instead of the walking cursor: size +20,
  frame 0x20, 274 masked
- L160 inline first-pixel extracts (no red0/green0/blue0 before the first
  store): size 0 but frame 0x58, 253 masked
- full m2c-shaped interleaved preamble/loop with extra extract locals:
  size +56, frame 0xE0, 310 masked

Colour landscape (`tools/web_footprint.py --every-colour`, 117 probes over
17 webs, same-kind only): two probes beat 249 at delta 0, and they are
rivals with identical radius. Best is `p1:w21=c13` at 247 (+2); rival
`p1:w65=c5` at 248. Predicted packing is that one force, floor 247. Web 26
has no second colour. Two probes moved size and were excluded. 64 probes
moved nothing. L160 after a zero-scoring force does not apply: no force
scores 0.

Do not repeat the colour landscape, flag lattice, or the named size levers
above. Next: close the five insertion pairs in the filter preamble and the
per-iteration store schedule (the extra moves, the missing lui, and the
ori-1 vs shift order). Colour cannot move them (L155).
<!-- plateau-handoff:func_overlay_066_F0000040_18C64A8:end -->
