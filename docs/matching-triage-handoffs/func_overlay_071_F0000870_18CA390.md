<!-- plateau-handoff:func_overlay_071_F0000870_18CA390:start -->
### `func_overlay_071_F0000870_18CA390` plateau handoff

- source: `src/overlays/o071/func_overlay_071_F0000870_18CA390.c`
- score: 9 differing words
- frame: 0x30
- relocations: 13
- first mismatch: 0x5c
- summary: Second type-3 in blocks 9, 11, 13 failed: s16 cast merged dtype 6 at -4; !=0 emitted +20; &0xFFFF absorbed the type-4 ucvt at -4.

Summary before this remeasure: One type-3 AND occupier at zero draw moves the second pair a0 to a1. A second simultaneous occupier is still missing. Force w59=c5 then scores 4.

#### 2026-09-24, lane p1-o071: three spellings of a second type-3, none kept

Hypothesis: one more type-3 symbol, live in blocks 9, 11 and 13 with the AND occupier, save at least 2.0 and numbered below the type-4 pair, that does not CSE into pred, does not DCE, and does not emit. The pred form was remeasured at 9 masked words, delta 0: w57 save 3.0 type-3 dtype-8 on a0 in blocks 9, 11, 13, and w59 save 2.0 type-4 dtype-6 on a1 in blocks 11, 12, 13. The itable names w59 as the ucvt of the flags load, which is the value behind the five late words.

Spelling 1 assigned an s16 gate from state->flags masked by 6, 4 and 2, then pred = (u16)gate, and tested pred. One type-3 remains and its dtype changes from 8 to 6; it still takes a0. Score 82, size delta -4. A same-width signed cast merges. It does not split.

Spelling 2 set a u16 gate to pred != 0 and branched on gate. No second type-3 appears. Score 97, size delta +20. The compare is already the branch, and materialising it emits.

Spelling 3 set a u16 gate to state->flags & 0xFFFF and pred to gate masked by 6, 4 and 2. The type-4 ucvt web disappears and pred's blocks become 9, 11, 12, 13. Score 81, size delta -4. uopt folds the redundant mask into the occupier.

Stall after these three: no second web at delta 0, and masked words did not fall. The body is reverted to the 9-word form. Do not repeat an s16 same-width cast, a compare-to-zero materialisation, or a u16 redundant 0xFFFF mask on this shape.

#### 2026-09-18, lane w17-o071: the missing type-3 is a u16 of the AND, reassigned across the call

Baseline reproduced: 728 bytes, delta 0, 9 masked and 9 raw, first +0x5C, frame 0x30, 13 static relocs. Aligner: 173 exact, 9 naming, 0 immediate, 0 structural. Census: four a0-to-v0 at +0x5C (head) and five a0-to-a2 at +0x228 (web 57). Stock and instrumented .text are identical. CDX_PROC is 0. Baseline p1:w57=c5 accepted (forced=5) and scores 4 at delta 0.

The copy-prop fold the reopen named is real. `s32 pred = state->flags; if (pred & 6)` parks a type-3 in a new block 9 (score 9) and leaves the type-4 pair in 11-13 on a0. An s32 of the AND itself draws (68). An s32-to-u16 narrowing is still 37. Capturing `state->flags & 4` as s32 is w67 save 2.0 after the pair, score 37, same as the inherited narrowing.

The form that creates the asked-for web without a draw is a function-scope `u16 pred` whose uses are the second-pair tests themselves:

    pred = state->flags & 6;
    if (pred) {
        call
        pred = state->flags & 4;
        if (pred)
        pred = state->flags & 2;
        if (pred)

That is w57 save 3.0 nocs 2 tot 6 type-3 dtype-8 bbs 9,11,13 colour a0, numbered below the type-4 pair (now w59 save 2.0 a1). Size delta 0, masked 9, first still +0x5C. Draw count is not raised: the AND is the test that already existed. This is not a raw dtype-8 flags load (those still join w32 and score 16) and not an s32-to-u16 narrowing. One such web on a0 moves the second pair to a1, as the inherited note required. On this shape `CDX_FORCE=p1:w59=c5` is accepted (forced=5) and scores 4 at delta 0: only the four head words remain.

Delete-head plus this occupier is 11 at first +0x94: the pointer is v0 with command (the ROM shape), both flag pairs sit on a1, and flags stays save 2.0 in the first-test block. Extending the existing `u16 flags` carrier into the second pair raises it to save 4.0 and evicts state from v1 (109 to 112). Keep flags at 2.0; the occupier has to be a second symbol.

The second simultaneous occupier is the remaining decision variable. A second u16 that is only assigned is DCE'd. 6 versus 6U, a u32 copy, and OR-zero of pred CSE into the first occupier. Using the second local in a bitwise-OR test or `pred && pred2` emits extra words (95 at +12, 157 at +28). L109 OR-zero / XOR-zero on a second u16 either DCE's it or changes size (-8) and raises the type-4 pair to save 3.0 so it colours first onto a0. A vertexBank u8 with OR-zero is DCE'd (9, one occupier). Inner-only u16 AND is w73 save 2.0 a1 numbered after the pair (9, no help).

Next: a second type-3 live in the same blocks as the AND occupier (9,11,13), save at least 2.0 numbered below the type-4 pair, that does not CSE into pred, does not DCE, and does not emit. Then delete head. Do not re-run raw flags-carrier reuse, s32-to-u16 narrowing, or block-11 DCE discards.

#### 2026-09-16, lane lm-a: two cycles on the argument chain, no interferer created

Baseline reproduced: 9 masked at delta zero (the head pointer's four words
on `a0` where the ROM reads `v0`, the second flag pair's five on `a0` where
the ROM reads `a2`). The mechanism this lane found on overlay 86 (a call's
unused result is a `v0` web occupying its block) does not reach this
function: the second pair's web (57, blocks 11-13) already has `v0` and
`v1` forbidden by `command` and `state`, and the ROM's `a2` needs `a0` and
`a1` held by webs nothing in the emitted code shows. Eleven cells: the
dereferenced resource index named in the call's block (9, absorbed as the
shard predicted), the pointer named (65), either hoisted above the
`flags & 6` test so the symbol spans two blocks (17 and 71: real webs,
wrong codegen), the second call's result kept in a declared local or both
calls declared `s32` with results discarded (9: `v0` was already
forbidden), and `commands` passed through a local copy (9, absorbed).
Next: the two invisible webs are not carriers of anything this body
computes; read the ROM's block 11 for a value the source must compute and
discard -- a second `object->resourceIndex` read or a `state` reload --
rather than a carrier of one it keeps.

#### 2026-09-10, lane `w8-tu`: the colour requirement stated as an ordering, from the trace

Baseline reproduces: 728 bytes, eleven relocation-masked words, zero size
delta. All eleven follow from the two flag-load webs; the two argument-setup
words that move with them are consequences, not separate residuals.

**The two webs are named and their decision records read out.** Both are p1
(callee-saved sweep) with `class=1`, `save=2.000000`, `bestcost=0`:

- web 27 -- the first two reads -- `forbidden0=0x70020000`, i.e. c1, c2, c3 and
  s0 are taken, so it takes c4;
- web 50 -- the last two reads -- `forbidden0=0x60020000`, i.e. c1, c2 and s0,
  so it takes c3.

Both want c5. So web 27 needs **one** further interferer holding c4, and web 50
needs **two**, on c3 and c4.

**The order the sweep runs in is strictly descending `save`, with ties broken
by ascending web number**, which is L100's p1 half reproduced on a third
function: 15 at 16.0 takes c1, 11 at 5.6 takes s0, 1 at 3.5 takes c2, then
25, 27 and 50 all at exactly 2.0 in web-number order. Web 25 is the invisible
`u16 flags` carrier the earlier pass named; it interferes with web 27 and
**not** with web 50, which is precisely why the two land one colour apart.

**That turns the residual into an ordering requirement with no free slot.**
A web that fixes both must interfere with 27 and 50, must be coloured before
them -- so `save` strictly above 2.0, or exactly 2.0 with a web number between
25 and 27, and no web 26 exists -- and must land on c3, pushing web 25 to c4.
Every other interferer of 27 and 50 is a **constant** web (the display-list
command words, the bank stride, the two segment bases) with `save` between 0.0
and 1.0, so all of them are coloured after both and none can help. There is no
candidate in the current web set, and the lever has to *create* one.

**Newly falsified this pass, each measured against the whole 182-instruction
target text.** All seven subsets of reusing the single `flags` carrier at the
second, third and fourth test sites; a separate carrier per site and per pair;
a chained second carrier on the first test; the carrier's type over `u8`,
`u16`, `u32`, `s32` and `s16` (`s16` is 109 words, the rest are 11 or 12); and
uopt region boundaries before the second, third and fourth tests and around the
first test's assignment. Floor 11 throughout, and every cell that moves at all
moves upward.


#### 2026-09-11, lane s1-one: the structural pair is a consequence, and three routes die for real

Baseline reproduces: 728 bytes, 182 of 182 instructions, delta 0, masked 11,
first mismatch +0x94. Aligner on a register-erased shape: 171 byte-exact, 9
register naming, **2 really different**, 0 immediate-only.

**The two structural words are the same colour, checked against the streams
rather than assumed.** They are a straight swap of two argument-setup
instructions in the same window; both objects hoist three of the four argument
definitions above the branch and push the fourth below it, the fourth being
whichever register the flags value occupies, which is an anti-dependence.
Receipt: `CDX_FORCE=p1:w27=c5` **alone** is accepted and takes masked 11 to 5,
moving the first mismatch from +0x94 to +0x228 -- every word in the 0x94..0xAC
window, both structural ones included, goes with that one colour. `p1:w50=c5`
alone gives 6; both together give **0 masked at delta 0**.

**The decision variable, restated from the cost list.** Web 27's `p1cost` shows
every available caller-saved colour at cost 0.0, with the running best already
0.0 from c5 on, so the scan keeps the incumbent and each web takes the *first*
available colour. Reaching c5 therefore needs c1 through c4 all forbidden. In
the natural order only **three** caller-saved colours are consumed before the
flag webs are decided -- the command counter takes c1, the command pointer
takes a callee-saved colour and blocks nothing in that scan, and the state
pointer takes c2 -- so web 27 needs a fourth interferer and web 50 a fourth and
a fifth.

**Route one, the existing constant webs, is now dead for real rather than dead
by a declined force.** Plain forces on them come back declined with the colour
absent from their own available set, which by L101 proves nothing. Freeing the
interferers first -- webs 25, 27 and 50 forced onto callee-saved colours, twice
with different triples, so that a0 through a3 were genuinely free -- and then
re-reading the masks shows the object pointer, the context pointer, the command
pointer, the state pointer and every constant web still carry c3 through c6
**forbidden intrinsically, because they span a call**. Only the command counter
could legally hold a1, and it holds c1 because it is coloured first; forcing it
to a1 simply shifts the whole ladder down and web 27 lands on a0, never a2.

**Route two, a new flag carrier, cannot be created, and the decision records say
why.** Flag chains coalesce: a named carrier assigned from the field, in both
directions, in two- and three-link chains, across all 36 type pairs, yields the
*identical* decision table to no carrier at all -- two webs, never a third. A
second read is CSE'd and then coalesced into one web. Carriers of values already
live in the flag-read's own block -- the command counter plus one at all twelve
command sites, its casts, a double-pointer alias, a named local for the command
word -- are absorbed entirely: byte-identical object and **no web at all** in the
records.

**Route three, L115 on the context parameter, fails structurally.** It is a
stack-homed parameter that spans calls, so it splits in all twelve placements
and never takes a caller-saved colour.

##### Two laws measured on this symbol

- **L109 is inert here.** Fifteen placements crossed with the three probe
  spellings the law names: web 25 stays at `totalsave` 2.0 and `nocs` 1 in every
  one of the 45. The probe is dropped before uopt sees it, so the one documented
  way to raise a save at zero instruction cost does not apply to this value.
- **L100 is quantified on this body, and it closes the carrier family
  arithmetically.** `totalsave` is exactly the source reference count -- replacing
  one field read with the carrier moves the state pointer's total from 14.0 to
  13.0 -- and `nocs` is the live-range component count. A statement-form flag
  carrier is therefore fixed at save 2.0 per site, and a pointer carrier at a
  command site is fixed at save 4.0, which lands *above* the state pointer's 3.5
  and evicts it from c2 (109 to 112 masked on every such variant). The window the
  closure needs, strictly between 2.0 and 3.5, is not reachable by any carrier
  shape in this body.

##### Newly covered axis: the per-TU compiler flags

48 cells over four optimisation levels, three ISA levels and four multiply and
unroll settings. The configured pair is *uniquely* 11; everything else is 13 to
289. The previous closure never varied this axis.

##### Scale, and what was therefore held fixed

About 3,100 measured candidates, every one delta 0 and none under 11: a
2,820-cell carrier lattice over carrier form, type, identity, site and
declaration order; a 130-cell two-carrier product; the 36-cell chain-type
matrix; constant and pointer carrier forms; twelve command-pointer site
reassignments; vertex-bank carrier subsets; twelve context placements; the 45
probes; and structural rewrites -- early-return inversion, declaration order, an
inner scope, explicit comparisons, `register` (inert at 11), `volatile` (155),
seven kinds of extra unused local (all 11), dropping the carrier (12) and
hoisting the read within its block (101 and 110). Held fixed by construction:
the ucode itself, since every form varies source around a fixed 182-instruction
sequence; the function signature and the four callee prototypes; and the shipped
state structure's member types, only the local carrier's type being varied.

**Reopen condition for the next lane:** a source form that makes some
**non-call-spanning** value, other than the flag reads and the command counter,
live in the flag-read basic blocks. That is the single missing ingredient, and
it could not be constructed here because every value in those blocks is either
already inside the command counter's web or is a constant whose web spans a
call.

##### Instrument notes

The colour table was decoded from this procedure's own records rather than from
L114's wording, and it is a second witness that the law was too strong: the cost
list prints a register beside every colour and runs c4 a1, c5 a2, c6 a3, c7 t0
through **c10 t3** and c12 t5, and the available mask enumerates exactly those
colours. c5 = a2 is confirmed by force-and-diff. The instrumented compiler's
text is byte-identical to the tree's for this TU with and without logging, which
is the identity gate.

The warm candidate loop assembles the *target* into a shared path under the
build directory, so two harnesses cannot run concurrently even with separate
scratch directories. Two runs collided; everything above was re-measured
serially, each batch ending with a baseline recheck at 11.


#### 2026-09-11, lane p2-close: four interferers, not one, and five more routes closed

Baseline reproduces: 728 bytes, 182 of 182 instructions, delta 0, masked 11,
first mismatch +0x94. Aligner: 171 byte-exact, 9 register naming, 2 really
different, 0 immediate-only. `frame_census` agrees on every slot, so nothing
here is a home-set problem.

**The colour table and all three decision records were re-derived from this
procedure's own instrumented log rather than quoted**, and they reproduce: c1
v0, c2 v1, c3 a0, c4 a1, c5 a2, c6 a3, c7 t0 through c13 t6, c14 s0. Web 25 is
save 2.0, nocs 1, totalsave 2 and takes c3; web 27 is save 2.0, nocs 2,
totalsave 4 and takes c4; web 50 is save 2.0, nocs 2, totalsave 4 and takes c3.

**What the earlier passes did not record is that the object carries FOUR flag
loads, not two.** The shipped code holds all four in a2. This candidate holds
loads one and two in a1 and loads three and four in a0. Because webs 27 and 50
each have two live-range components and the components are separated by calls,
no single web can interfere with both. So the lever has to supply an interferer
on c3 and on c4 for each component separately -- four webs to create, not one --
and each has to be a web that does not span a call, because a call-spanning web
is forbidden a0 through a3 intrinsically.

**Newly falsified this pass, each measured against the whole 182-instruction
target text.** A `mode` carrier holding the 0x17 argument, reassigned before
each flag test and passed to each call, in all three declaration positions;
separate `mode1`/`mode2` carriers; a carrier for the trailing zero argument;
the same carrier at only the first or only the second test; the carrier's type
over `u8`, `u16`, `s16`, `u32`, `s32`. All of these are byte-identical to the
baseline at the two declaration positions that keep the 0x30 frame, and 17 or
18 at the position that does not, because the constant is propagated to the
call and no web is ever created. Reusing the `context` parameter the same way
is likewise byte-inert at 11. Carriers for the shared `0x05710080` command word
and for the two asset addresses, hoisted above tests three and four where no
call separates them, are all 11 for the same reason.

**Also falsified: the ugen free-list lever.** The same day, a peephole-removable
no-op mask closed `overlay101DrawTransformed` by making ugen allocate one more
expression temporary at zero byte cost. The full 31-cell lattice of `& 0xFFFF`
masks over the four flag reads here runs 30 to 109 and every cell is worse.
That is the expected result and it is worth writing down: this residual is a p1
colouring decision, and the expression ring is a different allocator.

**Reopen condition, restated.** Unchanged in kind but four times larger in
degree: two non-call-spanning webs live at the first test and two more live at
tests three and four, each pair coloured before webs 25, 27 and 50 -- so save
strictly above 2.0, or exactly 2.0 with a lower web number. Every value in
those blocks is still either inside the command counter's web, a constant that
uopt propagates, or a web that spans a call.

#### 2026-09-11, lane p6-mid: re-measured and unchanged, with the consequence chain confirmed off the objects

Baseline reproduces: 728 bytes, 182 of 182 instructions, delta 0, 11 masked
words, first mismatch +0x94. Aligner: 171 byte-exact, 9 register naming, 0
immediate-only, 2 really different. Bank-corrected `register_census` reads the
nine as two source registers collapsing onto one of the target's, five sites and
four sites, 100% coherent, one window, no cycle -- which is the signature of
four short webs that should all have taken the same colour and instead took two,
and it agrees with the p2-close reading of four flag loads in two webs.

The two structural words were re-checked against both streams and are the same
consequence the earlier passes described: the call's four argument definitions
are identical on both sides, three scheduled above the branch and one below, and
which one goes below is decided by the register the flag value occupies. Fixing
the colour fixes all eleven; nothing here is a second residual.

No new lever. The reopen condition stands as p2-close wrote it, and this pass
adds one thing to it from a sibling: the same day, `func_overlay_027`'s residual
turned out to be one forced colour away from closing, and the reason a carrier
could not supply it there is the same coalescing this function recorded -- a
named local assigned from a value uopt already has folds into the existing web
rather than becoming a new one. Treat "create a web" as the hard part on both.

#### 2026-09-12, lane `p10-near`: the web gets built, 11 -> 9, and two closing claims fall

Baseline reproduced at 11 masked, 728 bytes, delta 0, first mismatch +0x94.
The instrumented toolchain's `.text` is byte-identical to the tree's for this
TU (identity gate), and with `CDX_PROC` set the two forces reproduce exactly:
`p1:w27=c5` alone is accepted (`forced=5`) and gives 5, `p1:w50=c5` alone gives
6, both give **0 masked at delta 0**.

**Two of the closure's load-bearing claims are false.**

- *"L109 is inert here."* It is inert on web 25, which is what the earlier pass
  measured. It is not inert on this function: an or-with-zero, an and-with-minus-one and an
  xor-with-zero on a named local all raise that local's `totalsave` by 2 and its
  `save` from 2.0 to 4.0, at size delta 0 and 728 bytes.
- *"The (2.0, 3.5) save window is unreachable by any carrier shape in this
  body."* It is reachable two ways, both at delta 0. A three-reference local
  lands at exactly 3.0 -- or-ing the address expression with the carrier
  multiplied by zero adds the third reference without
  emitting anything -- and **an L97 region across a four-reference local's
  advance splits its web over two basic blocks, so `nocs` is 2 and the save is
  2.0 with a web number below the flags carrier's** (L147). The second of those
  is the adopted edit.

**Adopted, 11 -> 9 at delta 0.** The first command site gets its own pointer
local with `if (1) { *commands = head + 1; }` across the advance. The ladder
then reads: command 14.67 v0, commands 5.60 s0, state 3.50 v1, **head 2.00 a0**,
flags 2.00 a1, **first flag pair 2.00 a2**, second flag pair 2.00 a0. The first
pair is on the shipped register. Eight of the 108 cells in the lattice (which
site takes the fresh local, three region wrappers on each of its three
statements, two statement orders) reach 9 and all eight wrap the fresh local at
the FIRST site; every second-site cell is 11 or worse.

**What the nine are, and the sharpened requirement.** Aligner: 173 byte-exact,
9 register naming, 0 immediate-only, 0 really different. Four are the first
site's pointer, now a0 where the ROM has v0 -- the price of using a pointer to
carry the a0 web -- and five are the second pair of flag loads, still a0. So
**the ROM's block holds a non-call-spanning a0 web that emits no instruction**,
exactly like the `u16 flags` carrier whose zero-extend the peephole deletes, and
the open question is what that second invisible web is. A pointer cannot be it:
its four references materialise and cost four words whichever site carries it.

Also measured this pass, all against the full 182-instruction target:

- **the L142 arity lever is inert on this function.** Webs 25, 27 and 50 carry
  no intrinsic call denial -- their `forbidden` masks are v0/v1/s0 only, all
  from interference with already-coloured webs -- so they do not span a call and
  call arity has no axis on them. The webs that do span calls (the two pointer
  parameters and the state pointer) are already on v1 and s0.
- **splitting `command` into per-region locals is byte-identical.** All 63
  subsets of size 1 to 3 over the seven command-emitting regions: every one 11,
  delta 0. uopt coalesces them and no new web appears.
- **the `func_overlay_086` fold-defeat idiom does not transfer.** Six advance
  spellings applied to all twelve sites -- `(*commands)++`, `*commands += 1`,
  `++*commands`, an embedded assignment, `&command[1]`, a byte-cast advance --
  are either byte-identical (three) or 132 (three).
- a three-reference `base` local for the vertex-bank address moves the SECOND
  flag pair from a0 to a1 at delta 0, the first source form ever recorded to
  move it, but the region-2 codegen changes with it (53 words), and pairing it
  with the block-B edit is 13.

#### 2026-09-12, lane p23-lastmile2: exhaustive landscape and cursor-law transfer

Fresh configured baseline: 728 bytes, zero size delta, aligned buckets 173
byte-exact, 9 naming, 0 immediate, 0 structural; no unmatched offsets.
The first four naming rows remain in the initial pointer region at +0x5C;
the later five remain in the flag-test region. Named Ucode maps procedure 0
of 1, and stock/instrumented full-TU text is byte-identical.

The exhaustive same-kind landscape samples all 10 coloured webs in 82 probes:
79 accepted forces and three declined cells on web 90, each missing the required
unique decision/final-colour pair. Thirty-four accepted cells preserve size.
The COMPLETE improving winners list is `p1:w57=c5`, giving 4 at zero size
delta and removing five rows in the +0x200 window. Its singleton lattice floor
is 4; the initial four-row pointer region remains. No two-force packing is
available from this winners list, and no global floor is inferred for arbitrary
regressing combinations.

The newly exact strip renderer motivated a specific transfer: preserve a
read-modify-write through allocation, then let stock IDO remove it. This pass
compiles 98 source cells against the full configured TU:

- 30 self-reassignment cells cross the fifteen nonempty flag-site subsets with
  the retained head-pointer shape and the earlier shared-command shape. None
  improves the retained 9; several lose four bytes.
- 36 separate-carrier cells cross one/two OR-zero links, three integer widths,
  first/later/all flag sites and both pointer shapes. Best is 11 at the correct
  size. A malformed scratch declaration batch failed compilation and is not
  included in this count or treated as negative evidence.
- Eight OR-zero/XOR-zero head-pointer placements are byte-flat at 9.
- 24 zero-valued guard-operand cells try to retain an initialized extra carrier
  through multiplication or masking by zero. They remain at 11 or worse.

The retained source body is unchanged. ADR 0018 stop: the complete landscape
localizes the only improving colour handle, while the distinct lifetime forms
above neither improve source nor create the required invisible interferer.
The named source question is still a non-call-spanning invisible value ahead
of the later flags web, together with preserving the first command pointer's
original value-register use. Copying the successful strip idiom alone does not
supply that value here. No new matching bytes are claimed.

Evidence remains ignored under build/p23/func_overlay_071_F0000870_18CA390.
Commands: residual_map.py, allocator_trace_receipt.py --map-only,
web_footprint.py --every-colour, configured full-TU source lattices and
finalize_plateau.py. The header now reports matching words consistently:
173/182 exact, not the old ambiguous 9/182 notation.

#### 2026-09-13, lane k1: authenticated draw-census follow-up

Fresh configured stock compilation reproduces 728 target bytes,
size delta 0, 9 raw and
9 masked differences, first +0x5c.
Candidate frame is 0x30; the procedure-0 census records
65 draws and 270 emission records. Stock and traced full-TU text
compare identically. Static relocation counts are 13 candidate and
11 extracted target, with 6 identical offset/type/symbol tuples.
These are static measurements; overlay runtime identity proof remains separate.

The aligned baseline has 173 exact and nine naming rows, with no immediate,
structural or gap rows. The existing complete landscape supplies a five-row
late colour handle but leaves the four-row initial pointer cost. All recorded
carrier chains, identity reassignments, region wrappers and dead guard operands
were reviewed before proposing an edit. They do not create the required
non-call-spanning invisible interferer while retaining the original pointer
register. No new source mechanism was identified beyond those receipts.
ADR 0018 early evidence stop applies with zero new source attempts; no colour
sweep or known-flat probe was repeated. This is baseline confirmation, not a
new exhaustive source claim.

The original guarded body and assembly fallback are retained. Sources, stock
and traced objects, frame and scalar measurements, draw profiles and aligned
maps remain ignored under build/k1/func_overlay_071_F0000870_18CA390.
Commands: lane_status.py, configured stock compilation, draw_census.py,
residual_map.py --object/--against where compared, finalize_plateau.py and
tools/gates.sh. No executable bytes are newly credited.

#### 2026-09-16, lane nx-c: block 11 discards nothing; the head pointer is command's web in the ROM

Baseline reproduced at 9 masked, delta zero, first +0x5C. One cycle, eight
cells, nothing adopted. Instrumented `.text` identical to the tree's.

The ladder (re-derived): `command` 14.67 v0 (blocks 5,7,8,11-16), `commands`
5.6 s0, `state` 3.5 v1, `head` 2.0 a0 (blocks 3-5, forbidden v0/v1/s0 --
v0 by `command` sharing block 5), `flags` 2.0 a1 (block 5), first pair 2.0
a2 (5,8,9), second pair (web 57) 2.0 a0 (11,12,13, forbidden v0/v1/s0 only).
The ROM's head pointer is v0, so in the ROM the head and `command` are one
web and the a0 interferer at block 5 is something else invisible.

The named step -- a value the ROM's block 11 computes and discards -- found
nothing: block 11's instruction stream is identical on both sides but for
the two `lhu` registers. A second `u16`, `u8` or `s16` carrier for the
second pair, the single carrier reassigned after the call, two carriers,
all 82 at -4; `head = *commands` re-read after the call 13; the resource
index in a named local 9 (absorbed).

**Next:** two invisible non-call-spanning webs on a0 and a1 in blocks 11-13
and one on a0 in block 5, all coloured before the flag webs. Look for
narrow-typed values whose only instruction is the load that defines them
(the `u16 flags` pattern), such as `state->active` or `state->vertexBank`
read into a `u8` -- but note the ROM keeps those loads on t-registers.

#### 2026-09-17, lane w4-o071: named levers falsified; dtype split and numbering found

Baseline reproduced: 728 bytes, delta 0, 9 masked and 9 raw, first +0x5C,
frame 0x30, 13 static relocs. Aligner: 173 exact, 9 naming, 0 immediate,
0 structural. Census: four a0-to-v0 at +0x5C (the `head` local) and five
a0-to-a2 at +0x228 (web 57). Stock and instrumented `.text` are identical
(identity gate). `CDX_PROC` from procindex is 0. `p1:w57=c5` is accepted
with `forced=5` and scores 4 at delta 0; first mismatch stays +0x5C.

Current ladder, re-derived: command w23 save 14.67 v0 bbs 5,7,8,11,12,14-16;
commands w11 5.60 s0; state w1 3.50 v1 bbs 1-12; head w15 2.00 a0 bbs 3-5;
flags w30 2.00 a1 type-3 dtype-8 bbs 5; first pair w32 2.00 a2 type-4 dtype-8
bbs 5,8,9; second pair w57 2.00 a0 type-4 dtype-6 bbs 11-13. Head and w57
share a0 because they do not interfere.

Named next 2, L145/L160 delete the `u16 flags` carrier: 16 with `head`
kept, 12 with `head` deleted. No missing web appears; dropping the carrier
loses the a1 occupancy that holds the first pair on a2.

Named next 1, compute-and-discard in block 11: bare `*object->resourceIndex`
and `object->state` statements, L109 or/xor/and-minus-one, comma operands,
and `* 0` folds into existing constants are all 9 (DCE or CSE).
`state = object->state` after the call is 68. A named `s32 resource` used
only as `resource * 0` in `w1` is 63 at delta 0: it does create w61 save
1.0 a1 bbs 11, but that web colours after w57 and the fold still spends a
ugen draw. `u8 bank` used in the second vertex expressions is 144 at -20.

New identity: w32 is dtype 8 and w57 is dtype 6, which is why the same
`state->flags` spelling is two webs today. A `u16 later = state->flags`
used in an inner test is dtype 8, so it joins w32, raises that web to save
2.67 nocs 3 tot 8, and colours it before `head` onto a0 (16 at delta 0).
Offset, member-address, and `(u32)` spellings still join w32. An `s32 later`
coalesces into w57 (9). The one form that creates a new block-11 symbol
without touching w32 is a narrowing copy:

    tmp = state->flags; later = tmp; if (later & 4)

That is w67 save 2.0 type-3 dtype-6 bbs 11, colour a1, score 37 at delta 0.
It colours after w57 (67 greater than 57, both save 2.0) so w57 still takes
a0, and the narrowing spends a ugen draw. OR-zero reassignment, an early
`later = 0`, and a flags copy in block 5 leave it at 37. Two such locals
used for both inner tests combine the two lhus (41 at -8); the ROM has two.

`head` deleted is 11 at first +0x94, the p10-near inverse: the four a0-vs-v0
words are that local, and the ROM's first pointer is v0 with `command`.

Next: a type-3 web live in bbs 11-13, numbered below 57 or save strictly
above 2.0, that is not a dtype-8 flags load (those join w32) and not an
s32-to-u16 narrowing (that draws). One such web on a0 only moves w57 to a1;
two are required for a2. Deleting `head` remains the ROM pointer shape and
must be solved together with those occupiers, not by keeping the 9-word
local optimum.
<!-- plateau-handoff:func_overlay_071_F0000870_18CA390:end -->
