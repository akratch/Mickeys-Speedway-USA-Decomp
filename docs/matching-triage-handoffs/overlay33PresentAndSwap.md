<!-- plateau-handoff:overlay33PresentAndSwap:start -->
### `overlay33PresentAndSwap` plateau handoff

- source: `src/overlays/o033/overlay33PresentAndSwap.c`
- score: 0/39 words
- frame: 0x18
- relocations: 18
- first mismatch: none
- summary: ROM-exact and promoted. Non-volatile `g ^= 1` then `refs[g]` keeps the buffer-index address as one coloured web through both regions and forwards the xor into the subscript. The build-list call is a same-overlay SYMBOL reloc through the Reloc placeholder.

<!-- plateau-handoff:overlay33PresentAndSwap:end -->
