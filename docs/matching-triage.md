# Matching triage: which tool each walled function needs

A living routing log for the hard tail. Every remaining unmatched function is
walled by a *specific* mechanism, and each mechanism has a *right tool*.
Sending a function to the wrong tool burns effort (a permuter cannot fix a
structural gap; hand analysis cannot out-search the register allocator). This
document records, per function, the wall class and the tool — so work is routed,
not guessed.

Scores are the workbench `diagnose` differing-word count at the time of the
note; they are point-in-time, not tracked numbers. Recompute before acting.

## Wall classes and the tool each needs

| Class | Mechanism | Right tool | Why |
|---|---|---|---|
| **P — permuter-tractable** | register / colour / schedule *allocation permutation* (opcode+frame already exact; only *which* register/slot differs) | **decomp-permuter** (`tools/permute.sh`, capped `-j4` niced) | The winning source spelling exists but is not derivable by reasoning; brute-force search over spellings finds it. |
| **P! — permuter-stuck** | same as P, but the 20-min cap ends above 0 | **permuter, more budget** (re-seed, longer cap, or a manual scratch variant) then escalate to F if it plateaus | Close but the search space is large; more iterations or a better base often closes it. |
| **I — import-blocked** | the candidate does not cleanly isolate (`Syntax error in base.c` on import) or does not compile under `-DNON_MATCHING` in-TU | **fix the candidate first** (manual scratch / `--preserve-macros`), or route to **F** (trace-lever needs no permuter import) | The permuter never actually searches these until the base compiles standalone. |
| **F — Fable + workbench** | a *specific* compiler decision with a known *source lever* (ugen ring phantom-pop, float-invariant load form, declaration-order stack home, call-arg colour affinity) | **Fable subagent + Tier-2 traces** (`docs/breakthrough-campaign.md`, workbench `diagnose`/ring traces) | The lever is identifiable from a trace and applied by a targeted edit; the permuter would not stumble onto it efficiently. |
| **S — structural RE** | genuinely wrong shape: missing/extra code, wrong control flow or types | **Fable/Opus reverse-engineering + reference donors** (`mips_to_c`, JFG/BK/etc. with PROVENANCE) | Not an allocation problem at all; needs real decompilation. |
| **W — hard wall** | pure list-scheduler slot-fill (a *ready* zero-dependency op lands in a fixed latency bubble) or interference-forbidden colour with no lever | **permuter (last resort); else record as toolchain-heuristic wall** | No C source steers the decision; only a rescheduler/permuter might, and some are genuinely unreachable through the project toolchain as source. |

## Per-function log (resident register-only / schedule-only tail)

Updated as the permuter campaign and analysis proceed.

> **2026-08-27 root-cause correction.** The first sweep classed 8/12 targets as
> **I (import-blocked)** on a `Syntax error in base.c`. That was **not** the
> candidates — it was a bug in `tools/permute.sh`: its `-mips2` flag correction
> ran `gmake -n <obj>`, which prints nothing when the object is already built,
> so the scratch silently stayed at the importer's default `-mips1` and the
> search explored the wrong instruction space. Fixed (touch the source first;
> warn instead of failing silently). Those functions are really **P-class** and
> are being re-searched at the correct ISA. Lesson: a permuter that "finds
> nothing instantly" is almost always a setup/flag fault, not a hard function.

| Function | TU | Class | Evidence / note | Status |
|---|---|---|---|---|
| `func_8002C94C` | saves | **P** | globalcolor s5/s6 inversion; permuter base 40 → **0** | ✅ matched (permuter) |
| `func_8002B040` | matrix | **S/F bounded plateau** | configured C is 35 words against an exact 34-word frameless/no-padding target, with 1/34 positional words and first mismatch `+0x0`; target and candidate own zero relocations. Preflight authenticates four runtime callers across Overlays 69, 88, and 94. The excess is the second `f32` formal's spill/reload; the target moves all three incoming GPR bit patterns directly to FP registers | all 119 flags and ten bounded type/punning, matrix-shape, temporary, K&R, register, operand-order, and uopt-O3 forms are nonexact. `-O2 -mips1` improves only to 2/34 while retaining the extra word. Preserve fallback; next lever is an authentic no-stack-home argument lowering or evidence that the DKR/JFG hand-assembly family explains the target |
| `func_8003A2C8` | menu | **P!/W bounded plateau** | fresh configured full-TU V0 is 24/32 raw/normalized words, first `+0x0`, exact `0x80` frameless/no-padding shape, and six exact relocations. Thirteen O2/MIPS-II rows tie in a repeated 119-configuration lattice. A fidelity-gated proc-20 trace separates the full-mode, narrowed-byte, state-address, and comparison-carrier webs. The stale 27/32 ranking used inert constant-expression aids and is diagnostic only. ORT 606 and three callers remain authenticated; linked equality is fallback-only | all six declaration orders, direct-global access, and narrow-first split assignment are flat; full-first association loses one instruction/relocation identity and a shared byte carrier regresses to 13/32. No strict natural gain, combination, or batch; resume only with a source-authentic three-web formation/coalescing mechanism |
| `func_80058250` | vehicle_sounds | **S/W bounded plateau** | configured full-TU V0 is 26 versus 22 frameless words, 3/22 positional matches, first `+0x0`, exact target range `0x58E50..0x58EA8` with no padding, and 24 versus 20 static relocations. All twelve target BSS identities are authenticated. The concrete excess is one redundant HI16 per 16-byte slot: target code uses separate handle and racer-object bases, then reuses the racer-object high half for the preceding float's distinct LO16. `func_80004FE0+0x54C` is the sole caller; no exact or structurally credible permitted donor was found; linked equality is fallback-only | the historical 119 flags, array/four-slot/volatile/split-tail families, plus fresh target-order, TU-local-scalar, comma, racer-base-pointer, and volatile-tail probes are nonexact. Preserve named-global V0 and fallback. Resume only with a source-authentic declaration/TU model that emits two direct bases plus three separately named LO16s per slot, or evidence that the initializer was handwritten; do not repeat these forms or flags |
| `func_80038878` | menu | **S/P!** | retained p6 is diagnostic: target 85 versus candidate 86 instructions, frame `0x18`, 66/85 positional target-word differences, first `+0x14`. Empty address condition was an artificial allocation lever and is removed; clean V0 is uncompiled. Historical route exhaustion is unretained prose | clean configured V0 and current oracle before a bounded initial-global/later-loop campaign |
| `func_8003E8D8` | particles | **P!/W bounded plateau** | configured full-TU C reaches 139/140 raw/normalized words, first `+0x38`, exact `0x230` size/frame `0x38`, no padding, and all ten tuples. Target declaration homes removed all eight call-live differences; one zero-count branch target remains. Sole caller/no-inbound status is authenticated, while linked equality proves fallback only | 124-build route exhausted: V0, 119 flags, three natural declaration forms, and one trace-selected CFG form; one fidelity-clean allocator trace and one gain-gated bounded batch found no further legal gain. Next lever is a source-faithful first-loop CFG spelling that retains the redundant pool-base reset |
| `overlay11UpdateMenu` | o11 standalone | **F** (was **P!/W**) | the two reversed spill stores at `+0x138/+0x140` need `as1`'s `$sp` disambiguation fact, which `uopt` emits only at an indexed reference over a named base; routing the offset through a pointer variable (`handle = D_1CC - 1`, indexed by the counter) supplies it at displacement zero, and trimming four folded locals keeps the `0x48` frame with every target stack home. The induction pointer's preheader initialiser is still emitted last, but `as1` reschedules that block and breaks the tie on physical source line, so putting the counter init in the `for` header closes 301/301 words and 102/102 relocation identities | ✅ matched (disambiguation fact + scheduler line tie) |
| `overlay2SplitRegion` | o2 standalone | **F** (was **W**) | clean V0 reproduced 65/72 words with the exact `0x120` extent and `0x30` frame. All 119 flag identities were nonexact. A fidelity-clean IDO 5.3 scheduler trace exposed a physical-line tie between the function and loop headers; folding the token-equivalent headers onto one line closes 72/72 words and all nine runtime relocation tuples/identities | ✅ matched (scheduler line-tag recovery) |
| `overlay99RenderSegments` | o99 standalone | **P!** | configured V0 had the exact 142-word extent and `0xA8` frame but ten relocation-masked differences. Explicit signed flag loads remove two opcode mismatches; direct display-list post-increment repairs a four-register web; placing the address-taken width/height outputs immediately after the two coordinate deltas restores their stack homes and closes 142/142 linked words. The runtime table plus linked ROM authenticates all 15 effective identities and addends | ✅ matched (declaration-home and lifetime recovery) |
| `func_overlay_071_F0000278_18C9D98` | o71 standalone | **P!** | configured V0 had the exact 332-word extent, `0xE0` frame, and register/schedule shape but 28 stack-offset words differed. Moving the existing point array between its two cursor declarations first restored its retail home; rotating the semantic state, index, point, distance, and vector declarations then restored every call-crossing home without padding or volatile scaffolding. Untouched configured IDO closes 332/332 words, and all six runtime relocation offsets, types, effective identities, linked owner/module, and full ROM are exact | ✅ matched (declaration-order stack-home recovery) |
| `overlay100InitializeMotion` | o100 standalone | **P!** | configured V0 was 94/133 words with the exact `0x78` frame and all 13 relocation offsets/types. One 119-row flag lattice was flat. A fidelity-clean allocator trace identified the pointer/count carrier web; reusing the velocity cursor for the frame phase, moving the existing countdown load before the frame-pointer store, and restoring natural declaration/assignment order close 133/133 words. The runtime table authenticates all 13 identities and zero addends; the object emits the same 13 offsets/types, and linked-ROM equality proves the five friendly call proxies resolve to the required resident identities | ✅ matched (trace-guided carrier/lifetime recovery) |
| `overlay100UpdateMotion` | o100 standalone | **S** | authenticated V0 was 95/96 words with an exact `0x18` frame and one missing countdown-carrier move. Restoring the natural `if (count--)` / `while (count--)` spelling closes 96/96 words. The runtime table proves all three tuples and identities: local `overlay100RemoveEntry` at `+0x278` and the `+0x984` data address pair with addend 4 | ✅ matched (post-decrement carrier recovery) |
| `overlay16ApplyGradient` | o16 complete TU | **F** (was **P!/W**) | Tier A: 145/145 configured words, frame `0x20`, six exact relocation offsets/types/identities, and owned `+0x1E0..+0x424`; following padding is excluded. Store/readback phase formation takes 60 to 55 differences; separating the active-cursor increment takes 55 to 9; input-channel definition order closes the remaining nine. The linked owner and full ROM are byte-identical | matched; the old phase-chain and channel-order plateau did not cover store/readback plus separate cursor advancement |
| `overlay15MoveStars` | o15 mixed TU | **F/W bounded plateau** | configured full-TU C is 58 versus the exact 54-word / `0xD8` target, frame `0x40`, first `+0x30`, with 33 ownership-aware differing words. The target owns 21 runtime records: one resident call plus 20 LOCAL records for the star movement, pointer, count, and nine contiguous bounds; C emits 25 because it rematerializes four bound high halves. ORT 1663 exports `+0x428`; no direct resident/cross-overlay relocation inbound or target padding exists. JFG `starfieldMove` is the same size and role but remains tier-B context only; linked equality proves fallback only | prior 119-row lattice is nonexact. Four natural BSS forms this pass—pair structs, external pair arrays, file-static scalars, and pointer-derived adjacent fields—produced no strict size gain and worsened schedule or relocation alignment, so the lattice was not expanded or repeated. Preserve baseline/fallback; next lever is a natural direct-load spelling that shares one HI16 across each adjacent bound pair without forming a general-register base |
| `overlay34CreateRecord` | o34 standalone | **S/F/W bounded plateau** | retained configured C is exact-sized at 125 words, 93/125 raw and 95/125 relocation-aware positional words, first `+0x0`, with frame `0x28` versus target `0x30`. Only two loop-shape opcodes remain before seven downstream register webs. All 12 runtime records agree by offset, type, identity, and addend: resident calls at `+0x70/+0x194`, local records/pointers/count at module `+0x900/+0x904/+0x908`, and active-count pairs at `+0x0/+0x4` and `+0x1AC/+0x1B0` for module `+0x90C`. ORT 1609 and calls from Overlay 8 `+0x28C0` and Overlay 89 `+0x734` authenticate ownership and ABI; `+0x2C8` starts the next owner with no padding | all 119 flag identities were attempted; 53 compiled and seven canonical O2/MIPS-II rows tie. One fidelity-clean proc-0 trace records 12 p1 integer decisions but no source or stack-home attribution. Ten natural state, loop, statement-order, declaration, and scope forms found two strict word gains; the retained ordering also closes the complete relocation surface. Preserve fallback. Resume only with new evidence for the distinct loop-current carrier plus eight-byte frame/home mechanism; do not repeat this lattice, trace, forms, or launch a generic batch without an independent legal gain |
| `overlay29DrawGroups` | o29 standalone | **S/W bounded plateau** | configured C is exact-sized at 129 words with the exact `0x58` frame, 81/129 positional words, and first mismatch `+0x50`; 15 opcode and 45 register sites remain. The corrected C call identities now prove all three runtime `R_MIPS_26` records by offset, type, identity, and zero addend: `camPushModelMtx` at `+0x138`, `camRestoreModelMtx` at `+0x18C`, and `func_80034920` at `+0x1A8`. ORT 1320, two resident callers, owned `+0x14C8..+0x16CC`, and separate four-byte following padding are authenticated. The nearest permitted donor-oracle result is only 0.093 and supplies no credible body; linked equality remains fallback-only | all 119 flag configurations are nonexact and the configured `-O2 -mips2` recipe ties for best. Ten historical CFG/offset forms and a bounded permuter, plus five fresh group-selection and negative-offset/address forms, are nonexact; every fresh form regressed to 41–43 positional matches or changed extent. Preserve the configured body and fallback. Resume only with new source-authentic statement/expression ordering evidence for the pre-loop constant setup and node-address formation; do not repeat the lattice, these forms, or an ungated permutation batch |
| `overlay25InitializeEffect` | o25 mixed TU | **F** (was **S/W**) | preflight authenticates the exported `+0x000..+0x17C` owner, no padding, resident caller, and nine runtime records. Reading the owner back through `state->owner` restores the exact saved-register web; the established `-Wab,-r4300_mul` mode inserts the sole missing FP hazard nop. The resulting object is 95/95 instruction words with all nine offsets, types, and identities exact | ✅ matched (natural owner web plus R4300 multiply-hazard scheduling); linked owner, complete overlay, and full ROM exact |
| `overlay48InitializeState` | o48 standalone | **S/W bounded plateau** | retained configured C is 53 versus 57 target words, frame `0x18`, 10/57 positional words, first `+0x0`. Exact ownership is `+0x60..+0x144` with no padding; ORT 1423 and sole resident caller `func_80038BC4+0x188` are authenticated. The target has 25 fully decoded runtime records; C emits 24, with 12 offset/type alignments, eight resolved candidate identities, and one aligned identity. Exact-donor scan is negative; JFG `levelFreeAll` is an unrelated 0.0769 skeleton result | 14 configured builds covered baseline plus twelve natural direct-array, pointer-loop, hand-unrolled, aggregate/scalar-BSS, declaration, and qualifier forms before restoring the best source. Exact `0x60` BSS geometry was reachable but worsened text. Loop forms peel the first record or add address producers; hand unrolling removes the target's retained index/base setup. Preserve baseline/fallback; resume only with source-authentic declaration/TU evidence for that unrolled setup, not flags or generic permutation |
| `overlay59Advance` | o59 standalone | **S/F/W bounded plateau** | configured C is exact-sized at 240/262 raw and 243/262 relocation-aware positional words, first `+0x4`, frame `0x58`, with every integer, FP-pool, and temporary lane exact. Target and C each have 15 relocations; 13 offsets/types and 12 identities align, with the factor pair displaced by the prologue order. The retained `+0x76C` switch table is owned/rebound. ORT 1435 and `func_80038E1C+0x2C4/+0x320` authenticate the one-word ABI and padding-free `+0x36C..+0x784` owner. No credible donor applies | all 119 flags and ten source-faithful assignment, constant-carrier, scope, declaration, and physical-line forms are nonexact. A fidelity-clean `as1 -R` trace isolates the ready-chain choice: stock schedules the factor save/load chain before retail's constant-register chain. Preserve fallback; resume only with new source-authentic scheduler-line/dependency evidence, not the exhausted forms, flags, or post-compile reordering |
| `overlay5InitializeAudio` | o5 mixed TU | **F/P!/W** | retained pre-HEAD/current-body, current-layout full-TU and selective C agree at diagnostic 211/233 words/frame `0x98` with all 71 sites. An unauthenticated config tail is removed and the four collided ORTs are split, so clean V0 is uncompiled. ORT 1558 has one Overlay 18 inbound; linked equality is fallback-only | identity-correct V0, exactly 119 flags, one allocator trace, three independent natural forms and one improving-only combination; cap 123 stock candidates plus trace |
| `overlay99RenderSortedEntries` | o99 standalone | **F/P!** | retained pre-current-body configured C is diagnostic 216/233 raw and 218/233 normalized, frame `0x148`, all ten records; no isolated object survives. Four volatile gaps hid a real `CameraSprite + MtxF` layout, now restored in clean uncompiled source. Sole local caller/unexported status are authenticated; linked equality is fallback-only | clean V0, 119 flags, four natural local placements, one trace-led lifetime, improving-only combination; cap 125 builds plus trace |
| `overlay68RebuildSecondaryEntry` | o68 standalone | **F/P! bounded plateau** | policy-clean configured C emits 120 versus 122 target words, frame `0x38` versus `0x40`, and matches 17/122 positional words from first mismatch `+0x0`; all 19 runtime roles remain represented, but structural drift shifts sites after `+0x64`. All 119 flag configurations were nonexact; the closest debug family still differs in 77 words and has the wrong extent. One codegen-faithful allocator trace supported lexical stack-home scoping, which gained one word; sentinel order was byte-identical, so the combination was flat and no batch was authorized. ORT 1163 has sole inbound `func_80004FE0+0x4C8`; linked equality remains fallback-only | retain the policy-clean scoped source and fallback; revisit only with a source-authentic shared-data-base and frame-allocation mechanism, not duplicate reloads, split aliases, or generic permutation |
| `func_overlay_045_F0000764_188CBBC` | o45 standalone | **F** (was bounded plateau) | the compiler emits all 637 instruction words with the exact `0x88` frame. Six existing literal-pool relocations are rebound to an ABS `+0x24` carrier without editing any instruction field, and the duplicate 16-byte pool is removed only after its SHA-256 agrees. All 24 runtime offsets/types and effective identities are exact (14 static, 10 runtime-table plus linked-ROM); the linked owner, complete overlay, and full ROM are exact | ✅ matched (retained-pool metadata rebind; no instruction edits) |
| `func_overlay_079_F0001290_18CE230` | o79 standalone | **P!/W bounded plateau** | retained configured C is exact-sized at 123 words with frame `0x48`, 119/123 raw/runtime-normalized words, first `+0xC8`, and four register-only sites; the integer temp lane is exact. Fresh ordinary preflight proves all 15 offsets/types but fails closed at 1/15 candidate identities; a prior identity-canonical diagnostic proves all 15 identities/addends without supplying promotion machinery. ORT 1297 and resident relocation 139 authenticate sole inbound `func_8000AEEC+0x43C`; owned `+0x1290..+0x147C` has no target padding, while four following alignment bytes are non-owned. Linked equality proves fallback only | all 119 flags remain nonexact and were not repeated. Explicitly preserving the `u8` width as `(value & 0xFF) | 4` consumes one backend temp while emitting no instruction, closing the prior one-slot FIFO phase. Opaque-pointer and zero-offset typed state-slot forms were byte-flat; the sole executable residual is the four-site linked-state v1/v0 pool web. No credible donor applies. Preserve fallback; resume only with a newly proved natural linked-state coalescing form, not the lattice, temp-FIFO/type forms, or a generic batch |
| `overlay97InitScale` | o97 standalone | **F/P! reproof** | historical configured C under `-O2 -mips2 -32 -Wab,-r4300_mul` was measured at 143/144 raw/normalized words, frameless, first/only `+0xD0`, with zero relocations; no candidate/target object, report, or linked-C artifact survives. Current unchanged body is uncompiled. Historical C used values cursor `a1+0x3E`; target uses equivalent `a3+2`. Owned `+0x508..+0x748` has no padding. The isolated three-word row omitted `-Wab,-r4300_mul`; ORT 1194 and resident record 36 authenticate sole inbound `func_8000AA38+0x7C`; linked equality is fallback-only | configured V0; assign bounds first, then independently try `(u8 *)bounds+2` and `&bounds->bounds[1]`; normal cap three builds, conditional narrow-scope fourth only after V0 drift plus strict shape-preserving gain; no generic permutation |
| `func_8002CF6C` | saves | **F/W bounded plateau** | policy-clean configured V0 is 85/88 instructions, 10/88 positional words, frame `0x30`; all 11 identities remain but offsets drift. All 119 flags are nonexact. One proc-26 allocator trace identifies the `s1` buffer and `a2/a3` saved-state webs. Lexical saved-state scope restores frame `0x48` and improves to 11/88, first `+0x8`; a saved-header form regresses to 83 instructions. ORT 505 and sole caller remain authenticated; linked equality is fallback-only | park until a natural source model explains the target's stack-homed buffer without synthetic padding, volatile scaffolding, false arguments, or dead carriers; do not repeat flags or launch a generic batch without a structural gain |
| `func_80020D8C` | models | **P!/W bounded plateau** | configured full-TU C remains 31/48 raw/normalized words, first `+0x38`, exact `0xC0` size/frame `0x8`, zero relocations. The four-case carrier matrix reproduces the old retained 12-word residual, not a new gain. Only combined product staging and constant-index reuse adds an initial reservation; product-only changes downstream allocation with baseline initial membership/coloring, while constant-only equals baseline. All stock/off/on section, symbol and relocation fidelity checks pass. The earlier UGEN-only/complete-FIFO attribution is withdrawn; target ownership remains unknown. ORT 374 and nine calls across four functions remain authenticated; linked equality is fallback-only | park unchanged source and retained controls; per-symbol shard binds the evidence. Reopen only with a new source-faithful mechanism for remaining register fields; do not repeat ablations, exhausted flags, or unchanged-seed searches |
| `func_8000DDE4` | track | **P** (was **P!/W**) | clean V0 reproduced 94/118 words, frame `0x28`, first `+0x24`, and 24 register-field sites. The reference scan found no credible donor; all 119 flag combinations were nonexact. One codegen-faithful proc-21 allocator trace preserved the complete TU text and relocation table and exposed zero-cost colour ties. The first permitted natural form, merging the disjoint `recordIndex`/`passCount` lifetimes, closes 118/118 words with exact call tuples, linked range, runtime identity, and ROM | ✅ matched (natural lifetime merge) |
| `debug_text_width` | diprint | **F/P! bounded plateau** | fresh configured full-TU C under `-O2 -mips2 -32 -Wab,-r4300_mul` is 59/66 raw/normalized words, frame `0x138`, first `+0x38`, with five exact tuples and no target padding. All 119 flag rows were attempted; thirteen O2/MIPS-II rows tie and none is exact. One trace/workbench pass isolates a five-site current-byte `v1`/`v0` web plus two structural newline-lowering words. A separate classification scalar regressed to 57/66 through a stack home; an explicit newline-tail jump was flat, so no combination or batch was permitted. ORT 862 has no caller after exhaustive scans; linked equality is fallback-only | preserve the 59/66 body and assembly fallback; revisit only with a new source-level web-splitting mechanism |
| `func_8001A154` | lights | **P** (was mis-called **W**) | permuter cracked it: drop the redundant `& 0xFFFFU` blue mask (phantom ring pop) + fold `flare.scaledSize`. The 'no source lever' verdict was a FALSE FLOOR. | ✅ matched (permuter) |
| `mainUpdateZBCheck` | main | **P** (was **I** via -mips1 flag bug) | permuter cracked it once flags fixed: compare `(u16*)row+columns` directly, no cached pixel. | ✅ matched (permuter) |
| `func_80028FCC` | main | **F/W** | fresh configured full-TU C with canonical `-Wo,-Olimit,100` is 17/27 words, exact `0x6C` boundary/frame `0x18`, first `+0x1C`, with all three calls exact at `+0x14/+0x30/+0x4C`. The shared-result/shared-epilogue probe regressed to 25 words and moved the second and third calls, so it did not unlock the retained lattice or trace. ORT 663 has no authenticated caller; linked equality remains fallback-only | park until a new source-faithful Boolean-lowering mechanism explains the target's normalized temporary before each branch and shared epilogue; do not repeat flags, trace, or generic permutation without a strict structural gain |
| `func_overlay_009_F0000540_1866BB8` | o9 mixed TU | **P!/W bounded plateau** | unchanged configured full-TU C under canonical `-O2 -mips2 -32 -Wab,-r4300_mul` is exact-sized at 129 words with frame `0x58`, 120/129 raw and 121/129 runtime-linked, first `+0x4C`. All ten runtime records agree by offset/type/identity: three LOCAL pairs through module base `+0x18B0`, ORT 707/279 calls to `mathDiffAngle`/`func_8002A8C0`, and one LOCAL pair through BSS base `+0x1930`. The eight linked sites retain the four-way saved-FPR color cycle; the assembly fallback remains canonical. | park until a new saved-FPR coalescing mechanism exists; do not repeat flags, trace, source variants, or permutation |
| `overlay7DispatchModes` | o7 tail TU | **P!** | the prior 129/131 plateau was a one-web temp-FIFO phase difference. Expressing the unsigned flags sign bit as a left shift followed by a right shift preserves the target branch schedule while restoring its carrier allocation, closing 131/131 words with frame `0x20`. A metadata-only pair binds the retained seven-entry switch table at stored addend `+4`; the compiler duplicate is digest-checked and externalized. All 23 text plus seven table runtime identities, the 524-byte owner, complete overlay, and full ROM are exact | ✅ matched (natural sign-bit extraction plus retained-table binding) |
| `overlay7DispatchSelection` | o7 tail TU | **P!/W bounded plateau** | current configured full-TU C is 55/60 raw and 58/60 runtime-normalized, exact `0xF0` size/frame `0x20`, first substantive `+0x4`; only one unnamed flags/selection-offset temp web differs at `+0x4/+0x10`. All 13 emitted offsets, types, and identities agree with runtime metadata, including the repaired `+0xA4/+0xD8` calls. The 119-row lattice is nonexact; proc-2 trace shows named webs already clean, and two scalar/scope forms regress to 39/60. ORT 1510 has fourteen calls; linked equality is fallback-only | park until a new UGEN temp-coalescing mechanism exists; do not repeat flags, explicit locals, or generic permutation |
| `overlay7CommitSelection` | o7 tail TU | **P!/W bounded plateau** | current configured full-TU C is 67/72 raw and 69/72 runtime-normalized, exact `0x120` size/frame `0x30`, first substantive `+0xBC`; one post-`mathRnd` u16 conversion carrier differs at `+0xBC/+0xC4/+0xD4`. Runtime metadata proves all 17 emitted offsets, types, and identities. The 119-row lattice is nonexact; proc-3 trace places the candidate carrier in uopt pool slot 20 versus target temp-FIFO slot 9, and explicit-assignment/scoped-u16 forms regress to 68/72 normalized. ORT 1345 and seventeen resident/cross-overlay calls are authenticated; linked equality is fallback-only | park until a new source route can move the conversion from the uopt pool into the UGEN temp FIFO; do not repeat flags, explicit carriers, or generic permutation |
| `func_8005A7A0` | models_5B300 | **F/W bounded plateau** | fresh unchanged configured full-TU V0 is exact-sized at 106 words, 96/106 raw and 99/106 relocation-normalized, with frame `0x50` versus target `0x38`; real-address linkage is 94/106 raw and 99/106 normalized. All ten relocation identities agree and nine tuples are exact; the second loader call shifts `+0xBC` → `+0xC0` | park pending a new frame/carrier allocation mechanism; do not repeat flags, trace, source variants, or batch |
| `overlay59PrepareEntry` | o59 standalone | **P! bounded plateau** | policy-clean full-TU C is exact-sized at 62 words with frame `0x28`, 52/62 raw and 53/62 relocation-normalized words, first substantive `+0x54`. The complete 119-configuration lattice was nonexact with canonical `-O2 -mips2` tied for best. One UOPT trace isolated the descriptor/call-argument carrier while the temporary lane stayed exact; the trace-supported natural carrier-coalescing form was byte-identical to baseline. Runtime authenticates all six roles, no owned padding/export, and four local callers; linked equality remains fallback-only | preserve the split-carrier basin; next pass needs a different source-faithful call-argument carrier/coalescing lever, not more flags or a generic batch |
| `overlay19ClassifyEdge` | o19 standalone | **P!/W bounded plateau** | configured full-TU V0 is exact-sized at 120 words / `0x1E0`, frameless, 110/120 raw/normalized words, first `+0x138`, with zero relocations and no owned padding. Six sites are one pool carrier; four are two final y/z load-order pairs. The full 119-row lattice is nonexact with thirteen O2/MIPS-II ties. One fidelity-clean uopt/ugen trace confirms separate zero-cost pool and temporary-lane choices. Unexported; sole inbound is `overlay19FindAdjacent+0xD8`; linked equality is fallback-only | 122 deterministic builds exhausted: lattice plus three natural forms. Direct DKR-style fields regress to 108/120, staged y/z values to 98/120, and reversed equality order is flat at 110/120 while trading schedule for register sites. No strict gain, combination, or generic batch. 2026-09-10 (lane c6-close): the carrier is a uopt CSE temporary; removing the reversed block's duplicate field read moves it to the target's `t3` and closes all six words, but flips `candidateStart`/`candidateEndIndex` between `v0` and `v1` for 24. 4,190 forms leave that colour order untouched; the named blocker is now that colour order, not the carrier |
| `func_overlay_022_F0000D30_1878E38` | o22 standalone | **P!/W bounded plateau** | identity-correct configured C is exact-sized at 91 words / `0x16C`, frame `0x28`, 48/91 raw/runtime-normalized words, first `+0x10`; all 43 sites are register-only and all 12 runtime tuples are exact. Runtime metadata replaces the old catch-all callee with `partUpdateTriggers`, two `func_80002FE0` calls, and `func_80006EA0`. The owner ends at `+0xE9C`; `+0xE9C..+0xEA0` is separate padding. ORT 1465 authenticates seven resident inbounds, with local table-2 JUMPs at `+0x274/+0x5C4`; linked equality is fallback-only | 119 flags and three natural forms exhaust 122 stock builds. A fidelity-clean proc-0 trace finds 11 high-confidence integer webs; the count/found lane chooses `v1/a2` against target `a2/a3`. Two scoped forms are flat and the initialized-`while` form regresses, so no combination or batch. Resume only with a new source-faithful web-formation mechanism |
| `overlay1ResolvePathPoint` | o1 end TU | **P!** | retained evidence is exact-sized at 152 words with ten raw/eight normalized schedule/register sites and 19/22 runtime tuples; source had an invented clear alias and a nine-argument declaration for the observed seven-argument ABI | ABI/identity-correct V0, then one bounded annotated-target permutation if reproduced |
| `overlay14ResetMode` | o14 standalone | **F/W reproof** | historical pre-current-alias C was measured at 45/56 raw and 49/56 normalized words, frame `0x30`; no candidate object/report survives and “later text exact” applies only after normalization. Runtime owns 18 records; historical C reportedly carried 17, fallback exposes ten, and current `+0x54` identity repair is uncompiled, so current score/shape/tuples are unknown. Five local inbounds/no export; retained structural oracle is negative, best 0.167 | identity V0 among 119 configurations; pre-alias control replaces a later form only on regression; one trace, two natural `s3`/base-lifetime forms, strict-gain combination; cap 122 deterministic builds plus trace, one 20-minute/2,000-candidate batch only after legal gain |
| `overlay1FindBestRecord` | o1 tail TU | **P! bounded plateau** | current configured full-TU V0 is exact-sized and frameless at 18/30 words, first `+0x4`; all twelve sites are one pool-level `a1/a3` bijection and the temp lane is exact. All 119 configurations were attempted; seven O2/MIPS-II rows tie V0 and none is exact. A fidelity-clean proc-38 trace records ten caller-saved decisions, and the selectedType/value declaration swap, block-local value, and record/value declaration-position forms are all byte-identical to V0. Runtime records 884..887 authenticate both pairs and distinct D_220/D_1D88 identities; the function is unexported with two local callers. Linked equality remains fallback-only, and the caller/callee private record types remain unshared because promotion was not reached | park until a new natural allocator-order mechanism exists; do not repeat flags, trace, the three flat forms, post-compile edits, or a generic batch. Exact promotion still requires a shared natural record type and distinct metadata-only D_220/D_1D88 rebinds |
| `overlay1FindType5ByKey` | o1 start TU | **P!/W bounded plateau** | fresh configured full-TU V0 is exact-sized at 39 words/frame `0x30`, 22/39 positional words, first substantive `+0x1C`, with 17 register-only sites split across loop/cursor and key-comparison FIFOs. All 119 flag identities were attempted; 53 compiled/extracted and thirteen O2/MIPS-II-or-III rows tie V0. A fidelity-clean proc-11 trace records six uopt integer-pool decisions; mixed-TU ugen temporary decisions are not attributable. Runtime proves the sole `+0x14` tuple's offset/type/identity to resident `func_8000572C`; ORT 1485 and sole inbound `overlay1TransitionState+0x40` are authenticated; linked equality remains fallback-only | explicit array/cursor splitting is byte-identical to V0; a scoped record-key carrier remains 17 positional words and introduces two opcode/alignment discrepancies. No strict gain, combination, or batch is eligible. Preserve V0/fallback and resume only with a new source-faithful ugen FIFO/web-formation mechanism |
| `rcpClearZBuffer` | rcpFast3d | **F/W bounded plateau** | fresh configured full-TU V0 is exact-sized and frameless at 103/107 raw/normalized words, first `+0x74`; all four fields are one ugen temp-phase residual, target `t8/t9` versus candidate `t7/t8` at slots 9-10. Both HI16/LO16 tuples at `+0x00/+0x04` are exact in offset/type/identity to `D_800D2FAC`. All 119 flag identities are nonexact; seven O2/MIPS-II variants tie V0. A fidelity-clean receipt maps proc 6 and records 29 integer plus one FP uopt decision; the mixed-TU producer withholds automatic ugen attribution, while source-line stamps locate the candidate `t7/t8` pops. ORT 765 and four callers are authenticated; linked equality is fallback-only | 121 deterministic builds plus one trace are exhausted: pointer-truth and staged-`alignedX1` forms are byte-identical to V0. No strict gain authorized a combination or batch. Preserve V0/fallback and resume only with a new natural preceding phantom-pop or web-existence mechanism; do not repeat this route |
| `overlay62Update` | o62 standalone | **P!/W bounded plateau** | fresh configured full-TU C has exact 294-word extent/frame `0x88`, 287/294 relocation-masked words (286/294 literal object words), and first mismatch `+0x44`. All 119 flags are nonexact with seven O2/MIPS-II ties. A fidelity-clean proc-0 trace leaves all 85 temp assignments exact and first diverges at pool slot 6. The candidate emits all 71 runtime records at exact offsets/types; 18 friendly identities resolve and 53 proxies remain unmapped. Owned `+0xD4..+0x56C` has no padding; ORT 1444 and sole inbound `func_80038E1C+0x3A4` are authenticated; linked equality is fallback-only | 121 deterministic builds plus trace exhausted: explicit shared-negation lifetime regresses to 297 words/frame `0x90`; removing `register` is byte-identical. No strict gain, so no batch. Resume only with a new natural pool/web-formation spelling plus complete proxy-identity mapping; do not repeat flags or these forms |
| `overlay43FilterImage` | o43 standalone | **F/P! bounded plateau** | policy-clean configured full-TU C is exact-sized at 43 words, frameless, 8/43 raw/normalized words, first `+0x4`, and owns zero relocations. All 119 flag recipes were nonexact; canonical `-O2 -mips2` ties for best. One UOPT trace identified four global webs, while a diagnostic forced-color oracle reached 19/43 and proved global color is only part of the residual. The retained staged sum emits every pixel offset in target order; direct-store, mask-carrier, register-qualifier, association, and staged-sum forms were bounded and nonexact. Owned `+0x1378..+0x1424` excludes separate three-word padding; two local JUMPs are the only inbounds and linked equality is fallback-only. Exact-donor scan is negative; the 0.574 JFG relative is assembly-only and supplies no donor C | preserve the source and fallback; resume only with a new source-faithful temporary-FIFO/web-coalescing mechanism. Do not repeat the lattice, exhausted forms, forced-color diagnostic, or a generic batch |
| `func_80047CD8` | fx | **Closed: ROM-exact C** | Tier A: 936 owned executable bytes / 234 words, frame `0x68`, four exact `func_800349A4` relocation identities and no padding. A faithful macro-block import enabled a bounded sweep; the retained unsigned RGB XOR-zero expression and logical-negation test reproduce ordinary full-TU output and linked ROM without changed flags or instruction editing | retired from the plateau queue; historical lattice and trace attempts remain in Git, and `docs/resident.md` records the exact closure |
| `func_800498FC` | fx | **P!/W** (one CSE web too many) | the residual was a four-web integer bijection at 91/100 words with an exact `0x190` extent, `0x30` frame and all five relocation tuples. It was one pool web, not a colouring search: spelling either packed flag test as `(u8)(flags & 0x80)` re-derives the stored value from the argument, IDO CSEs it with the store, and the extra long-lived web pushes `flags` out of `v0` and rotates `t3/t4/t5`. Reading both `u8` fields back (`record->value1E`, `record->value1F`) leaves the mask values as expression temps: 9 -> 6 -> 0 words. The 25-minute permuter run over the argument form found nothing in ~7,000 iterations, because no permutation of that source removes the web | ✅ matched (read the stored field, not the argument) |
| `func_8004BA8C` | font | **F/P! reproof** | historical policy-defective C is diagnostic 38/46 raw/normalized words/frame `0x30`, first `+0x30`, with all nine tuples exact; it used artificial `stackPad`/empty-condition aids. Clean C removes them but is uncompiled, so every current metric is unknown. ORT 880 has three resident and two overlay callers; linked equality is fallback-only | configured V0; historical control only on material drift; then 119 configurations, one trace, block-scoped glyph index and one trace-selected lifetime form, combining only independent strict gains. Cap 122 deterministic builds plus trace, 123 only for control, no generic batch |
| `func_overlay_014_F0001830_1871108` | o14 standalone | **F/W** | retained 196/201 normalized words and 21-record/exact-table shape, but all eight calls used one false callee identity; five case-7 load/increment schedule sites remain | ABI-correct V0, then one target-shaped positive-index call rung; park if flat |
| `levelGetCounts` | level | **reproof** | historically measured pre-endpoint full-TU C is 255/259 raw and 256/259 normalized, frame `0x58`, with all 37 offsets/types but only 35 identities; no attributable object survives. Raw first `+0x50` is the wrong endpoint pair; normalized first `+0x13C` and `+0x148/+0x154` are one carrier. Inferior isolated import was 251/259 raw and 252/259 normalized. Current endpoint-correct source has unknown score/frame/identities. ORT 518 has sole Overlay 18 inbound; no padding; linked equality is fallback-only | current full-TU V0; historical control only on regression; if the three-site carrier reproduces, exactly 119 configurations, one trace, and one natural block-scoped carrier. Strict gain alone permits ≤2,000 candidates/20 minutes; otherwise park |
| `func_8002B524` | memory | **F/P!** | retained pre-cleanup C is diagnostic 102/116 raw/normalized words, frame `0x58`, first `+0xE0`, with all 12 tuples exact. Artificial volatile pad is removed; clean V0 is uncompiled and linked equality is fallback-only. ORT 547 has nine calls plus one stored pointer | clean V0, 119 flags, two carrier/guard forms and strict-gain combination, one trace-selected lifetime; cap 123 stock builds plus one trace, no generic batch |
| `func_overlay_073_F0000000_18CAAC0` | o73 standalone | **P bounded plateau** | fresh configured full-TU and isolated canonical V0 are text-identical at 90/100 raw and 92/100 runtime-normalized words, exact 100-word extent/frame `0x8`, first `+0x40`. All 119 configurations are nonexact; thirteen O2/MIPS-II rows tie. A fidelity-clean trace finds equal-cost `a2/a3` endpoints; the never-promotable forced-`a3` result clears all normalized sites and cascades the paired web. The sole natural early-initialization form regresses to 55/100 and shifts the first LOCAL pair, so the scoped form and batch were not authorized. All eight runtime LOCAL identities compare exact (offset/type and identity 8/8, all resolved); ORT 1248 has sole inbound `func_8000AA38+0x42C`. Owned `+0x0..+0x190` / ROM `0x18CAAC0..0x18CAC50` has no target padding; linked equality remains fallback-only | park at the exhausted 122-stock-build cap. Resume only with a natural source topology that moves the equal-cost web without shifting code or relocations; do not repeat the lattice, trace, forced diagnostic, or early-initialization form, and do not run a scoped form or batch without strict natural gain |
| `func_8005A948` | models_5B300 | **F/W bounded plateau** | configured V0 is exact-sized/frame `0x38`, 62/94 raw and relocation-normalized (68/94 workbench-normalized), first `+0x40`, with all 13 tuples exact. All 119 flags were nonexact; seven O2/MIPS-II rows tie V0. A fidelity-clean proc-4 trace shows the colored lane exact and temporary FIFO one pop late at slot 4. The sole natural boolean form improves to 85/94 raw/relocation-normalized and 91/94 workbench-normalized; three `t7`/`t8` sites and six call-home sites four bytes below target remain. Sole caller/no export/runtime/overlay/pointer inbound or padding; linked equality is fallback | cap exhausted: retain the natural form and fallback; resume only with a source-faithful first-loop FIFO plus call-home mechanism, not more flags or a generic batch |
| `func_80055B24` | anim | **P** | typed `void` callback alias removes the generic trap placeholder's phantom return web; metadata-only rebind restores the exact call identity | ✅ matched (ABI reconstruction) |
| `func_8002C69C` | saves | **P** | indexed `writer->cursor[0]` stores generate the walking pointer; on that shape the bit advance belongs before the mask store. 28/28 words, frameless, zero relocations | ✅ matched (L145/L160 generated cursor plus L146 advance-before-store) |
| `func_80041CE4` | particles | **P** | the residual was never a pool phase: the hand-written point cursor, the shared display-list carrier, and the named `vertexAddress` were all absent from the source. Indexing `line->points[j]` from the `for` counter, giving each display-list command its own carrier, inlining the vertex address, and dropping the redundant pre-guard point-count read leave 153/153 words, frame `0x80`, and all nine relocations exact | ✅ matched (10-local declaration census; `displayList` is the fifth declared local) |
| `func_8001D880` | charControl | **P!/W bounded plateau** | fresh configured full-TU C is exact-sized and frameless at 7/36 positional words (29 differ), first `+0x4`, with no padding or relocations. Thirteen natural builds plus four fidelity-clean trace diagnostics found no strict gain; the prior 119-flag sweep remains exhausted. Proc 21 isolates the second-base FP web occupying the target constant color and cascading the final load/multiply schedule. ORT 405 has two overlay inbounds, but their friendly caller ABIs conflict with the observed `f32 (f32, f32, f32 *, f32)` target ABI; linked equality remains fallback-only | preserve V0 and fallback. Resume only with a source-authentic base pool-to-temp/web-formation mechanism; do not repeat flags, tested natural forms, trace, or generic permutation absent a strict natural gain |
| `func_8002BB40` | memory | **P!/W** | canonical C is exact for all 72 frameless words and all eight relocation tuples. Dead-carrier reuse gives the slot count `slotIsTaken`'s incoming web; natural 20-byte indexing fixes the temporary chain; reusing dead `slotSize` for the remainder link closes the final five register sites | ✅ matched (allocator coalescing) |
| `func_800320F0` (`runlinkEnsureJumpIsValid` donor alias) | runlink | **P** | retained canonical object has 101 words/frame `0x20` and 21 relocations; linked ROM `0x32CF0..0x32E84` is byte-identical | ✅ matched; remove from living unmatched queues |
| `levelInit` | level | **F/P!/W** | retained pre-cleanup configured full-TU/isolated C is diagnostic 394/516 raw words/frame `0x80`, first `+0x238` in the resource-list selector carrier, with all 110 static tuples and no padding. The unused volatile pad is removed and seven runtime trap identities adopt authenticated ABIs; clean score/frame are unknown. ORT 526 has one caller/no inbound and eight runtime calls inside | pad-free V0, typed-alias V1, one donor-supported `s16 tune` lifetime; if nonexact, one trace and at most one trace-supported natural form, no generic search absent a legal gain |

### Already resolved by the Fable/workbench (F) route — pattern reference
`func_8003A520` (ugen FIFO phantom-pop), `func_800508D4` (float-invariant load
form), `func_80055970` (declaration-order stack home), `func_80038750` (call-arg
colour affinity). These are the signature of class **F** — see
`docs/breakthrough-campaign.md`.

### Structural / donor (S) route — reference
SDK/libultra donors (`__osSpSetPc`, `__osContRamRead/Write`, `__osBlockSum`,
`__osPfsReleasePages`, `osPfsDeleteFile`, `joyInit`) matched via BK/JFG donors.
`func_8004B1DC` (font, 509w) is an open S target (largely-wrong body, multi-hour RE).

## Routing rule of thumb
1. Frame-exact + small word-diff + register/colour/schedule → **P** (permuter first — cheapest, and it has cracked cases hand analysis called unlevered).
2. Permuter import fails → **I**: fix the base or hand it to **F**.
3. Permuter improves then plateaus → **P!** then **F** if a trace shows a real lever.
4. Wrong shape / missing code → **S**.
5. Proven no-lever scheduler slot-fill → **W** (permuter last resort; else record the wall).

## Structural survey findings (2026-08-27, Opus S-class agent)

Most "structural" resident functions turned out to be **P (permuter)** once the
shape was understood — another false-floor cluster. Newly routed:

| Function | TU | Class | Note |
|---|---|---|---|
| `func_8004B1DC` | font | **P** | Frame-exact (0x80). The display-list `dList++` idiom batches base+displacement stores; target materialises a fresh pointer per command. m2c's distinct-next-pointer form defeats the batching but spills to a 9th saved reg — net-neutral, so keep the frame-exact base and **permute the rolling-pointer register allocation**. Micro-facts recorded in-source: window-0 y2 is standalone `D_800D64F2`; font ptr is `window->font`. |
| `func_80046AA8` | diCpu | **P** | −3 insns loop-guard, but residual dominated by v0/v1/a0 register swaps in the packed-glyph blit. |
| `func_80012574` | track | **P! reproof** (was a false **P**-matched) | historical pre-cleanup configured full-TU diagnostic C was measured at 50/57 raw/normalized words, first `+0x50`, frame `0x48`, with `sqrtf` at `+0xA4`; no candidate object/hash survives. Dead stack-home aids are removed, so clean V0's score/shape/tuple are unknown. The isolated row omitted required `-Wab,-r4300_mul`. Five direct calls and ORT 308 are authenticated, with no runtime/overlay/pointer inbound. Rejected invented-guard 55/57 and normalized-stack zero remain non-creditable | clean V0; historical control only on structural regression; 119 configurations including V0, one trace, at most two trace-supported forms and strict-gain combination; cap 122 deterministic builds plus trace, 123 only for control, one 20-minute/2,000-candidate batch only after legal gain |
| `func_8000D018` | track | **B — merged-TU blocker** | ✅ matched. Fixed surgically: a typed `#pragma weak trackCamPosTrap = TrapDanglingJump` alias + `objcopy --redefine-sym trackCamPosTrap=TrapDanglingJump` in the track.c.o rule, so the camera-position call passes (f32,f32,f32) single-precision without touching the shared TrapDanglingJump placeholder or its other call sites. |

### New wall class: **B — merged-TU symbol/placeholder blocker**
`func_8000D018` double-promotes `f32` call args to double because
`TrapDanglingJump` (real function at **0x800333A0**, size 0x1E0) is declared as an
unprototyped placeholder `s32 TrapDanglingJump()` and that ONE placeholder symbol
is reused across incompatible call sites (0/1/2/3 args, ints/pointers/floats) in
already-matched plain-C functions. IDO rejects any incompatible declaration
TU-wide, so a correct `(f32,f32,f32)` prototype cannot be added in isolation.
**Fix (project-level, also improves codebase health):** disassemble and identify
the real distinct function(s) hiding behind the `TrapDanglingJump` placeholder,
give each its real name + prototype, and repoint the call sites. This is a
structural-debt cleanup that unblocks func_8000D018 (and likely other
double-promotion residuals) — a good Fable/Opus task, tracked here.

**Update (2026-08-27): func_8000D018 resolved without the full web cleanup.**
The surgical per-call-site fix already used elsewhere (anim.c `animResetTrap`,
weather.c `rainInitTrap`) applies: declare a typed weak alias
(`#pragma weak trackCamPosTrap = TrapDanglingJump`, `extern void *trackCamPosTrap(f32, f32, f32)`),
call it at the one dangling site, and canonicalize its undefined symbol back to
TrapDanglingJump with `objcopy --redefine-sym` in the track.c.o build rule. The
alias gets the correct prototype in isolation, so IDO passes the three f32 args
single-precision (no `cvt.d.s`/`sdc1`), while every other TrapDanglingJump call
site and the shared placeholder itself are untouched. ROM stays byte-identical.
The broader web cleanup (naming the real distinct callees) remains optional
future health work, not a matching blocker.

## Permuter scratch fidelity (2026-08-27, tooling)

A permuter score of 0 is only trustworthy if the scratch object it searches is
**bit-identical to the real per-TU object** *and* the scorer counts every byte a
`gmake verify` counts. Two false-ceiling causes were found and fixed in
`tools/permute.sh`; a third is documented but not yet fixed. Rule of thumb:
**a permuter 0 that has not passed `gmake verify` is a hypothesis, not a match.**

1. **Post-compile `objcopy` not replicated (fixed).** Some TUs apply an
   `objcopy --redefine-sym A=B` after `cc` via the Makefile's per-file
   `POSTPROCESS` (e.g. `src/main/track.c`: `trackCamPosTrap=TrapDanglingJump`).
   The importer's scratch ran `cc` only. `permute.sh` now recovers that step
   from the same `gmake -n <obj>` dry-run it already uses for flags and appends
   it to the scratch `compile.sh` after `cc`, retargeted to `$OUTPUT`.
   Digest-guarded ELF surgery (`add_elf_relocations.py`, `trim_elf_section.py`)
   is deliberately *not* replicated — it is tied to the matched bytes and would
   abort on a permuted object — such TUs get a warning instead.

2. **Scorer normalizes stack offsets (fixed — this was the actual `func_80012574`
   ceiling).** decomp-permuter's scorer defaults to `stack_differences=False`
   (`src/scorer.py`/`src/objdump.py`), which strips every `sp`-relative offset
   before diffing. A candidate whose only residual is a spill/local at the wrong
   `sp` offset (`sw v1,0x18(sp)` vs `0x1C(sp)`) then scores **0** while its bytes
   still differ and `gmake verify` fails. Proven on `func_80012574`: its
   "winning" candidate was 4 stack-home words off the ROM yet scored 0.
   `permute.sh` now always passes `--stack-diffs`; with it the base is 61 and a
   15-min run plateaus at 16 (honest), instead of a false 0. *This is the fix
   that actually mattered for `func_80012574` — the objcopy replication does not
   touch that function's bytes, since it never references `trackCamPosTrap`.*

3. **Injected gfx-macro expansion (documented, NOT fixed).** The importer keeps
   the TU's `g[DS]P*`/`_SHIFTL`/`gDma*` macros as `#pragma _permuter latedefine`
   entries (`tools/permuter_settings.toml` `[preserve_macros]`) and restores them
   at candidate-compile time (`ast_util.py process_pragmas`). The definitions come
   from the TU's real headers via `cpp -dD -fdirectives-only`, so the *bodies* are
   correct. The historical `func_80041CE4` scratch frame discrepancy is now
   resolved: retained isolated and configured full-TU C are byte-identical at
   frame `0x80`. Other gfx-heavy imports still require this byte-diff preflight,
   because a preserved macro can expand through an unpreserved sub-macro or
   pycparser can alter macro-call argument structure. Treat any gfx-heavy
   permuter zero as unverified until its scratch baseline equals the configured
   full-TU object. (particles.c also carries a
   `trim_elf_section .rodata 0x28` POSTPROCESS, which is correctly *skipped* by
   fix #1 with a warning — its scratch is not bit-identical for that reason too.)

## Epoch 14 sweep findings (2026-08-28, running log)

The fidelity-fixed batch runner (`docs/permute-batch.md` "Scratch fidelity")
is sweeping the whole NON_MATCHING queue closest-first. Entries here are
routing facts from that sweep; the sweep's `summary.json` is the measurement.

| Function | TU | Class | Note | Status |
|---|---|---|---|---|
| `func_8004D40C` | font | **P** (was recorded as pass-owned "lifetime" plateau, 2 words) | permuter score 205 → 0 in 255 s once the scratch used the TU's real flags; another false floor in the "proven" column | ✅ matched (permuter) |
| `overlay1CloneRecord` | o001 | **I** | import.py's scratch redeclares the function with a conflicting return type against the TU's own prototype (`Overlay1PathState *`); scratch never compiles, base score None | needs a prototype-aware import (workbench backlog #7 preflight would have flagged it) |
| `overlay1AssignRecordIndex` | o001 tail TU | **F!/W** | retained pre-HEAD diagnostic C is 34/44 literal and 36/44 runtime-normalized words/frame `0x38`; five stack-home and three conditional-store sites remain. Its dummy volatile, false fourth argument, private identity, and literal write are removed. Clean V0 now uses the three-argument GetRomlistInfo alias and symbolic `D_1D8C` for all seven records, but is uncompiled. ORT 1219 and sole resident inbound are authenticated; historical lattice, trace, probes, and search are unretained | clean identity V0, run and retain 119 flags, one trace, at most four natural forms and two strict-gain combinations; batch only after a natural gain |
| `overlay40FadeRecords` | o040 | **P! bounded plateau** | the compiled output-origin spelling is the best stock object at 98/101 raw/normalized words, first `+0xC`, frame `0x8`; only `+0xC/+0x10/+0x24` differ, as one `v0`/`v1` globalcolor outcome, while the temporary-register lane and all ten runtime roles are exact in shape. One allocator trace found the pool swap and the complete 119-flag lattice produced no exact object; canonical `-O2 -mips2` ties for best. ORT 1314 and sole runtime trap inbound `func_8000D978+0x130` are authenticated; owned `+0x690..+0x824` excludes separate padding and linked equality remains fallback. | preserve this source/object basin; next pass needs a bounded source-level globalcolor/coalescing hypothesis, not more flags or a generic batch without a natural lever |
| `overlay40UpdateEntries` | o040 | **P! bounded plateau** | natural direct-shift C closes the prior one-word excess and matches 44/46 positional words in the exact `0xB8`, frameless owner; only the loop-count initialization and `gOverlay40Objects` LO16 completion are reversed at `+0xC/+0x10`. Three of four runtime tuples align by offset/type/identity; the fourth is displaced by that swap. All 119 ownership-aware cached flag rows are nonexact, with the 13 supported O2/MIPS-II rows tied. ORT 1451 and the sole resident inbound at `func_80051364+0x78` authenticate the owner and amount-only input ABI; JFG's nearest 0.2857 skeleton is assembly-only and contextual. | preserve the direct-shift source and fallback; next pass needs one source-authentic entry-scheduling/dependency lever, not more flags or allocator permutation |
| `overlay74Update` | o074 standalone | **P! bounded plateau** | policy-clean configured C is exact-sized at 100 words and emits all eight runtime tuples, but differs in 39 relocation-masked words from `+0x0`; its `0x70` frame has 16 more non-save bytes than the target's `0x60`, shifting the result array and cascading through the integer pool/temp assignments. All 119 lattice rows were attempted; 53 supported rows compiled, canonical `-O2 -mips2 -32` is best, and reversing the outer OR is byte-identical. Sole inbound `func_8000AEEC+0x34C` and owned `+0xB8..+0x248` excluding separate padding are authenticated; linked equality is fallback-only | preserve the policy-clean source and fallback; resume only with a newly proved frame-allocation/coalescing mechanism, not more flags or operand-order trials |
| `overlay34InitStorage` | o034 | **F→W reproof** | historical pre-type-repair full-TU/isolated C was 45/50 raw and 46/50 runtime-normalized with exact 50-word/`0x30`-frame shape and all eight identities; target used `sp+0x1C` and C `sp+0x18` at `+0x24/+0x30/+0x78/+0x7C`. Pointer aggregate and allocator tag types are now repaired, so clean V0 and every current metric are unknown. The `0xC8` range excludes eight alignment bytes and has no target padding. ORT 1352 has only `levelInit+0x3E0/+0x3F0`; linked equality is fallback-only | clean V0; exactly 119 configurations including V0, one stack-home trace, then only if supported one natural declaration/scope/lifetime form using semantic locals. Cap 120 deterministic builds plus trace, no dummy volatile or generic permutation; park if nonexact |
| `overlay34RemoveRecord` | o034 | **F/W** | retained 32/44 raw C is diagnostic: it used a false second argument, shadow liveness, a three-step pointer increment, an integer resource field, and the initializer's synthetic name for the release call. Clean pointer-typed, one-argument source is staged and uncompiled. Owned `+0x2C8..+0x378` has no padding. Seven runtime roles authenticate three active-count pairs, one pointer pair, and the `+0x6C` SYMBOL call through a zero carrier to resident ORT 140 (`func_800347A0`); fallback static C exposes only five records and linked equality is fallback-only | clean V0, exactly 119 flags including V0, one allocator trace, and at most one trace-supported natural declaration/lifetime form; cap 120 builds plus trace, stop on boundary or identity drift and retain fallback if nonexact |
| `overlay34UpdateRecords` | o034 | **F/P! source-shape closure** | moving the existing countdown initialization to the start of the live-record branch gives untouched configured IDO exactly 77 words / `0x134` and the retail `0x30` frame. All seven runtime relocations agree by offset, type, and effective identity; the remove call uses the overlay's metadata-only zero-field proxy for the shipped local `+0x2C8` identity. The linked owner, complete overlay, and full ROM are exact. JFG's assembly-only `src/overlays/o10/overlay_10.c:sparkUpdate` is a concrete structural counterpart and insertion point, but its additional behavior and field layout must be reconstructed independently. | ✅ matched (conditional-block lifetime restores the retail allocation) |
| `overlay20UpdateObjectResource` | o020 | **P! bounded plateau** | fresh configured full-TU C is exact-sized at 98 words with frame `0x70`, 90/98 raw/normalized words, and first mismatch `+0xB0`. All four call records have the target offsets/types and runtime authenticates ORT 202/203/1690/101; exact owned range/no padding/sole inbound ORT 1226 are proved. All 119 flag rows are nonexact. One stock-fidelity allocator/FIFO trace shows the scoped count carrier colors `v1` where the target routes it through temp `t6`; the later `value0F`/`start` swap is downstream of that same pool-to-temp transition. Direct field access regresses to 73/98, so no strict-gain combination or batch is eligible; linked equality remains fallback-only. | preserve this source/object basin; resume only with a newly proved legal pool-to-temp web-formation mechanism, not more flags, direct-count forms, forced splitting, or generic permutation |
| `overlay98CollectAccepted` | o098 | **S/W stack-home closure** | the fidelity-gated IDO trace exposed six automatic stack homes. Removing the two transient entry aliases and making the address-taken result the third surviving automatic restores the retail `0x50` frame and exact result home without padding or volatile scaffolding. Untouched configured C is 60/60 words; all six relocation sites/effective identities, the linked owner and overlay, and the full ROM are exact | ✅ matched (trace-selected natural lifetime/carrier form) |
| `overlay98CheckObject` | o098 | **F/W bounded public reproof** | public commit `f56d08c7` supplied the candidate, but its exact claim depended on frame/CFG/register rewrites and instruction insertion. Across all 119 flags, `-Wab,-r4300_mul` is best: exact 111-word extent and all six relocation offsets/types, frame `0x80` versus target `0xA8`, 78/111 linked words, first `+0x0`; 31 register and two operand sites remain. Runtime identities and sole local caller are authenticated | retain the best flag and fallback; next pass needs a natural 40-byte non-save frame/home mechanism before allocator work. Do not repeat the lattice or public normalizer |
| `overlay20RemoveEntry` | o020 | **P!/W bounded plateau** | retained configured full-TU C is 36/53 raw and runtime-normalized words, frameless, first `+0x4C`, with exact `0xD4` ownership and all ten LOCAL tuples/identities. The guarded scalar parameter is an ABI-equivalent allocator carrier; ORT 1174 and sole inbound `func_80007118+0x424` authenticate an owner pointer in `a0`. All 119 flags are nonexact and a fidelity-clean proc-0 trace records 13 caller-saved decisions. Narrow `newCount` scope is flat, independent compaction cursors regress to 19 differences, and direct count-carrier reuse supplies the retained one-word gain. No target padding; linked equality is fallback-only | preserve the 17-word residual and fallback. Resume only with a new natural temp-FIFO/web-existence spelling; do not repeat flags, these three forms, or a generic batch without another independent legal gain |
| `func_overlay_041_F0001650_1888988` | o041 | **P!/W** | retained configured-recipe isolated C is frameless and 49/55 words. Four count-web sites exchange `a1/v1` at `+0x10/+0x18/+0xCC/+0xD0`; the commutative float loads reverse at `+0xA0/+0xA4`. Runtime resolves `gOverlay41Slots` to resident `D_800D6C58` and local `D_0[0x15]` to module `+0x1E34`; historical route exhaustion is unretained | current V0 in the 119-flag lattice, multiplication-operand reversal, then only if needed one trace-selected natural count-web form; cap 121 stock builds plus trace |
| `overlay18Load` | o018 | **P!** | relocation-aware pass exposed a 64-bit first-argument ABI; overlay metadata resolves the call to `osSetTime(OSTime)` | ✅ matched (ABI reconstruction) |
| `overlay1ChooseModeObject` | o001 tail mixed TU | **P!** (stack-home declaration order) | configured V0 was 75/87 words with three unresolved call identities. All 119 flag identities were nonexact; one stock-fidelity allocator trace showed exact register lanes and supported a stack-home-only source change. Reversing the existing declaration order produces 87/87 words in the exact `+0x6270..+0x63CC` owner with a `0x50` frame. All 13 runtime records match by offset, type, and identity, including resident `func_80005750` and two `mathRnd` calls; the linked overlay and full ROM are exact. External and same-overlay scans found no useful donor. | ✅ matched (natural declaration order) |
| `func_8001D960` | charControl | **P!/F** (ugen ring class crossing) | declaration census plus three recovered source facts took this from 176 to 16 relocation-masked words, leaving instruction count, frame, stack map and all six relocation indexes exact with a `allocation-mismatch` verdict. The residual was one class crossing, not a phase to chase: case 2 named the scaled-angle call result in a local, which uopt coloured into `t4`, where the target pops a ugen ring temp. Inlining that expression alone was **worse** (+1 instruction, 103 words); inlining it *and* moving `actor->unk80 |= arg2` after the whole product -- so the product draws its four ring temps before the or draws its two loads -- was exact. Neither edit is an improvement alone, which is why declaration sweeps, shift/multiply spellings and loop reorderings were all flat | ✅ matched (two-lever composition; see IDO law L76) |
| `func_8001CB84` | charControl | **P!/F** (one reused scratch float) | the census took this from 138 to 53 words with an exact `0x80` frame and all 41 relocations agreeing. The residual read as an FP pool rotation; it was a class crossing. Naming the ballistic step `player->unk154 + step` keeps it in a coloured web where the inline expression spent FP ring temps: 53 -> 39 words. Declaring a *new* local for it then pushed every compiler temp 4 bytes down the frame and cost 25 displacement words; **reusing the existing `var_f12` scratch instead** was exact. The permuter generated the first half and scored it 717 against a base of 915 -- worse -- so its ranking had to be discarded and every output re-measured in the real build | ✅ matched (permuter as generator, real build as scorer) |

<!-- plateau-handoff:overlay57UpdateModeState:start -->
### `overlay57UpdateModeState` plateau handoff

- source: `src/overlays/o057/overlay57UpdateModeState.c`
- score: 267/354 words
- frame: 0x30
- relocations: 59
- first mismatch: +0xDC
- summary: Candidate emits 122 records; 58 offset/type pairs align; ten natural forms exhausted and the apparent 74-word gain breaks opcodes, alignment, and stack home.
<!-- plateau-handoff:overlay57UpdateModeState:end -->

<!-- plateau-handoff:overlay2QueryNode:start -->
### `overlay2QueryNode` plateau handoff

- source: `src/overlays/o002/overlay2QueryNode.c`
- score: 201/253 words
- frame: 0x68
- relocations: 51
- first mismatch: +0x58
- summary: V0 retains 52 raw and 40 masked differences with 22 opcode mismatches; 119 flags and nine natural forms exhausted. Next: leaf-loop and recursive CFG types.
<!-- plateau-handoff:overlay2QueryNode:end -->

<!-- plateau-handoff:overlay1AppendPathPoint:start -->
### `overlay1AppendPathPoint` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 22 differing words
- frame: 0x28
- relocations: 8
- first mismatch: +0x20
- summary: 119 flags and six coherent forms exhausted; next lever is source evidence for pool formation or anchor temporary FIFO behavior.
<!-- plateau-handoff:overlay1AppendPathPoint:end -->

<!-- plateau-handoff:overlay1FindPreviousUsable:start -->
### `overlay1FindPreviousUsable` plateau handoff

- source: `src/overlays/o001/overlay_001.c`
- score: 14 differing words
- frame: frameless
- relocations: 4
- first mismatch: +0x4
- summary: 119 flags and ten coherent forms exhausted; next lever is a source-faithful lifetime form retaining both preheader copies and the temporary-register lane.
<!-- plateau-handoff:overlay1FindPreviousUsable:end -->

<!-- plateau-handoff:func_overlay_073_F0000D70_18CB830:start -->
### `func_overlay_073_F0000D70_18CB830` plateau handoff

- source: `src/overlays/o073/overlay73Draw.c`
- score: 47 differing words
- frame: 0x30
- relocations: 5
- first mismatch: +0x1C
- summary: 119 flags exhausted; next lever is a source-authentic packet/local-layout form that changes the stack-home and register-allocation web.
<!-- plateau-handoff:func_overlay_073_F0000D70_18CB830:end -->

<!-- plateau-handoff:overlay27UpdateCoordinates:start -->
### `overlay27UpdateCoordinates` plateau handoff

- source: `src/overlays/o027/overlay_027.c`
- score: 19/65 words
- frame: frameless
- relocations: 10
- first mismatch: +0x0
- summary: The a1/a2 allocator web and X/Y table-address LO16 schedule remain; resume only with new source-authentic web-formation evidence.
<!-- plateau-handoff:overlay27UpdateCoordinates:end -->

<!-- plateau-handoff:func_8001BBB4:start -->
### `func_8001BBB4` plateau handoff

- source: `src/main/charControl.c`
- score: 38 differing words
- frame: 0x30
- relocations: 18
- first mismatch: +0x0
- summary: Target frame is 0x38. All 119 flags and ten coherent forms are nonexact; local structs reach 34 differences only with oversized frames. Next: a natural local/declaration layout adding eight non-save bytes, then the post-decrement loop CFG.
<!-- plateau-handoff:func_8001BBB4:end -->

<!-- plateau-handoff:overlay1FindNextAngle:start -->
### `overlay1FindNextAngle` plateau handoff

- source: `src/overlays/o001/overlay_001_middle.c`
- score: 4/50 words
- frame: 0x68
- relocations: 4
- first mismatch: +0x38
- summary: Five raw positions (four masked) remain; ten directed variants missed both swaps. Next: source-authentic scheduler evidence and resolved local-data identity.
<!-- plateau-handoff:overlay1FindNextAngle:end -->

<!-- plateau-handoff:overlay1FindPreviousAngle:start -->
### `overlay1FindPreviousAngle` plateau handoff

- source: `src/overlays/o001/overlay_001_middle.c`
- score: 4/50 words
- frame: 0x68
- relocations: 4
- first mismatch: +0x38
- summary: Five raw positions (four masked) remain; ten directed variants missed both swaps. Next: source-authentic scheduler evidence and resolved local-data identity.
<!-- plateau-handoff:overlay1FindPreviousAngle:end -->

<!-- plateau-handoff:levelFreeAll:start -->
### `levelFreeAll` plateau handoff

- source: `src/main/level.c`
- score: 117/117 words
- frame: 0x28
- relocations: 36
- first mismatch: none
- summary: MATCHED. Mutate the s32 entry carrier with the 14-bit mask, then add the table pointer first.
<!-- plateau-handoff:levelFreeAll:end -->

<!-- plateau-handoff:overlay1StartTimerCallbacks:start -->
### `overlay1StartTimerCallbacks` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 12 differing words
- frame: 0x28
- relocations: 13
- first mismatch: +0x50
- summary: 119 flags and nine natural forms exhausted; seven static identities remain unresolved and the next lever is source-authentic post-callback pointer liveness
<!-- plateau-handoff:overlay1StartTimerCallbacks:end -->

<!-- plateau-handoff:overlay1HandleCachedMode:start -->
### `overlay1HandleCachedMode` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 24 differing words
- frame: 0x18
- relocations: 13
- first mismatch: +0x14
- summary: 119 flags and ten coherent forms exhausted; shared-clear and return CFG stay two words long, with overlay1DispatchMode runtime identity ambiguous
<!-- plateau-handoff:overlay1HandleCachedMode:end -->

<!-- plateau-handoff:overlay1ConsumeNearbyPending:start -->
### `overlay1ConsumeNearbyPending` plateau handoff

- source: `src/overlays/o001/overlay_001_tail.c`
- score: 31 differing words
- frame: 0x48
- relocations: 1
- first mismatch: +0x40
- summary: 119 flags and ten coherent forms exhausted; next lever is source-authentic count/object/state allocator mapping, not the TU-wide diagnostic -g3 flag
<!-- plateau-handoff:overlay1ConsumeNearbyPending:end -->
