# Whale pass log — 2026-09-13

Historical per-pass record for `func_overlay_058_F000138C_18B0574`, split out
of the per-symbol handoff shard on 2026-09-16 when the function matched and
the shard stopped growing. These are the `w3`–`w10` lanes, which took the
function to and along the 187 plateau. Newest first.

The shard is
[`matching-triage-handoffs/func_overlay_058_F000138C_18B0574.md`](matching-triage-handoffs/func_overlay_058_F000138C_18B0574.md);
earlier passes are in
[`whale-pass-log-through-2026-09-12.md`](whale-pass-log-through-2026-09-12.md).
How it was finished is [`whale-self-read.md`](whale-self-read.md); the map is
[`WHALE_HANDOFF.md`](WHALE_HANDOFF.md).

## 2026-09-13 (lane `w10-whale`): late-only changes and restore-exit distribution stall

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Aligned buckets remain **3458 exact /
134 naming / 1 immediate / 22 structural**, with candidate +0x12F8 against
target +0x1260 the only gap pair. No executable bytes are promoted. This pass
chose structural reframing across both splits. It found no admissible repair
and makes no unrestricted impossibility claim. The corrected case-3 and runtime
import identities remain settled; no colour sweep or force is run.

### Baseline and two-component controls

Assignment is base-only. Configured, private-copy, instrumented and captured
objects pass full fidelity; the actual baseline self-comparison passes.
The census is **720 draws and 5,072 emissions**, with two draws at coordinate
line 650 and zero at capture line 660. All 35 frame homes and traffic agree.
The oracle remains JFG frontKeyboard at 0.081; no donor material is adopted.

The early-index control reproduces 2373 masked / 2449 raw, four excess bytes,
720 draws and 5,068 emissions. Representing savedPosition by one signed array
element reproduces 2377 / 2453, four excess bytes, 720 draws and 5,070 emissions.
Both preserve the two/zero site census and the baseline's complete draw order.
The inherited trace-only adapter passes stock fidelity on every measured cell;
named Ucode inputs and complete index captures map each to procedure zero.

The two-component control reproduces w9: the one-array form admits the row
entry at 24 interference entries and rejects restoration at 25 in the first
fragment. The later component instead admits restoration at 26 against its
26-entry bound. Reducing the first component is still insufficient.

### Three source families, eleven cells, no adoption

Attempt one replaces only the **nested coordinate induction**, after the
one-array control's first-component repair, with rowBase, textY, savedX or
letter0. It neither adds an independent counter nor changes the first title
coordinate role. rowBase is dead after its rowY copy; the other three roles
are absent from case 12. Original signed accesses, arithmetic, loop bounds and
call order remain intact.

These cells have respectively twelve, twenty-eight, eight and four excess
bytes, with 715, 724, 722 and 720 draws. Every cell keeps the two/zero site
census. The letter0 control retains the decisive component counts and
restoration admission. rowBase changes available-register counts enough to
reject the first row entry again; textY and savedX retain second-component
restoration admission. Changing only this late scalar role supplies no coupled
repair at target width.

Attempt two distributes restoration over the **row-loop guard's exits**,
distinct from the previously tested capture distribution. Each path still
performs each original signed store once, after its original calls and in the
same order. Position-only and both-coordinate forms are measured on early-index
and one-array controls, retaining physical source lines.

Position-only distribution costs twenty bytes and has 721 draws; distributing
both costs thirty-six bytes and has 722 draws. The two/zero site counts remain
unchanged. On the one-array controls, the first fragment admits the new
zero-contribution exit block, but rejects the subsequent restoration block;
the second fragment admits that restoration at 26 entries against 26. The
changed adjacency therefore retains the same restore-consumption problem and
adds width and draw costs. It is not a reuse repair.

Attempt three changes only the nested coordinate sum's association and
full-width unsigned arithmetic, converting the final value back to signed.
It preserves the original signed global loads and table index, and preserves
the result on the original expression's defined input domain using IDO's
32-bit conversion behavior. These are sum-expression controls, distinct from
the closed global-load and table-index type splits. All three forms reproduce
the one-array control's complete content sections, relocation map and symbol
surface. Their draw census and both split outcomes are unchanged.

These three consecutive families neither improve the retained residual nor
repair the two-component tradeoff: the ADR 0018 stall record. No further source
attempt is started. This excludes the tested forms, not all C source. End this
line at the preserved 187 without declaring it unmatchable or automatically
reopening adjacent spellings.

### Preservation and validation

Fresh analysis-only preflight reproduces 1,253 runtime/static records per side,
1,236 offset/type agreements and 671 stable identities. Candidate resolution
remains 858 resolved and 395 unresolved. The assembled ranking target's 526
relocations are a separate representation; exact runtime identity is not
claimed. The fresh-lane canonical link failure is repaired by overlay-syms
and a full-core-count build without a tracked alias change.

Private packet: `matching-evidence/w10-whale-20260913.tar.gz` under Git's common
directory. It retains all eleven cells and three controls: sources, commands,
objects, scores, first mismatches, aligned deltas, draw/split traces, Ucode
mappings, fidelity, oracle, preflight, gates and inherited adapter provenance.

tools/finalize_plateau.py preserves the guarded source; only EOF metadata and
this shard change. Gates and commit hash accompany the handoff. ROM verification
uses the assembly fallback. No merge, rebase or push is performed.

## 2026-09-13 (lane `w9-whale`): the first split's resumption test is insufficient

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Aligned buckets remain **3458 exact /
134 naming / 1 immediate / 22 structural**. The remaining pair is candidate
+0x12F8 against target +0x1260. No executable bytes are promoted. The case-3
and runtime-import repairs remain settled; no colour sweep or force is run.

This pass chose structural reframing. The new witness satisfies w8's stated
first-component admission test and the two/zero draw constraint, yet still
loses transition reuse. It therefore refutes that test's sufficiency; it does
not prove the function or unrestricted source space unmatchable.

### One saved-coordinate array reaches the old resumption bar

Replace only savedPosition's scalar declaration with a one-element signed
array, using its sole element at every original access. Its declaration stays
in the original position. The same experiment on savedOffset is independent.
Both preserve the original signed global loads, capture and restore order,
all calls, and every access width. Neither local's address escapes.

The savedPosition form measures **2377 masked / 2453 raw, four excess bytes**.
The savedOffset form measures **2386 / 2460, four excess bytes**. Both retain
all 35 frame homes and identical traffic, **720 draws**, the baseline's complete
draw-register sequence, and **two draws at line 650 / zero at line 660**.
The first form changes only two case-3 emission counts relative to the ordinary
early-index control, one additional event at each line. In particular, its
case-12 transition instructions still equal that control's: restoration does
not keep the address later used for the transition update. Emission events
are not final instruction counts. Neither source is an adoption.

The address trace verifies the intended first-component change:

- The first-loop header seed still has 21 interference entries.
- The capture block contributes one new range instead of two, leaving 23
  entries after the first-loop call block joins the component.
- The row-loop entry adds one entry and leaves 12 available registers.
  Its combined count of 24 meets the admission bound.
- The restore would add one more entry at the same 12 available registers.
  Its combined count of 25 fails the bound, so it is excluded from this split.

Both array controls therefore meet the previous handoff's exact resumption
test, including the unchanged signed coordinate load and paired draw census.
They nevertheless remain four bytes too long.

### A later negative-saving split takes the restore instead

The trace adapter was extended only to report the second seed's existing
membership and neighbours. Stock versus traced content sections, relocations
and symbols agree on every inspected source; the adapter changes no decision.
Named Ucode captures and complete procedure-index receipts map all source
cells to ordinal zero. The address identity is checked using the same static
location and expression representation as the inherited adapter.

In the retained baseline, the second seed, at the inner-loop coordinate
calculation, has 25 interfering ranges. Its entry adds one and leaves 13
available registers. The row-loop exit adds none. Adjoining the restore would
make 27 entries against a bound of 26, so the restore is rejected here too.
A later profitable fragment contains both restoration and transition update.

The one-array controls remove the saved scalar from **this second component
as well**. Their second seed has 24 entries; entry and exit leave 25.
The restore now reaches 26 against the same bound of 26 and is admitted.
The measured negative-saving fragment contains the inner coordinate calculation,
row-loop exit and restore. The later transition fragment consequently contains
only the update. Fixing the first split merely changes which unprofitable
fragment consumes the restore; it does not restore address sharing.

This gives a limited arithmetic exclusion, not a source-space floor. Holding
these observed block, availability and contribution counts fixed, removing k
capture ranges makes first-entry admission require k at least one, while
keeping the restore out of that first fragment requires k less than two.
Thus exactly one removal is needed. But any such removal makes the later
restore admissible. The measured controls cover neither removal, each single
removal, and both captures represented by one two-element array. This retires
**uniform capture-range removal under this fixed split topology**. Source forms
that change block membership, available registers or where a range interferes
are outside that model and are not declared exhausted.

### Source experiments and stopping evidence

All 27 source cells retain their source, stock and traced objects, scores,
first mismatch, aligned per-window comparisons and draw profiles privately.

- Attempt one consumes rowBase into rowY earlier, then reuses rowBase as the
  first title-array induction. Entry and header placements have non-reuse
  controls. The strongest seed reduction is replaced by another range at
  the row entry; the cells cost four or twelve bytes.
- Attempt two changes the literal type in the player-count-minus-one table
  index, retaining a signed final index and signed coordinate arithmetic.
  Separate coordinate and stride forms restore first-component admission but
  are twelve bytes short and add a coordinate draw. Changing both keeps the
  paired census but costs eight bytes. This is distinct from changing the
  coordinate global's load representation.
- Attempt three groups the saved coordinates in a struct or two-element
  array. The correctly ordered struct reproduces the baseline and early-index
  objects exactly; reversed homes add displacement differences. The array
  removes both capture ranges and admits the restore too, with sixteen missing
  bytes on the early-index control.
- Attempt four changes each saved coordinate separately to a one-element
  array, on both baseline and early-index controls. The early controls provide
  the two-component witness above. This is the last material causal finding.
- Attempt five captures into existing dead locals and copies those values to
  the original saved locals on both paths of the row-loop guard. Global read
  timing is unchanged. These two-phase local rebindings cost four or eight
  bytes and do not repair reuse; they differ from earlier duplicated global
  captures across a guard.
- Attempt six precomputes the inner text coordinate through two dead locals,
  before or after its font call. The calculation is within the original
  bounded row domain. All four forms propagate back to the one-array control's
  object; the second split remains unchanged.
- Attempt seven gives only the nested counter-array access an independent
  induction, preserving the original visible bound counter. The added late
  range also changes available registers: its loop entry is rejected, and the
  restore remains admissible. One form costs eight bytes; the other is 272
  bytes short. Neither supplies the required reuse or an adoption.

The last three families repeat the observed range-replacement or width tradeoff
without improving the retained residual or repairing the coupled split outcome.
This is the ADR 0018 stall record. Stop this line here. Any future reopening
must distinguish **both** negative-saving components: the restore must survive
both splits and remain with the later update, while retaining the per-site
census and target extent. A favourable first seed or first-component admission
is demonstrably insufficient. No unrestricted impossibility claim is made.

### Preservation and validation

The assignment gate returns base-only. The size-filtered oracle remains JFG
frontKeyboard at 0.081; no donor material is adopted. Configured full-TU,
private-copy and instrumented baseline objects pass fidelity, and the actual
baseline object's workbench self-comparison passes. Analysis-only preflight
reproduces 1,253 runtime/static records per side, 1,236 offset/type agreements
and 671 stable identities. Candidate resolution remains 858 resolved and 395
unresolved. The assembled ranking target's 526 relocations are a separate
surface; exact runtime relocation identity is not claimed.

The private packet is `matching-evidence/w9-whale-20260913.tar.gz` under Git's
common directory. It includes every source cell, commands and object receipts,
Ucode mapping and fidelity checks, the inherited and extended trace adapter,
asserted two-component comparison, draw/frame comparisons, oracle, preflight
and closing gate logs. No raw compiler or ROM-derived evidence is tracked.
The guarded source is preserved with tools/finalize_plateau.py; only its EOF
metadata and this shard change in the function commit. Closing gate results
and commit hashes accompany the handoff. ROM verification uses the original
assembly fallback, not the nonexact C. No merge, rebase or push is performed.

## 2026-09-13 (lane `w8-whale`): address splitting, not initial load formation

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Aligned buckets remain **3458 exact /
134 naming / 1 immediate / 22 structural**. The only remaining pair is
candidate +0x12F8 against target +0x1260. No executable bytes are promoted.
The case-3 and runtime-import repairs in w4 remain settled. No colour force,
colour sweep, compiler-behavior override or post-compilation edit is used.

The configured baseline, private source copy and instrumented copy agree.
The baseline object passes an actual workbench self-comparison. Its census
is 720 temporary draws and 5,072 emission events, with **two draws at line
650 and zero at line 660**. Fresh frame census reproduces the same 35 homes
and traffic on both sides. The early-index control has four excess bytes,
720 draws, the same complete draw-register sequence and 5,068 emissions.
Emission counts include source-location records and are not instruction counts.

### The first changed split decision is now identified

Stock-fidelity Ucode captures map both controls to procedure ordinal zero.
The first-loop coordinate calculation and saved captures retain the same
signed load representations. The output difference is later: the baseline
materializes the coordinate address at the row-loop exit and reuses it for
the restore and transition update; the early-index control uses a direct
restore and rematerializes the address inside the transition branch.

A private, trace-only uopt adapter identifies the address through its actual
static-symbol location, offset and expression kind, cross-checked against the
named Ucode input. It records initial membership, per-block savings, split
adjacency tests and interfering-range identities. The adapter changes no
optimizer decision. Full content-section, relocation and symbol fidelity
passes against stock output for every instrumented source used below. The
first exploratory address-field reading was a pointer rather than an identity;
that reading is retained but superseded by the verified location-field mapping.

All 44 normalized initial formation records for the coordinate address agree.
The first differing membership appears during greedy splitting, not when the
coordinate load is initially formed. The decisive sequence is:

- At the first-loop header, the baseline split seed has 20 interfering ranges;
  early index has 21. The additional range is the named portraitX scalar.
- After adjoining the title-loop entry and saved captures, the baseline has
  23 interference entries; early index has 24. The two capture additions are
  the savedPosition and savedOffset ranges.
- Adjoining the row-loop entry adds its generated entry-array cursor and one
  interference entry, leaving 12 available registers. IDO admits the baseline
  at 24 entries and rejects early index at 25: twice the available-register
  count is the relevant bound in this split mode.
- That rejection restores the early control's available count to 13. It then
  admits the restore block at 25 entries. The baseline, still at 12 available,
  rejects that block from this split instead.
- The early control's admitted restore block belongs to a negative-saving
  fragment. The later transition fragment consequently loses the restore's
  address reuse. The baseline transition fragment includes both sites.

Thus the loss is an interaction between the added induction carrier and
**which adjacent blocks enter a split**, even though the address's initial
membership and the paired draw counts agree. The source lever must change the
whole component's interference before the row-loop entry. Merely reducing the
initial seed count is insufficient: guarded explicit cursors and a fresh
scoped index return the seed to 20, but add two ranges at the title-loop entry
instead of one and still reach the decisive 24-entry state.

### Source experiments and stopping evidence

All edits remain private. Every admitted cell preserves the original calls,
access widths, signed coordinate arithmetic and loop bounds; every cell has
its stock/traced objects, first mismatch, aligned per-window comparison and
per-line census retained.

- Attempt one names the existing header coordinate reads through their later
  saved-coordinate locals. Six cells on baseline and early-index controls
  change width and/or add header draws. Their best retained source remains 187.
- Attempt two reverses the visible and generated index increment order, or
  moves those increments into the existing array/coordinate expressions.
  These do not repair reuse: three cells retain four excess bytes and the
  array-argument postincrement costs eight.
- Attempt three removes the independent scalar and indexes directly with i.
  Signed and full-width unsigned forms agree, cost eight bytes and have 722
  draws. The required two/zero site counts alone do not make them acceptable.
- Attempt four moves only the independent index initialization into the first
  nonempty guard, initializes it from the existing zero i, or defines both in
  the title call. Copying i is inert; guard forms cost 24 bytes and the title
  form costs 36. These are distinct from the previously tested coordinate
  and row-initialization guard moves.
- Attempt five rebases the sole-i title access at an interior or one-past
  array pointer, compensating the index, and tests byte-relative equivalents.
  All admitted forms normalize to the eight-byte direct-index control. Two
  initial byte casts are excluded for subobject-boundary ambiguity; reviewed
  controls use the complete array's byte representation and agree.
- Attempt six makes the coordinate and/or step lookup pointer-relative so the
  index subtraction can become a load displacement. Single changes cost eight
  bytes and add a header draw. The combined form is twelve bytes short with
  718 draws; its split seed grows to 22, so it does not remove the extra
  interference. No padding or compensating work is added.
- Attempt seven uses equivalent while, for and explicit-tail-break spellings
  for the title loop. While and for reproduce four excess bytes and the same
  24-entry split decision; explicit break costs sixteen bytes.
- Attempt eight initializes an explicit title cursor only within the nonempty
  guard, through two existing dead pointer roles. Both retain four excess
  bytes, 720 draws and two/zero site counts. The extra title-array address and
  cursor ranges replace the old extra scalar; the decisive count stays 24.
- Attempt nine declares and initializes a fresh index within that guard, with
  and without register storage. Both retain four excess bytes and the paired
  census, change frame-home geometry, and reproduce the 24-entry decision.

The last three families do not improve the retained residual or change the
identified split outcome. They repeat the measured extra-interference/width
tradeoff rather than providing a new allocation mechanism; this is the ADR
0018 stall record. Source space is not declared exhausted. The resumption test
is now concrete: keep early visible-index initialization and the signed shared
coordinate load, but remove an interfering range from the **whole header,
first-loop entry and capture component**, not just from its seed. Demonstrate
that the row-loop entry is admitted and the restore block excluded from the
unprofitable fragment, while retaining the two/zero census, before considering
any subsequent naming residual. Existing carrier substitutions, guard captures,
unsigned-load splits and the colour landscapes remain recorded negatives.

### Preservation and validation

The assignment gate returns base-only. The size-filtered oracle remains JFG
frontKeyboard at 0.081; no donor material is adopted. Analysis-only preflight
reproduces 1,253 runtime/static records per side, 1,236 offset/type agreements
and 671 stable identities; candidate identities remain 858 resolved and 395
unresolved. The target assembler's 526 relocations are a separate surface.
There is no claim of exact runtime relocation identity.

The private packet is `matching-evidence/w8-whale-20260913.tar.gz` under Git's
common directory. It contains pinned source/command manifests, every source
cell and comparison, named Ucode captures, the trace adapter and its fidelity
checks, the normalized split comparison, and gate logs. No raw compiler or
ROM-derived evidence is tracked. The guarded source is preserved with
`tools/finalize_plateau.py`; only its EOF metadata and this shard change in the
function handoff commit. A separate generic learning records the split-admission
mechanism. Closing gate results and local commit hashes accompany the handoff;
ROM verification uses the original assembly fallback, not the nonexact C.

## 2026-09-13 (lane `w7-whale`): capture destination and first-loop join controls

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Aligned buckets remain **3458 exact /
134 naming / 1 immediate / 22 structural**. The remaining pair is candidate
+0x12F8 against target +0x1260. No executable bytes are promoted. The latest
w4-w6 handoffs supersede the dispatch's old case-3 and import-conflict claims;
neither resolved item nor the colour landscape is reopened.

Configured compilation, private source-copy compilation, and instrumented
compilation reproduce the baseline. The actual baseline object's workbench
self-comparison passes. The baseline has 720 temporary draws and 5,072 ugen
emission events; its coordinate calculation at line 650 has two draws and
savedPosition capture at line 660 has zero. Both frame ladders contain the
same 35 homes. The early-index control reproduces four excess bytes, 720 draws,
unchanged complete draw order and 5,068 emissions. Its transition loses the
coordinate address shared between restoration and later update.

### Three additional source families do not repair the paired constraint

Attempt one changes only case 12's savedPosition capture-and-restore role to
one of eleven existing scalars absent from the enclosing case. The global
load stays signed and full width; every capture, restoration and call retains
its order. This tests the saved local's cross-case lifetime identity, not the
first-loop coordinate carrier previously tested by w5. No declaration is added.

The countdownX and highlighted forms reach **970 masked / 1098 raw**, delta
zero, buckets **2662/920/2/32**, with 1,255 static relocations. They retain the
two/zero site counts but have 719 total draws: the restoration at line 692
loses its stack reload draw because the saved value now occupies a saved
register. The transition address is still not reused. Two replacement gap
pairs and the subsequent naming regression rule out adoption. Other carriers
cost four to 76 bytes. The x spelling is rejected by the conservative source
filter before compilation because the case already contains an x field;
no field rename or invalid candidate is compiled.

Attempt two distributes one or both captures over the outgoing paths of the
**first title-loop guard**. This differs from w5's second row-loop guard:
the positive path captures after all title-loop calls, and the empty path
captures at its corresponding exit. Each original global is still read once
on either path, in the original order. Signed-capture and unsigned-global-load
controls give four cells, all with twelve to twenty excess bytes. The signed
position-only form keeps two coordinate draws but adds one capture draw;
the unsigned forms retain the inherited load-representation cost. Moving the
capture before this earlier join therefore supplies no width/draw repair.

Attempt three changes the saved local's destination access rather than the
global load: signed or corresponding unsigned 32-bit lvalue stores, restores,
or both. The original signed global load and saved bits are preserved. All
five savedPosition forms have **2377 masked / 2453 raw**, four excess bytes,
720 draws, an unchanged complete draw order, the original two/zero site counts,
and 1,255 static relocations. Their census changes only the earlier case-3
emissions at lines 565 and 566, one additional event each. A sixth cell on
savedOffset likewise adds four bytes. Address-taking or unsigned access to
the destination does not reproduce the global-load representation's reuse
repair; the transition remains the early-index control's shape.

These three families provide no adoption or repair of the known reuse/draw
tradeoff, meeting ADR 0018's stall rule. This is not exhaustion of source
space. The remaining question is the first-loop coordinate load's expression
identity across the loop join and the saved capture. The retained baseline and
early-index Ucode inputs and outputs provide the next comparison: locate where
the transition's coordinate-address range is lost while preserving the signed
load name and two/zero site census. Merely changing the saved carrier, splitting
these captures before the first join, or taking the saved local's address does
not meet that requirement.

### Preservation and validation

The assignment gate returns base-only. The nearest size-filtered oracle is
still JFG frontKeyboard at 0.081; no donor code or names are adopted. Fresh
analysis-only preflight reproduces 1,253 runtime/static records on each side,
1,236 offset/type agreements and 671 stable identities. Candidate identities
remain 858 resolved and 395 unresolved. Exact relocation identity is not
claimed; the ranking target's 526 assembler relocations are a separate surface.

All 21 compiled source cells retain their source, stock and traced objects,
scores, first mismatches, aligned per-window deltas, draw comparisons, and
hypotheses. Named Ucode inputs and complete procedure-index captures map every
cell to ordinal zero, with stock/capture and stock/trace content, relocation,
and symbol fidelity checks. No raw evidence is tracked. The private packet is
`matching-evidence/w7-whale-20260913.tar.gz` under Git's common directory.
The guarded source is preserved with tools/finalize_plateau.py; only its EOF
metadata and this shard change. All six closing tools/gates.sh checks pass:
verify, cleanroom, check-docs, check-nonmatching-builds, check-scoreboard and
check-overlay-syms. Final recompilation reproduces 187/358, the four aligned
buckets and the baseline's complete section/relocation/symbol surface.
Verification prints US ROM SHA-1 507341c0a40ca3e9a7cee969b396ee53facfb548
using the original assembly fallback, not the nonexact C. The fresh-lane link
failure was repaired by overlay-syms and a full-core-count build without a
tracked alias change. No merge, rebase or push is performed.

## 2026-09-13 (lane `w6-whale`): equal total draws still hides the capture defect

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Aligned buckets remain **3458 exact /
134 naming / 1 immediate / 22 structural**. The remaining pair is candidate
+0x12F8 against target +0x1260. No executable bytes are promoted. The dispatch's
case-3 pair and import-identity conflict were already corrected in w4; neither
was reopened. No colour force or colour sweep is run.

### New controls separate address-taking from load representation

The configured baseline, private source-copy baseline and traced compilation
agree. The baseline has 720 temporary draws and 5,072 ugen emission events.
Its actual object passes the workbench self-comparison. The assignment gate
returns base-only. The size-filtered oracle remains JFG frontKeyboard at 0.081;
no donor material is adopted.

The inherited early-index control reproduces four excess bytes, 720 draws and
an unchanged complete draw order. The unsigned savedPosition capture reproduces
1088 masked / 1197 raw, delta zero, buckets 2529/1060/2/24 and 722 draws.
These are controls, not new source improvements.

Attempt one tests whether taking the coordinate's address causes that tradeoff.
The same object is instead declared as a four-byte union with signed and
unsigned full-width members. All ordinary accesses use its signed member;
only the case-12 capture uses the unsigned member. This preserves the symbol,
member offset, access width and signed arithmetic. The signed union controls
reproduce their respective baseline objects. The unsigned member reproduces
1088 and both extra draws, including the earlier case-2 emission changes.
Thus pointer address-taking is not the discriminator: the load representation
itself suffices. This is a private diagnostic, not an adopted global type.

Attempt two preserves one unsigned load representation at both the first-loop
coordinate calculation and saved capture, then partitions one additional read
at the title, negation, node fill, nested calculation or later update. Explicit
signed conversions preserve each original signed arithmetic operation and
comparison. All paired cells retain 720 draws, including the original two at
line 650 and zero at line 660, but still cost four bytes. Isolated additional
read controls likewise supply no width repair. No signed threshold is changed.

### Staged coordinate assignments restore the total, not the per-site census

Attempt three exposes the original left-associated coordinate calculation as
ordinary value-producing assignments, using the coordinate itself or the
existing dead x local. Every admitted cell preserves the same global reads,
widths, arithmetic domain and call order. Operand reversal alone is inert.
Partial and three-step assignments change the draws, which the equivalent
lvalue spellings in the previous pass could not do.

The strongest new diagnostic is **253 masked / 411 raw**, delta zero, frame
0x138, buckets **3364/225/2/24**. It first assigns the signed coordinate load
to columnX, then adds the table value, then the other coordinate, retaining
the inherited unsigned capture. Its **720 draws and entire draw-register
sequence equal the baseline's**. Nevertheless, line 650 falls from two draws
to one and line 660 rises from zero to one. Emission events rise from 5,072
to 5,078. The earlier case-2 emission changes survive.

The old pair disappears but a replacement remains at candidate +0x136C against
target +0x134C. Aligned window deltas add 94 residual rows, including changes
outside case 12. All 35 frame homes and their traffic still agree with the
target. This fails the dispatch's per-site draw constraint and is not an
adoption. A 256-word alternative has the same per-site draw defect. Equal
total draws, even with an equal register sequence, do not establish equal
source-line placement or matching output.

An exploratory nested-assignment spelling was rejected during semantic review
because it modifies columnX twice without an intervening sequence point.
Its two captured cells are excluded from admissible evidence and never touch
tracked source. The exclusion stays with the private artifacts.

### Three follow-up families fail to repair the measured tradeoff

- Attempt four combines the staged calculations with unsigned header-only or
  paired header/capture loads. The target-width cells still spend an extra
  capture draw; the shared-load cells avoid that draw but lose width. No cell
  satisfies both constraints.
- Attempt five gives the independent title-array induction a fresh scalar,
  declared at function, case or first-loop scope. Each still costs four bytes
  with 720 draws and the original two/zero site counts. The frame stays 0x138,
  but the extra scalar changes stack-home geometry. No frame compensation or
  padding adjustment is attempted.
- Attempt six carries the title-array address explicitly through four existing
  dead pointer roles, reading through the actual character-pointer array type.
  Cursor and path retain four excess bytes with 720 draws; saves and slot cost
  28 bytes and 724 draws. These reproduce the address/body cost rather than
  repairing it.

Attempts four through six supply no adoption or further repair of the measured
width/draw tradeoff, meeting ADR 0018's stall rule. This is not exhaustion of
source space. The next concrete question remains how to keep the coordinate
calculation and saved capture as one cached load name while retaining the
transition's shared address. The new negative is sharper: replacing the cast
with a named union, merely matching total draws, or removing the title index's
cross-case scalar identity does not achieve that combination. Require the
two/zero site census and compare aligned windows before ranking any new form.

### Preservation and validation

Fresh analysis-only preflight reproduces 1,253 runtime/static relocation
records on each side, 1,236 matching offset/type shapes and 671 stable
identities. Candidate resolution remains 858 resolved and 395 unresolved.
No exact runtime relocation-identity claim is made. The assembled ranking
target's 526 relocations remain a distinct representation.

All admitted candidates retain stock/traced section, relocation and symbol
fidelity receipts. Baseline and the two principal diagnostics additionally
retain named Ucode/procedure mapping evidence. Sources, objects, scores, first
mismatches, aligned deltas, draw profiles, traces, frame comparisons, hypotheses,
semantic exclusions and configured commands are preserved privately in
`matching-evidence/w6-whale-20260913.tar.gz` under Git's common directory.
Nothing ROM-derived from that archive is tracked.

The guarded source is finalized with tools/finalize_plateau.py. All six
closing tools/gates.sh checks pass: verify, cleanroom, check-docs,
check-nonmatching-builds, check-scoreboard and check-overlay-syms. Final
configured recompilation reproduces 187/358 and passes full content,
relocation and symbol fidelity against the entry object. Verification prints
US ROM SHA-1 507341c0a40ca3e9a7cee969b396ee53facfb548 using the original
assembly fallback, not the nonexact C. The fresh-lane link failure was repaired
with overlay-syms and a full core-count build; no tracked alias changes result.
No merge, rebase or push is performed.

## 2026-09-13 (lane `w5-whale`): capture-type/reuse tradeoff measured

**Retained source unchanged: 187 masked / 358 raw, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50.** Fresh aligned buckets are **3458 exact /
134 naming / 1 immediate / 22 structural**. The only retained gap pair remains
candidate +0x12F8 against target +0x1260. No executable bytes are promoted.
Only the EOF handoff metadata and this shard change.

The dispatch's case-3 pair and import-identity conflict were already resolved
by w4. Neither was reopened, and no colour landscape was repeated. The
assignment gate returned base-only. Configured stock compilation, the private
source-copy harness, and the baseline instrumented capture agree on content
sections, relocations and symbols. The actual baseline object's workbench
self-comparison passes. The nearest size-filtered oracle remains JFG
frontKeyboard at 0.081; no donor material is adopted.

### A new source witness restores the transition but spends two draws

The reproduced early-index control initializes visible i in the first title
colour call and gives the title array its own portraitX induction. It costs
four bytes and has 1,255 candidate static relocations. Its **720 temporary
draws and complete draw order equal the baseline's**, but its transition no
longer shares the restored coordinate address with the later update. The
census changes at the restores and following tests. Total ugen emission
events fall from 5,072 to 5,068 despite the extra final instruction; these
pre-assembly events are not a final executable-word census.

Attempt one changes only case 12's savedPosition capture to read through the
corresponding unsigned 32-bit lvalue of D_o058_5E9C, assigning the value back
to the existing signed local. This preserves the accessed object and width;
IDO's signed conversion preserves its bits. The source reaches **1088 masked /
1197 raw, delta zero, buckets 2529/1060/2/24**, with 1,253 static relocations.
Untouched stock and traced objects pass full content/relocation/symbol fidelity.
All 35 frame homes and their traffic remain identical to the baseline/target.

This is a width/reuse witness, not an adoption. The old gap disappears but is
replaced by candidate +0x136C against target +0x134C. The transition once again
retains the coordinate address from its restoration through the later update.
The cost is **722 draws**, with one additional draw at the first-loop
coordinate calculation (source line 650) and another at the savedPosition
capture (line 660). Its census also changes earlier case-2 emissions, without
changing draw counts there. The broad naming regression cannot be described
as only the removed positional shadow. Every aligned window delta is retained.

The other isolated unsigned-lvalue changes, at the restore, loop negation and
later update, reproduce the four-byte control. Attempt two establishes that
an ordinary value cast or unsigned savedPosition declaration is also inert.
An explicit signed conversion around the unsigned capture reproduces 1088.
Making the header coordinate read use the same unsigned representation as the
capture removes both extra draws, but loses transition reuse and returns to
four extra bytes. Making both saved-coordinate captures unsigned changes width
by minus twelve. Thus the source lever is the load representation/CSE
partition, not merely the signedness of the saved scalar.

### Bounded follow-ups and stopping evidence

Three further families supply no repair of that measured tradeoff:

- Attempt three separates the first-loop coordinate from its later nested-loop
  role, using eight existing scalars dead in the enclosing case. These change
  width by minus sixteen, minus twelve or plus four bytes. None preserves both
  the target width and transition reuse.
- Attempt four puts one or both saved-coordinate captures into both branches
  of the existing player-count guard, so each path still captures each value
  once. Signed and unsigned-capture controls cost eight to sixteen bytes.
  Correcting the harness's extra blank-line accounting reproduces every score;
  both versions are preserved, and only the corrected profiles are used for
  source-line attribution.
- Attempt five spells the capture through equivalent signed indirect and
  single-element-array lvalues. They reproduce the four-byte control; the
  explicit long spelling is also identical, since PR/ultratypes.h defines
  s32 as long. The corresponding unsigned array load reproduces the 1088
  witness. No new instruction repair or draw mechanism appears.

One overbroad exploratory cell was rejected during semantic review and is not
admissible evidence: a whole-case unsigned substitution changes a signed
threshold. It never touched tracked source. The rejection is recorded beside
the private artifact so its scalar score cannot become a future candidate.

Attempts three through five repeat the known width/draw tradeoff without an
adoption or further causal repair, meeting ADR 0018's stall rule. This does
not exhaust source space. The next concrete question is the expression-name
relationship between the first-loop coordinate calculation and savedPosition's
load: separate the capture sufficiently to preserve transition-address reuse
while keeping the original draws at lines 650 and 660 and the earlier case-2
emission behavior. Repeating these coordinate carriers, guard-local captures,
value casts or equivalent lvalue spellings does not answer that question.

### Preservation and validation

Fresh analysis-only preflight reproduces **1,253 runtime/static records on
each side, 1,236 matching offset/type shapes and 671 stable identities**;
858 candidate identities resolve and 395 remain unresolved. Exact runtime
relocation identity is not claimed. The ranking target's 526 assembler
relocations are a separate representation. The final source remains guarded
NON_MATCHING and is preserved with tools/finalize_plateau.py.

The private archive is `matching-evidence/w5-whale-20260913.tar.gz` under Git's
common directory. It contains configured commands, source snapshots, candidate
objects, scores, first mismatches, aligned deltas, census profiles and traces,
frame comparisons, semantic exclusions, hypotheses, fidelity/self-comparison
receipts, preflight and gate logs. None of that raw evidence is tracked.
All six gates pass: verify, cleanroom, check-docs, check-nonmatching-builds,
check-scoreboard and check-overlay-syms. The final configured source reproduces
187/358 and passes full fidelity against the retained entry object; final
cleanroom and check-docs also pass. ROM verification prints the expected US
SHA-1 507341c0a40ca3e9a7cee969b396ee53facfb548, using the original assembly
fallback rather than the nonexact C. No merge, rebase or push is performed.

## 2026-09-13 (lane `w4-whale`): final 187 and runtime import correction

**Retained: 187 masked / 358 raw, 14,456 bytes, delta zero, frame 0x138,
first masked mismatch +0x50.** Aligned buckets are **3458 exact / 134 naming /
1 immediate / 22 structural**, against entry **3454 / 135 / 2 / 25** and
217 masked. The case-3 pair is closed without replacement. The remaining pair
is target +0x1260 against candidate +0x12F8. The C remains NON_MATCHING;
**zero new matched executable bytes** are claimed. The first improvement was
immediately finalized, gated and committed as `13a3cb1a`.

### The inherited identity conflict is corrected in the source

Nineteen resident-looking data import names conflict with their uniquely
correlated runtime relocation identities. The existing `_o058Reloc` naming
convention separates these runtime imports from resident-address inference;
no identity producer, comparison threshold, shared header, linker policy or
alias-generation rule is changed. The new names preserve the compiled text,
rodata and data, and preserve every relocation after the proved name mapping.
This is an import-identity correction, not a colour or instruction edit.

The old claim that the halfword placeholders D_800D304E and D_800D3050 name
distinct objects is **refuted**. All six complete owned HI/LO pairs independently
bind reserved selector 0xFFF and BSS-relative base +0x4D60E, with zero candidate
addends: four pairs under the former first spelling and two under the second.
Each pair agrees in exact function-relative offset/type and target identity;
no common synthetic address or guessed alignment supplies this result.
Coalescing the names into D_800D304E_o058Reloc preserves all three compiled
sections and the normalized relocation map. The redundant declaration and
false distinct-object comment are removed. Eighteen relocation-specific names
therefore replace the nineteen former conflicting names.

Fresh configured `function_preflight.py --analysis-only` now completes:
target and candidate have **1,253 runtime/static relocation records each**;
**1,236/1,253** offset/type shapes and **671/1,253** stable identities agree.
There are 858 resolved candidate identities and 395 unresolved records.
Neither relocation exactness flag is true. These runtime-table counts are
separate from the fallback assembler object's 526 relocation records quoted
in the older scoring notes. The preflight asm view reports structure mismatch;
its first difference is +0x2C, whereas the ranking's relocation-masked first
difference remains +0x50. No promotion proof is implied by analysis-only exit
zero or by the normal ROM verification, which uses the fallback assembly.

### Bounded exploration retained privately

The source correction that closes case 3 is explained in the preservation
checkpoint below. The full source bundle is necessary: the guarded row
initialization alone is inert on the inherited 217 and 207 sources. Against
the natural cursor source with four excess bytes, it changes the first row
assignment from two temporary draws to one, restores width and transition
address reuse, and reaches 187. Stock/traced section, relocation and symbol
fidelity passes. Both sides retain all 35 stack homes and identical traffic.

Attempts one through three investigated the same coupled row/transition
problem before finding that guard boundary. Unnamed assignment-expression
forms were inert. Naming the transition result inside its store/condition
produced a delta-zero 621 control, better than the earlier 1068 diagnostic but
much worse than the retained source. Naming the computed row result was inert;
whole-row carrier substitutions changed width. Naming coordinate negations
removed temporary draws at the negation and node fill, but did not repair the
row-load/transition coupling. Every source, object, score and census is kept.

Attempt four exhaustively measured that changed 621 source: **1,912 probes
across 141 coloured webs**, with 513 accepted target-size results, 1,140
size-changing results and 259 declined or unverifiable force receipts.
Ten singles improved that control; the best was 606. The disjoint-radius
packing predicted 585, but that combination was not compiled and is not a
measured floor. The later ordinary 187 source superseded this diagnostic.
The original 217/227 colour landscape was not repeated.

Attempt five found and banked 187. Attempts six and seven then tested case
12's independent first-loop array index with coordinate/row initialization
inside their nonempty guards, and the later entry-array induction's type or
sharing. The early-index control has four excess bytes, unchanged temporary
draw order, and changed transition emissions. The guard moves do not restore
target width; signed and shared later-entry forms reproduce the control.
The extra cost comes with loss of the shared coordinate address between its
restoration and later transition update, not a new stack home.

Attempt eight measured **all 1,896 legal single-force probes across 141
coloured webs of that changed case-12 source**: 54 accepted target-size
results, 1,593 size-changing results and 249 declined/unverifiable receipts.
The best accepted target-size score is 286. Its aligned comparison against
187 adds 35 residual rows and replaces the old pair with two other pairs.
All objects, receipts and the complete footprints JSON were written before
the report renderer entered exponential subset enumeration; only that final
rendering was interrupted. No combination floor is claimed. In particular,
the four-byte baseline's positional shadow makes its packing predictions
unsuitable evidence for source adoption.

Attempt nine is the runtime-identity correction above. The single texture
import rename, all proved import renames, and halfword coalescing each retain
187/358 and delta zero. The final configured source and instrumented source
also reproduce those measurements. The temporary draw order remains unchanged.

Three further source families fail to repair the case-12 coupling:

- Attempt ten carries the coordinate address through two existing dead pointer
  roles or reuses its saved value in the later update. The six cells cost four
  or eight bytes. Nesting the ordinary update into its test is inert.
- Attempt eleven gives the first title-array index three equivalent full-width
  unsigned/address spellings. All reproduce the four-byte control; the aimed
  schedule does not move.
- Attempt twelve replaces the declared walking coordinate with its captured
  initial value plus the independent index times the stride. Both product
  operand orders cost 44 bytes. They do not supply the wanted generated cursor.

The requested case-3 closure is banked. The follow-ups provide no additional
repair of case 12; the retained plateau does not claim exhaustion of source
space. Do not repeat these address carriers, index casts or indexed-coordinate
forms as if they were untested. The next concrete question is the **formation
and reuse of case 12's coordinate-address value after early index definition**.
Compare the retained source with the private early-index control at the restore
and later update; preserve the row-load draw order and target width. The
semantic-web comparison has ambiguous formation fingerprints, so its numeric
web IDs or presence changes alone are not source attribution. The previous
D_800D31C8 conflict is no longer the first blocker: remaining tuple-shape and
unresolved-identity deficits are now measurable independently.

### Validation and handoff

The assignment gate returned base-only before work. The configured build
command was derived from nm_ranking, including the asm-processor wrapper.
Private-copy and instrumented controls reproduce configured owned bytes;
the actual baseline workbench self-comparison passes. The nearest-oracle
check was repeated without adopting any donor material. Both changed-source
sweeps have authenticated single-procedure ordinal-zero Ucode mappings.

Commands and evidence include tools/draw_census.py, tools/residual_map.py with
retained-object aligned comparisons, tools/frame_census.py, stock/traced
workbench fidelity, tools/allocator_trace_receipt.py, private configured-command
source harnesses, tools/function_preflight.py --analysis-only and
tools/finalize_plateau.py. The preservation checkpoint passed
`tools/gates.sh verify cleanroom check-docs check-nonmatching-builds`.
The final source passes tools/finalize_plateau.py and all six gates:
verify, cleanroom, check-docs, check-nonmatching-builds, check-scoreboard and
check-overlay-syms. Verification rebuilds the unchanged fallback ROM.
All raw evidence stays private under the Git common directory's
`matching-evidence/w4-whale-20260913.tar.gz`; no objects, raw traces, instruction
rows, compiler binaries or experiment ledgers are tracked. Source snapshots,
objects, scores, first mismatches, per-window deltas, census profiles, hypotheses
and exact commands travel in that archive. No merge, rebase or push is performed.

## 2026-09-13 (lane `w4-whale`): case-3 pair closed, preservation checkpoint

**217 to 187 masked**, raw 386 to 358, 14,456 bytes, delta zero,
frame 0x138, first mismatch +0x50. Aligned buckets improve from
**3454/135/2/25** to **3458/134/1/22**. Candidate +0xDDC and target
+0xD5C disappear without replacement. Only the original case-12 pair remains,
candidate +0x12F8 against target +0x1260. No executable bytes are promoted;
the C remains guarded NON_MATCHING.

The earlier natural-s4 source was missing one source-level boundary:
**case 3's row initialization belongs inside the nonempty player-count
block**. Moving that local assignment across the guard simultaneously
preserves the cached row load, restores transition-address sharing, and
removes the extra four bytes. On the inherited partial-sharing source the
change reaches 187 directly. The ordinary transition expression is retained;
no named transition value or force is adopted.

The retained bundle initializes visible i through case 3's title colour,
uses portraitX for the independent array induction in cases 3 and 13,
and retains case 12's unsigned entry index. Both counters start at zero and
advance once per row under the original visible-index bound. portraitX is
dead at each loop's entry; in case 13 its preceding coordinate role has ended.
Every index remains within the same original player-count domain. The
unsigned conversion preserves these nonnegative values. On the empty case-3
path rowY is unused, so moving its local initialization changes no observable
access or call. The declarations, global identities, loads, stores, bounds
and call order otherwise remain unchanged.

The draw census changes from 721 on the four-byte partial-sharing control
to **720**, restoring the first row assignment from two draws to one.
The inherited 1068 named-value alternative and this lane's 621 nested-value
alternative both regress when combined with the guard move; their extra
transition naming is unnecessary. Moving the row assignment is inert on
the earlier 217 and 207 shapes, which is why the shared-cursor context matters.
These are coupled source changes, not a general claim about guard motion.

Stock and traced 187 objects pass full section, relocation and symbol fidelity.
The configured source-copy harness reproduces baseline owned text and
relocations, and the actual baseline self-comparison passes. Aligned per-window
comparison against 217 changes only the case-3 window: one fewer naming row,
one fewer immediate row, one fewer paired structural row and the removed gap
pair. The candidate retains 1,253 relocations against the assembled target's
526; exact runtime relocation identity is not claimed. The inherited
D_800D31C8 selector conflict remains a promotion blocker.

The source is immediately preserved with tools/finalize_plateau.py. All
material source snapshots, objects, scores, aligned deltas and draw censuses
are retained privately. Investigation continues on case 12 after this
checkpoint is gated and committed; this is not a final stall claim.

## 2026-09-13 (lane `w3-whale`): target cursor reached in C; two temp-draw changes remain

**Retained source unchanged: 217 masked / 386 raw**, 14,456 bytes, delta
zero, frame 0x138, first mismatch +0x50. Fresh aligned buckets remain
**3454 exact / 135 naming / 2 immediate / 25 structural**. Both original
pairs remain: candidate +0xDDC / +0x12F8 against target +0xD5C / +0x1260.
No executable bytes are promoted. Only the EOF handoff metadata and this
shard change; all experimental bodies remain private.

The inherited 207 alternative reproduces **207/378**, delta zero,
**3438/154/1/22**, with only the case-12 pair. It still loses sixteen exact
aligned rows against 217 and is not adopted. The retained source, 207 control,
and the new size-zero alternative below all have identical 35 frame homes
and traffic. Their candidate objects each have 1,253 relocation records;
the assembled target representation has 526. Exact runtime relocation
identity is not claimed.

### The first cursor's wanted colour is now naturally source-reachable

Attempt two extends the 207 control's independent portraitX entry-array
induction into **case 13's later draw loop only**. That loop keeps its visible
i, original bound, calls, and accesses. portraitX starts at zero immediately
beside the loop, advances once with i, and is dead from its earlier portrait
loop. Case 12 retains the unsigned entry-array subscript from the 207 control.
There is no change to an array's runtime identity.

This ordinary C produces the same complete content sections, relocation map
and symbol surface as the accepted case-3 cursor-to-s4 intervention on 207.
The fidelity tool confirms the equality. Both objects cost four bytes, so
neither is an adoption. This is a source witness for the wanted cursor
allocation, beyond the inherited force-only witness.

The subsequent size-zero source's fresh allocator capture gives the shared
entry cursor **202/9 = 22.444445**, selected to s4, versus **71/6 = 11.833333**
and s0 in the 207 control. The capture identifies this as web 309 in its own
ordinal-zero procedure; these numbers must not be transplanted to other
sources. A retained named Ucode stream plus complete procedure-index capture
maps the new source to ordinal zero, one procedure and 434 decisions. Stock,
detail-capture, index-capture and ugen-trace objects pass section, relocation
and symbol fidelity checks.

The eight partial-sharing cells include both later cases, signed/unsigned
entry indices, and either visible-index or array-index loop bounds. All
change width except the case-13 unsigned form, which reproduces 207 exactly.
The signed case-13 entry with the original visible-index bound is the source
witness above. The other size changes range from four to twelve bytes.

### A named transition value restores width, but changes two temp-draw sites

Attempt three names the updated case-3 coordinate through a dead scalar:
compute x from the original coordinate minus the original rate product,
store x back to the same global, and test x. Both saved-coordinate restores
remain ordered before it. The original rate and later rate product are
unchanged. No call is crossed and no store is dropped in the source.

On the partial-sharing source, this gives **1068 masked / 1186 raw**, delta
zero, buckets **2564/1020/1/32**. The transition once again keeps its two
coordinate addresses in v1/a2 and reuses the first through the zero store.
Thus the four-byte transition-address cost is source-repairable. x, opponent
and columnX give the same measurements; their objects need not be identical. Other tested carriers or naming the
other coordinate's later addition change width. All eighteen cells are kept.

The result is far worse overall because it exposes another coupled fact.
In the loop's first node fill, the coordinate addresses now occupy v0/v1;
the rowY load that the target keeps in v0 goes into a ring temporary. The
identity-gated ugen traces locate the first extra draw at that exact row
assignment. Relative to 207, the new source has:

- one additional integer temporary draw on the row assignment;
- one fewer draw on the transition coordinate update;
- one fewer draw on its following test;
- **719 total integer draws against 720**, with every other source line's
  draw count unchanged.

So this is not a single extra draw persisting unchanged to the end. There
are three local draw-count changes at two source sites. The per-window
register census contains a nine-register cycle after the node fill and a
different tail mapping after the transition. The frame census has no new
home, missing home, or traffic change. Dropping a declaration merely because
the workbench's heuristic suggests a stack-home lever would contradict that
measured frame evidence.

The old case-3 delayed-index pair is absent, but two new scheduling pairs
appear at candidate +0xDE0 / +0xE98 against target +0xDEC / +0xEA8. The
case-12 pair remains. This is not the requested gap closure without replacement,
and the 1068 source must not displace the stronger 217 source.

### Remaining attempts and bounded stall

Attempt one, before the partial-sharing witness, explicitly carries either
case-3 transition coordinate address through each of three dead pointer
roles. The six cells add four or eight bytes and reproduce the known pointer
alias/body tradeoff. They do not preserve target width.

After the source witness and width repair, three follow-ups provide no
adoption or further causal repair, meeting ADR 0018's stall rule:

- Attempt four moves the row store to five earlier positions and separately
  caches its value through seven dead scalar roles at two positions. The
  nineteen cells keep width but never beat 1068. Every cache-only form folds
  back to the same object; moving the store worsens scheduling.
- Attempt five changes one or both case-3 coordinate negations to unsigned
  arithmetic or an unsigned load, on 217, 207 and the 1068 source. All
  twenty-seven cells reproduce their own control's owned text and relocation
  map. This literal/type spelling does not repartition the address contest.
- Attempt six combines case 12's early visible-index/independent-array split
  with named transition values. Three first-loop carriers each reproduce the
  known four-byte control cost. Six value carriers on each produce deltas
  minus twelve, plus four or plus eight; none preserves width. The case-3
  transition repair does not transfer unchanged to this case.

These are bounded source-family negatives, not exhaustion of source space.
The next concrete question is **the coupled caller-register allocation of
case 3's loop coordinate addresses and rowY load**, on the preserved natural
s4/size-zero source. It must keep the row load out of the temporary ring while
retaining the transition's address reuse, and it must account for the two
transition draws the named value removes. Repeating the declared-pointer
recipe, these row-cache forms, unsigned negations or case-12 value carriers
does not answer that question. No colour landscape is rerun in this lane.

### The inherited relocation conflict is a selector distinction

Preflight reproduces the D_800D31C8 conflict after the ordinary fresh-lane
alias regeneration and build. A read-only audit of its unchanged identity
producers finds two distinct proposals:

- the resident-looking name proposes selector zero, resident offset +0xD2D78;
- all admitted runtime-correlated pairs propose reserved selector 0xFFF,
  BSS-relative base offset +0x4D788.

The resident base and the runtime BSS base both resolve these to the
address encoded in the existing name. Equal resolved addresses do not make
those runtime relocation identities interchangeable. The matched-sibling
witness producer supplies no additional identity for this name. No producer,
comparison threshold, alias rule, source binding or symbol policy is changed.
A promotion still requires explicit, authenticated selector-aware naming;
this diagnosis does not waive that gate.

### Preservation and validation

The assignment gate returns base-only. The configured wrapper and private
source-copy harness agree on baseline owned text and relocation maps. The
actual baseline object's workbench self-comparison accepts identical words
and known relocation-kind layout. The size-filtered oracle still selects JFG
frontKeyboard at 0.081; no donor source or names are adopted. Every material
source/object, score, first mismatch, aligned map and per-window control delta
is retained, together with compiler traces, identity audit and gate logs.

Commands used include lane_status with the symbol gate, align_symbol,
residual_map with retained objects and aligned control deltas, frame_census,
function_preflight in analysis-only and no-build modes, allocator_trace_receipt
in map-only mode, and workbench compare, fidelity, diagnose and capture make.
The private harness derives its complete compiler command from
nm_ranking.configured_compile_commands and scores each retained object directly.

The private packet is archived as
`matching-evidence/w3-whale-20260913.tar.gz` under Git's common directory.
It contains ROM-derived evidence and must never be staged or published.
The source remains guarded by NON_MATCHING and is finalized with
`tools/finalize_plateau.py`. Closing configured recompilation reproduces
217/386 and passes owned text/relocation equality and full section fidelity
against the entry object. All four tools/gates.sh checks pass: verify,
cleanroom, check-docs and check-tooling. Verify prints expected and built
US ROM SHA-1 `507341c0a40ca3e9a7cee969b396ee53facfb548`. This proves the
assembly fallback, not the nonexact C candidate. No merge, rebase or push
is performed.
